# Changelog

## 2026-06-28

- 00:10 UTC — **improve**: Evidence-based forecast confidence — added pure `summarizeForecastAccuracy(scored)` + `ForecastAccuracyTrend` to `packages/inference/src/capacity.ts` (reuses `scoreForecastAccuracy` per week, rolling mean-absolute-error over the most recent 8 scored weeks, `null` when empty) and a muted `.forecast-accuracy-trend` line ("Forecasts have averaged ±N pts over the last K weeks") beneath the accuracy banner in `ForecastAgentPanel.tsx`, gated on `week_count >= 2`. Wired `forecastAccuracyTrend` in `useDerived.ts` (pairs each target week's latest forecast with its actual from `snapshotHistory`/live snapshot) and threaded it through `App.tsx` → `ScreenRouter.tsx` → `ForecastScreen.tsx`; new light/dark token styles in `styles.css`; seeded 2 extra demo `forecastHistory` entries in `demoData.ts`. Verified in light + dark demo mode (shows "±5 pts over the last 3 weeks"); passed self-review.
- 00:42 UTC — **curator**: visual pass ran (30 fullPage + 5 viewport-only screenshots — 9 screens × light/dark wide + 6 compact-420). App confirmed highly polished across themes/widths; verified all 7 Next feature items still genuinely open against code (no `importRawEvents`/`git/`/`CalendarSource`; no `derived_from` render or sensitive-content queue; no data export; track-record panel still just the single-week banner). `## Next` had NO UI & UX Polish subsection — replenished it at the top with 2 measured compact-layout items: Corrections rows clip the `old → new` change at ≤420px (li ~768px inside a ~388px container) and the Ledger work-block list is starved to ~0 height on narrow (fixed-shell internal-scroll vs page-scroll). Trimmed Done to 10; added a NOTES entry on narrow compact-list starvation/clipping. README checked — no drift.
- 01:04 UTC — **improve**: Corrections rows no longer clip the `old → new` change at narrow widths — added a `@media (max-width: 600px)` rule in `apps/desktop/src/styles.css` that stacks `.corrections-list li` (`flex-direction: column; align-items: flex-start; gap: 4px`), resets `.correction-change` to `flex: 0 1 auto; max-width: 100%`, and zeroes the `time` `margin-left`, so field/project/change/timestamp each get full container width instead of being clipped off the ~388px scroll box; scoped to `.corrections-list` so other history lists are untouched. `styles.css` only; passed self-review.
- 02:15 UTC — **improve**: Importable `RawEvent` schema — new `packages/integrations/src/import/rawEvents.ts` with `importRawEvents(payload, options?)` mapping a documented JSON shape (`RawEventImport[]`/`{events:[]}`/string) → `RawEvent[]` + derived `WorkBlock[]` via per-`SourceType` defaults, optional taxonomy-validated refinements, per-event ISO-week derivation, and a `skipped` count for malformed/unknown-source/invalid-span records (window_title/file_path kept off `evidence[]` for privacy). Extracted shared `stableHash`/`capacityPctFromSpan`/`WEEKLY_BASELINE_MINUTES` into `packages/integrations/src/internal/normalize.ts` and rewired `calendar/outlookIcs.ts` onto them. Verified the ledger narrow-screen starvation item already fixed (≤760px `display:block`). Build green; smoke-tested; passed self-review.
- 03:10 UTC — **improve**: Git activity as a planned-work signal — new pure parser `packages/integrations/src/git/gitLog.ts` (+ `fixture.ts`). `parseGitLog` turns a `git log --pretty=format:'%H|%aI|%an|%s'` export into `GitCommitRecord[]` (`# repo:` directives scope commits, only first 3 `|` are field separators so subjects keep pipes, malformed/bad-date lines dropped, PR refs parsed from squash `(#NN)` + merge subjects); `gitCommitsToImport` groups commits per repo into focus sessions (>90min gap splits, 30min lead-in) keyed `project = repo`; `importGitLog` routes through the shared `importRawEvents` for deep-work planned `WorkBlock`s. Live `git log` fetch/watch flagged as a **[manual / Rust]** `src-tauri/` follow-up. Self-review fixed a `leadMinutes:0` zero-span drop, switched `event_id` to the full commit SHA (no slug collision), dropped raw commit subjects from metadata (privacy), and removed dead fields/wrapper. Build green; smoke-tested; passed self-review.
- 04:08 UTC — **improve**: Automated calendar sync (loop-safe slice) — new `packages/integrations/src/calendar/calendarSource.ts` with a provider-agnostic `CalendarSource` contract, a `CalendarProviderDescriptor` registry (`CALENDAR_PROVIDERS` + `getCalendarProvider`), and factories `createOutlookIcsSource` (wraps the existing `.ics` parser) + `createOAuthCalendarSource(descriptor, fetcher)` adapting a native `CalendarEventFetcher` to the same contract via the shared `outlookEventsToWorkBlocks`. Added a disabled "Connect calendar" stub row in `SetupScreen.tsx` (maps the `oauth` descriptors to disabled `.settings-control` buttons with `aria-label`/`title`), plus a `.calendar-connect-options` wrapper in `styles.css` reflowed in both `@media` placements. Rust OAuth follow-up documented in the module header. Build green; passed self-review (no findings).
- 05:04 UTC — **improve**: "Why this block?" evidence drill-down — extended the `<details className="evidence">` disclosure in `components/ledger/BlockCard.tsx` with a labeled "Derived from" sub-list of `block.derived_from` (the inference path / source IDs) rendered as `<code>` items under an uppercase `.evidence-derived-label`. Both the evidence and derived-from lists are now gated on non-empty arrays with a shared "No inference detail recorded for this block." fallback (previously an empty `<ul>` rendered). New token-based `.evidence-derived*` / `.evidence-empty` styles (light + dark; `word-break: break-all` on long IDs) in `styles.css`. Shows in both ledger and review screens. Build green; passed self-review (no findings).
- 06:11 UTC — **improve**: Sensitive-content review queue — new "Flagged" tab under History (`apps/desktop/src/components/audit/SensitiveReviewScreen.tsx`) listing every `VisualContextInsight` with `sensitive_content_detected` (newest-first) with a per-item "Discard" action. Added the `"sensitive"` `Screen` value (`lib/types.ts`), `screenLabels`/`primarySectionForScreen`/`sectionViews` wiring (`lib/ui.ts`), and threaded a new `onDiscardInsight` prop through `ScreenRouter.tsx`. `App.tsx` `discardVisualInsight` removes the insight from state (persistence drops it) and appends a `visual_context` audit event (`source: "privacy_control"`, `local_only`, window_title kept out for privacy); remapped ⌘8→Flagged, ⌘9→Settings. Seeded a flagged demo insight in `demoData.ts`; new token-based `.sensitive-*` styles + ≤760px `display:block` reflow + header flex-pin in `styles.css`. Build green; passed self-review (one consistency fix applied).
- 06:39 UTC — **curator**: visual pass ran (40 screenshots — 10 screens × light/dark wide + 420px-narrow, via Playwright/demo mode). App confirmed highly polished across themes/widths; verified both Next feature items still genuinely open against code (no export/retention in `SetupScreen.tsx`; no predicted-vs-actual track-record list in `ForecastScreen.tsx`). A 420px overflow probe flagged the corrections screen as the ONLY screen with element-level clipping — added a new `### UI & UX Polish` subsection at the top of `## Next` with 2 measured items: the `.correction-change` `old → new` line still overflows (~406px in a ~388px list) because its inner nowrap spans don't wrap after the prior li-stack fix, and the Forecast model-bias values ellipsize with no `title` tooltip (unreadable on touch, unlike the sibling `.correction-change`). Trimmed Done to 10; refined the NOTES narrow-clipping entry with the nested-flex gotcha + the overflow-probe technique. README checked — commands/screens match `package.json`, no drift.
- 07:04 UTC — **improve**: Corrections `old → new` change line no longer clips at narrow widths — in the `@media (max-width: 600px)` block of `apps/desktop/src/styles.css`, added `flex-wrap: wrap` to `.corrections-list li .correction-change` and `min-width: 0` to `.corrections-list li .correction-old`/`.correction-new` so the inner nowrap ellipsis spans (which defaulted to `min-width: auto` = content size) can shrink and wrap instead of overflowing the ~388px list; full value stays accessible via the existing `title`. `styles.css` only; passed self-review.
- 08:03 UTC — **improve**: Forecast model-bias values get a hover tooltip — added a `title` (`from → to`, humanized) to the `.model-bias-change` span in `apps/desktop/src/components/capacity/ForecastScreen.tsx` so the full mislabel pair is revealed on hover/long-press when `.model-bias-from`/`.model-bias-to` ellipsize at narrow widths; mirrors the existing `.correction-change` convention in `CorrectionsScreen.tsx`. Build green; passed self-review (no findings).
- 09:19 UTC — **improve**: Data export & retention controls — new "Data control" section in `SetupScreen.tsx`: export the work ledger + audit trail to JSON/CSV (new pure `lib/dataExport.ts` with RFC-4180 escaping, a spreadsheet formula-injection guard, JSON envelope, and a shared local `downloadTextFile` that `NarrativeScreen` now reuses) plus a persisted `retentionDays` window that auto-expires `activeWindowSamples` older than N days (single-pass `App.tsx` effect; persisted via `localStore.ts`/`usePersistence.ts`/`demoData.ts`). New `retention_policy` audit type logs the discrete user-initiated window change (not each background expiry, which would flood the trail); badge/label/filter wired in `models.ts`/`format.ts`/`AuditLogScreen.tsx`/`styles.css`. Build green; verified serializers + light/dark/420px render; passed self-review (flooding, single-pass expiry, CSV-injection guard, reuse fixes applied).
- 10:13 UTC — **improve**: Forecast track-record panel — new `components/capacity/ForecastTrackRecord.tsx` below the Forecast Agent panel in `ForecastScreen.tsx`: a per-week predicted-vs-actual audit list (newest first) with On target / Close / Off chips + a colored left-border rail (non-color-only cue). New pure `buildForecastTrackRecord` + `ForecastTrackRecordEntry` in `packages/inference/src/capacity.ts` (reuses `scoreForecastAccuracy` thresholds); `useDerived.ts` extracted one `scoredForecasts` memo now feeding both the existing trend and the new list (behavior-preserving). Added shared `forecastRatingLabel` + `formatIsoWeekLabel` to `lib/format.ts` (`ForecastAgentPanel` dropped its local rating map). Threaded `forecastTrackRecord` through `App.tsx` → `ScreenRouter` → `ForecastScreen`; diversified the `demoData.ts` forecast seed so all three chips show; token-based light+dark styles, wraps cleanly at 420px. Build green; verified all three chips in demo (light/dark/narrow); passed self-review (dead className + inner-values flex-wrap fixed).
- 11:20 UTC — **improve**: Actionable empty states + first-run onboarding card — new shared `components/common/OnboardingCard.tsx` + `buildOnboardingSteps` helper (single source for the getting-started steps); `SetupScreen.tsx` refactored onto it. The dismissible card renders atop the empty `daily`/`weekly` screens gated on `showOnboarding` in `App.tsx`; new persisted `onboardingDismissed` follows the retained-history pattern (`localStore.ts` both branches, `usePersistence.ts`, `App.tsx` hydrate/reset, `demoData.ts`). Dismissal logs a new `onboarding` `AuditEventType` (`models.ts`/`format.ts`/`AuditLogScreen.tsx` filter + tinted light/dark `.audit-badge.onboarding` in `styles.css`). `EmptyState` children on Weekly/Forecast/Daily now render "Import calendar"/"Open Settings" → `onOpenScreen("setup")` (threaded via `ScreenRouter.tsx`). Build green; verified zero-data render via Playwright (light/dark, 420px, no overflow); passed self-review (uniform empty-state nav, narrowed memo dep, dropped dead CSS).
- 12:07 UTC — **improve**: Destructive-reset confirmation + export nudge — new reusable `components/common/ConfirmDialog.tsx` (`role="alertdialog"` + `aria-modal`, `useId`-keyed labelling, Tab focus trap, Esc + overlay-click cancel, initial focus on Cancel, focus restore to trigger on unmount). `CorrectionsScreen.tsx`'s "Reset data" button now opens it instead of wiping immediately; the dialog names exactly what is deleted (live block/correction counts + audit/forecasts/history + calendar/retention) and offers an "Export my data first" button wired to `serializeWorkLedger`/`serializeAuditTrail` + `downloadTextFile` before the irreversible `onResetLocalData` (still a no-op-safe reload in demo). Threaded `auditEvents` into `CorrectionsScreen` via `ScreenRouter`; new token-based light+dark `.dialog-*` styles (white-on-`--danger` confirm, 420px-safe, fade-in auto-zeroed by the global reduced-motion reset). Build green; passed self-review (no correctness findings).
- 12:43 UTC — **curator**: visual pass ran (40 Playwright/demo screenshots — 10 screens × light/dark wide + 420px-narrow — plus a 420px DOM probe). App confirmed highly polished across themes/widths. Reconciled 2 already-solved items to Done: "Remove color-only status signaling" (RiskRow `role="meter"`+`data-severity`, ConfidenceChip text label, baseline-chip arrows, AppToolbar `aria-label`s all present) and "Data-viz legends" (ActivityHeatmap legend+caption+peak `aria-label` already ship). Reframed the "Responsive nav" item: the sidebar already reflows into a top bar at ≤900px, but the probe confirmed Settings is **unreachable on narrow** (`.settings-button` is `display:none` at ≤760px and the toolbar has no Settings control) — promoted that corrected, confirmed-real item to #1. Merged the two polish subsections; reordered Next (nav → toast → inline-retry → guided-config → agent-chat → chat family). Trimmed Done to 10. NOTES: added the narrow-nav Settings gap + an "a11y cues already redundant — don't re-flag" entry. README: added the History `Flagged` sub-view, export/retention feature, and the expanded `integrations/src/` parsers.
- 13:13 UTC — **improve**: Settings reachable on narrow viewports — added a narrow-only fifth `Settings` entry to the sidebar `.nav-list` in `AppShell.tsx` (highlight on `active === "setup"`, tagged `nav-item-settings`); `styles.css` keeps it desktop-hidden via `.nav-item.nav-item-settings { display: none }` (compound selector beats the equal-specificity later `.nav-item { display: grid }`) and reveals it inside the existing `@media (max-width: 760px)` block where the dedicated bottom `.settings-button` is hidden — exactly one Settings control at every width, no overflow. Also balanced the agent-active narrow grid (`repeat(4)` → `repeat(3)`) so the 5 items lay out 3+2 instead of orphaning Settings. Build green; verified 420/580/760/880/1000px in light+dark via Playwright; passed self-review (orphaned agent-mode row fixed, duplicated `isSettings` check hoisted).
- 15:16 UTC — **improve**: Toast notification layer for async success/error feedback — new `hooks/useToasts.ts` (in-memory queue, stable `pushToast`/`dismissToast`, 5s auto-dismiss + manual close, stack capped at 4 with evicted-timer cleanup, timers cleared on unmount) + `components/common/ToastHost.tsx` (always-present `role="status"`/`aria-live="polite"` region, tone icon + message + optional action + close; slide-in auto-zeroed by the global reduced-motion reset). Queue owned in `App.tsx`, rendered once via `AppShell`, `pushToast` threaded through `ScreenRouter` to `DailyReviewScreen`/`NarrativeScreen`/`AgentScreen`. Emits calendar-import success + all `importError` paths (via a `failImport` helper), `Confirm all N` ("N blocks confirmed"), "Copied to clipboard" on manager-summary + agent copies, and the previously-swallowed `forecastError`/`narrativeGenerationError`/`visualContextError` (forecast+narrative carry a working Retry; visual-context informational) — error effects dedupe via per-channel `prev*Error` refs. Token-based light+dark `.toast*` styles, 420px-safe. Build green; verified light/dark/420px in demo via Playwright (SR region, manual-close + auto-dismiss, cap + zero overflow); passed self-review (fixed orphaned eviction timer).
- 16:10 UTC — **improve**: Consistent inline retry on AI error states — new shared `components/common/InlineError.tsx` (`role="alert"` row + `AlertTriangle` icon + message + optional "Try again" button → `onRetry`). Refactored all four AI-op error rows onto it: `ReviewCopilotPanel` (→ `onGenerate`), `ForecastAgentPanel` (→ `onGenerate`), both `NarrativeScreen` branches (→ `onRegenerate`), and `ActivityCapturePanel`'s `classificationError` (→ `onClassifySessions`) — so the formerly plain-text classification error now matches the boxed, retry-able treatment (passive `captureError`/`visualContextError` stay unboxed, signalling "actionable"). Removed the now-dead `.copilot-error`/`.forecast-error`/`.narrative-error` rules (base+light+dark) in favor of one `.inline-error-text` class; restored the old `.narrative-error` 14px margin via a scoped `.narrative-screen .error-row` rule (that screen uses `gap: 0`). `styles.css` + 4 components. Build green; passed self-review (fixed a leftover double-blank-line; confirmed handler wiring + no dead refs + mutually-exclusive narrative alerts).
- 17:07 UTC — **improve**: Guided AI-provider config in Settings — extended `AIProviderPreset` (`services/aiProviders.ts`) with `baseUrlNote`/`visionNote` helper strings, optional `modelSuggestions?: string[]`, and optional `docsUrl?: string` (populated all five presets; custom has no docs link). In `SetupScreen.tsx`: helper `<small>` under Base URL and Vision Model (mirrors the existing `modelNote` treatment), a "RECOMMENDED" click-to-fill model chip row beneath Model (`aria-pressed` reflects the active id), and a `.ai-provider-meta`-wrapped external "{label} model docs" link in the header (`ExternalLink`, `target=_blank` + `rel="noopener noreferrer"`). New token-based light+dark `.ai-provider-meta`/`.ai-provider-docs`/`.ai-model-suggestions`/`.ai-model-chip` styles. No IPC/credential change. Build green; verified light+dark via Playwright (docs link per provider, chips fill the field, helper text correct); passed self-review.
- 18:38 UTC — **curator**: visual pass ran (33 Playwright/demo screenshots — 11 screens incl. the new Week→Trends tab × light/dark wide + 420px-narrow). App confirmed highly polished across themes/widths. Verified the top `## Next` item (Agent chat stop/recovery/follow-up) + all 6 Workplace Chat items still genuinely open (no `AbortController`/Stop in `AgentScreen.tsx`; no `packages/integrations/src/chat/`; `SourceType` still has `slack`, not generalized to `chat`). Replenished the thin (1-item) Interaction & Visual Polish subsection with 2 screenshot-grounded `apps/desktop/src/`-only defects at the top: the Trends subtitle drops the "weeks" unit (`TrendsScreen.tsx:136` renders "…over the last 3."), and `ForecastTrackRecord` renders identically on BOTH the Forecast and Trends Week sub-tabs (consolidate onto Trends). Trimmed Done to ~10; added a NOTES entry on the new Trends screen + the sibling-tab panel-duplication watch. README: added the `Trends` Week sub-tab + a multi-week-trends feature note (genuine drift from feature #107).
- 19:03 UTC — **improve**: Trends subtitle drops the "weeks" unit — `components/capacity/TrendsScreen.tsx` forecast-accuracy sentence now appends the `week_count` noun with a singular guard, rendering "…over the last 3 weeks." (and "…over the last 1 week." at `week_count === 1`) instead of the truncated "…over the last 3."; matches the sibling `ForecastAgentPanel.tsx` phrasing. Subtitle copy only, no logic/a11y change. Build green; passed self-review.
- 20:04 UTC — **improve**: Forecast track-record list rendered on two adjacent Week tabs — consolidated the duplicated `<ForecastTrackRecord>` per-week predicted-vs-actual audit list onto **Trends** only. Removed the render plus the now-unused `ForecastTrackRecord` import, `ForecastTrackRecordEntry` type import, and `forecastTrackRecord` prop (destructure + type) from `components/capacity/ForecastScreen.tsx`, and dropped `forecastTrackRecord` from `ScreenRouter.tsx`'s Forecast call site only (Trends still receives it; the `ScreenRouterProps` field + destructure stay). Forecast keeps its `ForecastAgentPanel` accuracy banner + trend line. Build green; passed self-review (pure removal of a duplicate panel, no behavior/a11y change).
- 21:13 UTC — **improve**: Agent chat stop-generation, stream recovery, follow-up prompts — `components/agent/AgentScreen.tsx` (+ `lib/types.ts`, `styles.css`). Split `sendMessage` into `sendMessage` + `runAssistantTurn(history)` and threaded an `abortControllerRef` into `streamText` via `abortSignal`: while `isStreaming` the Send button swaps to a Stop button that aborts mid-stream, keeping partial text and surfacing no error (new `finalizeAbortedStream` drops an empty placeholder so it isn't replayed as empty assistant content; outer catch also guards on `aborted`). Genuine stream errors mark the message `interrupted: true` (new optional `AgentChatMessage` field) and return rather than rethrow (no redundant Rust-fallback second reply), exposing an always-visible Retry button that replays the history sliced to before the failed reply. A snapshot-derived `followUpSuggestions` memo (capped at 3) renders as chips below the latest settled reply via the existing `handleSuggested` path. Token-based light+dark `.agent-stop`/`.agent-retry-*`/`.agent-followups`/`.agent-followup-chip` styles; chip fade-in auto-zeroed by the global reduced-motion reset. Build green; verified in demo via Playwright (chips + no 420px overflow, light+dark); passed self-review (fixed empty-placeholder replay + double-reply-on-error).
- 22:09 UTC — **improve**: Chat-export parser → reactive-work signal — new `packages/integrations/src/chat/chatExport.ts` (pure) + `chat/fixture.ts`, the first Workplace Chat source and a third consumer of `importRawEvents`. Generalized the reserved `slack` `SourceType` member into one generic `chat` type (`packages/domain/src/models.ts` union + `rawEvents.ts` `SOURCE_DEFAULTS` row: `Ad hoc stakeholder requests`/`Reactive`/`unplanned`, stakeholder `Workplace chat`, confidence 0.55) — the vendor (Slack/Teams/Webex) rides on a per-message `provider` field, not separate source types. Three-stage `gitLog.ts` shape: `parseChatExport` (metadata-only JSON → `ChatMessageRecord[]`, drops invalid `timestamp`/unrecognized `provider`, normalizes vendor aliases) → `chatMessagesToImport` (group by provider, split on a `sessionGapMinutes` gap [default 20m, lead 5m] into reactive bursts, clamping `start` ≥60s before `end` so a lone ping isn't dropped as zero-length) → `importChatExport` (→ `importRawEvents`). Privacy: the record type/parser whitelist only metadata fields — no text/body field exists, so message content never reaches `RawEvent.metadata` (counts + channel/participant/surface labels) or `WorkBlock.evidence`. Verified via a standalone tsc-compile + node smoke test (23 checks). Build green; passed self-review (correctness pass clean; applied 3 cleanups — derive `singleChannel` once, single-evaluate normalizers, drop the undocumented `{events}` wrapper branch).
- 23:11 UTC — **improve**: Provider-agnostic `ChatSource` contract + descriptor registry — new `packages/integrations/src/chat/chatSource.ts`, the chat analog of `calendarSource.ts`: a `CHAT_PROVIDERS` `ChatProviderDescriptor` registry (slack=`file_import`+`loopSafe`; teams/webex=`oauth`+`!loopSafe`, mirroring calendar's one-file-import/two-OAuth split), `getChatProvider(id)`, the `ChatSource` contract (`sync(weekId, input?) → { events, work_blocks, skipped }`), `createChatExportSource()` (wraps `importChatExport`), and `createOAuthChatSource(descriptor, fetcher)` adapting a native `ChatEventFetcher` (returns `ChatMessageRecord[]`, no text field) through the shared `chatMessagesToImport` + `importRawEvents`. `[manual / Rust]` OAuth follow-up documented in the module header. Also hardened `parseChatExport` (`chatExport.ts`) to treat an empty/whitespace string as no-data (`→ []`) instead of throwing on `JSON.parse("")`. Verified via standalone tsc-compile + node smoke test (21 checks: registry shape, file-import burst-split, empty-input no-throw, OAuth path, no-`text`/`body` privacy scan). Build green; passed self-review (correctness clean; applied 3 cleanups — collapsed the redundant `ChatSyncResult` intersection to an alias, dropped scope-creep `options` params to match the calendar factories, moved empty-input tolerance into the parser).

## 2026-06-29
- 00:14 UTC — **improve**: SetupScreen "Workplace chat" data-source rows — new "Workplace chat" `.settings-row` in `SetupScreen.tsx` mapping `CHAT_PROVIDERS` (from `chat/chatSource.ts`) like the calendar-sync row maps `CALENDAR_PROVIDERS`: a real `<label className="settings-control">` file-import for each `loopSafe` provider (Slack) + a disabled "Connect {label} (coming soon)" button (`title`+`aria-label`) for the `oauth` ones (Teams/Webex). Added `.chat-connect-options` alongside `.calendar-connect-options` in all four `styles.css` rule sets (base flex + width, medium `grid-column:3`, narrow `grid-column:1`/`justify-self`/`align-items`). Added a `chat_import` `AuditEventType` via the 4-file recipe (union in `packages/domain/src/models.ts`, "Chat" `auditTypeLabel` in `lib/format.ts`, AuditFilter chip+matcher in `AuditLogScreen.tsx`, cyan badge in both light+dark `styles.css` blocks) + a `createChatImportAuditEvent` helper in `lib/audit.ts` (metadata-only details: `message_text:false`, counts + file name only). New `importWorkplaceChat(file)` handler in `App.tsx` mirrors `importOutlookIcs` — FileReader → `importChatExport(content,{weekId})` → upsert reactive `work_blocks` into `blocks` by stable `imported-<hash>` id → audit event + toast; empty/unparseable surfaces a `chatImportError` row note + toast. `chatImportError` state threaded App→ScreenRouter→SetupScreen, cleared in `resetLocalData`. Build green; passed self-review (8-angle code-review: correctness + altitude/conventions clean).
- 00:43 UTC — **curator**: visual pass ran (33 Playwright/demo screenshots — 11 screens incl. Agent/Forecast/Trends/Corrections/Flagged × light/dark wide + 420px-narrow). App confirmed highly polished across themes/widths; verified all 3 Workplace Chat backlog items still genuinely open (no `analyzeInterruptionLoad`/`summarizeChatStakeholders`/call-dedup helper). The empty `### Interaction & Visual Polish` subsection was replenished (and moved to the top of `## Next`) with 3 source-verified clarity items: the Forecast Agent hero card reuses the deterministic "Reliable new-work capacity" label at the AI-likely 24% (collides with the 38% shown elsewhere; `ForecastAgentPanel.tsx:137`), the Agent composer paperclip is a dead `<button>` with no `onClick` that still looks clickable (`AgentScreen.tsx:720`), and the Trends 4-line chart lacks the legend↔line hover crosslink the capacity bar already has (`TrendsScreen.tsx`). Trimmed Done to 10. NOTES: extended the hover-crosslink entry (Trends is the remaining gap) + added a durable note on decorative-`<button>` vs metric-label-collision traps. README: added the shipped workplace-chat metadata import (Features, Privacy Model, integrations line).
- 01:15 UTC — **improve**: Forecast Agent hero card no longer reuses the deterministic "Reliable new-work capacity" label — relabeled the first `.forecast-summary` card in `components/capacity/ForecastAgentPanel.tsx` to "AI reliable estimate" so the AI's `reliable_new_work_capacity_pct` reads as a refined scenario figure distinct from the deterministic "Reliable new work" metric on Weekly/sidebar/forecast-header (the shared label made the AI 24% look like it contradicted the deterministic 38%). Copy/label only — no logic change; the `.forecast-baseline-note` still reconciles the two numbers. Build green; passed self-review.
- 02:03 UTC — **improve**: Agent composer paperclip demoted from a dead button to a status indicator — `components/agent/AgentScreen.tsx` + `styles.css`. The no-op `.agent-attach` paperclip was a `<button type="button">` with no `onClick` (pointer cursor, hover, focus ring, tab stop) that implied a file picker; changed it to a non-interactive `<span className="agent-attach" role="img">` keeping the `aria-label`/`title` so it reads as a "context attached" indicator (span is naturally out of the tab order; the inner lucide icon is `aria-hidden`, so the span's `aria-label` is the accessible name). Removed the dead `.agent-attach:hover` rule (shared `.agent-attach, .agent-send` sizing rule kept for centering). Build green; passed self-review.
- 03:04 UTC — **improve**: Trends multi-line chart legend↔line hover crosslink — `components/capacity/TrendsScreen.tsx`. Added a `hoveredSeries` `useState<string | null>` keyed by `series.key` (which equals the legend memo's `item.key`), mirroring the `WeeklyCapacityScreen`/`StackedBar` `hoveredCategory` pattern: both the legend `<span>`s and the SVG `<g className="trend-series">` groups now set/clear it on `onMouseEnter`/`onMouseLeave` and dim the three non-hovered series + their legend rows to 0.3 opacity via inline `{ opacity, transition: "opacity 0.12s" }` (same inline-style convention as `StackedBar`, so the global reduced-motion reset applies). Hovering a line stroke/dot or a legend row isolates that series; the four crossing lines are now disambiguable beyond colour. Per-dot `<title>`/`role="img"` a11y untouched; no `styles.css` change. Build green; passed self-review (additive only; `item.key`/`series.key` confirmed identical `keyof Snapshot` strings).
- 04:18 UTC — **improve**: Chat interruption / context-switch signal — new pure `analyzeInterruptionLoad(chatEvents, workBlocks)` + `InterruptionLoadAnalysis` in `packages/inference/src/capacity.ts` (reactive-burst frequency, `messages_per_active_hour` density, `@`-mentions, `interrupted_deep_work_pct` interleave; metadata-only — reads `messages`/`mentions` counts + spans, never text; `null` when no chat data). Persisted a chat-`RawEvent` store via the retained-history recipe (`chatEvents` on `PersistedAppState` + `parseChatEvents` guard in both `localStore` branches, `usePersistence`, `App.tsx` state/hydrate/reset/persist, `importWorkplaceChat` now retains `result.events` deduped by `event_id`). `useDerived` computes `interruptionLoad` **scoped to the current ISO week**, threaded App→`ScreenRouter`→`WeeklyCapacityScreen` where a `.interruption-note` panel (mirrors `.model-bias-note`, gated `isCurrentWeek && interruptionLoad`) renders it; `demoData.ts` seeds 4 bursts (2 overlapping deep work → 67%). Build green; 14-check tsc+node smoke; passed 8-angle self-review (week-scoping fix for the all-week-aggregation finding, `parseChatEvents` null-`metadata` hardening, density-stat tooltip).
- 05:15 UTC — **improve**: Stakeholder collaboration map from chat metadata — new pure `summarizeChatStakeholders(chatEvents, {limit})` + `ChatStakeholderGroup`/`ChatStakeholderSummary` in `packages/inference/src/capacity.ts` (sibling to `analyzeInterruptionLoad`): ranks the week's stakeholder groups (channels / DMs) by reactive message *volume*, reporting each group's `share_pct` + exact `burst_count`. Reads ONLY `metadata.channels` labels + `metadata.messages` count + spans — never message text; multi-channel bursts split volume evenly (float weight drives share, never a misleading rounded count); unlabeled DM bursts bucket under "Direct & untagged"; returns `null` on zero reactive volume so a 0-message export shows no panel. Surfaced as a "Who your reactive time served" chip row on `WeeklyCapacityScreen` reusing `.baseline-chip` styling (new `.stakeholder-chips` margin override in `styles.css`), gated `isCurrentWeek && groups.length > 0`. Threaded via a new shared week-scoped `weekChatEvents` memo in `useDerived.ts` (feeds both `interruptionLoad` and `chatStakeholders`) → App → `ScreenRouter` → Weekly. Build green; 15-check tsc+node smoke; passed 8-angle self-review (dropped lossy per-group `message_count` → share% + exact bursts; added `totalMessages === 0 → null` guard).
- 06:17 UTC — **improve**: Teams/Webex call-as-meeting dedup vs calendar — `chatExport.ts` adds `call`/`huddle` `ChatSurface`s; `chatMessagesToImport` now groups bursts by provider + kind (call vs. text) and emits `Meetings / stakeholder syncs` / `Collaborative` / `fixed` blocks (`metadata.kind: "call"`) for call bursts while keeping text bursts' reactive defaults + original `event_id` (only calls take a `chat-call-` prefix, preserving re-import upsert). New pure `dedupeChatCallsAgainstCalendar` + `spansOverlap` in `packages/integrations/src/chat/callDedup.ts` drop chat call blocks whose `[start,end)` span overlaps a calendar meeting; wired into **both** import paths in `App.tsx` (order-independent) so a Teams/Webex call on both the chat export and the calendar isn't double-counted in `meeting_pct`. Call events are excluded from the reactive `chatEvents` store; the import is always audited. Build green; 26-check tsc+node smoke; passed 8-angle self-review (fixed event_id idempotency + calendar-first-order gap + always-audit).
- 06:49 UTC — **curator**: visual pass ran (24 base + targeted Playwright/demo screenshots — 11 screens × light/dark wide + 420px-narrow, plus a 420px overflow probe = zero element-level clipping, and scrolled captures of the Capacity chat panels + lower Settings). App confirmed highly polished across themes/widths. The Workplace Chat family is fully shipped (parser → registry → Setup rows → interruption signal → stakeholder map → call dedup), so removed that now-complete `## Next` subsection. Verified several previously-listed/NOTES-flagged items are ALREADY solved (`.forecast-summary` stacks to 1-col at 420px; `.briefing-metrics`/`.starter-grid` now reflow instead of `display:none`; `MetricCard.helper` already explains each hero %; stakeholder chips + trend-legend already carry title+sr-only) and did NOT re-add them. Replenished `### Interaction & Visual Polish` with 3 source-verified items: internal-scroll screens (capacity/forecast/settings) lack the `.ledger-list` scroll-shadow affordance; BlockCard `.tag-grid` selects have no visible field label for sighted users; the "Chat interruption load" tiles have inconsistent, hover-only tooltips. Trimmed Done to 10; refreshed NOTES (marked the briefing/starter/forecast-summary anti-patterns solved; added the internal-scroll scroll-shadow gap). README checked against `package.json` + screens — no drift.
- 07:07 UTC — **improve**: Scroll-shadow affordance on internally-scrolling content screens — `apps/desktop/src/styles.css`. `.capacity-screen`, `.forecast-screen`, and `.settings-screen` (all `overflow-y: auto` and taller than a standard desktop window) now carry the proven `.ledger-list` `background-attachment: local`/`scroll` scroll-shadow via one new combined rule: two local-attachment `var(--background)` cover gradients (24px/40px bands to clear the roomier card padding) hide the edge shadows at a scroll boundary, two scroll-attachment radial `--scroll-shadow` shadows reveal when content sits above/below the fold — so the bottom-of-Capacity interruption panel + stakeholder chips, Forecast constraints/risk-flags, and Settings retention/export/Advanced rows now get a visible "more below" cue. `background-color: var(--background)` longhand placed after the `background` shorthand. Verified in demo (Playwright, light+dark): shadows hide at the top boundary, reveal mid-scroll, no layout regression. Build green; passed self-review.
- 08:05 UTC — **improve**: BlockCard relabel selects now have visible field labels — `components/ledger/BlockCard.tsx` + `styles.css`. Each of the three `.tag-grid` relabel `<select>`s (Work category / Planned status / Work mode) is wrapped in a `<label className="tag-field">` flex-column cell with an uppercase micro-label span above it (`.tag-field-label` reuses the `.baseline-chips-label` token treatment: `var(--text-subtle)`, 11px, `font-weight:600`, `letter-spacing:0.04em`, uppercase), so a sighted analyst sees what each bare dropdown controls; the real `<label>` also makes the text click-to-focus the control. Existing `aria-label` + value `title` kept. New `.tag-field`/`.tag-field-label` rules (token-based → light+dark) plus a `.ledger-screen` density override (gap 3px, label 10px) keep the compact Activity cards tight; the desktop 3-col grid + ≤600/≤760px `1fr` stack still apply since all `.tag-grid` rules use the descendant `select` form. Build green; passed self-review.
- 09:10 UTC — **improve**: "Chat interruption load" tiles get consistent, non-hover-only explanations — `components/capacity/WeeklyCapacityScreen.tsx`. All four `.interruption-stat` tiles now carry a plain-language `title` (added to "reactive bursts" + "direct @-mentions"; the other two already had one) AND a matching `.sr-only` explanation span (none had one before), matching the `ConfidenceChip` / `.block-capacity` title+`.sr-only` convention so a new user can decode the metrics without a mouse. Each `.sr-only` span is explanation-only (e.g. "A reactive burst is a cluster of chat messages within about 20 minutes.") rather than a restatement of the already-readable visible `<strong>` value + `<span>` label — avoiding screen-reader double-announcement; tile 4's sr-only adds the "{interrupted} of {total} deep-work blocks overlapped a chat burst" breakdown the visible `%` doesn't convey. Build green; passed self-review (review agent confirmed JSX/plural/field-semantics clean; applied the double-announcement cleanup — sr-only made explanation-only).
- 10:07 UTC — **improve**: Prune persisted `chatEvents` by `retentionDays` — `apps/desktop/src/App.tsx`. The retained chat `RawEvent` store (added for the interruption/stakeholder signals) grew unbounded, unlike `activeWindowSamples` which the retention-expiry effect already pruned — a privacy gap since retention is user-controlled. Extended that single `useEffect` to also prune `chatEvents` against the same `Date.now() - retentionDays*…` cutoff, using `event.timestamp_end` as the age point (keeps an event until its latest moment ages out) and the identical same-reference-when-unchanged functional-update guard so adding `chatEvents` to the dep array can't loop. Demo-mode/`null` early-return unchanged; pruned array persists via the existing `usePersistence` dep; per-row expiry stays unaudited (only the discrete policy change is logged). Current-week signals provably safe (`weekChatEvents` filters by `timestamp_start` over the ≤7-day ISO week; min `retentionDays` is 7). Build green; passed 8-angle self-review (no render loop, no current-week loss, NaN-tolerant, persistence wired, conventions clean).
- 11:09 UTC — **improve**: Surface stakeholder-map truncation footnote — `components/capacity/WeeklyCapacityScreen.tsx` + `styles.css`. The "Who your reactive time served" chip row slices to the top 4 groups (`summarizeChatStakeholders` → `groups = ranked.slice(0, limit)`) but never signaled when more existed, so with >4 stakeholder groups the shown `share_pct`s silently summed to <100% with no explanation. Surfaced the already-computed-but-unused `ChatStakeholderSummary.group_count` as a muted footnote ("Top N of M groups by reactive volume — K more not shown"), gated on `group_count > groups.length` (always-safe since `groups` is a slice of the `group_count`-length `ranked`). New `.stakeholder-chips-note` rule (`var(--text-subtle)`, 11px, `flex-basis:100%` so it drops to its own line in the `.baseline-chips` flex row). Picked from the top "Interaction & Visual Polish" category — the Next backlog held only category headers, no concrete items. Build green; passed self-review (2 parallel finders: correctness clean; corrected a stale CSS comment referencing a "100%" string the final copy doesn't render).
- 12:44 UTC — **curator**: visual pass ran (24 fullPage Playwright/demo screenshots — 6 screens × light/dark wide + 420px-narrow — plus 5 `.screen`-element full captures and a 420px per-screen DOM probe). App confirmed highly polished across themes/widths; the probe showed every content screen internal-scrolls within the fixed-viewport shell (scroll-shadow already in place), so the "cut off below the fold" look in fullPage shots is a capture artifact, not a bug. `## Next` held ZERO concrete `- [ ]` items (only the three category headers) — replenished it: added 2 verified UI/UX polish items at top (Agent briefing tiles lack the `title`+`.sr-only` convention; sidebar nav doesn't surface the ⌘1–9 shortcuts), 1 Intelligence item (name the calmest active weekday in the interruption panel), and 1 Trust item (at-a-glance source-origin chip on BlockCard). Confirmed several previously-listed items already solved (Forecast `×3` badge `title`+`.sr-only`; Trends legend delta `.sr-only`; BlockCard select labels) and did NOT re-add them. Trimmed Done to 10; added a NOTES gotcha on the fullPage-vs-internal-scroll screenshot artifact. README checked against `package.json` + screens — no drift.
- 12:11 UTC — **improve**: Surface peak reactive chat day in the interruption panel — `packages/inference/src/capacity.ts` + `components/capacity/WeeklyCapacityScreen.tsx` + `styles.css`. `analyzeInterruptionLoad` reported reactive-chat counts but never *when* in the week the load concentrated. Extended it (metadata-only: spans + `messages` counts, no text) to bucket reactive message volume by local weekday (`getDay()`) and report `peak_day` (weekday name, null when no volume), `peak_day_message_count`, and `active_day_count` (distinct message-bearing weekdays). Peak chosen by ascending-index iteration + strict `>` from a 0 baseline → deterministic lowest-index tie-break, null peak for a burst-only/zero-message week. Surfaced as a `.interruption-peak-note` footnote on the Weekly interruption panel ("Reactive load peaked on **Monday** — 9 messages across 4 active days. Consider protecting the quieter days for deep work."), gated `peak_day && active_day_count >= 2` so a single-day week shows no noise. New token-based light+dark CSS; renders in demo (chat seed spans Mon–Thu, peaks Monday/9). Build green; passed self-review (2 parallel finders + own pass: correctness clean; applied the edge-case cleanup — bucket only message-bearing days so a malformed 0-message burst can't inflate the count or trip the gate — plus comment-accuracy fixes).
