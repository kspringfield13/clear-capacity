# Changelog

## 2026-07-01

- 00:21 UTC — **improve**: D2 — useAcceleration hook (opt-in AI synthesis via `ai_complete`) — new `apps/desktop/src/hooks/useAcceleration.ts` + `App.tsx` + `components/accelerate/AccelerationScreen.tsx` + `components/shell/ScreenRouter.tsx` + `services/localStore.ts` + `hooks/usePersistence.ts` + `services/demoData.ts` + `styles.css`. Shipped the Acceleration Engine's optional AI layer, mirroring `useForecastAgent`: the hook builds the derived-only D1 prompt over `accelerationSignals`, routes it through the EXISTING generic `ai_complete` command (`aiCompleteJson<AuthoredAccelerationPlays>` + `jsonSchemaFormat("acceleration", accelerationSchema)`, NO Rust), then re-whitelists every authored field (filters `data.plays` to currently-mined `signal_id`s, dedups `recommended_tools`, NaN-guards numbers), persists a `PersistedAccelerationRecord`, and emits a `derived_only` `acceleration_engine` audit event (`sent_to_openai:true`, `window_titles:false`); the error path audits the failure. Persistence follows the C3 recipe (`generatedPlays` field on `PersistedAppState` parsed in both read paths via `parseAccelerationRecord`, written through `usePersistence`, hydrated at all four `App.tsx` sites incl. `resetLocalData`/`resetAcceleration`). Authored payload merges onto live signals by `signal_id` (`accelerationPlays` memo) — deterministic figures stay authoritative; AI overlays only `detail`/`recipe`/`recommended_tools`. `AccelerationScreen` renders the AUTOMATE recipe (collapsible `<details>`, native marker like `.evidence summary`) + TOOL chips, plus an AI-key-gated "Generate/Regenerate skills" control (disabled while "Authoring skills…", `formatAuditTime` note) or a "no key → Open Settings" dashed hint; failures via the shared `InlineError` + an App-level retry toast. Demo seed gains `generatedPlays: null`. Privacy holds structurally (derived-only prompt/schema; miner never emits window titles). Verified via Playwright/demo in light + dark wide. Build green; passed self-review (two parallel finders — correctness 0 bugs, confirmed 4-site hydration + merge null-safety + both read paths + hook ordering + privacy; conventions/a11y found + fixed a `<details>` double-marker and a tool-chip duplicate-key risk).
- 00:42 UTC — **curator**: visual pass ran (32 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup/forecast/trends/agent/corrections/accelerate in light + dark wide + the 420px compact layout). App confirmed at a high-polish plateau across themes/widths — no dark-contrast or narrow-clipping regressions; the new flagship Acceleration screen renders 3 evidence-cited Plays cleanly, its per-play confidence + est-saved figures already carry the title+`.sr-only` gloss (verified in `AccelerationScreen.tsx` — not re-flagged), and the demo numbers still reconcile (22% reliable / ~58% committed). Verified D3 (Saved Skills library) is genuinely unbuilt (no `savedSkills`/library view in `apps/desktop/src/`) — kept it open. `## Next` was depleted (only D3 + empty subsection headers); replenished the top `### Interaction & Visual Polish` subsection with 2 source-verified Acceleration items so polish precedes the D3 feature: the `.play-actions { margin-top: 2px }` action rows don't bottom-align across the equal-height `.play-grid` (`styles.css`), and the header prints the reclaimable total twice (subhead bold + `.acceleration-total` pill, `AccelerationScreen.tsx`). Trimmed Done to 10. README: added the shipped Acceleration Engine to Current Features + the Agent `Ask`/`Accelerate` sub-views to App Navigation (drift). NOTES unchanged (no new durable learning — conventions already captured).
- 01:07 UTC — **improve**: Acceleration Play action rows now bottom-align across cards — `apps/desktop/src/styles.css`. Changed `.play-actions` from `margin-top: 2px` to `margin-top: auto` (+ `padding-top: 2px` to keep the prior breathing gap) so the Save/Dismiss row pins to the card foot. It's the last child of the flex-column `.play-card`, so in the `align-items:stretch` `.play-grid` the auto top-margin absorbs the free space in shorter cards (e.g. an AUTOMATE card lacking an AI recipe/tools) and the action rows align across the row. Build green; passed self-review.
- 02:07 UTC — **improve**: Acceleration header no longer prints the reclaimable total twice — `apps/desktop/src/components/accelerate/AccelerationScreen.tsx`. The `.screen-subhead` bolded "…could reclaim roughly **{totalSaved} min/week**" while the `.acceleration-total` pill beside it showed the identical "~{totalSaved} min · est. saved / week". Reworded the subhead to "Mined locally from your observed work — no AI, no network. {N} {plays}, each citing the evidence it was derived from; review before you act." — dropping the duplicated figure while keeping the play count and the cites-evidence/review-before-you-act framing; the scannable pill is now the sole home for the total (unchanged). `totalSaved` stays referenced by the pill, so no unused-var break. Build green; passed self-review (pure copy edit — pill + its title/`.sr-only` gloss untouched, singular/plural wording preserved, no logic/a11y/behavior change).
- 03:19 UTC — **improve**: D3 — Saved Skills library — new `apps/desktop/src/components/accelerate/SavedSkillsScreen.tsx` + `App.tsx` + `components/accelerate/AccelerationScreen.tsx` + `components/shell/ScreenRouter.tsx` + `lib/types.ts` + `lib/ui.ts` + `lib/audit.ts` + `lib/dataExport.ts` + `services/localStore.ts` + `hooks/usePersistence.ts` + `services/demoData.ts` + `packages/domain/src/models.ts` + `styles.css`. New `SavedSkill` type snapshots an AUTOMATE play's recipe TEXT (deduped by `signal_id`) into a persisted `savedSkills` store (retained-history recipe: `parseSavedSkills` in both `localStore` read paths, `usePersistence` field+dep, 4-site `App.tsx` hydration incl. `resetLocalData`) so generated skills survive regeneration/re-mining. `saveSkill`/`removeSkill` handlers (save audited via a new `"saved_to_library"` `acceleration_engine`/`derived_only` action; remove = unaudited undo). Play-card recipe `<details>` gains Copy (robust `try/catch`, mirrored "Copied" a11y) + "Save to library" toggle; new `skills` sub-tab screen (add-a-screen recipe; third "Ask | Accelerate | Saved skills" tab) lists saved recipes with per-card Copy/Remove and a header Export → new `serializeSavedSkills` (JSON/CSV) + `downloadTextFile`; `EmptyState` when empty. Demo seeds one AUTOMATE skill. Privacy holds structurally (all stored/exported/audited fields derived, no window titles). Build green; verified via Playwright/demo (light+dark wide + 420px, humanized chip, no console errors); passed self-review (two parallel finders confirmed full wiring + privacy; their one worthwhile finding — Copy falsely announcing success when the clipboard is absent/rejects — fixed in both handlers).
- 04:13 UTC — **improve**: Acceleration play cards now flag AI-authored guidance — `apps/desktop/src/components/accelerate/AccelerationScreen.tsx` + `App.tsx` + `packages/domain/src/models.ts` + `styles.css`. The `accelerationPlays` merge overlays AI-authored `detail`/`recipe`/`recommended_tools` onto a deterministic signal, but the card gave no per-play cue which prose was model-derived vs AI-written (Trust & Verification UX). Added a required `authored: boolean` to `AccelerationPlay`, set `authored: Boolean(match)` in the merge memo (only construction site), and rendered a `Sparkles` "AI-authored" `.play-ai-badge` in the header — gated on `signal.authored`, mirroring the `.play-type-chip` a11y pattern (title + explanation-only `.sr-only` noting the estimate/confidence/evidence stay model-derived), wrapped with the type-chip in a new `.play-header-tags` group so the header keeps its two-child `space-between` layout (only existing `--info`/`--surface-muted`/`--border` tokens). Demo (`generatedPlays: null`) → `authored:false` → badge hidden (the deterministic no-AI showcase). Build green; passed self-review (two independent adversarial reviewers, 0 defects).
- 05:12 UTC — **improve**: Chat interruption load now names the peak reactive HOUR of the peak day — `packages/inference/src/capacity.ts` + `apps/desktop/src/lib/format.ts` + `apps/desktop/src/components/capacity/WeeklyCapacityScreen.tsx`. Intelligence Engine slice extending `analyzeInterruptionLoad` with the time-of-day axis the existing `peak_day`/`calm_day`/`after_hours` signals can't show. New `peak_hour: number | null` computed per-(day,hour): a second `dayHourMessages` Map keyed by `dayIndex * 24 + startHour` (populated in the same `messages > 0` guard as its siblings, reusing the already-computed `startHour`), then scan hours 0–23 of the peak day with strict `>` (ascending → earliest-hour tie-break). Day-SCOPED (the peak day's own peak hour) so "peaked on Monday around 10am" never conflates the week-wide busiest hour with a different day's; `peak_hour` is non-null exactly when `peak_day` is, so it renders inline in the existing `.interruption-peak-note` (no new gate/`<p>`). New `formatHourOfDay(hour)` helper in `lib/format.ts` (12-hour "10am"/"2pm" label; inference returns the raw 0–23 number, UI humanizes). No new persisted/demo field; privacy holds (counts-by-hour, never text). Build green; verified via Playwright/demo ("Reactive load peaked on Monday around 10am — 9 messages across 4 active days", correct spacing, after-hours note unregressed); passed self-review (peak_hour⇔peak_day invariant, no conflation, deterministic tie-break, dropped an unused `peak_hour_message_count` field).

- 06:52 UTC — **curator**: visual pass ran (37 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup/forecast/trends/agent/accelerate/skills/corrections/sensitive × light+dark wide + 420px-narrow). App confirmed at a high-polish plateau across themes/widths (no dark-contrast or narrow-clipping regressions; spot-verified StackedBar per-segment `role="img"`+aria, the sidebar reliable-capacity gauge title+`.sr-only`, the daily `role="progressbar"`, and the corrections narrow flex-wrap are all already solved — did NOT re-flag). `## Next` held ZERO concrete items — replenished a new top `### UI & UX Polish` subsection with 2 source-verified items (Flagged-Captures "Discard" fires `onDiscardInsight` with NO `ConfirmDialog` despite being an irreversible local-storage delete — `SensitiveReviewScreen.tsx`; the Narrative "Copy as Markdown" + Audit "Copy JSON" buttons announce success even when `navigator.clipboard` is absent/rejects, the bug D3 fixed for Acceleration/SavedSkills — `NarrativeScreen.tsx`/`AuditEventRow.tsx`) plus a Trust & Verification item (surface repeated-relabel patterns in the corrections log — `CorrectionsScreen.tsx`). Trimmed Done 16→10. README: added the D3 `Saved skills` Agent sub-tab to App Navigation (drift). NOTES unchanged (clipboard-false-success + ConfirmDialog-for-destructive learnings already captured).
- 06:11 UTC — **improve**: @-mention interruption tile now conveys scale — `packages/inference/src/capacity.ts` + `components/capacity/WeeklyCapacityScreen.tsx`. The four Weekly `.interruption-stat` tiles all showed scale except "direct @-mentions" (a bare count — 5 of 400 messages and 5 of 12 read identically). Added `mention_pct` to `InterruptionLoadAnalysis` (share of reactive messages that were direct @-mentions; floored to 1 when non-zero and capped at 100, mirroring `after_hours_pct`; guarded `messageCount > 0` so a mentions-only 0-message burst can't divide by zero) and enriched the tile's `title` + `.sr-only` with the denominator/share (the deep-work tile's "N of M" convention), gated on `mention_pct > 0` so the "N of 0 (0%)" edge never renders (falls back to the original strings). Metadata-only/privacy-safe; interruptionLoad is live-derived so demo showcases a real %. Build green; passed self-review (adversarial diff: pct arithmetic finite/bounded, gate on `mention_pct > 0` proves both counts > 0, sr-only adds omitted info per the mirror convention).
- 07:08 UTC — **improve**: Discarding a flagged capture now confirms first — `apps/desktop/src/components/audit/SensitiveReviewScreen.tsx`. The Flagged Captures "Discard" button irreversibly removed a visual-context insight from local storage (and appended a `visual_context` audit event via `discardVisualInsight`) with no confirmation — the only irreversible destructive action skipping the shared modal. Gated it behind `components/common/ConfirmDialog.tsx` (the "Reset data" convention): a local `pendingDiscardId` `useState`, a null-safe `pendingInsight` lookup (`flagged.find` by id), the button sets the pending id, and the `tone="danger"` dialog names the app + derived `activity_summary` in the description and restates the audit/irreversibility claim; `onConfirm` discards then clears, `onCancel` clears. Privacy-safe (`activity_summary` is a derived insight already on the card, never a raw window title). Build green; passed self-review (state cleared on both paths, null-safe lookup, audit claim verified against `discardVisualInsight`, ConfirmDialog convention followed).
- 08:08 UTC — **improve**: Copy buttons no longer falsely report success when the clipboard is unavailable — `apps/desktop/src/components/narrative/NarrativeScreen.tsx` + `components/audit/AuditEventRow.tsx` + `components/audit/AuditLogScreen.tsx` + `components/shell/ScreenRouter.tsx`. The Narrative "Copy as Markdown" and Audit "Copy JSON" buttons both called `void navigator.clipboard?.writeText(...)` then unconditionally flipped to the "Copied"/"JSON Copied" state (Narrative also toasted success) — so on an insecure webview where `navigator.clipboard` is undefined, or when the write rejects, they announced success while nothing was copied (UI & UX Polish). Extracted `async handleCopyMarkdown`/`handleCopyJson` mirroring the D3 `AccelerationScreen`/`SavedSkillsScreen` pattern: non-optional `navigator.clipboard.writeText` (a missing clipboard throws into the catch), copied-state + success toast run only after the `await` resolves, and the catch pushes the shared "Couldn't copy to the clipboard" error toast. `AuditEventRow` had no toast channel, so threaded a required `pushToast` prop through `AuditLogScreen` (its only consumer) and the `ScreenRouter` call site. Build green; passed self-review (copied/success state gated on the resolved-await path, error toast on failure, `tone:"error"` valid, `pushToast` threaded to the sole call sites, matches the D3 reference verbatim).
- 09:12 UTC — **improve**: Corrections log now flags systematic (repeated) relabels — `apps/desktop/src/components/review/CorrectionsScreen.tsx` + `apps/desktop/src/styles.css`. The Corrections history rendered every edit as a flat chronological row, so a one-off tweak read the same as the 3× systematic pattern the Forecast bias panel (`analyzeCorrections`) already weighs. Added a `repeatCounts` `useMemo` over ALL corrections (not the search-filtered subset) keyed by `JSON.stringify([field, old_value, new_value])` — the same dedup key the inference uses, ignoring no-op edits — and rows with `repeatCount >= 2` now render a subtle info-tinted `.correction-repeat` "repeated N×" pill inline after the old→new change, with a `title` tooltip + explanation-only `.sr-only` gloss. New CSS uses only existing `--border`/`--surface-muted`/`--info` tokens; no auto-margin so the timestamp keeps its `margin-left:auto` right-alignment. Build green; passed self-review (count over unfiltered corrections, JSON-triple key matches inference semantics, `>= 2` gate = "recurs", sr-only explanation-only, theme-complete tokens, layout preserved).
- 10:14 UTC — **improve**: Empty forecast categories now show "None identified" instead of a blank list — `apps/desktop/src/components/common/ForecastList.tsx` + `apps/desktop/src/styles.css`. Trust & Verification UX slice, self-identified after the Next backlog ran dry (all theme-header work is complete in code). The four `ForecastList` tiles (Constraints / Risk flags / Recommended actions / Assumptions, rendered from `ForecastAgentPanel.tsx`) rendered an unconditional `<ul>`, so a live AI forecast returning `[]` for a category (reachable — a clean week has no risk flags; the domain type is plain `string[]`) showed a titled-but-blank list that reads as a semantically-empty "list, 0 items" to screen readers and a data gap to sighted users. Gated the list on `items.length > 0` and render a muted `.forecast-list-empty` "None identified" `<p>` otherwise (placeholder, not `null`, so the 4-cell grid stays stable) — the accurate positive signal. New CSS uses only the existing `--text-muted` token (all three theme blocks), 13px to match `.forecast-list li`, italic as a soft absence. Build green; passed self-review (high effort, 0 findings: `items` is a required non-null `string[]` so the guard is safe, truthy branch preserves list+keys verbatim, only the 4 `ForecastAgentPanel` callers and signature unchanged, shared-component fix = right altitude, demo arrays non-empty so demo unchanged).
- 11:08 UTC — **improve**: Memoized the LedgerScreen render-hot derivations — `apps/desktop/src/components/ledger/LedgerScreen.tsx`. The screen recomputed four non-trivial derivations in the function body on every render — `classifiedSessionIds` (a `new Set(blocks.flatMap(...))`), `unclassifiedSessionCount` (a full `activeWindowSessions.filter`), `visibleBlocks` (the 4-field lowercase search filter over all blocks), and `current` (a `blocks.reduce` latest-by-time scan) — so a search keystroke rebuilt the Set + re-ran the session filter + reduce even though only `visibleBlocks` depends on `searchQuery` (and vice-versa). Wrapped each in a `useMemo` with minimal deps (`classifiedSessionIds`/`current` → `[blocks]`; `unclassifiedSessionCount` → `[activeWindowSessions, classifiedSessionIds]`, the memoized Set being referentially stable; `visibleBlocks` → `[blocks, searchQuery]` with the lowercased needle computed inside), mirroring the `useDerived.ts` convention. Pure refactor — logic copied verbatim so output is byte-identical (incl. `current === undefined` on empty blocks and the empty-query short-circuit returning the original `blocks` reference); only per-keystroke allocations drop. Build green; passed self-review (high effort, 0 findings: all four `useMemo` calls unconditional/top-level before any return so rules-of-hooks hold; each dep set covers exactly the free vars read; no behavior change).
- 11:52 UTC — **improve**: Guard `ConfidenceChip` against a NaN/Infinity confidence — `apps/desktop/src/components/common/ConfidenceChip.tsx`. The chip special-cased `value === 0` → "Unscored" but let a `NaN`/`±Infinity` value fall through: `Math.round(NaN * 100)` is `NaN`, so it rendered "**Needs review NaN%**" (NaN fails every `>=` → the `Needs review`/`low` branch). `WorkBlock.confidence` is a plain `number` with no non-NaN guarantee (a malformed import or a divide-in-derivation could produce it), and this chip renders per-block across the ledger/daily surfaces. Extended the early return to `if (value === 0 || !Number.isFinite(value))` → the existing "Unscored" chip, so a missing/garbage score reads as an honest absence rather than "NaN%" (also catches `±Infinity`). One-line change, `ConfidenceChip.tsx` only. Build green; passed self-review (`Number.isFinite` is the correct total guard; the truthy path is unchanged for every finite non-zero value so all real scores render exactly as before; no other call site touched).
- 12:42 UTC — **curator**: visual pass ran (30 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup/forecast/trends/agent/accelerate/skills/corrections/sensitive × light+dark wide + 420px-narrow on daily/weekly/ledger/accelerate). App confirmed at a high-polish plateau across themes/widths — no dark-contrast, clipping, or reflow regressions; demo numbers reconcile (22% reliable / ~58% committed / 32% planned / 21% reactive across Weekly/sidebar/Agent/narrative/forecast); the corrections "repeated N×" pills, Acceleration 3-play grid, audit badges, and Flagged-capture discard all render cleanly. `## Next` was well-stocked (no replenishment needed) but mis-ordered for the polish-first mandate: promoted `### UI & UX Polish` above `### Performance & Robustness` (a UX correctness bug — the Agent "Copy response" clipboard-lie, verified still open at `AgentScreen.tsx:522` — and a11y wins outrank a no-visible-change `slice(-N)` refactor), and reordered the subsection by user impact (Agent copy → SavedSkills Remove aria-label → Trends non-visual data path → BlockCard guard → ActivityHeatmap consolidation). Reconciled all 5 UI items against code (all genuinely open) and fixed stale line refs (SavedSkills Remove ~95→~125; BlockCard helpers ~25/~30; ActivityHeatmap ~22–45); corrected the DRY-slice item (2 live sites, not 3). Trimmed Done 16→10. README checked against `package.json` + screens — no drift. NOTES unchanged (all conventions already captured).
- 13:07 UTC — **improve**: Agent "Copy response" no longer falsely reports success when the clipboard is unavailable — `apps/desktop/src/components/agent/AgentScreen.tsx`. The last holdout of the clipboard-lie pattern D3 fixed everywhere else: `copyMessage` did `await navigator.clipboard.writeText(...)` then unconditionally set the "Copied" check + toasted success, invoked via `void copyMessage(m)` — so on an insecure webview where `navigator.clipboard` is undefined, or when the write rejects, the promise rejected unhandled while the UI showed "Copied" and toasted success. Wrapped the write in try/catch (non-optional `writeText` so a missing clipboard throws into the catch): `setCopiedMessageId` + the success toast run only after the `await` resolves, and the catch pushes the shared `{ tone: "error", message: "Couldn't copy to the clipboard" }` toast (matching the `NarrativeScreen`/`SavedSkillsScreen`/`AccelerationScreen`/`AuditEventRow` copy verbatim). The `void` call site can no longer reject unhandled. Build green; passed self-review (mirrors the D3 reference verbatim, success state gated behind the resolved await, `pushToast` in scope, no other behavior touched — clipboard-lie now fully eradicated app-wide).
- 13:52 UTC — **improve**: Name the SavedSkills "Remove" button for assistive tech — `apps/desktop/src/components/accelerate/SavedSkillsScreen.tsx`. The Remove button had only a generic `title` ("Remove this skill from your library") + visible "Remove" text, so across a grid of saved cards a screen reader announced identical ambiguous "Remove" actions with no indication of *which* skill. Added `aria-label={`Remove ${skill.title} from your library`}` mirroring the sibling Copy/SKILL.md buttons' descriptive-label convention, so each Remove action is self-identifying. `skill.title` is a required `SavedSkill` field already interpolated into the `remove()` info toast, so the reference is safe. One-line additive change, `SavedSkillsScreen.tsx` only. Build green; passed self-review (additive `aria-label` only, no behavior change, `skill.title` non-null, no other call site or attribute touched).
- 14:38 UTC — **improve**: Give the Trends chart a non-visual data path — `apps/desktop/src/components/capacity/TrendsScreen.tsx`. The chart exposed per-point values only through hover-only SVG `<title>` tooltips and mouse-driven legend/line isolation, so keyboard/screen-reader users could read the top-level `aria-label` but reach no actual week/series value (UI & UX Polish a11y). Added a visually-hidden (`.sr-only`) `<table>` — a `useId`-keyed caption, a `scope="col"` header per `SERIES` label, and one `scope="row"` week row per in-view week with cells rendered via `pct(Math.round(week.snapshot[series.key]))` (byte-identical to the existing dot-title format), reusing `formatIsoWeekLabel` + `pct` — linked via `aria-describedby={dataTableId}` on the `<svg>`. Purely additive: the table is `.sr-only` (already defined in every theme, no CSS change) so no visual change, and both SR navigation modes work (aria-describedby from the chart + independent table landmark with column/row headers). `useId` sits above the memos and the early return so it's called on both return paths (rules-of-hooks safe); this branch only renders when `weeks.length >= 2` so the table is never empty. Build green; passed self-review (values mirror the chart exactly, hooks ordered correctly, correct table header scopes, no behavior/visual change).
- 16:07 UTC — **improve**: Extract + guard the BlockCard local-time helpers — `apps/desktop/src/components/ledger/BlockCard.tsx` + `apps/desktop/src/lib/format.ts`. Moved the file-local `toLocalTimeInput`/`applyLocalTime` (ISO ↔ "HH:MM") into `lib/format.ts` (beside the sibling time helpers) and imported them, and hardened `applyLocalTime` with a `Number.isFinite(hours) && Number.isFinite(minutes)` guard returning the original ISO unchanged — a malformed `hhmm` yielded NaN and `d.setHours(NaN, NaN).toISOString()` throws. No behavior change for valid inputs (the sole call site passes validated `<input type="time">` values). Build green; passed self-review.
- 17:08 UTC — **improve**: Consolidate the ActivityHeatmap hour/day label helpers into `lib/format.ts` — `apps/desktop/src/lib/format.ts` + `apps/desktop/src/components/ledger/ActivityHeatmap.tsx`. The heatmap carried four private formatters (`getDayLabel`/`getDayFullLabel`, `formatHour`/`formatHourA11y`) duplicating time-of-day + relative-day logic. Added three sibling exports beside `formatHourOfDay`: `formatHourCompact` ("12a/2p"), `formatHourA11y` ("12 am/2 pm"), and `formatRelativeDayLabel(diffDays, { long })` (Today/Yest.↔Yesterday/short↔long weekday), then imported them and deleted the locals. All three adopt `formatHourOfDay`'s `((Math.round(h) % 24) + 24) % 24` normalization (vs the old bare `h % 24`), byte-identical for every real call site (integers 0–24) while hardening against negative/fractional inputs. Build green; passed self-review (output identical for all rendered labels re-read before/after, no name collision, pure de-duplication with no behavior change).
- 18:03 UTC — **improve**: DRY the `slice(-N)` rolling-window logic in `capacity.ts` — `packages/inference/src/capacity.ts`. `summarizeForecastAccuracy` (`.slice(-ACCURACY_TREND_WINDOW_WEEKS)`) and `computeCapacityBaselines` (`.slice(-BASELINE_WINDOW_WEEKS)`) both repeated the "take the last N of a chronologically-sorted array" pattern. Extracted a generic pure `sliceLastN<T>(items, n)` beside `clamp` (matching the `roundPct`/`clamp` helper style) with a `n <= 0` guard returning `[]`, and routed both sites through it, keeping each named `*_WINDOW_WEEKS` const as the argument so the window semantics stay self-documenting. Pure refactor — both consts are positive (8, 6) so it's byte-identical to the old `slice(-N)`; the guard is a correct latent-bug hardening (`array.slice(-0)` is `slice(0)`, returning the whole array not an empty tail). Array-copy-before-sort preserved; sort/reduce/median logic untouched. Build green; passed self-review (both call sites re-read, identical output, generic `<T>` fits both window shapes, no behavior change).
- 18:44 UTC — **curator**: visual pass ran (39 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup/forecast/trends/agent/accelerate/skills/corrections/sensitive × light+dark wide + 420px-narrow). App confirmed at a high-polish plateau across themes/widths — no dark-contrast, clipping, or reflow regressions; demo numbers reconcile (22% reliable / ~58% committed / 32% planned / 21% reactive) and the Acceleration 3-play grid, corrections "repeated N×" pills, forecast bias panel, and audit badges all render cleanly. Reconciled the top `## Next` items against code: `work_mode_allocation` is computed (`capacity.ts:153`) but unrendered in `WeeklyCapacityScreen`, `mean_signed_error_pts`/`fragmentation_penalty_pct`/`concentration_pct` absent, and Trends `SERIES` omits `meeting_pct` — all 7 Insight & UX Enhancements items genuinely open with correct file refs; confirmed the top pick (work-mode allocation split, a strong visible Weekly addition) is already polish-optimal so left the ordering intact. Backlog healthy (12 open items) — no replenishment needed. Candidate polish gaps checked (Weekly hero MetricCard title/sr-only, Acceleration play confidence gloss) already solved — not re-flagged. Trimmed Done 15→10. README/NOTES no drift.
- 19:09 UTC — **improve**: Surface the work-mode allocation split on the Weekly screen — `packages/domain/src/taxonomy.ts` + `apps/desktop/src/components/capacity/WeeklyCapacityScreen.tsx` + `apps/desktop/src/styles.css`. `computeWeeklyCapacitySnapshot` already returns `work_mode_allocation` but only `category_allocation` was rendered, discarding the Deep-work/Reactive/Collaborative/Fragmented/Blocked split each week. Added a `modeColors: Record<WorkMode,string>` map (mirroring `categoryColors`, reusing the same hues) and rendered a second "How the week fragmented" allocation block inside the capacity-model section using the same `.allocation-grid`/`.allocation-row`/`.dot` markup + `pct()`, with a `modeUnallocatedPct` memo for the buffer row and a scoped `.mode-allocation-grid .allocation-row { cursor: default }` (rows are non-interactive — no StackedBar hover pairing) + `.mode-split-title` spacing (tokens only). Build green; passed self-review (empty-week behavior mirrors the category grid, `modeColors[item.label]` type-checks, additive with no existing behavior touched).
- 20:10 UTC — **improve**: Expose the fragmentation & WIP point-cost baked into committed load — `packages/domain/src/models.ts` + `packages/inference/src/capacity.ts` + `apps/desktop/src/components/common/RiskRow.tsx` + `apps/desktop/src/components/capacity/WeeklyCapacityScreen.tsx` + `apps/desktop/src/styles.css` + `apps/desktop/src/services/demoData.ts`. The "Delivery risk modifiers" rows showed `context_switch_score`/`wip_load_score` as bare "/100" indices, but the model already folds concrete penalties (`contextSwitchScore × 12`, `wipLoadScore × 10`) into `committed_utilization_pct` then discarded those point-contributions. Added `fragmentation_penalty_pct`/`wip_penalty_pct` to `WeeklyCapacitySnapshot` (returned from the already-computed penalties), a `caption?: string` slot on the shared `RiskRow` (full-width `.risk-caption` p, `grid-column: 1 / -1`, `--text-muted` token), and a "Costs ~N% of your committed week" line under each row gated on `penalty > 0`. Demo seed gains both fields (5/5). Build green; passed self-review (fields read only off the freshly-computed `snapshot`, never persisted history/`BASELINE_METRICS`; empty-week penalties 0 → caption suppressed; additive, no existing behavior touched).
- 21:09 UTC — **improve**: Show forecast bias direction, not just magnitude — `packages/inference/src/capacity.ts` + `apps/desktop/src/lib/format.ts` + `apps/desktop/src/components/capacity/ForecastAgentPanel.tsx` + `apps/desktop/src/components/capacity/TrendsScreen.tsx`. `summarizeForecastAccuracy` rolled up only `mean_abs_error_pts`, discarding whether the model systematically over- or under-predicts even though `scoreForecastAccuracy` already returns `signed_error_pts` (predicted − actual; positive = over-predicted). Added `mean_signed_error_pts` to `ForecastAccuracyTrend`, computed in a single-pass reduce over the same rolling window (reusing `scoreForecastAccuracy`), plus a `forecastBiasPhrase(meanSigned)` humanizer that returns "tends to over-/under-predict by ~N pts" or "" when the rounded bias is 0. Surfaced on both accuracy lines (`ForecastAgentPanel`/`TrendsScreen`) as a self-correcting cue. Build green; passed self-review (only `summarizeForecastAccuracy` constructs the trend so no other literal needs the new field; sign convention verified; near-zero/mixed-error weeks suppress the phrase; additive, existing ±magnitude copy unchanged).
- 22:09 UTC — **improve**: Add meeting density as a Trends series — `apps/desktop/src/components/capacity/TrendsScreen.tsx` + `apps/desktop/src/styles.css`. The Trends chart tracked four series (allocated / reactive / deep-work / reliable) but `meeting_pct` — retained in every snapshot and already surfaced as a per-week RiskRow + baseline chip — was never trended, so silent meeting creep was invisible over time. Added `meeting_pct` as a fifth `SERIES` entry ("Meeting density", `betterWhen: "lower"`, `className: "trend-meeting"`); the legend, SVG polyline/dots, and sr-only data table all iterate `SERIES`, so the new series flows into all three in lockstep. Added a `.trend-meeting` CSS class (amber `#f59e0b`, distinct from the four existing hues) following the documented data-layer raw-hex palette exception the sibling `trend-*` classes use, plus subhead/empty-state copy updates. Build green; passed self-review (`meeting_pct` is a non-nullable numeric snapshot key; `.trend-legend` is `flex-wrap: wrap` so the fifth item wraps cleanly; additive, no existing behavior touched).
- 23:14 UTC — **improve**: Reactive-load concentration index in the interruption panel — `packages/inference/src/capacity.ts` + `apps/desktop/src/components/capacity/WeeklyCapacityScreen.tsx`. `analyzeInterruptionLoad` knew `active_day_count`/`peak_day`/`peak_day_message_count` but never expressed how *concentrated* the reactive load was — a heavy 2-of-5-day cluster is batchable, an even spread endemic. Added `concentration_pct` (share of total reactive volume in the busiest `min(2, active days)`, from sorting the existing `dayMessages` values descending — no new inputs; `sum(dayMessages)===messageCount` so the share can't exceed 100), `concentration_day_count`, and a `concentration_is_clustered` boolean, surfaced as a gated interruption note ("N% of your reactive load clustered into your busiest 2 days — batching it into set windows could reclaim focus"). Per the self-review altitude finding, the notability decision (top days lead an even spread by ≥`CONCENTRATION_MARGIN_PCT`=15 pts AND quieter days remain → forces ≥3 active days) lives in `capacity.ts`; the view just reads the boolean, mirroring the mention/after-hours floors. Metadata-only, privacy holds structurally. Build green; passed self-review (finders found no correctness bugs — invariant verified, no div-by-zero, flat/≤2-day weeks excluded; applied cleanup findings by moving the threshold into inference, dropping a pass-through memo, and rewording the CTA so it doesn't duplicate the peak-day note).

## 2026-07-02

- 00:18 UTC — **improve**: Undo the last correction from the Daily Review — `apps/desktop/src/App.tsx` + `apps/desktop/src/components/review/DailyReviewScreen.tsx` + `apps/desktop/src/components/shell/ScreenRouter.tsx` + `apps/desktop/src/hooks/useBlocksLedger.ts` + `apps/desktop/src/styles.css`. The review flow recorded relabels as corrections but offered no quick reversal. Added an "Undo last correction" affordance (a subtle `.review-undo-button` with an `Undo2` icon + visible label, shown in both the review-queue and all-confirmed states) wired to a new `onUndoLastCorrection` handler + `canUndoLastCorrection` flag in `App.tsx`; the handler replays the inverse relabel through the existing `updateBlock` path, so the reversal is recorded as a fresh (inverse) correction + audit event — non-destructive and redoable. Toast humanized via `fieldLabel`/`humanizeCorrectionValue`. Self-review hardening: scoped undo to single-field MANUAL relabels (gated on the exported `MANUAL_REVIEW_ADJUSTMENT_REASON`) so the multi-correction Review Copilot bulk apply can not be half-undone with a misleading singular toast; dropped `start_time`/`end_time` from the undoable set (a time edit records a start+end pair); reused `getLocalDateKey` for the today check; and gated on the target block still holding the changed value so an AI reclassification that already moved it back can not produce a lying "Reverted…" toast. Build green; passed self-review (two finder agents surfaced the bulk-apply partial-undo, the time half-undo, the no-op toast lie, and the getLocalDateKey reuse — all fixed).
- 01:13 UTC — **improve**: Show an import delta when re-importing an Outlook `.ics` — `apps/desktop/src/App.tsx` + `apps/desktop/src/lib/audit.ts` + `apps/desktop/src/components/shell/ScreenRouter.tsx` + `apps/desktop/src/components/settings/SetupScreen.tsx` + `apps/desktop/src/styles.css`. Re-importing a calendar read as a silent no-op (Settings showed only "Imported {timestamp}", the toast a flat count). `importOutlookIcs` now diffs the parsed events against the stored `calendarEvents` BEFORE the merge — added / updated / unchanged, via a module-level `calendarEventChanged(prior,next)` that compares title/start/end/location/organizer/attendee_count and ignores `imported_at` (stamped fresh each parse). The success toast now reads "12 events imported (+3 new, 1 updated, 8 unchanged)" on re-imports (gated on `previousEventCount > 0`; empty parts omitted so an identical re-import reads "…(12 unchanged)"), a session-only `lastCalendarImportSummary` surfaces the delta as a lingering `.import-delta` Settings line, and the `calendar_import` audit event is enriched with `added/updated/unchanged/previous_event_count` via a new `createCalendarImportAuditEvent` helper (mirrors `createChatImportAuditEvent`; summary byte-identical). No "removed" count — the merge is an UPSERT that never drops stored events, so a "−N removed" figure would be a lie (omitted deliberately, documented). Build green; passed self-review (delta computed pre-merge from closure `calendarEvents`, privacy holds — counts/ids only, no titles; AuditLogScreen renders `details` generically; additive elsewhere).
- 02:00 UTC — **curator**: visual pass ran (Playwright/demo — daily/weekly/narrative/ledger/audit/setup/accelerate/forecast/trends/agent/corrections/sensitive × light+dark wide + 420px-narrow, PLUS the true no-demo first-run empty Weekly/Daily). App confirmed at a high-polish plateau — no dark-contrast, clipping, or reflow regressions; demo numbers reconcile (22% reliable / 58% committed / 32% planned / 21% reactive); aria-pressed on all filter chips/sub-tabs, sr-only glosses, skeletons, ConfirmDialog, and empty-state CTAs all present; agent quick-prompt cards are real `<button>`s. `## Next` held only the E1–E5 Acceleration-v2 feature items (UI & UX Polish subsection was EMPTY), so per the polish-first mandate replenished the top subsection with 2 source-verified items: the empty/first-run Weekly "Summary confidence" renders a bare `--` that reads as a stray double-hyphen (`WeeklyCapacityScreen.tsx:149`), and the block-field relabel `<select>`s carry generic `aria-label`s with no per-block context (`BlockCard.tsx:159/167/175`, the ambiguous-repeated-control gap already fixed for SavedSkills Remove / Review Copilot). Trimmed Done 15→10 and CHANGELOG to 2 days. NOTES: added the repeated-control per-instance aria convention. README checked against `package.json` + screens — no drift.
- 08:12 UTC — **improve**: Empty-week "Summary confidence" no longer renders a bare `--` — `apps/desktop/src/components/capacity/WeeklyCapacityScreen.tsx` + `apps/desktop/src/styles.css`. On the empty/first-run Weekly view the no-data summary-confidence showed `<strong>--</strong>`, which at the 28px `.summary-score` font read as a stray double-hyphen rather than an intentional "no estimate yet" placeholder — one of the first things a new analyst sees. Replaced with a muted em-dash "—" via a new `.summary-score strong.summary-score-empty` rule (`color: var(--text-muted)`; the (0,2,1) selector beats the base `.summary-score strong` `--text` rule in both themes and the responsive override) and marked it `aria-hidden` since it is purely visual; the div `title` + paired `.sr-only` span (both formerly the misleading "How confident the model is…") now read "No capacity estimate yet — confidence appears once work blocks exist" so screen readers get an accurate cue instead of a bare em-dash. Populated branch untouched. Build green; passed self-review (`--text-muted` confirmed light+dark; layout consistency preserved; no double-announcement — visible label + sr-only carry the meaning).
- 08:34 UTC — **improve**: Block-field relabel dropdowns now name their block for screen readers — `apps/desktop/src/components/ledger/BlockCard.tsx`. The three relabel `<select>`s carried generic `aria-label`s ("Work category" / "Planned status" / "Work mode"), so a screen reader listing form controls across the Ledger or Daily Review heard the same three names repeated per block with no cue which one each edited (the ambiguous-repeated-control gap already fixed for SavedSkills Remove / Review Copilot). Folded the block's `project_name` (the card headline) into each `aria-label` (`Work category — ${block.project_name}`, etc.); the visible `.tag-field-label` span and the value-showing `title` are unchanged. Build green; passed self-review (`project_name` is a required string already rendered in the `<h3>`; visible labels stay generic per the convention — only the accessible name gets the identifier).
- 08:40 UTC — **improve**: E1 — "Acted on" Play state (Acceleration Engine v2 foundation) — `apps/desktop/src/services/localStore.ts` + `apps/desktop/src/hooks/usePersistence.ts` + `apps/desktop/src/App.tsx` + `apps/desktop/src/components/accelerate/AccelerationScreen.tsx` + `apps/desktop/src/components/shell/ScreenRouter.tsx` + `apps/desktop/src/lib/audit.ts` + `apps/desktop/src/services/demoData.ts` + `apps/desktop/src/styles.css`. A Play was only save/dismiss, so the E3 feedback loop had no record of which signals the user actually acted on. Added a third persisted `actedOnPlayIds: string[]` following the C3 persist-user-actions recipe end-to-end (`PersistedAppState` field + `parseStringIdList` in both read paths, `PersistableState` field + effect dep, all four `App.tsx` hydration sites, `actedOnPlayIds: []` in the demo seed), an `App.tsx` `markPlayActedOn(signal)` handler that audits the discrete action via `createAccelerationPlayAuditEvent` (action union extended with `"acted_on"`; `derived_only`, `window_titles:false`; `acceleration_engine` is a pre-existing AuditEventType so no new badge/filter recipe) plus an unaudited `unmarkPlayActedOn(id)` undo, and an "I acted on this" toggle on the play card (`aria-pressed`, `CircleCheck`↔`CheckCircle2` icon swap mirrored into label + `title`, `.play-action-acted.is-acted` in `--info` blue, `flex-wrap` on `.play-actions`). Privacy holds structurally (ids only). Build green; passed self-review (two finder agents confirmed no other `PersistedAppState` literal omits the field, the write path spreads+deps it, handlers structurally match save/unsave, and the toggle a11y/CSS/wrap are correct).
- 08:52 UTC — **improve**: E2 — Weekly acceleration snapshot + cross-week recurrence (Acceleration Engine v2) — `packages/domain/src/models.ts` + `apps/desktop/src/services/localStore.ts` + `apps/desktop/src/hooks/usePersistence.ts` + `apps/desktop/src/App.tsx` + `apps/desktop/src/hooks/useDerived.ts` + `packages/inference/src/accelerate.ts` + `apps/desktop/src/components/accelerate/AccelerationScreen.tsx` + `apps/desktop/src/styles.css` + `apps/desktop/src/services/demoData.ts`. The engine mined every Play from this week alone with no memory. Persisted a per-ISO-week `accelerationHistory: PersistedAccelerationSnapshot[]` (id/type/minutes summary only — never evidence/window titles) following the `snapshotHistory` recipe end-to-end (interfaces + defensive `parseAccelerationHistory` in both read paths, `usePersistence` field+dep, all four `App.tsx` hydration sites, a retention effect mirroring the snapshot one that stores the summary sorted by `signal_id` so the equality guard compares sets not display order, demo seed of 3 prior weeks). A `recurrenceBySignalId` memo in `useDerived` (prior weeks only → no render loop) threads into `buildAccelerationSignals` via a new optional input; the miner tags each signal with a `recurrence_weeks` field + evidence line and the card shows a `.play-recurring-badge` ("Recurring N weeks", `--warning` tokens, `Repeat` icon, `title` + sr-only). Self-review hardening (6 finder angles): the recurrence boost is applied as a TWO-PHASE ranking — deterministic selection/dedup/nested-collapse/cap, then a final recurrence re-sort of the survivors — so recurrence adjusts ordering/emphasis but can never change which signals surface or their displayed minutes (harness-proven identical set + unchanged minutes). Build green; passed self-review.
- 08:55 UTC — **improve**: E3 — Realized-savings track record (Acceleration Engine v2 flagship) — `packages/inference/src/accelerate.ts` + `apps/desktop/src/hooks/useDerived.ts` + `apps/desktop/src/App.tsx` + `apps/desktop/src/components/shell/ScreenRouter.tsx` + `apps/desktop/src/components/accelerate/AccelerationScreen.tsx` + new `apps/desktop/src/components/accelerate/AccelerationTrackRecord.tsx` + `apps/desktop/src/lib/format.ts` + `apps/desktop/src/styles.css` + `apps/desktop/src/services/demoData.ts`. Turns the engine's forward-looking `estimated_minutes_saved_per_week` claims into a proven record. Added pure scoring helpers mirroring the forecast machinery: `scoreRealizedSaving` inverts each detector's estimate back into observed minutes via a type-aware `captureFraction` (`1/SAVINGS_FRACTION` for automate/tool whose estimate is the conservative 25% cut, `1` for technique whose estimate IS the refocus minutes) then rates beat/met/missed against the projection (`max(5, 20%)` tolerance); `buildRealizedSavings` emits one entry per acted-on play per completed week, scoring each week against the following one; plus `summarizeRealizedSavings` + `RealizedSavings{Entry,Summary,Week,Rating}` types. A `useDerived` `realizedSavings`/`realizedSavingsSummary` pair feeds the new `AccelerationTrackRecord` surface (rows humanized via `realizedSavingsRatingLabel`/`accelerationTypeLabel`/`formatIsoWeekLabel`, per-play title from the live plays with a type-label fallback, blue/green/amber beat/met/below chips reusing `--blue/green/amber-soft`). Demo seeds two acted-on plays + tuned `accelerationHistory` estimates so `?demo=1&screen=accelerate` shows a distinct beat/on-track/below trio (verified via a compiled tsc+node harness). Privacy holds structurally (reads only id/type/minutes summaries). Build green; passed self-review (8-angle finder pass → three material fixes: headline mislabeled a cross-week SUM as "min/week" (reworded to a cumulative total + beat/met/below breakdown so a big beat can't mask misses); a signal's ABSENCE next week fabricated false large beats when it merely fell off the capped top-6 (now only present-in-both-weeks transitions are scored); the in-progress current week caused mid-week jitter (now excluded via a `currentWeekId` filter); the time-less-`actedOnPlayIds` limitation documented + copy reframed as correlational).
- 06:47 UTC — **curator**: visual pass ran (Playwright/demo — daily/weekly/narrative/ledger/audit/setup/forecast/trends/agent/accelerate/corrections/sensitive × light+dark wide + 420px-narrow, plus scrolled Weekly interruption panel + Acceleration play-card action rows/track record). App confirmed at a high-polish plateau across themes/widths — no dark-contrast, clipping, or reflow regressions; the E1–E3 Acceleration v2 additions (acted-on toggle, recurrence badges, realized-savings track record) all render cleanly and reconcile (~100 min est / 22% reliable / 58% committed). `## Next` UI & UX Polish subsection was EMPTY (only the E4/E5 Acceleration-v2 feature items were open), so per the polish-first mandate replenished it with 2 source-verified a11y items ahead of the features: BlockCard "Confirm"/"Exclude" (+ time-edit) buttons announce identically across the review list with no per-block name (`BlockCard.tsx:189/193`, while the sibling relabel `<select>`s already carry `— ${project_name}`) — the highest-impact remaining repeated-control instance; and the Acceleration `PlayCard` Save/"I acted on this"/Dismiss (+ recipe Copy/Save-to-library) controls announce identically across the play grid (`AccelerationScreen.tsx:213/223/237`). Verified E4/E5 genuinely open (no `detectMeetingLoad`; `AccelerationMiningInput` takes neither calendar nor interruption load). Trimmed Done 15→10. README checked against `package.json` + screens — no drift. NOTES unchanged (repeated-control per-instance convention already captured).
- 07:21 UTC — **improve**: E4 — Reactive-load Play from the existing interruption analysis (Acceleration Engine v2) — `packages/inference/src/accelerate.ts` + `apps/desktop/src/hooks/useDerived.ts`. The engine ignored the reactive-chat load `capacity.ts` already measures. Added a fourth deterministic detector `detectReactiveLoad` that turns the pre-computed `InterruptionLoadAnalysis` into one `technique` Play — "Batch reactive comms into set windows" (with a focus-guard angle), threaded via a new optional `AccelerationMiningInput.interruptionLoad` field and wired from `useDerived` (the `interruptionLoad` memo already existed one hook above). Self-review (8 finder angles → 3 agents) drove two real fixes: (1) the estimate initially keyed off `active_hours` (chat-burst wall duration), which a line-scan agent showed doesn't track interruption intensity (a 3h/3-message window over-claimed ~36 min and could evict real automate/tool Plays from the top-6 cap) and double-counts overlapping bursts — reworked to the refocus tax of interruptions beyond a couple of set windows, `REFOCUS_MINUTES_PER_SWITCH × max(0, burst_count − REACTIVE_BATCH_WINDOWS)`, reusing the same Mark-et-al.-grounded per-interruption constant the sibling `detectContextSwitchHotspots` uses (also unifying the two technique detectors on one refocus model); (2) a cross-file agent caught the "guard {peak} for focused work" suggestion firing even when the peak fell after-hours (advising the user to protect an 8pm slot for deep work, contradicting the Play's own after-hours-bleed cue) — now gated on the peak being inside core hours while still citing it in evidence at any hour. Verified with a compiled `tsx` harness: demo shows a distinct 4th card (~6 min, others' minutes unchanged); after-hours-peak/1-burst-3h/2-overlapping-burst/null edge cases all hold. Stable `technique-${stableHash("reactive-load")}` id (no hotspot collision) so E2 recurrence + E3 realized-savings follow it (capture fraction 1). Privacy holds structurally (burst/message/mention counts, hours, time-of-day/percentages — never message text; `derived_from: []`). Build green; passed self-review.
- 08:15 UTC — **improve**: E5 — Calendar / meeting time-sink detector (Acceleration Engine v2, biggest coverage win) — `packages/inference/src/accelerate.ts` + `apps/desktop/src/hooks/useDerived.ts` + `apps/desktop/src/services/demoData.ts`. `calendarEvents` were never mined, so meetings — analysts' largest reclaimable sink — were invisible to the engine. Added a fourth deterministic detector `detectMeetingLoad` that mines `OutlookCalendarEvent[]` for recurring low-value meetings (same normalized title ≥ `MIN_RECURRENCES` times → an async-update `technique` Play, estimate = shared conservative `SAVINGS_FRACTION` × series minutes) and the day whose meetings stack back-to-back with no real focus gap (`< 30`min, ≥2 tight transitions → a "protect a focus block on {weekday}" `technique` Play, estimate = shared `REFOCUS_MINUTES_PER_SWITCH` × total tight transitions). Threaded via a new optional `AccelerationMiningInput.calendarEvents` (mirrors E4; existing detectors untouched; aggregator fans `detectMeetingLoad(calendarEvents ?? [])` in) + a week-scoped `weekCalendarEvents` memo in `useDerived`. Stable distinct-seed `technique-` ids (no collision with hotspot/reactive-load) so E2 recurrence + E3 track record follow them; both Plays set `derived_from: []` (aggregate metadata, matching E4). Demo seeds a daily standup Mon–Fri so `?demo=1&screen=accelerate` shows a distinct 5th "Recurring meeting" card (~19 min) — harness-verified across ref hours with the other four detectors' minutes unchanged (standups are calendar events, not blocks, so capacity is untouched) and empty/below-threshold/malformed/blank-title edge cases emitting 0 plays. Privacy holds: counts/durations/recurrence/day-of-week + the meeting title (same class of user-facing label the `tool` detector already sends as `project_name`) — never event bodies, no attendee PII beyond a count. Build green; passed self-review (no findings survived).
- 09:20 UTC — **improve**: Block Confirm / Exclude (and time-range edit) buttons now name their block for screen readers — `apps/desktop/src/components/ledger/BlockCard.tsx`. The core review surface's "Confirm"/"Exclude" action buttons carried only generic visible text and no `aria-label`, and the three time-range edit buttons (Save/Cancel/Edit) carried generic static `aria-label`s, so across the Daily Review queue and the Activity ledger a screen reader listing form controls heard the same names repeated identically per block with no cue which block each acts on (the ambiguous-repeated-control gap already fixed for the sibling relabel `<select>`s). Folded `block.project_name` (the card `<h3>` headline, a required `WorkBlock` string) into each button's `aria-label` (`Confirm — ${project_name}` / `Exclude — …` / `Edit block time range — …` / `Save time changes — …` / `Cancel time edit — …`); visible labels stay generic per the convention, and the Save button's error state remains announced separately via its existing `role="alert"` sr-only span. Build green; passed self-review (only accessible names enriched — no visible text/handler/error-announcement regression; matches the per-instance-accessible-name convention exactly).
- 10:08 UTC — **improve**: Acceleration play-card controls now name their play for screen readers — `apps/desktop/src/components/accelerate/AccelerationScreen.tsx`. The `PlayCard` Save / "I acted on this" / Dismiss action buttons carried only a state `title` + generic visible text with no `aria-label`, and the recipe `<details>` "Save to library" button had none while its sibling Copy button already did — so across the `.play-grid` a screen reader listing controls heard the same action names repeated identically for every play (the ambiguous-repeated-control gap already fixed for BlockCard/SavedSkills). Folded `signal.title` (the required `.play-title` `<h3>` headline) into each button's `aria-label`; for the three toggle buttons the aria-label mirrors the transient state ("Saved"/"Save", "Acted on"/"I acted on this", "In library"/"Save to library") so overriding the visible text doesn't drop the state cue, and the Copy button's copied-state mirror is preserved with the title appended. Visible labels stay generic per the convention. Build green; passed self-review (only accessible names enriched — no visible text/handler/aria-pressed/state-announcement regression).
- 11:15 UTC — **improve**: Weekly hero now explains what makes up the committed-load number — `apps/desktop/src/components/capacity/WeeklyCapacityScreen.tsx` + `apps/desktop/src/styles.css`. The target-utilization model makes `committed_utilization_pct` the center of the reliable-capacity math (`reliable = 80 − committed`) and the hero surfaces "X% already committed", but the user could never see WHAT that committed load comprised. Added a `committedBreakdown` memo + a `.committed-breakdown` section under the hero metrics decomposing committed utilization into its five model inputs — Recurring & fixed, Reactive ×0.72, Carryover risk, Fragmentation, WIP load (each gated `> 0`) — with a plain-language note explaining the 0.72 reactive discount (Mark et al., CHI 2008) and that the parts add up to committed. Exactness is structural: `capacity.ts` computes `committed = round(recurring + carryover + reactive*0.72 + frag + wip)` where every term except reactive is already an integer, so deriving the reactive part as the REMAINDER (`committed − recurring − carryover − frag − wip`) equals `round(reactive*0.72)` exactly and the five parts sum to the displayed committed with zero rounding drift — no `capacity.ts`/demo change (proven over 200k randomized cases). View-layer only; reuses the existing `baseline-chips-label`/`baseline-chip-row`/`baseline-chip` styling (stakeholder-chips precedent) so only a thin wrapper + note are new CSS. Build green; passed self-review (correctness finder: no bugs; cleanup finder's REUSE finding applied — 6 duplicated chip classes collapsed onto the baseline-chip classes).
- 12:13 UTC — **improve**: Audit-log "Copy JSON" buttons now name their event for screen readers — `apps/desktop/src/components/audit/AuditEventRow.tsx`. The per-event Copy JSON button (rendered once per event by `AuditLogScreen`'s `filteredEvents.map`) carried only a generic icon + visible text with no `aria-label`, so across the audit list a screen reader listing controls heard "Copy JSON" repeated identically for every event with no cue which one it acts on (the ambiguous-repeated-control gap already fixed for BlockCard/PlayCard/SavedSkills/SensitiveReview). Folded `event.title` (the required `AuditEvent` headline already rendered in the `<summary>`) into an `aria-label` and mirrored the transient copied state into it (`Copy JSON for ${title}` ↔ `JSON copied for ${title}`) so overriding the visible text with an accessible name doesn't drop the success cue the visible "JSON Copied" swap was carrying — matching the icon-swap/toggle accessible-name convention. Visible text stays generic; only the accessible name got the identifier. Build green; passed self-review (`event.title` is a required non-null string — no null/undefined in the label; no visible-text/handler/icon/copied-timer regression).
- 12:49 UTC — **curator**: visual pass ran (33 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup/forecast/trends/agent/accelerate/skills/corrections/sensitive × light+dark wide + 420px-narrow, plus scrolled Weekly interruption panel + Forecast agent). App confirmed at a high-polish plateau across themes/widths — no dark-contrast, clipping, or reflow regressions; demo numbers reconcile (22% reliable / 58% committed / 32% planned / 21% reactive), and the E1–E5 Acceleration v2 surfaces, forecast bias/track-record panels, corrections "repeated N×" pills, and `role="meter"` risk bars all render cleanly. `## Next` held ZERO concrete checkbox items (every subsection was prose-only) — replenished the top `### UI & UX Polish` subsection with 3 source-verified items: the Agent "Clear" chat wipes the saved conversation with NO `ConfirmDialog` — the only destructive action skipping the shared modal (`AgentScreen.tsx:503/565`); the Weekly "Delivery risk modifiers" panel exposes the 0–100 scale direction only in per-row hover tooltips, no at-a-glance "higher = worse" cue (`WeeklyCapacityScreen.tsx:372–419`); and the capacity category-isolation dim is mouse-only with no keyboard/focus path (`WeeklyCapacityScreen.tsx:309–329` + `StackedBar.tsx`). Verified candidates already solved and did NOT re-flag: the "80% knee" MetricCard already carries a plain-language `title` gloss, RiskRow is a full `role="meter"`, the daily progress bar is `role="progressbar"`, and the coming-soon calendar/chat connect buttons already have `aria-label`+`title`. Trimmed Done 16→10 and CHANGELOG to the 2-day window. README checked against `package.json` scripts + screens — no drift. NOTES unchanged (ConfirmDialog-for-destructive and mouse-only-needs-keyboard conventions already captured).
- 13:08 UTC — **improve**: Agent "Clear" chat now confirms before wiping the saved conversation — `apps/desktop/src/components/agent/AgentScreen.tsx`. The header "Clear" button called `clearChat()` directly, resetting all messages AND removing `CHAT_STORAGE_KEY` from `localStorage` the instant it was clicked — the only destructive/irreversible action in the app that bypassed the shared `ConfirmDialog`. Gated it behind a `tone="danger"` `ConfirmDialog` (default tone) via a local `confirmingClear` `useState`, mirroring the CorrectionsScreen reset pattern: the button sets the flag, the dialog `onConfirm` closes it then runs the wipe, `onCancel` clears the flag; the description names the irreversibility ("This clears this saved conversation from your device. It can't be undone."). Reused the shared modal (a11y focus-trap/Esc/overlay-cancel/focus-restore built in) — no new markup or CSS. Build green; passed self-review (clearChat empties `messages` which hides the trigger as before; focus-restore onto the unmounted trigger is a harmless no-op; dialog default tone is already `danger`).
- 14:09 UTC — **improve**: Weekly "Delivery risk modifiers" panel now shows the 0–100 scale direction at a glance — `apps/desktop/src/components/capacity/WeeklyCapacityScreen.tsx` + `apps/desktop/src/styles.css`. The five `RiskRow`s (Context switch burden / WIP overload / Carryover risk / Meeting density / Active blockers) carried the "0 = minimal … 100 = very high" meaning ONLY in per-row hover tooltips, so a sighted analyst couldn't tell a higher score is *worse* without hovering each row. Added one muted sub-caption between the `.section-title` and the `.risk-list` — "Higher scores add more delivery risk to next week — lower is better." — reusing the existing muted-note styling by folding a new `.risk-scale-note` selector onto the `.committed-breakdown-note` rule (12px, `--text-subtle`, both themes) plus a thin `-2px 0 10px` margin override, so no tokens/padding are re-declared. Accurate for all five rows (higher = worse on every one), read by SR in document order right after the header. Build green; passed self-review (all five RiskRow directions confirmed higher-is-worse; additive-only, no behavior/layout regression; shared-selector reuse avoids the note/chip duplication a cleanup finder flags).
- 15:08 UTC — **improve**: Weekly capacity category-isolation now works from the keyboard — `apps/desktop/src/components/capacity/WeeklyCapacityScreen.tsx` + `apps/desktop/src/styles.css`. The category `.allocation-row`s dimmed their peers + the paired `StackedBar` segment to spotlight one category on `onMouseEnter`/`onMouseLeave` (`hoveredCategory`), but there was no keyboard path so keyboard users couldn't trigger the same isolation. Mirrored the mouse handlers with `onFocus`/`onBlur` (identical `setHoveredCategory` bodies) + `tabIndex={0}` on the interactive category-map rows only, and added a `.allocation-row-interactive` class carrying a `:focus-visible` box-shadow ring reusing the app's established focus recipe (`0 0 0 2px var(--focus-gap), 0 0 0 4px #006bff` — needed as its own selector because the global focus rule targets only form controls, not divs) + a 4px radius so the ring rounds cleanly with zero layout shift. Non-interactive rows (Unallocated/buffer + every mode-split row) untouched, so they stay out of the tab order; percentages remain SR-readable via the segment `aria-label`s and row text, so this only adds the isolation affordance. Build green; passed self-review (only the category-map rows made focusable; focus/blur bodies mirror the mouse handlers; focus-ring token works light+dark; no layout/behavior/scope regression).
- 16:11 UTC — **improve**: Weekly delivery-risk rows now ground their 0–100 index in a concrete percentage — `apps/desktop/src/components/capacity/WeeklyCapacityScreen.tsx`. The Context-switch and WIP `RiskRow`s already carried a concrete-percentage `caption`, but the Carryover risk (`carryover_risk_pct / 40`) and Meeting density (`meeting_pct / 35`) rows showed only a bare `N/100` index — ungrounded, and the magic 40/35 denominators hid what the score meant. Added a gated `caption` to each mirroring the Context/WIP treatment: Carryover → "~{pct(carryover_risk_pct)} of this week's load is at risk of slipping"; Meeting density → "Meetings fill ~{pct(meeting_pct)} of your tracked week". Both gated `> 0` (a clean week shows no "~0%"), both reading a snapshot field already present (no new snapshot field, no `capacity.ts` change), both reusing the existing `RiskRow.caption` → `.risk-caption` slot (no CSS). When `meeting_pct` exceeds the 35 denominator the index clamps at 100/100 but the caption shows the true % — resolving the hidden-saturation ambiguity. Build green; passed self-review (correctness: `pct` rounds already-integer fields, no NaN; semantics confirmed against `capacity.ts`; redundancy defused — the pre-existing Context/WIP captions already restate committed-breakdown numbers, so contextualizing the index at point of use is established precedent; no behavior/scope/other-row regression).
- 17:14 UTC — **improve**: SetupScreen optional-alert toggles now name their alert for screen readers — `apps/desktop/src/components/settings/SetupScreen.tsx`. The four `OPTIONAL_ALERT_RULES.map` toggle buttons (End-of-day review nudge / Heavy-day-ahead warning / Fragmentation nudge / Weekly summary ready) carried only the bare state verb "Enable"/"Disable" as their accessible name, so a screen reader listing controls heard the same name four times with no cue which alert each toggles — even though every sibling toggle in the same file self-identifies ("Enable Visual Context", "Disable Alerts", "Mute Guardrail"). Folded `rule.label` into a per-instance `aria-label` (`${verb} — ${rule.label}`), mirroring the state verb into the accessible name per the toggle-accessible-name convention so overriding the visible text doesn't drop the Enable/Disable state cue (the "On"/"Off" status `<strong>` still carries state too). Visible text stays generic per the repeated-list convention; only the accessible name got the identifier. Build green; passed self-review (`rule.label` is a const display string and `proactiveAlertSettings[rule.key]` is the same boolean already driving the className + visible text — no NaN/undefined; only the accessible name enriched, no handler/className/visible-text regression; matches the documented BlockCard/PlayCard/AuditEventRow per-instance-accessible-name recipe).
- 18:16 UTC — **improve**: shared `formatDurationMinutes` humanizer for session/observed-time labels — `apps/desktop/src/lib/format.ts` + `apps/desktop/src/components/ledger/ActivityCapturePanel.tsx` + `apps/desktop/src/components/compact/CompactWidget.tsx`. Duration labels were formatted inconsistently: `CompactWidget` hand-rolled `${Math.floor(m/60)}h ${m%60}m` for "Observed today" (a 45-min day read "0h 45m") plus a bare `{duration_minutes} min` for the current session, while `ActivityCapturePanel`'s session list rendered raw `{session.duration_minutes} min` — so a 125-min combined session read "125 min" and a summed/fractional duration could render "12.333 min". Added `formatDurationMinutes(minutes)` to `lib/format.ts` ("45 min" below an hour, "2h 5m" at/above one; rounds to whole minutes and clamps NaN/negatives to 0) and reused it at all three sites (the `> 0 ? … : "--"` guard on "Observed today" preserved). No prior minutes→duration humanizer existed (verified) — consolidates the two ad-hoc formats per the reuse-`lib/format.ts` convention. Build green; passed self-review (all 8 angles, no findings — helper handles NaN/negative/fractional/boundary; the removed `observedHours` const's only behavior change is sub-hour "0h 45m"→"45 min", an intentional consistency win; two callers only, both correct).
- 18:47 UTC — **curator**: visual pass ran (26 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup/accelerate/forecast × light+dark wide + daily/weekly/ledger/accelerate/setup at 420px-narrow). App confirmed at a high-polish plateau across themes/widths — no dark-contrast, clipping, or reflow regressions; demo numbers reconcile (22% reliable / 58% committed / 32% planned / 21% reactive) and the Weekly committed-breakdown chips, delivery-risk captions, Acceleration 5-play grid + realized-savings track record, forecast bias panel, and audit badges all render cleanly. `## Next` was **fully depleted** — every subsection was prose-only with ZERO unchecked items (the hourly loop had consumed the entire UI/UX + E1–E5 backlog). Replenished the top `### UI & UX Polish` subsection with 5 source-verified items (each opened + confirmed genuinely-open against code, ordered by impact): audit detail header prints `event.source` as raw snake_case (`AuditEventRow.tsx:43`, no `sourceLabel()` in `format.ts`); the Live-capture health chip reuses `ConfidenceChip` and shows a wrong "classification confidence" tooltip (`ActivityCapturePanel.tsx:60`); visual-context insight confidence renders bare "N%" with no gloss/NaN-guard (`ActivityCapturePanel.tsx:112`); CompactWidget stat tiles are unlabeled navigation buttons (`CompactWidget.tsx:86`); and ActivityHeatmap defaults to open when empty but collapsed when populated (`ActivityHeatmap.tsx:31/44`). Trimmed Done 16→10. README checked against `package.json` scripts + screens — no drift. NOTES: added a durable "humanize every user-visible enum/source field, including `event.source`" note.
- 19:08 UTC — **improve**: audit detail header now humanizes `event.source` instead of printing raw snake_case — `apps/desktop/src/lib/format.ts` + `apps/desktop/src/components/audit/AuditEventRow.tsx`. The expanded-event detail header rendered `<span>{event.source}</span>` verbatim, so drilling into an audit event surfaced internal identifiers (`review_layer`, `openai_responses_api`, `macos_active_window`, `outlook_ics`, `proactive_alerts`, …) while the badge already routed through `auditTypeLabel` and the privacy pill through `privacyLevelLabel`. Added a `sourceLabel(source)` humanizer — an `AUDIT_SOURCE_LABELS` map covering all 12 `source` values emitted anywhere in the app (grep-verified) plus a Title-Case-from-snake_case fallback for anything unmapped — and routed the header through it. Mirrors the existing `PLANNED_STATUS_LABELS`/`PRIVACY_LABELS` map-with-fallback idiom. Build green; passed self-review (full source-value coverage confirmed by grep; fallback `word ? … : word` guard handles empty/leading/trailing/double-underscore segments without crashing; `source` typed `string` so no null deref; single consumer, no call-site regression).
- 20:08 UTC — **improve**: Live-capture health chip no longer claims "classification confidence" — `apps/desktop/src/components/common/ConfidenceChip.tsx` + `apps/desktop/src/components/ledger/ActivityCapturePanel.tsx`. `ConfidenceChip` hardcoded its tooltip to `` `${pct}% classification confidence` ``, so the Live local capture panel's health chip — which feeds a capture-RELIABILITY heuristic, not any classification — claimed "72% classification confidence". Added an optional `glossLabel` prop defaulting to `"classification"` (so the sole other caller `BlockCard` stays byte-identical), interpolated into the title as `` `${pct}% ${glossLabel} confidence` ``, and passed `glossLabel="capture health"` from the capture panel → "N% capture health confidence". Also lifted the three magic health numbers into named module consts (`CAPTURE_HEALTH_ACTIVE` 0.9 / `CAPTURE_HEALTH_PAUSED` 0.72 / `CAPTURE_HEALTH_ERROR` 0.4). Matches the exact NOTES.md `glossLabel`-default recipe. Build green; passed self-review (default preserves BlockCard's tooltip; consts equal the prior literals; prop optional so no call-site/type break; non-finite/zero "Unscored" path untouched; no other callers).
- 21:08 UTC — **improve**: visual-context insight confidence now carries a gloss + NaN guard — `apps/desktop/src/components/ledger/ActivityCapturePanel.tsx`. Each visual-context insight row rendered `<strong>{Math.round(insight.confidence * 100)}%</strong>` — a bare percentage with no cue what it measures (unlike the Acceleration play confidence's sr-only gloss and `BlockCard`'s `ConfidenceChip`) and no non-finite guard (a `NaN`/`Infinity` confidence would render "NaN%"). Mirrored the Acceleration `play-confidence` pattern in place rather than swapping in `ConfidenceChip` (whose visible "High/Medium/Needs review" pill + classification-tuned thresholds don't fit the compact session-list metric cell): guarded with `Number.isFinite(insight.confidence) ? Math.round(...) : 0` (non-finite → "0%") and added a `.sr-only` " insight confidence" gloss inside the `<strong>` (SR reads "72% insight confidence") plus a sighted `title` hover cue. Visible output for a normal value is byte-identical; only the NaN case and the accessible name/tooltip changed. Build green; passed self-review (`Number.isFinite` handles NaN/Infinity; leading space lives inside the sr-only span so no stray visible whitespace after "72%"; `.sr-only` + sr-only-gloss pattern reused, no new CSS; layout unchanged; single call site).
- 22:08 UTC — **improve**: CompactWidget stat tiles now signal they're navigation buttons — `apps/desktop/src/components/compact/CompactWidget.tsx`. The menu-bar quick-view "Observed today" and "Reliable capacity" `.quick-stats` tiles are real `<button>`s that open the Ledger / Weekly screens, but read as static stat tiles with no cue they navigate. Added a `title` ("Open work ledger" / "Open weekly capacity") for a sighted hover affordance, plus an `aria-label` that PRESERVES the metric+value AND adds the action (`Observed today: {value}. Open work ledger` / `Reliable capacity: {value}. Open weekly capacity`) rather than the bare action string — a bare `aria-label` overrides the button's contents-derived name and would have dropped the announced stat value (self-review-caught a11y regression). The `--` empty state is glossed in the spoken name only ("no activity yet" / "not yet estimated"); the visible `--` is unchanged. Consolidated the two value expressions into `observedTodayValue`/`reliableCapacityValue` consts. Build green; passed self-review (bare-aria-label value-drop caught and fixed; values always strings, no null/NaN; visible output byte-identical; no handler/layout/scope regression).
- 22:24 UTC — **curator**: visual pass ran (70+ Playwright/demo screenshots — daily/weekly/forecast/trends/narrative/ledger/corrections/audit/sensitive/agent/accelerate/skills/setup × light+dark wide, 6 key screens at 420px-narrow, plus scrolled passes over Weekly/Setup/Ledger/Accelerate/Forecast). App holds its high-polish plateau — demo numbers reconcile (22% reliable / 58% committed / 32% planned / 21% reactive), the E1–E5 Acceleration surfaces, committed-breakdown chips, risk captions, and corrections pills all render cleanly, and narrow layouts reflow without clipping. The scrolled pass surfaced a NEW real glitch: the Settings sticky `.screen-header` pins below the `.screen`'s 24px top padding, so scrolled content bleeds through the strip above it in both themes — added as the top UI & UX Polish item (`styles.css:956/6124`), alongside 2 more source-verified items (the native "Copy manager summary" is the last silent clipboard-lie holdout, `App.tsx:712`; `.play-confidence` loses right-alignment when the play-card header wraps, `styles.css:2012`). Verified the existing ActivityHeatmap disclosure item still open (kept, re-ranked 3rd); checked-but-not-flagged: the `.copilot-inline` 760px cap is a deliberate readability max-width, and `auto-fit` correctly collapses the lone-suggestion track. Trimmed Done 14→10; CHANGELOG already within the 2-day window; README checked against `package.json` scripts + screens — no drift (demo/tauri scripts remain intentionally undocumented). NOTES: added the sticky-header-over-padded-scroll-container gotcha.
