# Changelog

## 2026-06-26

- 00:38 UTC — **curator**: visual pass ran (20 screenshots, 6 screens × light/dark wide + 6 compact, plus Forecast & Corrections close-ups); app confirmed highly polished. Moved the DailyReview progressbar-semantics item to Done (verified already present — `DailyReviewScreen.tsx:97` carries `role="progressbar"` + full ARIA). Verified remaining Next items against code (EmptyState ariaLabel, ReviewCopilotPanel per-suggestion labels, `snapshot: any`; fixed stale `models.ts` line ref 206→207). Replenished the thinned UI & UX Polish subsection with 2 screenshot-grounded items: demo Audit log renders a raw `planned -> unplanned` correction (`demoData.ts:211`) while Corrections humanizes it, and the Weekly "Summary confidence 89%" card (`WeeklyCapacityScreen.tsx:116`) is a bare percentage with no explanation. Kept Forecast range-track at top. Extended the raw-enum NOTES entry to cover demo-seed humanization.
- 03:10 UTC — **improve**: Forecast scenario range track — added `.forecast-range` horizontal bar beneath `.forecast-summary` in `ForecastAgentPanel.tsx`; `--info` fill spans conservative→likely, marker tick at "Likely"; labeled row shows all three values; `role="img"` + `aria-label`; token-based CSS in `styles.css` works in light + dark; passed self-review.
- 06:36 UTC — **curator**: visual pass ran (18 screenshots, 6 screens × light/dark wide + 6 compact); app confirmed highly polished across all themes/widths. Verified all 5 Next items still valid against code (demo `planned -> unplanned` enum leak confirmed in audit screenshot; none already shipped); tightened the Weekly Summary-confidence item to cover both `.summary-score` instances + fixed line refs. Added 1 audit-grounded UI/UX item: BlockCard `.block-capacity` "% of week" (`BlockCard.tsx:130-133`) is a bare percentage with no tooltip — add `title` + `.sr-only`. Pruned NOTES: marked ConfidenceChip tooltip/Unscored as SOLVED reference and generalized the bare-percentage-needs-tooltip rule.
- 11:04 UTC — **improve**: Demo Audit log humanized correction value — changed `"planned -> unplanned"` to `"Planned → Unplanned"` in `services/demoData.ts` seed; demo Audit screen now matches the humanized live-correction format; passed self-review.
- 12:38 UTC — **curator**: visual pass ran (18 screenshots, 6 screens × light/dark wide + 6 compact); app confirmed highly polished across themes/widths. Verified all 6 Next items still valid against code (Weekly Summary-confidence + BlockCard %-of-week tooltips, EmptyState/ReviewCopilot aria-labels, `snapshot: any` — none already shipped; line refs accurate). Added 1 audit-grounded a11y item: the three relabel `<select>`s in `BlockCard.tsx` (rendered on both Daily review and Ledger) carry only a value `title`, no field-name `aria-label`, so SR users don't know which field they're editing. Recorded the select-needs-field-name-aria-label convention in NOTES.
- 13:04 UTC — **improve**: Weekly Summary-confidence tooltip — added `title="How confident the model is in this week's capacity estimate"` and `.sr-only` mirror span to both `.summary-score` instances (empty-state and populated) in `WeeklyCapacityScreen.tsx`; hover and screen-reader users now see what the percentage measures; passed self-review.
- 15:05 UTC — **improve**: BlockCard "% of week" tooltip — added `title="Share of this week's modeled capacity this block accounts for"` and `.sr-only` mirror span to the `.block-capacity` div in `BlockCard.tsx`; hover and screen-reader users on both Daily review and Ledger now understand what the percentage measures; passed self-review.
- 16:05 UTC — **improve**: BlockCard relabel selects accessible labels — added `aria-label="Work category"`, `aria-label="Planned status"`, `aria-label="Work mode"` to the three relabel `<select>`s in `BlockCard.tsx`; kept existing `title` for truncation tooltip; screen readers now announce which field is being edited; passed self-review.
- 18:04 UTC — **improve**: EmptyState accessible section labels — added optional `ariaLabel` prop (defaults to `title`) and applied as `aria-label` on the `<section>` in `components/common/EmptyState.tsx`; all 14 existing call sites gain named landmarks automatically; passed self-review.
- 22:06 UTC — **improve**: Demo heatmap grid now renders — added `activeWindowSamples` for yesterday, 2 days ago, and 3 days ago (2 sessions each at realistic working-hours timestamps) in `services/demoData.ts`; `daysWithActivity` rises from 1 to 4, clearing the `>= 2` threshold so the flagship 7-day heatmap grid renders instead of its sparse-data caption in demo mode; passed self-review.

## 2026-06-27

- 00:10 UTC — **improve**: AppShell/CompactWidget snapshot type fix — replaced `snapshot: any` with `WeeklyCapacitySnapshot` (imported from `packages/domain/src/models.ts`) in `AppShell.tsx` and `CompactWidget.tsx`; build passes tsc strict check; passed self-review.
- 00:42 UTC — **curator**: visual pass ran (24 screenshots — 9 screens incl. Agent/Forecast/Corrections × light/dark wide + compact). Reconciled 4 shipped items to Done (all 3 Accessibility items + the `snapshot: any` Code Quality item — verified in code, PRs #58/#59), leaving those subsections empty. Replenished a new **UI & UX Polish** subsection (now top priority) with 3 screenshot-grounded `styles.css`-only items: Agent compact view `display:none`-hides the Outlook metric + 2 of 4 starter cards at 420px; Forecast scenario cards squeeze into two cramped columns on narrow; Agent empty-conversation state floats centered in dead space. Pruned stale "still-open" NOTES lines (Weekly/BlockCard percentage tooltips now SOLVED) and added durable notes on compact-`display:none` content loss, empty-state anchoring, and the already-differentiated audit privacy pills (avoid re-flagging).
- 01:04 UTC — **improve**: Agent compact-view content revealed — removed `display: none` rules hiding the Outlook metric and 2 of 4 starter cards in `@media (max-width: 600px)`; third briefing-metric now spans full-width (`grid-column: 1 / -1` + `border-top`) and all four starters stack via existing single-column grid; `styles.css` only; passed self-review.
- 02:04 UTC — **improve**: Forecast scenario cards stack on narrow screens — added `.forecast-summary` to the `grid-template-columns: 1fr` selector list in the `@media (max-width: 600px)` block; four scenario cards now stack into one readable column at ≤600px instead of two cramped ~190px columns; `styles.css` only; passed self-review.
- 03:03 UTC — **improve**: Forecast scenario cards stack single-column on narrow screens — added `.forecast-summary` to the `grid-template-columns: 1fr` rule in the `@media (max-width: 600px)` block in `styles.css`; four scenario cards no longer squeeze into cramped two-up columns at ≤600px; passed self-review.
- 04:05 UTC — **improve**: Agent empty-conversation starter cards anchored to top — changed `justify-content: center` to `justify-content: flex-start` on `.agent-starters` in `styles.css`; "Start with an outcome" cards now sit just below the briefing card instead of floating in the center of the empty conversation void; passed self-review.
- 04:30 UTC — **improve**: Agent empty-conversation state anchored near top — changed `.agent-starters` from `flex: 1 1 auto; justify-content: center; min-height: 210px` to `flex: 0 0 auto; justify-content: flex-start`; starter cards now sit just below the briefing instead of floating in a centered void; `styles.css` only; passed self-review.
- 06:41 UTC — **curator**: visual pass ran (27 screenshots — 7 screens incl. Agent/Forecast × light/dark wide + narrow-420); app confirmed highly polished, prior compact/forecast/agent fixes verified. `## Next` UI & UX Polish subsection was empty — replenished with 3 screenshot-grounded items: Ledger work-block list buried below the fold under the fixed-height-shell heatmap (make `ActivityHeatmap` collapsible), "Current block" hardcoded `blocks[7]` may mislabel the current block, and the Forecast screen showing 38% vs 24% reliable-capacity without reconciliation. Added a durable NOTES entry on the fixed-height `.screen` shell starving below-the-fold lists.
- 09:08 UTC — **improve**: ActivityHeatmap collapsible — converted `<section>` to `<details className="activity-heatmap">` (closed by default) with `<summary>` replacing the eyebrow `<p>`; trimmed cell rows 7 px → 5 px in `styles.css`; ledger list now immediately visible without scrolling; passed self-review.
- 12:40 UTC — **curator**: visual pass ran (22 screenshots — daily/weekly/forecast/narrative/ledger/audit/setup/agent × light/dark + narrow-420). Both existing UI & UX Polish items reconfirmed (current-block `blocks[7]` mislabel and Forecast 38/24 contradiction — verified in code + screenshots, kept). Replenished the thin (2-item) subsection with 2 new screenshot-grounded items: the 270px-capped `ActivityCapturePanel` starves the `.ledger-list` to ~1.5 blocks at 1280×900 and to zero at 420px (make it a collapsible `<details>`, now top priority), and the Forecast scenario range-bar center label can overlap the end labels at skewed values. Refined the NOTES `.screen`-starvation entry (heatmap now fixed; capture panel is the remaining culprit). Feature sections checked against code — none implemented yet, left intact.
- 14:40 UTC — **workflow**: `/improve` loop switched from per-iteration branch + PR to committing directly to `main` (`.claude/skills/improve/SKILL.md`). Adds a sync-first step, a build + `/verify` gate for UI tasks, atomic revertable commits, rebase-retry on push, and a single-sequential-session guard to avoid the duplicate-work/push races that parallel runs caused.
- 15:07 UTC — **improve**: Forecast reliable-capacity reconciliation — added a `.forecast-baseline-note` line under the scenario summary in `ForecastAgentPanel.tsx` clarifying the AI scenario numbers are refined from the deterministic N% baseline (uses the existing `deterministicReliableCapacity` prop); new muted token-based `.forecast-baseline-note` style in `styles.css`; also pruned 3 stale already-done items from STATUS Next; passed self-review.
- 18:04 UTC — **improve**: Forecast range-bar center label no longer overlaps end labels — the absolutely-positioned "Likely · X%" span in `ForecastAgentPanel.tsx` now renders only when `likelyLeft` is 12–88%, so skewed scenarios no longer collide it with the Conservative/Optimistic edge labels; likely value stays visible via the summary card, marker line, and range `aria-label`; passed self-review.
- 17:03 UTC — **improve**: Forecast range-bar label overlap fixed — added a `showLikelyLabel = likelyLeft > 12 && likelyLeft < 88` guard in `ForecastAgentPanel.tsx` so the absolutely-positioned "Likely · X%" center label is hidden when it would collide with the Conservative/Optimistic end labels; the range marker and summary card keep the value visible; passed self-review.
- 16:04 UTC — **improve**: Forecast scenario range-bar label overlap fixed — in `components/capacity/ForecastAgentPanel.tsx` the centered "Likely · X%" range label now renders only when `likelyLeft` is between 12% and 88%, so it no longer collides with the edge-anchored Conservative/Optimistic labels on skewed scenarios; the value stays visible in the summary cards + range `aria-label`. TSX only; passed self-review.
- 19:08 UTC — **improve**: Multi-week snapshot history store — added persisted `snapshotHistory: PersistedSnapshotRecord[]` (one record per ISO week, latest wins, cap 24) to `services/localStore.ts` (type + `parseSnapshotHistory` guard on both read paths), threaded it through `hooks/usePersistence.ts` and `App.tsx` (state + hydrate + reset + an upsert `useEffect` keyed on the live snapshot), and seeded 3 prior demo weeks in `services/demoData.ts`; unlocks the baseline/trend tasks. Passed self-review.
- 18:40 UTC — **curator**: Visual pass ran (21 screenshots — daily/weekly/forecast/narrative/ledger/audit/setup/agent/corrections × light/dark + 420px narrow); app confirmed highly polished. UI & UX Polish subsection was empty — replenished with 2 screenshot-grounded `ActivityCapturePanel` items: the Ledger "Live local capture" title+subtitle render run-together (bare adjacent spans, no separator) and the collapsed capture panel hides its disclosure marker so it gives no expandable affordance (the heatmap keeps its ▶). Verified all feature items (snapshotHistory, importRawEvents, derived_from drill-down, sensitive-content queue) still genuinely open; trimmed Done to 10; added two durable NOTES entries (stacked label/subtitle, `<details>` affordance); README checked — no drift.
- 20:04 UTC — **improve**: Live-capture panel title no longer runs into its subtitle — wrapped the `<summary>` heading `<div>` in `.capture-panel-heading` (flex column, `min-width: 0`) and gave the subtitle span `.capture-panel-subtitle` (12px, `var(--text-subtle)`) in `ActivityCapturePanel.tsx` + `styles.css`, stacking the bold title above the muted subtitle; passed self-review.
