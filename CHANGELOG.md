# Changelog

## 2026-06-22

- 12:13 UTC — **improve**: Dark mode token audit — fixed `.error-retry` invisible (#fecaca) in light mode, restored dark-appropriate pastel `.bar-track` segment colors overwritten by light mode CSS, fixed `.capture-error`/`.import-error` low contrast in dark mode (`styles.css`)
- 12:41 UTC — **curator**: Visual pass ran (Playwright, all 6 screens × light/dark/compact); reprioritized ## Next around UI/UX polish — surfaced dead Split Block button, clipped capacity-model legend, toolbar icon a11y gaps, unrendered blocker_flag; reconciled narrative-export item (plain Copy Summary already exists) and App.tsx line count (1510); added headless-dark-mode + a11y notes to NOTES.md
- 13:15 UTC — **improve**: Dead "Split Block" button — added `disabled` + `title="Block splitting is coming soon"` to the no-op button in `apps/desktop/src/components/ledger/BlockCard.tsx`; passed self-review
- 14:05 UTC — **improve**: Capacity-model legend unclipped — removed `max-height`/`overflow` caps from `.allocation-grid` and `.capacity-model` in `styles.css` so all 8 category rows render fully; passed self-review
- 15:04 UTC — **improve**: Toolbar icon-button accessibility — added `aria-label` + `aria-pressed` to sidebar-toggle, pause, and window-mode buttons in `apps/desktop/src/components/shell/AppToolbar.tsx`, matching the theme-toggle reference; passed self-review
- 16:07 UTC — **improve**: Blocker flag visual treatment — added red "Blocker" pill badge in `BlockCard.tsx` next to `ConfidenceChip`; added "Active blockers" count row (red fill, red number) to WeeklyCapacityScreen "Delivery risk modifiers"; added `.blocker-badge` + `.risk-blocker-count` token styles in `styles.css` (light + dark); passed self-review
- 17:10 UTC — **improve**: Capacity percentage ring — added `CapacityRing.tsx` (SVG arc, `--info`/`--surface-muted` tokens, 0.4s ease transition); extended `MetricCard` with optional `showRing` prop; wired to the "Reliable new work" hero card in `WeeklyCapacityScreen.tsx`; passed self-review
- 18:05 UTC — **improve**: Downloadable / markdown narrative export — added "Download .txt" button and replaced "Copy Summary" with "Copy as Markdown" (week-label header + `# / ##` Markdown derived from editable manager text) in `apps/desktop/src/components/narrative/NarrativeScreen.tsx`; passed self-review
- 18:38 UTC — **curator**: Visual pass ran (Playwright, all 6 screens × light/dark/compact); reconciled standing ## Next items (App.tsx still 1510 lines, no heatmap, duration edit still read-only — all kept); replenished empty UI & UX Polish subsection with 4 audit-grounded items: label the bare `estimated_capacity_pct` number (BlockCard/LedgerScreen) + swap misleading clock icon, persist theme across reloads (App.tsx clobbers saved theme on mount), disambiguate triple "Pause Tracking" on Settings, and add `title`/width fix for truncated category selects; corrected the headless-dark-mode recipe in NOTES.md (evaluate after mount, not addInitScript) and logged the theme-persistence bug
- 19:15 UTC — **improve**: Label the bare capacity percentage — wrapped `<strong>` in `.block-capacity` with `"of week"` caption in `BlockCard.tsx`; swapped `TimerReset` → `PieChart` and added same caption in `LedgerScreen.tsx` pulse-meter; added `.block-capacity` / `.pulse-meter-val` / `.capacity-caption` CSS using `var(--text-subtle)` token; fixed `.pulse-meter` `align-items: flex-start` for icon-to-number alignment; added `flex-shrink: 0` to guard against flex collapse; passed self-review
- 20:04 UTC — **improve**: Theme preference persisted across reloads — added `themeHydrated` ref + mount hydration effect in `App.tsx`; write-back effect now guards `writeThemePreference` until hydration resolves, preventing the "light" default from clobbering a saved dark preference; passed self-review
- 21:06 UTC — **improve**: Ambiguous "Pause Tracking" on Settings — replaced the duplicate global-pause button on the "Active window activity" source row with a read-only "Active" / "Paused" status badge in `SetupScreen.tsx`; added `.source-status` pill styles (light + dark) in `styles.css`; passed self-review
- 22:04 UTC — **improve**: Long category labels truncate silently in selects — added `title` attribute to all three selects (category, planned_status, mode) in `BlockCard.tsx`; widened category column in `.tag-grid` from `1.35fr` to `1.8fr` in `styles.css`; passed self-review
- 23:07 UTC — **improve**: Block duration edit — added inline time-range editor (hover-reveal clock icon → two `<input type="time">` with Save/Cancel + end-after-start validation) to `BlockCard.tsx`; wires through `onRelabel` for `start_time`/`end_time`; added those fields to `UserCorrection.field` in `models.ts` and their labels in `format.ts`; added `.block-time`/`.time-edit-btn`/`.time-range-editor` styles in `styles.css`; passed self-review

## 2026-06-23

- 00:05 UTC — **improve**: Activity heatmap — added `ActivityHeatmap.tsx` to LedgerScreen showing 7-day × 24-hour session density grid; 5-level `color-mix(--info)` intensity cells with hover tooltips; renders null when no sessions; CSS in `styles.css`; passed self-review
- 00:37 UTC — **curator**: visual pass ran (Playwright, 6 screens × light/dark + compact); ## Next was depleted, replenished UI & UX Polish with 4 grounded items (split week-nav chevrons, heatmap intensity legend, raw snake_case audit privacy pill, orphaned summary-confidence chip); reconciled Code Quality Split-App.tsx item (still valid, App.tsx 1522 lines); added recurring-UX-rough-edges notes to NOTES.md
- 01:05 UTC — **improve**: Week-nav chevrons regrouped — moved both ← → buttons into `.week-nav-controls` div beside the eyebrow label (above `<h1>`) in `WeeklyCapacityScreen.tsx`; added `.week-nav-controls` flex row + `cursor: not-allowed` on disabled next-week chevron in `styles.css`; passed self-review
- 02:05 UTC — **improve**: Activity heatmap intensity legend — added "Less ▢▢▢▢▢ More" row beneath the grid in `ActivityHeatmap.tsx` reusing `.heatmap-cell[data-level]` styles; added `.heatmap-legend` / `.heatmap-legend-label` CSS (flex, right-aligned, `--text-subtle` token) in `styles.css`; also removed spurious `corrections` / `onResetLocalData` props from `DailyReviewScreen` call in `App.tsx` (pre-existing TS error); passed self-review
- 03:05 UTC — **improve**: Audit privacy pill humanised — added `privacyLevelLabel()` + `privacyLevelTooltip()` helpers in `lib/format.ts`; `AuditEventRow.tsx` now renders "Local only" / "Derived only" / "Excluded" with a hover tooltip explaining each scope; color-coded `.audit-privacy--*` pill variants (green/blue/muted) in `styles.css` (light + dark); passed self-review
- 04:06 UTC — **improve**: Summary-confidence chip alignment fixed — moved `.summary-score` from orphaned `header-actions` into a new `.headline-with-score` flex row alongside the `h1` in `WeeklyCapacityScreen.tsx`; chip top-aligns next to the headline; added `.headline-with-score` layout + mobile `flex-direction: column` in `styles.css`; passed self-review
- 05:00 UTC — **improve**: Split App.tsx — extracted 5 async AI ops into `useClassification.ts`, `useReviewCopilot.ts`, `useForecastAgent.ts`, `useNarrativeGeneration.ts`, `useVisualContext.ts`; each hook owns its `useAsyncStatus`, Tauri IPC interfaces, prompt building, and audit events; `App.tsx` reduced from ~1510 to ~870 lines; self-review passed

- 06:38 UTC — **curator**: Visual pass ran (Playwright, all 6 screens × light/dark/compact-420); `## Next` was fully depleted, replenished UI & UX Polish with 4 screenshot-grounded items (unlabeled/mixed-unit Delivery-risk modifiers in `RiskRow`, color-only capacity-model legend with two colliding purples, cryptic "politics-to-math translator" eyebrow, marooned single Review-Copilot suggestion grid) + 1 Code Quality item (App.tsx crept back to 984 lines — extract toolbarActions/screen-routing); added two recurring-UX-rough-edges notes (numeric-scale/units, color-only legend collisions) to NOTES.md
- 07:10 UTC — **improve**: Delivery risk modifiers labelled — added `tooltip`/`hint`/`displayValue`/`dangerActive` props to `RiskRow.tsx`; four index rows now show "/100" scale hint + hover tooltip; Active-blockers converted from inline JSX to `RiskRow` with raw count (no "/100"); `.risk-row` grid column widened from `36px` to `auto`; added `.risk-hint` muted style in `styles.css`; self-review passed
- 08:02 UTC — **improve**: Capacity-model legend color collision fixed + hover crosslink — changed "Blocked / waiting / dependency delay" from `#9333ea` (purple) to `#be185d` (rose) in `taxonomy.ts`; added `hoveredCategory`/`onHoverCategory` props to `StackedBar.tsx` dimming non-hovered segments to 0.3 opacity; legend rows in `WeeklyCapacityScreen.tsx` dim to 0.35 and also drive the bar highlight on hover; each row gains a `title` tooltip; `cursor: pointer` added to `.allocation-row` in `styles.css`; self-review passed
- 09:00 UTC — **improve**: Cryptic section eyebrow replaced — changed "politics-to-math translator" to "where your hours actually went" in `WeeklyCapacityScreen.tsx`; self-review passed
- 10:06 UTC — **improve**: Lone Review-Copilot suggestion marooned fix — switched `.copilot-inline .copilot-list` from `auto-fill, minmax(280px, 1fr)` to `auto-fit, minmax(280px, 360px)` in `styles.css`; phantom empty columns no longer appear with a single suggestion; self-review passed
- 11:14 UTC — **improve**: Split App.tsx screen-routing + toolbar — extracted `toolbarActions` IIFE to `lib/toolbarActions.ts` (`buildToolbarActions` pure fn) and screen-routing JSX to `components/shell/ScreenRouter.tsx`; App.tsx reduced 984 → 828 lines; removed 27 dead imports; pure refactor, self-review passed
- 12:07 UTC — **improve**: BlockCard display polish — added `plannedStatusLabel()` to `lib/format.ts` (maps "planned"→"Planned" etc.); planned-status `<option>` now shows sentence-case labels with explicit `value` preserving raw domain enum; added `title` tooltips to project-name `h3` and stakeholder `p`; added `overflow: hidden; text-overflow: ellipsis; white-space: nowrap` to `.ledger-screen .block-main h3` and `.ledger-screen .block-card p` in `styles.css`; self-review passed
- 12:37 UTC — **curator**: Visual pass ran (Playwright, all 6 screens × light/dark/compact-420); `## Next` was fully depleted — replenished UI & UX Polish with 3 screenshot-grounded items (daily-review header buttons overflow off-screen on narrow because `.review-header-actions` is missing from the ≤600px media query, unlabeled gray remainder in the "100% weekly capacity model" stacked bar, and screen-reader-invisible RiskRow/StackedBar magnitude bars); added narrow-header and stacked-bar-remainder gotchas to NOTES.md
- 13:05 UTC — **improve**: Daily-review header buttons overflow fix — added `.review-header-actions` to the `width: 100%` selector list + added `flex-direction: column` rule inside `@media (max-width: 600px)` in `styles.css`; "Suggest cleanup" and "Confirm all N" now stack full-width on narrow screens; self-review passed
- 14:02 UTC — **improve**: Unlabeled gray remainder in stacked-bar explained — appended explicit `--surface-muted` remainder `<span>` (100 − allocation sum) with `title="Unallocated / buffer: N%"` in `StackedBar.tsx`; added matching "Unallocated / buffer" legend row (muted dot with `--border-strong` outline, `useMemo`-derived `unallocatedPct`) in `WeeklyCapacityScreen.tsx`; self-review passed
- 15:00 UTC — **improve**: Screen-reader accessibility for capacity/risk bars — added `role="meter"` + `aria-label`/`aria-valuenow`/`aria-valuemin`/`aria-valuemax`/`aria-valuetext` to `.risk-track` in `RiskRow.tsx` (index rows announce "N of 100"; blocker row announces "Active blockers: N"); added `role="img"` + `aria-label` to each segment span and the remainder span in `StackedBar.tsx`; self-review passed
- 16:11 UTC — **improve**: LedgerScreen search made functional — wired `useState` + filter in `LedgerScreen.tsx` across `project_name`, `stakeholder_group`, `category`, `mode` using `trim().toLowerCase()`; "No blocks match" EmptyState + "Clear search" CTA added for zero-result state; self-review caught and fixed `q = searchQuery.toLowerCase()` → `trim().toLowerCase()` to prevent space-padded non-matches
- 17:11 UTC — **improve**: ConfidenceChip medium style + block-exclude button style — added `.confidence.medium` yellow rules in all three style blocks (base, Geist-light, `html[data-theme="dark"]`); added `.block-actions button.block-exclude` muted-text base + destructive red hover for light and dark modes; `styles.css` only; self-review passed
- 18:41 UTC — **curator**: Visual pass ran (Playwright, all 6 screens + Agent + Weekly-Forecast + Ledger-Corrections × light/dark/compact-420); `## Next` was fully depleted — replenished UI & UX Polish with 4 screenshot-grounded items (Corrections list leaks raw `planned → unplanned` enum + raw ISO time values, completed onboarding steps struck through reading as "cancelled", heatmap cells invisible to screen readers, 7-day heatmap looks broken when only a day or two has data); extended the raw-enum and intensity-grid rough-edge notes in NOTES.md
- 19:04 UTC — **improve**: Corrections list humanized — added `humanizeCorrectionValue(field, value)` to `lib/format.ts` (routes `planned_status` → `plannedStatusLabel()`, `start_time`/`end_time` → `formatTime()` with NaN guard, all others pass through); applied to display text and `title` tooltip in `CorrectionsScreen.tsx`; self-review passed
- 19:30 UTC — **improve**: Completed onboarding steps no longer struck through — removed `text-decoration: line-through` from `.onboarding-step.is-done > span:last-child` in `styles.css`; steps read as settled (muted color + green check) rather than deleted; self-review passed
- 20:05 UTC — **improve**: Heatmap a11y — added `role="group"` + `aria-label` summary ("7-day activity heatmap, peak N min") to `.heatmap-grid`; each populated cell gets `role="img"` + descriptive `aria-label` via `getDayFullLabel()`/`formatHourA11y()` helpers; empty cells and axis labels marked `aria-hidden="true"`; legend changed to `aria-hidden` (decorative); `ActivityHeatmap.tsx`; self-review passed
- 21:00 UTC — **improve**: Heatmap sparse-data caption — when `daysWithActivity < 2`, render muted "Limited activity so far — the pattern fills in as you keep tracking" caption instead of the broken-looking sparse grid; added `.heatmap-sparse-caption` (`--text-subtle`) in `styles.css`; `ActivityHeatmap.tsx`; self-review passed

## 2026-06-24

- 00:00 UTC — **improve**: LedgerScreen search wired — added `searchQuery` state + case-insensitive filter across project_name/stakeholder_group/category/mode; Escape clears search; "No blocks match" `EmptyState` with "Clear search" CTA when no results; `LedgerScreen.tsx`; self-review passed
- 00:38 UTC — **curator**: visual pass ran (24 screenshots, 6 screens × light/dark × wide/narrow) — app confirmed highly polished; reconciled all 4 Next items (still valid, paths verified), reordered UI&UX by user-visible impact, added audit-derived ConfidenceChip-tooltip item (chip never says the % is classification confidence)
- 01:03 UTC — **improve**: ConfidenceChip value===0 now renders "Unscored" (muted chip, no %) instead of "Needs review 0%"; added `.confidence.unscored` light+dark styles; `ConfidenceChip.tsx`, `styles.css`; self-review passed
- 02:04 UTC — **improve**: ConfidenceChip tooltip added — `title="{pct}% classification confidence"` on the `.confidence` span in `ConfidenceChip.tsx` so hover explains what the bare percentage measures; extracted `pct` variable to avoid double computation; self-review passed
- 03:03 UTC — **improve**: WeeklyCapacityScreen next-week button disabled label fixed — `aria-label` and `title` now read "Cannot navigate past current week" when `isCurrentWeek` is true in `WeeklyCapacityScreen.tsx`; self-review passed
- 04:02 UTC — **improve**: AuditLogScreen Escape clears filter — added `onKeyDown` to the search `<input>` in `AuditLogScreen.tsx`; Escape resets both `query` and `filter` to defaults, matching the "Clear filters" button and the LedgerScreen Escape-clears-search pattern; self-review passed
- 05:10 UTC — **improve**: BlockCard time-error live region — added `role="alert"` `.sr-only` span ("End time must be after start time") inside `.time-range-editor` in `BlockCard.tsx` so screen readers announce validation failures; added reusable `.sr-only` utility class to `styles.css`; self-review passed
- 06:10 UTC — **improve**: Type-safety cleanup — fixed `persistedSnapshot: any` → `PersistedAppState | null` in `App.tsx`; tightened `addAuditEvent` param from `any` to `Omit<AuditEvent,"event_id"|"timestamp"> & {timestamp?:string}` in `useBlocksLedger.ts`; removed dead `importOutlookIcs` stub (console.warn) and its dead imports (`outlookEventsToWorkBlocks`, `parseOutlookIcs`, `createAuditEvent`, `removeSeeded*`); self-review passed
- 06:40 UTC — **curator**: visual pass ran (19 screenshots, 6 screens × light/dark × narrow) — app confirmed highly polished; reconciled Next (moved ForecastScreen empty-state item to Done — already present in `ForecastScreen.tsx`+`ForecastAgentPanel.tsx`; dropped weak DailyReview week-eyebrow item as a double-eyebrow); added two audit-derived UI/UX items (colorless `narrative_generation` audit pill seen on Audit screen; audit filter chips missing `aria-pressed`); recorded the per-type audit-badge color convention in NOTES.md
- 07:07 UTC — **improve**: Narrative audit-type pill tinted — replaced near-transparent `.audit-badge.narrative_generation` base rule with indigo tint; added light-mode override (`#eef2ff`/`#c7d2fe`/`#3730a3`) and explicit dark-mode override (`#141438`/`#252570`/`#a5b4fc`) in `styles.css`; self-review passed
- 08:05 UTC — **improve**: Audit filter chips aria-pressed — added `aria-pressed={filter === item.id}` to all 11 filter `<button>`s in `AuditLogScreen.tsx` so screen-reader users hear which filter is active; self-review passed
- 09:03 UTC — **improve**: CorrectionsScreen Escape clears search — added `onKeyDown` to the search `<input>` in `CorrectionsScreen.tsx`; Escape clears `query`, matching AuditLogScreen and LedgerScreen behavior; self-review passed
- 10:12 UTC — **improve**: AgentScreen `any` typed tool params — `createTool: any` → `typeof AiToolFn` (type-only import from `ai`); `execute: async (input: any, _options?: any)` → `execute: async (input: Record<string, unknown>)` (unused `_options` dropped); comment explains why `t` stays `any`; `AgentScreen.tsx`; self-review passed
- 11:05 UTC — **improve**: AgentScreen & ContextNavigation accessibility — added `aria-label` to icon-only copy/send/analysis toggle buttons in `AgentScreen.tsx`; added `aria-pressed` to ContextNavigation nav buttons; `AgentScreen.tsx`, `ContextNavigation.tsx`; self-review passed
- 11:07 UTC — **improve**: SetupScreen AI form label associations — added `htmlFor`/`id` pairs to all five AI-provider form fields (Provider select, API Key, Base URL, Model, Vision Model) in `SetupScreen.tsx`; clicking a label now focuses its field; self-review passed
- 12:05 UTC — **improve**: BlockCard time editor focus management — added `autoFocus` to start-time input, `onKeyDown` Escape-to-cancel on container div, and `aria-label="Time range editor"` on the container in `apps/desktop/src/components/ledger/BlockCard.tsx`; self-review passed
- 12:39 UTC — **curator**: visual pass ran (24 screenshots, 6 screens × light/dark × wide+narrow) — app confirmed highly polished; moved 2 stale items to Done (ConfidenceChip casing already normalized; heatmap legend already aria-hidden + cells labeled); verified + reordered remaining 6 by user-visible impact (React-key bugs first); added 2 audit-derived UI/UX items (BlockCard category select clips long labels on Daily review; DailyReview progress track needs progressbar role); refreshed NOTES (`.review-header-actions` orphan resolved, added select-truncation convention)
- 13:12 UTC — **improve**: ForecastList React key fix — changed `key={item}` to `key={\`${index}-${item.slice(0,20)}\`}` in `apps/desktop/src/components/common/ForecastList.tsx`; eliminates duplicate-key React warnings when AI returns identical bullet items; self-review passed
- 18:37 UTC — **curator**: visual pass ran (20 screenshots, 6 screens × light/dark × wide+narrow) — app confirmed highly polished; verified all 7 Next items still valid against code; reordered UI & UX Polish by user-visible impact (visible category-select truncation now top, React-key fix last); refined BlockCard item with the verified `.review-screen .tag-grid` 1.5fr value; added one audit-derived item (onboarding "Getting started" incomplete steps are dead-ends — no path to Advanced Settings); marked the RiskRow Active-blockers unit-mismatch RESOLVED in NOTES so it isn't re-suggested
- 20:04 UTC — **improve**: BlockCard category select clips label fix — widened `.review-screen .tag-grid` first column from `minmax(0, 1.5fr)` to `minmax(0, 2.4fr)` in `styles.css`; category now ~55% of row width, "Documentation / Requirements" renders unclipped; ledger/mobile rules untouched; self-review passed
- 20:47 UTC — **improve**: Onboarding checklist dead-end fix — added contextual `hint` to each step in `SetupScreen.tsx` rendered as `.onboarding-step-hint` (block span, 11px, `--text-subtle`) below the label for incomplete steps only; `align-items: flex-start` on `.onboarding-step`; `styles.css`; self-review passed
- 21:30 UTC — **improve**: SetupScreen provider status ARIA live region — always apply `ai-provider-status` base class (removed conditional `undefined`); added CSS rule that collapses element to 0 height/padding/border when no modifier class is active, keeping it in DOM + AT for live-region registration without `display:none`; `SetupScreen.tsx`, `styles.css`; self-review passed

## 2026-06-25

- 07:09 UTC — **improve**: Marooned single-suggestion layout fix — added `max-width: 760px` to `.copilot-inline` and changed `.copilot-inline .copilot-list` grid to `repeat(auto-fit, minmax(280px, 1fr))` so a lone card fills its container instead of floating at 360px in an 880px panel; `styles.css`; self-review passed
- 00:39 UTC — **curator**: visual pass ran (24 screenshots, 9 screens incl. forecast/corrections/agent × light/dark + 4 compact) — app confirmed highly polished; moved ActivityCapturePanel React-key item to Done (already fixed); verified remaining 5 Next items against code; replenished UI & UX Polish with 2 audit-grounded items (Audit log shows raw enum/ISO correction values while Corrections screen humanizes them — App.tsx:514; single AI suggestion marooned in Daily "Suggested cleanup" panel) and reordered by user-visible impact; refreshed the stale CorrectionsScreen note in NOTES (screen now humanizes; remaining leak is the audit-event summary in App.tsx)
- 06:38 UTC — **curator**: visual pass ran (20 screenshots, 6 screens × light/dark wide + narrow, plus narrative/corrections close-ups) — app confirmed highly polished; verified all 6 Next items still valid and accurately referenced against code (none already shipped); reordered UI & UX Polish by user-visible impact (marooned single-suggestion panel now top, audit enum + progressbar a11y after); added one audit-grounded item (Weekly "Delivery risk modifiers" index bars don't encode severity by color — RiskRow.tsx, 90/100 reads same as 10/100); noted the severity-tinting gap in NOTES as distinct from the solved count-vs-index split
- 08:35 UTC — **improve**: Risk bar severity tinting — added `data-severity="low|mid|high"` to `.risk-track > span` in `RiskRow.tsx`; CSS threshold rules (low <34: slate, mid 34–66: amber, high ≥67: orange) for light + dark in `styles.css`; `dangerActive` red row untouched; `aria-valuetext` now announces severity level; self-review passed
- 13:04 UTC — **improve**: Audit log correction values humanized — added `humanizeCorrectionValue` import and applied it to both values in the `user_correction` audit summary in `App.tsx`; Audit log now reads "Planned → Unplanned" (and formatted times) matching Corrections screen; self-review passed
- 12:40 UTC — **curator**: visual pass ran (37 screenshots, 9 screens × light/dark wide + narrow); app confirmed highly polished. Verified all 5 Next items still valid against code (audit-vs-corrections enum leak confirmed side-by-side in screenshots; none already shipped); fixed DailyReview path to `components/review/`; refined "Why this block?" Trust item (evidence[] already renders via native `<details>`, remaining gap is `derived_from[]`); added one audit-grounded UI/UX item (onboarding "Getting started" header shows "3/4 complete" text with no progress bar — SetupScreen). Pruned NOTES: marked ConfidenceChip tooltip/Unscored and legend hover-crosslink as SOLVED reference patterns (both verified done).
- 16:04 UTC — **improve**: Audit log correction humanized — imported `humanizeCorrectionValue` in `App.tsx`; changed `addCorrection()` summary from raw `` `${old_value} -> ${new_value}` `` to `` `${humanizeCorrectionValue(field, old)} → ${humanizeCorrectionValue(field, new)}` `` so Audit log shows "Planned → Unplanned" and formatted times matching CorrectionsScreen; `App.tsx` only; self-review passed
- 17:04 UTC — **improve**: Onboarding progress bar — added slim `review-progress-track`/`review-progress-fill` progress bar with `role="progressbar"` + full ARIA attrs below the "Getting started" header in `SetupScreen.tsx`; `onboarding-progress-track` margin rule added to `styles.css`; self-review passed
- 18:40 UTC — **curator**: visual pass ran (20 screenshots, 6 screens × light/dark wide + 6 compact, plus Forecast & Corrections close-ups); app confirmed highly polished. Verified all current Next items still valid against code (none already shipped); fixed stale paths in the Code Quality `snapshot: any` item (`components/shell/AppShell.tsx` + `components/compact/CompactWidget.tsx`, model line ref). Replenished the thin UI & UX Polish subsection with 2 Forecast-tab-grounded items (detail cards wrap 3+1 stranding "Assumptions" — `.forecast-grid` `auto-fit`→2×2; scenario numbers lack a range visual) and reordered by user-visible impact. Logged the auto-fit-grid lopsided-wrap rough edge in NOTES.
- 19:05 UTC — **improve**: Forecast detail cards balanced 2×2 grid — changed `.forecast-grid` from `repeat(auto-fit, minmax(min(240px, 100%), 1fr))` to `repeat(2, minmax(0, 1fr))`; added `.forecast-grid` to the `@media (max-width: 600px)` single-column rule; `styles.css` only; self-review passed

## 2026-06-26

- 00:38 UTC — **curator**: visual pass ran (20 screenshots, 6 screens × light/dark wide + 6 compact, plus Forecast & Corrections close-ups); app confirmed highly polished. Moved the DailyReview progressbar-semantics item to Done (verified already present — `DailyReviewScreen.tsx:97` carries `role="progressbar"` + full ARIA). Verified remaining Next items against code (EmptyState ariaLabel, ReviewCopilotPanel per-suggestion labels, `snapshot: any`; fixed stale `models.ts` line ref 206→207). Replenished the thinned UI & UX Polish subsection with 2 screenshot-grounded items: demo Audit log renders a raw `planned -> unplanned` correction (`demoData.ts:211`) while Corrections humanizes it, and the Weekly "Summary confidence 89%" card (`WeeklyCapacityScreen.tsx:116`) is a bare percentage with no explanation. Kept Forecast range-track at top. Extended the raw-enum NOTES entry to cover demo-seed humanization.
- 03:10 UTC — **improve**: Forecast scenario range track — added `.forecast-range` horizontal bar beneath `.forecast-summary` in `ForecastAgentPanel.tsx`; `--info` fill spans conservative→likely, marker tick at "Likely"; labeled row shows all three values; `role="img"` + `aria-label`; token-based CSS in `styles.css` works in light + dark; passed self-review.
- 06:36 UTC — **curator**: visual pass ran (18 screenshots, 6 screens × light/dark wide + 6 compact); app confirmed highly polished across all themes/widths. Verified all 5 Next items still valid against code (demo `planned -> unplanned` enum leak confirmed in audit screenshot; none already shipped); tightened the Weekly Summary-confidence item to cover both `.summary-score` instances + fixed line refs. Added 1 audit-grounded UI/UX item: BlockCard `.block-capacity` "% of week" (`BlockCard.tsx:130-133`) is a bare percentage with no tooltip — add `title` + `.sr-only`. Pruned NOTES: marked ConfidenceChip tooltip/Unscored as SOLVED reference and generalized the bare-percentage-needs-tooltip rule.
- 11:04 UTC — **improve**: Demo Audit log humanized correction value — changed `"planned -> unplanned"` to `"Planned → Unplanned"` in `services/demoData.ts` seed; demo Audit screen now matches the humanized live-correction format; passed self-review.
- 12:38 UTC — **curator**: visual pass ran (18 screenshots, 6 screens × light/dark wide + 6 compact); app confirmed highly polished across themes/widths. Verified all 6 Next items still valid against code (Weekly Summary-confidence + BlockCard %-of-week tooltips, EmptyState/ReviewCopilot aria-labels, `snapshot: any` — none already shipped; line refs accurate). Added 1 audit-grounded a11y item: the three relabel `<select>`s in `BlockCard.tsx` (rendered on both Daily review and Ledger) carry only a value `title`, no field-name `aria-label`, so SR users don't know which field they're editing. Recorded the select-needs-field-name-aria-label convention in NOTES.
- 13:04 UTC — **improve**: Weekly Summary-confidence tooltip — added `title="How confident the model is in this week's capacity estimate"` and `.sr-only` mirror span to both `.summary-score` instances (empty-state and populated) in `WeeklyCapacityScreen.tsx`; hover and screen-reader users now see what the percentage measures; passed self-review.
- 15:05 UTC — **improve**: BlockCard "% of week" tooltip — added `title="Share of this week's modeled capacity this block accounts for"` and `.sr-only` mirror span to the `.block-capacity` div in `BlockCard.tsx`; hover and screen-reader users on both Daily review and Ledger now understand what the percentage measures; passed self-review.
- 16:05 UTC — **improve**: BlockCard relabel selects accessible labels — added `aria-label="Work category"`, `aria-label="Planned status"`, `aria-label="Work mode"` to the three relabel `<select>`s in `BlockCard.tsx`; kept existing `title` for truncation tooltip; screen readers now announce which field is being edited; passed self-review.
- 18:04 UTC — **improve**: EmptyState accessible section labels — added optional `ariaLabel` prop (defaults to `title`) and applied as `aria-label` on the `<section>` in `components/common/EmptyState.tsx`; all 14 existing call sites gain named landmarks automatically; passed self-review.
