# Changelog

## 2026-06-29
- 00:14 UTC — **improve**: SetupScreen "Workplace chat" data-source rows — new "Workplace chat" `.settings-row` in `SetupScreen.tsx` mapping `CHAT_PROVIDERS` (from `chat/chatSource.ts`) like the calendar-sync row maps `CALENDAR_PROVIDERS`: a real `<label className="settings-control">` file-import for each `loopSafe` provider (Slack) + a disabled "Connect {label} (coming soon)" button (`title`+`aria-label`) for the `oauth` ones (Teams/Webex). Added `.chat-connect-options` alongside `.calendar-connect-options` in all four `styles.css` rule sets (base flex + width, medium `grid-column:3`, narrow `grid-column:1`/`justify-self`/`align-items`). Added a `chat_import` `AuditEventType` via the 4-file recipe (union in `packages/domain/src/models.ts`, "Chat" `auditTypeLabel` in `lib/format.ts`, AuditFilter chip+matcher in `AuditLogScreen.tsx`, cyan badge in both light+dark `styles.css` blocks) + a `createChatImportAuditEvent` helper in `lib/audit.ts` (metadata-only details: `message_text:false`, counts + file name only). New `importWorkplaceChat(file)` handler in `App.tsx` mirrors `importOutlookIcs` — FileReader → `importChatExport(content,{weekId})` → upsert reactive `work_blocks` into `blocks` by stable `imported-<hash>` id → audit event + toast; empty/unparseable surfaces a `chatImportError` row note + toast. `chatImportError` state threaded App→ScreenRouter→SetupScreen, cleared in `resetLocalData`. Build green; passed self-review (8-angle code-review: correctness + altitude/conventions clean).
- 00:43 UTC — **curator**: visual pass ran (33 Playwright/demo screenshots — 11 screens incl. Agent/Forecast/Trends/Corrections/Flagged × light/dark wide + 420px-narrow). App confirmed highly polished across themes/widths; verified all 3 Workplace Chat backlog items still genuinely open (no `analyzeInterruptionLoad`/`summarizeChatStakeholders`/call-dedup helper). The empty `### Interaction & Visual Polish` subsection was replenished (and moved to the top of `## Next`) with 3 source-verified clarity items: the Forecast Agent hero card reuses the deterministic "Reliable new-work capacity" label at the AI-likely 24% (collides with the 38% shown elsewhere; `ForecastAgentPanel.tsx:137`), the Agent composer paperclip is a dead `<button>` with no `onClick` that still looks clickable (`AgentScreen.tsx:720`), and the Trends 4-line chart lacks the legend↔line hover crosslink the capacity bar already has (`TrendsScreen.tsx`). Trimmed Done to 10. NOTES: extended the hover-crosslink entry (Trends is the remaining gap) + added a durable note on decorative-`<button>` vs metric-label-collision traps. README: added the shipped workplace-chat metadata import (Features, Privacy Model, integrations line).
- 01:15 UTC — **improve**: Forecast Agent hero card no longer reuses the deterministic "Reliable new-work capacity" label — relabeled the first `.forecast-summary` card in `components/capacity/ForecastAgentPanel.tsx` to "AI reliable estimate" so the AI's `reliable_new_work_capacity_pct` reads as a refined scenario figure distinct from the deterministic "Reliable new work" metric on Weekly/sidebar/forecast-header (the shared label made the AI 24% look like it contradicted the deterministic 38%). Copy/label only — no logic change; the `.forecast-baseline-note` still reconciles the two numbers. Build green; passed self-review.
- 02:03 UTC — **improve**: Agent composer paperclip demoted from a dead button to a status indicator — `components/agent/AgentScreen.tsx` + `styles.css`. The no-op `.agent-attach` paperclip was a `<button type="button">` with no `onClick` (pointer cursor, hover, focus ring, tab stop) that implied a file picker; changed it to a non-interactive `<span className="agent-attach" role="img">` keeping the `aria-label`/`title` so it reads as a "context attached" indicator (span is naturally out of the tab order; the inner lucide icon is `aria-hidden`, so the span's `aria-label` is the accessible name). Removed the dead `.agent-attach:hover` rule (shared `.agent-attach, .agent-send` sizing rule kept for centering). Build green; passed self-review.
- 03:04 UTC — **improve**: Trends multi-line chart legend↔line hover crosslink — `components/capacity/TrendsScreen.tsx`. Added a `hoveredSeries` `useState<string | null>` keyed by `series.key` (which equals the legend memo's `item.key`), mirroring the `WeeklyCapacityScreen`/`StackedBar` `hoveredCategory` pattern: both the legend `<span>`s and the SVG `<g className="trend-series">` groups now set/clear it on `onMouseEnter`/`onMouseLeave` and dim the three non-hovered series + their legend rows to 0.3 opacity via inline `{ opacity, transition: "opacity 0.12s" }` (same inline-style convention as `StackedBar`, so the global reduced-motion reset applies). Hovering a line stroke/dot or a legend row isolates that series; the four crossing lines are now disambiguable beyond colour. Per-dot `<title>`/`role="img"` a11y untouched; no `styles.css` change. Build green; passed self-review (additive only; `item.key`/`series.key` confirmed identical `keyof Snapshot` strings).
- 04:18 UTC — **improve**: Chat interruption / context-switch signal — new pure `analyzeInterruptionLoad(chatEvents, workBlocks)` + `InterruptionLoadAnalysis` in `packages/inference/src/capacity.ts` (reactive-burst frequency, `messages_per_active_hour` density, `@`-mentions, `interrupted_deep_work_pct` interleave; metadata-only — reads `messages`/`mentions` counts + spans, never text; `null` when no chat data). Persisted a chat-`RawEvent` store via the retained-history recipe (`chatEvents` on `PersistedAppState` + `parseChatEvents` guard in both `localStore` branches, `usePersistence`, `App.tsx` state/hydrate/reset/persist, `importWorkplaceChat` now retains `result.events` deduped by `event_id`). `useDerived` computes `interruptionLoad` **scoped to the current ISO week**, threaded App→`ScreenRouter`→`WeeklyCapacityScreen` where a `.interruption-note` panel (mirrors `.model-bias-note`, gated `isCurrentWeek && interruptionLoad`) renders it; `demoData.ts` seeds 4 bursts (2 overlapping deep work → 67%). Build green; 14-check tsc+node smoke; passed 8-angle self-review (week-scoping fix for the all-week-aggregation finding, `parseChatEvents` null-`metadata` hardening, density-stat tooltip).
- 05:15 UTC — **improve**: Stakeholder collaboration map from chat metadata — new pure `summarizeChatStakeholders(chatEvents, {limit})` + `ChatStakeholderGroup`/`ChatStakeholderSummary` in `packages/inference/src/capacity.ts` (sibling to `analyzeInterruptionLoad`): ranks the week's stakeholder groups (channels / DMs) by reactive message *volume*, reporting each group's `share_pct` + exact `burst_count`. Reads ONLY `metadata.channels` labels + `metadata.messages` count + spans — never message text; multi-channel bursts split volume evenly (float weight drives share, never a misleading rounded count); unlabeled DM bursts bucket under "Direct & untagged"; returns `null` on zero reactive volume so a 0-message export shows no panel. Surfaced as a "Who your reactive time served" chip row on `WeeklyCapacityScreen` reusing `.baseline-chip` styling (new `.stakeholder-chips` margin override in `styles.css`), gated `isCurrentWeek && groups.length > 0`. Threaded via a new shared week-scoped `weekChatEvents` memo in `useDerived.ts` (feeds both `interruptionLoad` and `chatStakeholders`) → App → `ScreenRouter` → Weekly. Build green; 15-check tsc+node smoke; passed 8-angle self-review (dropped lossy per-group `message_count` → share% + exact bursts; added `totalMessages === 0 → null` guard).
- 06:17 UTC — **improve**: Teams/Webex call-as-meeting dedup vs calendar — `chatExport.ts` adds `call`/`huddle` `ChatSurface`s; `chatMessagesToImport` now groups bursts by provider + kind (call vs. text) and emits `Meetings / stakeholder syncs` / `Collaborative` / `fixed` blocks (`metadata.kind: "call"`) for call bursts while keeping text bursts' reactive defaults + original `event_id` (only calls take a `chat-call-` prefix, preserving re-import upsert). New pure `dedupeChatCallsAgainstCalendar` + `spansOverlap` in `packages/integrations/src/chat/callDedup.ts` drop chat call blocks whose `[start,end)` span overlaps a calendar meeting; wired into **both** import paths in `App.tsx` (order-independent) so a Teams/Webex call on both the chat export and the calendar isn't double-counted in `meeting_pct`. Call events are excluded from the reactive `chatEvents` store; the import is always audited. Build green; 26-check tsc+node smoke; passed 8-angle self-review (fixed event_id idempotency + calendar-first-order gap + always-audit).
- 06:49 UTC — **curator**: visual pass ran (24 base + targeted Playwright/demo screenshots — 11 screens × light/dark wide + 420px-narrow, plus a 420px overflow probe = zero element-level clipping, and scrolled captures of the Capacity chat panels + lower Settings). App confirmed highly polished across themes/widths. The Workplace Chat family is fully shipped (parser → registry → Setup rows → interruption signal → stakeholder map → call dedup), so removed that now-complete `## Next` subsection. Verified several previously-listed/NOTES-flagged items are ALREADY solved (`.forecast-summary` stacks to 1-col at 420px; `.briefing-metrics`/`.starter-grid` now reflow instead of `display:none`; `MetricCard.helper` already explains each hero %; stakeholder chips + trend-legend already carry title+sr-only) and did NOT re-add them. Replenished `### Interaction & Visual Polish` with 3 source-verified items: internal-scroll screens (capacity/forecast/settings) lack the `.ledger-list` scroll-shadow affordance; BlockCard `.tag-grid` selects have no visible field label for sighted users; the "Chat interruption load" tiles have inconsistent, hover-only tooltips. Trimmed Done to 10; refreshed NOTES (marked the briefing/starter/forecast-summary anti-patterns solved; added the internal-scroll scroll-shadow gap). README checked against `package.json` + screens — no drift.
- 07:07 UTC — **improve**: Scroll-shadow affordance on internally-scrolling content screens — `apps/desktop/src/styles.css`. `.capacity-screen`, `.forecast-screen`, and `.settings-screen` (all `overflow-y: auto` and taller than a standard desktop window) now carry the proven `.ledger-list` `background-attachment: local`/`scroll` scroll-shadow via one new combined rule: two local-attachment `var(--background)` cover gradients (24px/40px bands to clear the roomier card padding) hide the edge shadows at a scroll boundary, two scroll-attachment radial `--scroll-shadow` shadows reveal when content sits above/below the fold — so the bottom-of-Capacity interruption panel + stakeholder chips, Forecast constraints/risk-flags, and Settings retention/export/Advanced rows now get a visible "more below" cue. `background-color: var(--background)` longhand placed after the `background` shorthand. Verified in demo (Playwright, light+dark): shadows hide at the top boundary, reveal mid-scroll, no layout regression. Build green; passed self-review.
- 08:05 UTC — **improve**: BlockCard relabel selects now have visible field labels — `components/ledger/BlockCard.tsx` + `styles.css`. Each of the three `.tag-grid` relabel `<select>`s (Work category / Planned status / Work mode) is wrapped in a `<label className="tag-field">` flex-column cell with an uppercase micro-label span above it (`.tag-field-label` reuses the `.baseline-chips-label` token treatment: `var(--text-subtle)`, 11px, `font-weight:600`, `letter-spacing:0.04em`, uppercase), so a sighted analyst sees what each bare dropdown controls; the real `<label>` also makes the text click-to-focus the control. Existing `aria-label` + value `title` kept. New `.tag-field`/`.tag-field-label` rules (token-based → light+dark) plus a `.ledger-screen` density override (gap 3px, label 10px) keep the compact Activity cards tight; the desktop 3-col grid + ≤600/≤760px `1fr` stack still apply since all `.tag-grid` rules use the descendant `select` form. Build green; passed self-review.
- 09:10 UTC — **improve**: "Chat interruption load" tiles get consistent, non-hover-only explanations — `components/capacity/WeeklyCapacityScreen.tsx`. All four `.interruption-stat` tiles now carry a plain-language `title` (added to "reactive bursts" + "direct @-mentions"; the other two already had one) AND a matching `.sr-only` explanation span (none had one before), matching the `ConfidenceChip` / `.block-capacity` title+`.sr-only` convention so a new user can decode the metrics without a mouse. Each `.sr-only` span is explanation-only (e.g. "A reactive burst is a cluster of chat messages within about 20 minutes.") rather than a restatement of the already-readable visible `<strong>` value + `<span>` label — avoiding screen-reader double-announcement; tile 4's sr-only adds the "{interrupted} of {total} deep-work blocks overlapped a chat burst" breakdown the visible `%` doesn't convey. Build green; passed self-review (review agent confirmed JSX/plural/field-semantics clean; applied the double-announcement cleanup — sr-only made explanation-only).
- 10:07 UTC — **improve**: Prune persisted `chatEvents` by `retentionDays` — `apps/desktop/src/App.tsx`. The retained chat `RawEvent` store (added for the interruption/stakeholder signals) grew unbounded, unlike `activeWindowSamples` which the retention-expiry effect already pruned — a privacy gap since retention is user-controlled. Extended that single `useEffect` to also prune `chatEvents` against the same `Date.now() - retentionDays*…` cutoff, using `event.timestamp_end` as the age point (keeps an event until its latest moment ages out) and the identical same-reference-when-unchanged functional-update guard so adding `chatEvents` to the dep array can't loop. Demo-mode/`null` early-return unchanged; pruned array persists via the existing `usePersistence` dep; per-row expiry stays unaudited (only the discrete policy change is logged). Current-week signals provably safe (`weekChatEvents` filters by `timestamp_start` over the ≤7-day ISO week; min `retentionDays` is 7). Build green; passed 8-angle self-review (no render loop, no current-week loss, NaN-tolerant, persistence wired, conventions clean).
- 11:09 UTC — **improve**: Surface stakeholder-map truncation footnote — `components/capacity/WeeklyCapacityScreen.tsx` + `styles.css`. The "Who your reactive time served" chip row slices to the top 4 groups (`summarizeChatStakeholders` → `groups = ranked.slice(0, limit)`) but never signaled when more existed, so with >4 stakeholder groups the shown `share_pct`s silently summed to <100% with no explanation. Surfaced the already-computed-but-unused `ChatStakeholderSummary.group_count` as a muted footnote ("Top N of M groups by reactive volume — K more not shown"), gated on `group_count > groups.length` (always-safe since `groups` is a slice of the `group_count`-length `ranked`). New `.stakeholder-chips-note` rule (`var(--text-subtle)`, 11px, `flex-basis:100%` so it drops to its own line in the `.baseline-chips` flex row). Picked from the top "Interaction & Visual Polish" category — the Next backlog held only category headers, no concrete items. Build green; passed self-review (2 parallel finders: correctness clean; corrected a stale CSS comment referencing a "100%" string the final copy doesn't render).
- 12:44 UTC — **curator**: visual pass ran (24 fullPage Playwright/demo screenshots — 6 screens × light/dark wide + 420px-narrow — plus 5 `.screen`-element full captures and a 420px per-screen DOM probe). App confirmed highly polished across themes/widths; the probe showed every content screen internal-scrolls within the fixed-viewport shell (scroll-shadow already in place), so the "cut off below the fold" look in fullPage shots is a capture artifact, not a bug. `## Next` held ZERO concrete `- [ ]` items (only the three category headers) — replenished it: added 2 verified UI/UX polish items at top (Agent briefing tiles lack the `title`+`.sr-only` convention; sidebar nav doesn't surface the ⌘1–9 shortcuts), 1 Intelligence item (name the calmest active weekday in the interruption panel), and 1 Trust item (at-a-glance source-origin chip on BlockCard). Confirmed several previously-listed items already solved (Forecast `×3` badge `title`+`.sr-only`; Trends legend delta `.sr-only`; BlockCard select labels) and did NOT re-add them. Trimmed Done to 10; added a NOTES gotcha on the fullPage-vs-internal-scroll screenshot artifact. README checked against `package.json` + screens — no drift.
- 12:11 UTC — **improve**: Surface peak reactive chat day in the interruption panel — `packages/inference/src/capacity.ts` + `components/capacity/WeeklyCapacityScreen.tsx` + `styles.css`. `analyzeInterruptionLoad` reported reactive-chat counts but never *when* in the week the load concentrated. Extended it (metadata-only: spans + `messages` counts, no text) to bucket reactive message volume by local weekday (`getDay()`) and report `peak_day` (weekday name, null when no volume), `peak_day_message_count`, and `active_day_count` (distinct message-bearing weekdays). Peak chosen by ascending-index iteration + strict `>` from a 0 baseline → deterministic lowest-index tie-break, null peak for a burst-only/zero-message week. Surfaced as a `.interruption-peak-note` footnote on the Weekly interruption panel ("Reactive load peaked on **Monday** — 9 messages across 4 active days. Consider protecting the quieter days for deep work."), gated `peak_day && active_day_count >= 2` so a single-day week shows no noise. New token-based light+dark CSS; renders in demo (chat seed spans Mon–Thu, peaks Monday/9). Build green; passed self-review (2 parallel finders + own pass: correctness clean; applied the edge-case cleanup — bucket only message-bearing days so a malformed 0-message burst can't inflate the count or trip the gate — plus comment-accuracy fixes).
- 13:05 UTC — **improve**: Agent briefing metric tiles get the title + sr-only treatment — `components/agent/AgentScreen.tsx`. The three `.briefing-metrics` tiles (`Planned` / `Reactive` / `Outlook`) rendered a bare `<span>{label}</span><strong>{value}</strong>` with no `title` and no `.sr-only` explanation, unlike the Weekly metric cards and the four `.interruption-stat` tiles. Added a plain-language `title` tooltip + an explanation-only `.sr-only` span to each tile (Planned = share scheduled/planned ahead; Reactive = share unplanned, reacting to chats/interruptions/ad-hoc; Outlook = how likely unfinished work spills into next week) so a new user can decode "Planned 32%" / "Reactive 24%" / "Outlook · Low carryover risk" without prior context. Followed the explanation-only `.sr-only` convention (not a value-restating mirror — the visible label+value is already in the a11y tree); the new span is `position:absolute` so the `display:grid` tile is visually unchanged. `AgentScreen.tsx` only. Build green; passed self-review (additive a11y-only diff: no child-count selector breakage, explanations match briefing field semantics).
- 14:05 UTC — **improve**: Sidebar nav surfaces the ⌘1–9 screen shortcuts — `components/shell/AppShell.tsx`. The `.nav-item` buttons registered ⌘1–9 jump shortcuts (`SCREEN_KEYS` in `App.tsx`) but gave no hint they existed. Added a `shortcut`/`shortcutKey` pair to each nav item that maps to a `SCREEN_KEYS` entry (Today ⌘1/Meta+1, Week ⌘2/Meta+2, History ⌘5/Meta+5, Settings ⌘9/Meta+9), rendering `title="Today (⌘1)"` (conditional — omitted when no shortcut) + `aria-keyshortcuts="Meta+1"` per button so the shortcuts are discoverable on hover and announced to screen readers. Agent carries explicit `shortcut: undefined`/`shortcutKey: undefined` (no `SCREEN_KEYS` entry → both attrs omitted, stays hint-free; the explicit undefineds also keep the array element type uniform so `item.shortcut` access doesn't trip a TS union error). The desktop bottom `.settings-button` also got `title="Settings (⌘9)"` + `aria-keyshortcuts="Meta+9"`. No CSS/layout change. Build green; passed self-review (additive a11y-only diff: valid ARIA token format, conditional title omits for the no-shortcut item, both Settings controls correctly point at ⌘9).
- 15:06 UTC — **improve**: Name the calmest active weekday for protected deep work — `packages/inference/src/capacity.ts` + `components/capacity/WeeklyCapacityScreen.tsx`. `analyzeInterruptionLoad` reported `peak_day` (busiest reactive weekday) but the Weekly `.interruption-peak-note` footnote only said "protect the quieter days" without naming one. Mirrored the `peak_day` bucketing to also compute `calm_day` — the lowest-volume *active* weekday — from the SAME message-bearing `dayMessages` map: ascending-index iteration + strict `<` from a `+Infinity` baseline → lowest-index-wins tie-break, guarded `dayMessages.size >= 2` so a single-active-day week leaves it null (no quieter day to contrast). New `calm_day: string | null` on `InterruptionLoadAnalysis`. Footnote now names it ("…your quietest active day was **Thursday** — consider protecting it for deep work."), gated `calm_day && calm_day !== peak_day` (the outer `<p>` already gates `peak_day && active_day_count >= 2`, guaranteeing `calm_day` non-null); the original copy stays as the fallback when calm equals peak (all-equal-volume tie). Demo seeds Mon=9/Tue=7/Wed=5/Thu=4 → peak Monday, calm Thursday, so it renders. No CSS change. Build green; passed self-review (single construction site so TS enforces the field; tie → fallback copy avoids a contradictory "peaked AND quietest on the same day").
- 16:10 UTC — **improve**: At-a-glance source-origin chip on each work block — `components/ledger/BlockCard.tsx` + `styles.css` + `services/demoData.ts`. `.block-chips` (shared by the Activity ledger + Today review queue) now leads with a small muted origin chip naming a block's source, so provenance is visible without expanding the "Derived from" drill-down. New pure `blockOrigin(work_block_id)` helper reuses the prefix convention `App.tsx` keys cross-source dedup off of: `calendar-outlook-` → "Calendar"; `imported-` → "Workplace chat" (the only `imported-` source in the UI today — comment flags refining off `derived_from`/`evidence` if a future non-chat `imported-` source lands); else → "Activity capture". Each chip has a plain-language `title`; the visible label is self-descriptive (no `.sr-only` needed, matching `ConfidenceChip`). New token-based `.block-origin` chip (`var(--border)`/`var(--surface-muted)`/`var(--text-subtle)` → light+dark; min-height/padding/radius matched to sibling `.blocker-badge`). Also aligned the two demo calendar-meeting block ids (`demo-planning`/`demo-product-review`, referenced nowhere else) to the production `calendar-outlook-demo-*` convention so the demo cards read "Calendar" instead of mislabeling as "Activity capture". Build green; passed self-review (2 parallel finders + verify): correctness clean — confirmed real calendar=`calendar-outlook-<hash>`, chat=`imported-<hash>`, capture=`ai-session-`/`session-` (falls through), demo rename safe; applied the padding-parity cleanup (`0 9px`→`0 8px`).
- 17:05 UTC — **improve**: Document the hand-set capacity constants with citations — `packages/inference/src/capacity.ts` (comments only). The four worst-justified magic numbers now each point to `docs/heuristics-vs-research.md` and name their basis: the `MAX_RELIABLE_NEW_WORK = 40` cap (queueing-theory M/M/1 `1/(1−ρ)` ~80% knee, §1), the `carryoverRiskPct × 0.55` discount (hand-tuned spill fraction, §1–2), the `reactivePct × 0.72` penalty (CHI-2008 interruption stress-cost, §2), and the narrative `context_switch_score >= 0.45` fragmentation trigger (~85%-collaboration / ~47s-attention, likely under-weighted, §3). No behavior change, no UI. Smallest item in the new Capacity Model Calibration batch, done first so the remaining three edit already-annotated constants. Build green; passed self-review (each comment's citation + basis verified against the doc section it names; the 0.45 comment correctly annotates the *narrative* threshold, distinct from the 0.6 proactive-nudge cut).
- 18:05 UTC — **improve**: Re-baseline the "dense meetings" narrative trigger to the user's own median — `packages/inference/src/capacity.ts` + `hooks/useDerived.ts`. `generateWeeklyNarrative` flagged `denseMeetings = snapshot.meeting_pct >= 18`, an absolute cut that fires for almost everyone (collaboration is ~85% of the work week; doc §4 calls it cry-wolf). Added an optional `baselines?: CapacityBaselines | null` param: with `≥ 2` prior weeks of history it now flags the week only when `meeting_pct > meetingMedian × 1.25` (≥25% above the user's OWN rolling median — bare `> median` would fire ~half the time by construction), else falls back to the absolute `>= 18`. Threaded a `capacityBaselines` memo into `useDerived.ts` (prior weeks strictly before `currentWeekId`, mirroring the `WeeklyCapacityScreen` baseline-chip machinery via `computeCapacityBaselines`) and passed it to the only `generateWeeklyNarrative` call site; every other importer uses `ReturnType<>` typing only, so the optional param is backward-compatible. `meetingMedian` is provably non-null when `week_count >= 2` (median is null only for an empty window), so the `!== null` guard is belt-and-suspenders that also satisfies `number | null`. Build green; passed self-review (no other call sites need the param, prior-week filter matches the chip baseline, no-history case behavior-identical, degenerate median≈0 correctly flags any meeting week for a no-meeting user).
- 19:05 UTC — **improve**: Search inputs get a visible clear (×) button — `components/ledger/LedgerScreen.tsx` + `components/audit/AuditLogScreen.tsx` + `styles.css`. Both `.search-box` inputs cleared their query only via an `onKeyDown` Escape handler (invisible to mouse/touch, focus-gated). Added a trailing lucide-`X` clear button (`type="button"`, `aria-label="Clear search"`, `.search-box-clear`) rendered only when the input has a value: ledger clears `searchQuery`; audit clears `query` ONLY and leaves the filter chips (matching its inline-vs-Escape distinction). Each click restores focus to the input via a `useRef<HTMLInputElement>` so activating the button (which then unmounts) doesn't drop focus to `<body>` — standard clear-field behavior. New token-based `.search-box-clear` rule (transparent → `var(--surface-muted)` hover, `var(--text-subtle)` → `var(--text)`; both tokens theme per light/dark, no separate dark override). Build green; passed self-review (additive diff, no regression to the existing Escape clears; focus-restore added during review to fix the introduced focus-loss-on-unmount a11y wart).
- 18:41 UTC — **curator**: visual pass ran (21 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup × light/dark wide + 420px-narrow, plus Agent/Forecast/Trends). App confirmed highly polished across themes/widths. The empty `### Interaction & Visual Polish` subsection was replenished (at the top of `## Next`) with 3 source-verified items: both `.search-box` inputs (ledger + audit) only clear via Escape — no visible `×` for mouse/touch; the always-visible sidebar `.side-metric-value` "Reliable capacity" figure (`AppShell.tsx`) is the lone holdout from the title+`.sr-only` metric-convention sweep; the Trends x-axis ticks render bare "W26" with no full-form `<title>` (`TrendsScreen.tsx`). Confirmed both open Capacity Model items still genuine (`wipLoadScore` linear `/10`; `MAX_RELIABLE_NEW_WORK = 40` clamp intact) and that several previously-flagged items are already solved (daily `.review-progress` aria; audit filtered/empty `EmptyState`s; BlockCard select labels). Trimmed Done to 10; NOTES extended (sidebar holdout in the metric-convention map + a shared `.search-box` two-screen gotcha). README checked against `package.json` + screens — no drift.
- 20:03 UTC — **improve**: Add the title + sr-only convention to the sidebar "Reliable capacity" figure — `components/shell/AppShell.tsx`. The always-visible `.side-metric-value` rendered a bare `<strong>{pct(...)}</strong><small>Next week</small>` with no tooltip or screen-reader explanation — the lone metric figure still missing the title+`.sr-only` pair (per NOTES) that the Weekly `.summary-score`, BlockCard `.block-capacity`, the four `.interruption-stat` tiles, and the three Agent `.briefing-metrics` tiles all carry. Added a plain-language `title` ("Estimated share of next week that can absorb new planned work without slippage") on the container + an explanation-only `.sr-only` span inside, following the `.summary-score` reference (explanation, NOT a value+label restatement). The `.sr-only` span is `position:absolute` so the flex layout is visually unchanged; the no-signal "--" state keeps the same explanation, matching how `.summary-score` shows "--" with its title. `AppShell.tsx` only (`.sr-only` already in `styles.css`). Build green; passed self-review (additive a11y-only diff: explanation-only sr-only per convention, no layout/`nth-child` breakage).
- 21:05 UTC — **improve**: Trends x-axis week ticks get a full-form tooltip — `components/capacity/TrendsScreen.tsx`. Each x-axis week tick `<text>` rendered bare `shortWeekLabel(week.week_id)` → "W26" with no `<title>`, while the per-dot `<title>` already used the full `formatIsoWeekLabel(week.week_id)` → "Week 26, 2026" — so the axis label was undecodable and inconsistent with the dots. Added a `<title>{formatIsoWeekLabel(week.week_id)}</title>` child to the axis tick `<text>` (helper already imported); SVG `<title>` is non-visual, so the rendered "W26" tick is unchanged. `TrendsScreen.tsx` only, no CSS. Build green; passed self-review (additive one-line diff mirroring the existing `<circle>` dot `<title>` pattern, no behavior/layout change).
- 22:06 UTC — **improve**: Make the WIP penalty super-linear — `packages/inference/src/capacity.ts`. `wip_load_score` in `computeWeeklyCapacitySnapshot` was a linear, forgiving `uniqueProjectCount / 10`; replaced with a quadratic `clamp(Math.pow(activeProjectCount / 7, 2), 0, 1)` so context-switching cost curves upward (doc §3 `heuristics-vs-research.md`: fragmentation likely under-weighted, switching cost ~combinatorial). The curve sits near the old linear value around 5 projects, is gentler below, harsher above, and saturates the penalty at 7 concurrent projects (the "badly overloaded" knee). Verified super-linear (`score(5)=0.51 > 5×score(1)=0.10`) and edge-safe (0 projects → 0, no NaN; >7 → clamped to 1). The `Set(...project_name)` build is byte-identical and the downstream `× 10` penalty + `clamp` are preserved (penalty still caps at 10pp; field stays 0–1); all three consumers treat it as a 0–1 index, demo seeds the value directly so its display is unchanged. Updated the inline rationale with the §3 citation. Build green; passed self-review (8-angle equivalent — no findings).
- 23:09 UTC — **improve**: Target-utilization capacity model — `packages/inference/src/capacity.ts` + `packages/domain/src/models.ts` + `services/demoData.ts` + `components/capacity/WeeklyCapacityScreen.tsx`. Replaced the fixed "100% baseline minus penalties, clamped 0–40%" `reliable_new_work_capacity_pct` formula with a principled target-utilization model (doc §1). New `TARGET_UTILIZATION_PCT = 80` (M/M/1 ~80% queueing knee). The forward-committed load — `recurring + carryover + reactive×0.72 + fragmentation + wip`, the SAME terms the old formula subtracted from 100 — is now summed into a new `committed_utilization_pct` snapshot field (current utilization), and reliable new work = `clamp(80 − committed, 0, 40)`: headroom that brings TOTAL utilization to the knee rather than filling to 100%. `MAX_RELIABLE_NEW_WORK = 40` kept as the old-behavior guardrail (no over-promising on a near-empty week); `BASELINE_CAPACITY = 100` stays the accounting denominator. New required field threaded through `WeeklyCapacitySnapshot` + the demo seed (`committed_utilization_pct: 52`, reconciling the seed's reliable 28 → 80). Narrative copy (`generateWeeklyNarrative`) + the Weekly "Reliable new work" MetricCard helper now cite the committed-utilization + 80%-knee story. Forecast prompt untouched (its "Clamp to 0-40%" stays valid since the 40% cap is retained — no version bump). Build green; passed self-review (8-angle: no surviving correctness findings — all snapshot constructors updated, consumers read-only, demo history seeds never surface in the hero cards; applied one JSDoc-accuracy fix).

## 2026-06-30

- 00:12 UTC — **improve**: After-hours reactive-load signal — `packages/inference/src/capacity.ts` + `components/capacity/WeeklyCapacityScreen.tsx` + `services/demoData.ts`. Extended `analyzeInterruptionLoad` (the chat-metadata inference family) with two new metadata-only `InterruptionLoadAnalysis` fields, `after_hours_message_count` + `after_hours_pct`: reactive messages are bucketed by their burst's local START hour (reusing the `startDate` already built for weekday bucketing) and counted as after-hours when `< CORE_HOURS_START (8)` or `>= CORE_HOURS_END (18)` — new hand-set constants commented against `docs/heuristics-vs-research.md §5` (Pencavel, diminishing returns past long hours, applied to *when* load lands). The after-hours count is a strict subset of `message_count` by construction, so it can never exceed the total. Surfaced as a muted `.interruption-peak-note` footnote in the Weekly chat-interruption panel (reused class, no CSS) gated on `after_hours_message_count > 0`: "N% of reactive messages (K of M) arrived outside core hours (8am–6pm) — chat bleeding into personal time." Demo: shifted `demo-chat-3` from minute 300 (Wed 14:00) → 600 (Wed 19:00) so the footnote renders (5 of 25 → 20%) — same day/channel/volume, so peak/calm-day, stakeholder shares, and the Thursday-bounded analysis window are all provably unchanged. Build green; passed self-review (2 parallel finders + own pass): applied two finder items — floored `after_hours_pct` to 1 when any after-hours volume exists (the `1 of 300` round-to-0 case showed "0%" beside a non-zero count) and reworded the footnote to "(8am–6pm)" since the `>= 18` boundary is inclusive of 6pm sharp.
- 00:41 UTC — **curator**: visual pass ran (18 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup × light/dark wide + 420px-narrow). App confirmed highly polished across themes/widths (no dark-contrast or narrow-clipping regressions; the fullPage below-the-fold look is the known internal-scroll artifact). `## Next` held ZERO concrete items — replenished `### Interaction & Visual Polish` with 3 source-verified demo-staleness items: the demo "WIP overload" risk bar pins at **100/100** (the 9 distinct demo `project_name`s saturate the new super-linear `pow(9/7,2)` WIP score, which `useDerived.ts:45` live-computes — contradicting the seeded 0.52); the demo manager summary cites "24% / the current model" while the live deterministic compute is **17%**; and the "80% knee" jargon on the Reliable-new-work hero card has no plain-language gloss. Trimmed Done to 10; extended the NOTES demo-seed entry with the live-computed-current-week-snapshot trap. README: rewrote "How Capacity Is Calculated" to the shipped target-utilization (80% knee) model (was the stale `100 − penalties` formula).
- 01:06 UTC — **improve**: Demo "WIP overload" risk bar no longer pins at 100/100 — `services/demoData.ts`. Consolidated the demo work blocks from 9 distinct `project_name`s to exactly 5, so the live `computeWeeklyCapacitySnapshot` feeds `activeProjectCount = 5` into the super-linear `wipLoadScore = pow(5/7,2) ≈ 0.51` (was `pow(9/7,2)` → clamped 1.0), rendering the Weekly "WIP overload" RiskRow at ~51/100 (mid-range, matching the seeded `wip_load_score: 0.52`) instead of an alarming 100/100 beside its calm peers. Merges read as natural initiatives: demo-dashboard → "Capacity model v2"; both calendar meetings (planning sync + product review) → "Weekly operating metrics" alongside demo-reporting; demo-blocker ("Marketing attribution refresh") → "Revenue attribution mismatch" alongside demo-attribution; demo-retention + demo-requirements stay distinct. Updated `demo-correction-2`'s `new_value` ("Executive capacity dashboard" → "Capacity model v2") so the seeded correction log stays consistent with the dashboard block's new project. `demoData.ts` only; project_name feeds ONLY `activeProjectCount`, so the lone side effect is the intended ~5pp lift in reliable capacity (lower WIP penalty). Build green; passed self-review (adversarial diff re-read: exactly 5 distinct names, all 9 blocks `included` since the blocker has `blocker_flag:true`, window-title samples + calendar event titles are separate concepts left intact, a project spanning two stakeholder groups is realistic, no other capacity aggregate depends on project_name).
- 02:06 UTC — **improve**: Demo manager summary no longer cites a deterministic figure the Weekly hero contradicts — `services/demoData.ts`. The hardcoded manager-ready narrative attributed "24% reliable capacity … next week" to "the current model", but 24% is the *AI forecast's* figure; the deterministic `computeWeeklyCapacitySnapshot` computes a different number over the demo blocks. The curator's stated "17%" was itself stale (it predated #150's 9→5 project consolidation, which dropped `wipPenalty` 10→5). Verified the LIVE deterministic value by replicating the formula over the 9 demo blocks (included=9, projects=5, recurring=28, reactive=24, carry=4, ctx=0.33, wip=0.51 → frag=4, wipP=5, **committed=58, reliable=22**). Reworded the string to cite the deterministic **22% reliable** + **~58% already committed** (mirroring the live narrative's committed-utilization framing), kept textually distinct from the AI forecast's 24% per the deterministic-vs-AI convention. The seeded `demoSnapshot`/`snapshotHistory` feed only Trends/baselines (not the live hero), so they're untouched. Build green; passed self-review (high-effort `/code-review`: no findings).
- 03:05 UTC — **improve**: Gloss the "80% knee" jargon on the Reliable-new-work hero card — `components/common/MetricCard.tsx` + `components/capacity/WeeklyCapacityScreen.tsx`. The Weekly "Reliable new work" `MetricCard` helper reads "X% already committed · room to the 80% knee" but `MetricCard` rendered the helper as a bare `<small>` with no tooltip — queueing-theory jargon a new analyst can't parse. Added an optional `title?: string` prop that renders both as a hover tooltip on the `.metric-card` container AND an explanation-only `.sr-only` span (matching the `.summary-score` title+sr-only convention). The Reliable-new-work card passes the plain-language gloss "Past ~80% utilization, delays grow sharply — we hold back the last ~20% as buffer". Purely additive: `title` defaults `undefined` → no attribute + no sr-only span, so the other three hero cards and every other `MetricCard` call site are byte-identical to before. Build green; passed self-review (additive a11y-only diff: backward-compatible optional prop, `.sr-only` already in `styles.css`, no other call sites affected).
- 04:09 UTC — **improve**: Demo Trends chart no longer renders two of its four flagship lines dead-flat — `services/demoData.ts`. The seeded `snapshotHistory` varied only `reactive_pct`/`reliable_new_work_capacity_pct` (+ baseline-only `meeting_pct`/`context_switch_score`) per week, so the other two series `TrendsScreen` plots — `allocated_pct` (base 92) and `deep_work_pct` (base 38) — sat at the `demoSnapshot` default across all three history weeks and drew as flat placeholder lines. Added per-week `allocated_pct`/`deep_work_pct` overrides telling a coherent story (calm earlier week 86/44 → busy middle week 95/31 → recovering week 90/39) so all four lines show natural cross-week movement and non-zero legend deltas. Scope is Trends-only: `computeCapacityBaselines` medians just reactive/meeting/context/reliable and `scoredForecasts` reads only `reliable_new_work_capacity_pct` (both in `useDerived.ts`), so the baseline chips and the seeded forecast track-record chips (actuals 26/29, unchanged) stay byte-identical; `deep_work_pct < allocated_pct` holds each week. Build green; passed self-review (adversarial diff + cross-file consumer trace: the two new fields are read by `TrendsScreen` alone, values 0–100 and internally consistent).
- 05:10 UTC — **improve**: "Reliable new work" hero card no longer claims "room to the 80% knee" when already past it — `components/capacity/WeeklyCapacityScreen.tsx`. The card helper was unconditional (`${committed}% already committed · room to the 80% knee`), but `reliable_new_work_capacity_pct = clamp(80 − committed, 0, 40)` clamps headroom to **0** once `committed_utilization_pct >= 80` — so an over-the-knee user saw "0%" beside copy promising "room to the 80% knee". Made the copy conditional via a new module-scope `RELIABILITY_KNEE_PCT = 80` const (mirrors `TARGET_UTILIZATION_PCT` in `capacity.ts`): `committed >= 80` → "…past the 80% knee", else "…room to the 80% knee", with the const interpolated into both branches so threshold and displayed "80%" can't drift. Demo unaffected (committed 58 < 80 → byte-identical "room to the 80% knee"). `WeeklyCapacityScreen.tsx` only; no CSS/structure change, `pct()` formatting identical. Build green; passed self-review (adversarial diff re-read: boundary `==80` reads "past" = no-headroom correctly; `>100` still reads "past"; the `<80` cap-at-40 case still correctly says "room").
- 06:10 UTC — **improve**: De-binarize the core inference file `capacity.ts` (removed 2 NUL bytes) — `packages/inference/src/capacity.ts`. `analyzeCorrections`'s bias dedup key used two literal `\0` (NUL, 0x00) bytes as the field delimiter (`field\0old\0new`) — collision-safe (label values like "Planned analysis / project work" carry spaces/slashes that a space delimiter would alias) but it made `file` report the codebase's most-grepped inference file as "data" and grep/ripgrep skip it by default (hit this mid-run: "binary file matches", no results, couldn't search for `export function`/`computeCapacityBaselines`). Replaced the NUL-delimited template literal with a printable `JSON.stringify([correction.field, correction.old_value, correction.new_value])` composite key — injective over all string triples (strictly more collision-safe than the NUL key), fully plain text, idiomatic. Behavior byte-identical for realistic inputs: demo's 7 corrections still yield the same 2 systematic biases (planned_status planned→unplanned ×3, category mislabel ×3, count 3 each; `project_name` correction stays excluded; `labelCount` 6). After: `file` → "JavaScript source, UTF-8 text"; NUL count 2→0; grep/ripgrep search it normally. No behavior/type/UI change; returned `CorrectionBiasAnalysis` shape untouched. Build green; passed self-review (`/code-review` medium — 0 findings). Note: STATUS "Next" had no concrete items left; this defect surfaced while grepping `capacity.ts` to verify the top demo-staleness theme (itself fully resolved).
- 06:44 UTC — **curator**: visual pass ran (44 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup + forecast/trends/agent/corrections/flagged × light/dark wide + 420px-narrow). App confirmed highly polished across themes/widths (no dark-contrast or narrow-clipping regressions; the prior demo-staleness theme is fully resolved — live `computeWeeklyCapacitySnapshot` numbers reconcile with the seeds/copy). `## Next` held ZERO concrete items — replenished with 3 source-verified items, all targeting the Review Copilot suggestion card, the one surface lagging the app's display conventions: (1) it renders the raw `ReviewCopilotAction` enum + an un-glossed confidence % (`ReviewCopilotPanel.tsx:53`, visible in demo as "note · 91%") — add a `reviewActionLabel` humanizer + the title/sr-only convention; (2) the Agent copy-response button never announces its "Copied" state to SR (`AgentScreen.tsx:650`); (3) a suggestion names no affected block, only a bare "N block(s)" count. Verified the rest of the app is genuinely thorough (every metric tooltip, progressbar aria, evidence drill-down, forecast range, narrative copy, agent message affordances present) and did NOT fabricate items. Trimmed Done to 10; added a NOTES entry flagging the Review Copilot panel as the lagging surface (correcting the metric-sweep "COMPLETE" claim). README checked against `package.json` + screens — no drift.
- 07:05 UTC — **improve**: Humanize the Review Copilot suggestion action + gloss its confidence % — `lib/format.ts` + `components/review/ReviewCopilotPanel.tsx`. The card rendered the raw lowercase `ReviewCopilotAction` enum (`{suggestion.action}` → "note · 91%" in demo), the lone remaining "never render raw enums" violation, plus a bare confidence % with no tooltip/`.sr-only`. Added an exhaustive `reviewActionLabel(action: ReviewCopilotAction): string` (`Record<ReviewCopilotAction,string>` so TS fails the build on a new member; `note`→"Add note", `merge`→"Merge blocks", `split`→"Split block", `confirm`/`relabel`/`exclude` capitalized) and render it instead ("Add note · 91%"); gave the `· N%` a `title` ("How confident the AI is in this suggested cleanup action") + explanation-only `.sr-only` span per the `ConfidenceChip`/`MetricCard` convention. Frontend-only; the only display consumer of `suggestion.action` (App.tsx/useReviewCopilot uses stay raw logic/audit). Build green; passed self-review (exhaustive record verified against the union, sr-only explanation-only not a value restatement).
- 08:04 UTC — **improve**: Announce the "Copied" state on the Agent copy-response button — `components/agent/AgentScreen.tsx`. The copy-response icon button swapped its icon to a `Check` on success but kept a static `title`/`aria-label` of "Copy response", so screen-reader and tooltip users got no confirmation the copy landed (the icon swap is invisible to SR). Mirrored the existing visual predicate (`copiedMessageId === m.id`, already gating the `Check`/`Copy` icon on the same line) into BOTH `title` and `aria-label`: copied → "Copied", else → "Copy response", matching the icon-button title+aria a11y convention (NOTES "Icon-only button a11y"). Purely additive accessible-name change — the non-copied branch is byte-identical to the old static strings, the `copyMessage(m)` onClick + icon logic untouched, and the temporary "Copied" name reverts when `copiedMessageId` resets (same lifecycle as the icon). `AgentScreen.tsx` only; no CSS, no new state. Build green; passed self-review (focused adversarial pass: predicate matches the icon swap exactly, no inverted condition/removed guard, cross-file consumers unaffected).
- 09:06 UTC — **improve**: Name the block(s) a Review Copilot suggestion will change — `components/review/ReviewCopilotPanel.tsx` + `components/review/DailyReviewScreen.tsx`. The suggestion card showed only a bare "N block(s)" count, so a user couldn't tell WHICH block a suggested "Add note"/"Relabel" touched before clicking Apply — the lone surface lagging the app's cite-your-evidence convention. Threaded a memoized `work_block_id → project_name` (the BlockCard `<h3>` headline) `Map` from `DailyReviewScreen` (built over all `blocks`, so verified-block titles still resolve) into the panel as a new required `blockTitles` prop. New module-scope `affectedBlocksLabel(ids, blockTitles)` renders the affected title(s) inline: count from `ids.length` (byte-identical to the old singular/plural rendering), names deduped via `Set` (a merge can touch several blocks sharing one project_name → avoids "X, X"), collapsed to "first two, +K more" beyond two, with a `title` hover listing every name. Demo shows "1 block · Revenue attribution mismatch" (demo-suggestion-1 → demo-blocker). Frontend-only. Build green; passed self-review (`/code-review` high — 0 findings: count parity, type-predicate null/empty-title narrowing, dedup, empty-ids/>2-names edges, single cross-file consumer).
- 10:11 UTC — **improve**: Deterministic weekly narrative no longer claims "near the 80% knee" when committed load is past it — `packages/inference/src/capacity.ts`. `generateWeeklyNarrative` ended both user-reviewed strings with unconditional knee copy ("…enough to stay near the ~80% utilization knee where delivery reliability holds" / "…keeping total load near the ~80% reliability knee"), but `committed_utilization_pct` is an unbounded penalty sum that routinely exceeds 80 on a busy week — where `reliable_new_work_capacity_pct = clamp(80 − committed, 0, 40)` clamps to 0, so the manager-ready summary said "0% reliable capacity … keeping total load near the 80% knee where reliability holds" (a copy/value contradiction; the user is over the knee with no headroom). Adopted the clamp-aware branch already shipped on the Weekly "Reliable new work" card: `overKnee = committed_utilization_pct >= TARGET_UTILIZATION_PCT` (reuses the in-file const; inference must not import the apps-side `RELIABILITY_KNEE_PCT`) feeds over-branch clauses ("already past the ~80% utilization knee where delivery reliability degrades" / "putting total load past the ~80% reliability knee, so new work should wait until committed load drops") and keeps the under-branch BYTE-IDENTICAL (threshold interpolated as `~${TARGET_UTILIZATION_PCT}%` = "~80%"). Demo unaffected (committed 58 < 80 → under-branch). Return shape unchanged; no string-matching consumer. Also replenished the empty `## Next` with 3 source-verified NaN-edge-case items from an Explore sweep (format/blocks/outlookIcs malformed-timestamp guards). Build green; passed self-review (8-angle equivalent: under-branch byte-parity, removed-string behavior preserved in else, `WeeklyNarrative` consumers untouched, boundary `==80` reads "past"=no-headroom, deterministic narrative needs no prompt version bump).
- 11:09 UTC — **improve**: `formatRange` no longer renders "NaN min" for a malformed block time — `apps/desktop/src/lib/format.ts`. The duration suffix computed `Math.round((end.getTime() - start.getTime()) / 60000)` with no finite guard, so an empty/malformed `start_time`/`end_time` rendered "… (NaN min)" wherever a block time-range shows (BlockCard, ledger, evidence). Factored the start–end `head` string (computed exactly as before, so the end-time format call stays unconditional and its pre-existing "Invalid Date" rendering for a bad end is unchanged) and guarded the suffix with `Number.isFinite(startMs) && Number.isFinite(endMs)`: invalid → return `head` alone; valid → byte-identical `(N min)` suffix. Return type stays `string`, so every consumer is unaffected. Build green; passed self-review (NaN path returns head, finite path arithmetic identical, no regression to the unconditional end-time format).
- 12:06 UTC — **improve**: Invalid timestamps no longer mis-rank the activity ledger's app summaries — `apps/desktop/src/lib/blocks.ts`. `summarizeRecentSessions` ranked sessions by `new Date(start).getTime()`, so a malformed `start_time` → `NaN` silently broke two sites: line 48's "is this newer?" pick (`NaN > x` always false → `window_title`/`latest_start_time` froze on the first-seen session) and line 59's sort comparator (`NaN - x` → undefined order). Added a module-scope `comparableStartMs(iso)` returning the finite ms or `Number.NEGATIVE_INFINITY` (per the malformed-timestamp `Number.isFinite` convention, not `?? 0` which misses `NaN`); both sites route through it. Invalid times map to `-Infinity` so a bad timestamp can never win "latest" (ranks as oldest), and the sort uses a compare form (`leftMs===rightMs?0:rightMs>leftMs?1:-1`) instead of subtraction so two unparseable times (`-Infinity - -Infinity`) don't re-introduce `NaN`. Valid-timestamp behavior is byte-identical (the `>` compare and the descending sort sign are preserved). `blocks.ts` only; no type/UI change. Build green; passed self-review (adversarial re-read: valid-path sign-equivalence, all four invalid combos keep a deterministic non-overwriting pick, both-invalid sort returns stable 0, no NaN reachable).
- 12:44 UTC — **curator**: visual pass ran (25 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup/forecast/trends/agent/corrections in light + dark wide + the 420px compact layout). App confirmed highly polished across themes/widths — no dark-contrast or narrow-clipping regressions; spot-verified search-empty states (ledger/corrections/audit `EmptyState`), the "Suggest cleanup"→"Thinking…" disabled state, and the Reset `ConfirmDialog` (defaults `tone="danger"`). Reconciled the lone open `## Next` item (`outlookIcs.ts:116` malformed-DURATION NaN) → **verified already safe** (the `?? 0` is applied to the capture group before `Number()`, so `minutes` is always finite — the item even recommended the form already shipped) and moved it to Done; the Robustness/Edge-Cases batch is now complete. Replenished `### Interaction & Visual Polish` with 2 source-verified items, both on the Forecast Agent scenario panel: (1) the four `.forecast-summary` cards lack the metric `title`+`.sr-only` convention every other tile carries (`ForecastAgentPanel.tsx:135-156`); (2) the demo seeds `likely_capacity_pct === reliable_new_work_capacity_pct`, so two scenario cards show the identical % (`demoData.ts`). Trimmed Done to 10. NOTES: corrected the "metric title+sr-only sweep COMPLETE" overclaim (forecast cards are the holdout) + marked the outlookIcs batch item verified-safe. README checked against `package.json` + screens — no drift.
- 13:09 UTC — **improve**: Forecast scenario cards now carry the metric `title`+`.sr-only` convention — `apps/desktop/src/components/capacity/ForecastAgentPanel.tsx`. The four `.forecast-summary` cards (AI reliable estimate / Conservative / Likely / Optimistic) were the lone holdout from the app-wide metric tooltip+SR-gloss sweep — bare `<span>`/`<strong>`/`<small>` with no hover `title` and no `.sr-only` explanation. Added a plain-language `title` on each card container `<div>` plus an explanation-only `.sr-only` span as the last child, following the `.summary-score`/`MetricCard` convention exactly (what each scenario means, NOT a value+label restatement). `.sr-only` is `position:absolute` so the card flex layout is untouched; no CSS/structure change. Build green; passed self-review (faithful replication of the production MetricCard pattern — title on container, explanation-only sr-only last child, no double-announce, layout unaffected). The metric title+`.sr-only` sweep is now app-wide.
- 14:09 UTC — **improve**: A1 — Acceleration Engine domain types + audit event — `packages/domain/src/models.ts` (+ one forced line in `apps/desktop/src/lib/format.ts`). Laid the type foundation for the new flagship Acceleration surface: `AccelerationPlayType = "automate" | "tool" | "technique"`; `AccelerationSignal` (deterministic miner output — `signal_id`/`type`/`title`/`detail`/`evidence: string[]`/`estimated_minutes_saved_per_week`/`confidence`/`derived_from: string[]`, doc-commented with the derived-only-no-window-titles privacy rule); and `AccelerationPlay extends AccelerationSignal` adding the AI-authored payload `recipe: string | null`, `recommended_tools: string[]`, `dismissed: boolean`. Added `"acceleration_engine"` to the `AuditEventType` union; the exhaustive `Record<AuditEventType,string>` in `auditTypeLabel` forced `acceleration_engine: "Acceleration"` so `tsc -b` (the type-check IS this task's build gate) passes — the only behavior-free spillover. Deferred the rest of the 4-file audit recipe (AuditLogScreen filter chip + tinted badge CSS) to the task that emits the event (C3/D2); no `acceleration_engine` event exists yet so the missing filter/badge are inert. Types only, no behavior, no new consumers. Build green; passed self-review (diff matches spec field-for-field, `extends` inherits all signal fields, format.ts label is the minimal forced change).
- 15:10 UTC — **improve**: B1 — Deterministic repetitive-sequence detector — new `packages/inference/src/accelerate.ts`. First deterministic Acceleration miner: pure `detectRepetitiveSequences(sessions: ActivitySession[]): AccelerationSignal[]`. Orders sessions by `start_time` via a `comparableStartMs` helper (`Number.isFinite` → `NEGATIVE_INFINITY`, not `?? 0`; compare-form sort so two malformed times never re-introduce `NaN`), slides windows of length 2–3 over the consecutive `app_name` stream, counts each n-gram in a `Map` (skipping all-same-app runs, which are not handoffs). Any n-gram recurring ≥3 times emits one `type: "automate"` signal: `estimated_minutes_saved_per_week` = `round(0.25 × sum of involved sessions' duration_minutes)` (each `Number.isFinite`-guarded), `confidence` = `min(0.95, 0.5 + (count−3)×0.1)` (0–1, 2-dp), `evidence` = derived lines (app flow + counts + minutes — app names ONLY, no window titles), `derived_from` = the involved `session_id`s, deterministic `signal_id = automate-<stableHash(seq)>`. Dedup/ranking across detectors deferred to B4, so length-2 and length-3 n-grams of one flow are both emitted by design. `accelerate.ts` only (new file, inference tsconfig `include` glob). Build green; passed self-review (adversarial re-read: ascending sort verified, both-`-Infinity` returns 0, overlapping-window tally correct, privacy = app-names/counts/minutes/opaque-ids only, NaN-duration guarded, empty/short input → `[]`).
- 16:11 UTC — **improve**: B2 — Deterministic time-sink detector — `packages/inference/src/accelerate.ts`. Added the second Acceleration miner, pure `detectTimeSinks(blocks: WorkBlock[], sessions: ActivitySession[]): AccelerationSignal[]`. Tallies reviewed `WorkBlock[]` by `WorkCategory`, restricted to the four tool-able favorites (`TOOLABLE_CATEGORIES`: Recurring reporting / Admin / coordination / SQL / data modeling / query work / Dashboard development / edits), accumulating block count, total minutes, low-deep minutes (`mode !== "Deep work"`), block ids, and a `project_name → minutes` map. Minutes derive from `estimated_capacity_pct` via the new `minutesFromCapacityPct` (`(pct/100) × WEEKLY_BASELINE_MINUTES` = `40*60`, NaN-guarded through the existing `finiteMinutes`; baseline kept as a local const since inference must not import integrations/apps). Emits one `type: "tool"` signal per category that is recurring (`blockCount ≥ MIN_RECURRENCES = 3`) AND has non-deep time (`lowDeepMinutes > 0` ⇒ `totalMinutes > 0`, so the share denominator is safe), skipping any whose `estimatedSaved = round(0.25 × lowDeepMinutes)` rounds to 0. `confidence = min(0.9, 0.5 + (blockCount−3)×0.08 + lowDeepShare×0.1)` (2-dp); evidence = category + hours/week + non-deep share + recurrence count + dominant project (new `dominantProject` helper) — labels/counts only, never window titles (`project_name` is a derived label); `derived_from` = the block ids; deterministic `signal_id = tool-<stableHash(timesink:<category>)>`. Results sorted desc by minutes-saved with a `signal_id` tie-break (B4 re-ranks). `sessions` accepted for detector-signature parity (B4) but unused (`void sessions`). Reuses `stableHash`/`finiteMinutes`/`SAVINGS_FRACTION`/`MIN_RECURRENCES`. Build green; passed self-review (high-effort `/code-review` — 0 findings: NaN-guarded minutes, division-by-zero impossible, confidence bounded, fully deterministic, privacy-safe, empty/all-deep → `[]`).
- 17:13 UTC — **improve**: B3 — Deterministic context-switch hotspot detector — `packages/inference/src/accelerate.ts`. Added the third Acceleration miner, pure `detectContextSwitchHotspots(sessions: ActivitySession[]): AccelerationSignal[]`. Orders sessions by `start_time` (reuses the in-file `comparableStartMs` + the `l===r?0:r>l?-1:1` compare form), walks consecutive pairs, and counts a "switch" when `prev.app_name !== curr.app_name` — the same real-handoff notion `detectRepetitiveSequences` uses and the session-level analog of capacity's block-mode `context_switch_score` (both treat reactive app-hopping as the fragmentation cost). Each switch is bucketed by the LOCAL hour switched *into* (`localHourOfDay` → `getHours()`, NaN-guarded via `Number.isFinite`, matching capacity's local interruption bucketing; an unparseable time is skipped, not counted). Emits one `type:"technique"` signal per hotspot hour: `count ≥ MIN_HOTSPOT_SWITCHES (=MIN_RECURRENCES=3)` AND concentrated (`activeHours ≤ 1` OR `count ≥ meanPerActiveHour × 1.5`) — so evenly-spread switching flags nothing while a dominant hour surfaces. `estimated_minutes_saved_per_week = round(REFOCUS_MINUTES_PER_SWITCH 3 × count)` (3 min/switch is conservative, cited well below Mark et al. CHI 2008's ~23-min full-disruption cost). `confidence = min(0.9, 0.5 + (count−3)×0.05 + share×0.1)` (2-dp); `evidence` = derived counts only (switch count + an `hourWindowLabel` like "2pm–3pm" + `share%` floored to 1 + refocus minutes — no window titles); `detail` carries the batching/focus angle; `derived_from` = the session ids on either side of the bucketed switches; deterministic `signal_id = technique-<stableHash(hotspot:<hour>)>`. Sorted desc by minutes-saved with a `signal_id` tie-break (B4 re-ranks). New local `localHourOfDay`/`hourLabel`/`hourWindowLabel` helpers (no cross-import). `accelerate.ts` only. Build green; passed self-review (two parallel finder agents — correctness + reuse/conventions — both 0 findings: comparator total/NaN-free, 0/1-session → `[]`, no division-by-zero, confidence bounded, deterministic, privacy-safe; renamed a local `window` const → `timeWindow` to avoid shadowing the global).
- 18:13 UTC — **improve**: B4 — Ranked Acceleration signal aggregator — `packages/inference/src/accelerate.ts` + `apps/desktop/src/hooks/useDerived.ts`. Added `buildAccelerationSignals({ blocks, sessions }): AccelerationSignal[]`, the single entry point that fans `{blocks, sessions}` across the three deterministic miners (B1 `detectRepetitiveSequences`, B2 `detectTimeSinks`, B3 `detectContextSwitchHotspots`), dedupes, ranks, and caps. Ranking is a shared `compareSignals` total order: `signalScore = estimated_minutes_saved_per_week × confidence` desc → raw minutes desc → `signal_id` asc (deterministic, no NaN — every emitter `Math.round`s minutes and `Number(toFixed(2))`s confidence). Two-layer dedup: (1) defensive `signal_id` Map dedup keeping the higher-ranked on a clash (`compareSignals(signal, existing) < 0`); (2) a nested-evidence collapse restricted to `automate` signals — B1 emits BOTH the 2-gram and 3-gram of one flow, so iterating best-first and dropping a candidate whose `derived_from` set is a sub/superset (`isSubsetOrEqual` both directions) of an already-kept automate signal keeps only the top-ranked of each nested cluster. The collapse is `automate`-only by design: `tool` sets are disjoint per-category and `technique` is one-per-hour, so a generic collapse could wrongly drop a distinct hotspot whose sessions overlap another's; `size > 0` / `size === 0` guards stop an empty `derived_from` collapsing everything. Capped to `MAX_ACCELERATION_SIGNALS = 6` with an early `break` once the cap is reached (bounds the O(n²) collapse) plus a defensive trailing `slice`. `useDerived.ts` exposes `accelerationSignals` via a memo over a new shared `weekBlocks` memo (lifted out of `interruptionLoad`'s inline filter so neither re-filters the ledger per consumer) and the existing `activeWindowSessions`. Privacy holds structurally — B4 only merges/passes through the detectors' already-derived-only evidence + ids. Build green; passed self-review (two parallel finders, correctness + cleanup/conventions, 0 correctness bugs; applied the one worthwhile nit — the early-break cap bound).
- 18:42 UTC — **curator**: visual pass ran (27 Playwright/demo screenshots — daily/weekly/narrative/ledger/audit/setup/forecast/trends/agent/corrections in light + dark wide + the 420px compact layout). App confirmed highly polished across themes/widths — no dark-contrast or narrow-clipping regressions; every flagship metric tile carries title+`.sr-only` (the four Forecast scenario cards now done), live demo numbers reconcile (22% reliable / ~58% committed across Weekly/sidebar/Agent/narrative), and audit badges/risk bars/empty states are all in place. Confirmed `?screen=accelerate` still falls back to `daily` → C1 (the Acceleration screen) is genuinely unbuilt; the Acceleration C1–D3 sequence and the demo-forecast-duplicate item are all still open. Reprioritized: promoted `### Interaction & Visual Polish` (the demo Forecast "Likely"==​"AI reliable estimate" 24% duplicate, verified in the rendered forecast screen and at `demoData.ts:374/381`) above `### Acceleration Engine` per the polish-first mandate, with the flagship sequence resuming at C1 immediately after. Trimmed Done to 10. NOTES unchanged (no new durable learning — all conventions already captured). README checked against `package.json` + screens — no drift.
- 19:07 UTC — **improve**: Demo Forecast "Likely" no longer duplicates "AI reliable estimate" — `apps/desktop/src/services/demoData.ts`. All four demo forecasts seeded `likely_capacity_pct === reliable_new_work_capacity_pct`, so the Forecast Agent panel showed two scenario cards with the identical % (e.g. "AI reliable estimate 24%" beside "Likely 24%"). Nudged `likely` to `reliable + 2` in each (next-week 24→26; history weeks 24→26, 15→17, 31→33) so it reads as the expected-case middle just above the protected reliable figure. Every one keeps `conservative ≤ likely ≤ optimistic` (14≤26≤34 / 13≤26≤30 / 9≤17≤23 / 21≤33≤39) and stays near `reliable`; each computed `likelyLeft` (60/76/57/67%) stays inside the 12–88% range-label visibility gate. No copy cites `likely` (managerSummary cites the unchanged reliable=24 / live 22%). `demoData.ts` only. Build green; passed self-review (range invariant + marker-gate + no-copy-collision verified).
- 20:21 UTC — **improve**: C1 — Acceleration screen (deterministic-only, always on) — new `apps/desktop/src/components/accelerate/AccelerationScreen.tsx` + new shared `components/common/EvidenceDetails.tsx` + `lib/types.ts` + `lib/ui.ts` + `lib/format.ts` + `components/shell/ScreenRouter.tsx` + `App.tsx` + `components/ledger/BlockCard.tsx` + `styles.css`. Shipped the new flagship `accelerate` surface rendering the B4 `accelerationSignals` as evidence-cited Play cards. Followed the 5-spot add-a-screen recipe: `"accelerate"` → `Screen` union; `screenLabels.accelerate = "Acceleration"` (also gates `?demo=1&screen=accelerate`), `primarySectionForScreen → "agent"`, and a new **agent** `sectionViews` branch giving the Agent section an "Ask | Accelerate" sub-tab (it had none before); rendered in `ScreenRouter` via a new `accelerationSignals` prop threaded from the existing `useDerived` value through `App.tsx`. Each Play card: per-type chip (humanized via the new `accelerationTypeLabel`, never the raw enum; `--info`/`--success`/`--warning` token border, icon `aria-hidden`), title/detail, a confidence figure, the estimated time-saved metric (title + explanation-only `.sr-only`), and a "Why this play?" evidence drill-down; a header `.acceleration-total` pill sums reclaimable minutes; empty state routes through the shared `EmptyState`. Deterministic-only (no AI/API key); privacy holds structurally (derived-only evidence — app names/counts/session ids, never window titles). Self-review (two parallel finders: correctness/cross-file 0 bugs + confirmed no exhaustive `Record<Screen,…>`/`switch` break and no `sectionViews` consumer beyond `ContextNavigation`; conventions/a11y confirmed token-only colors, explanation-only `.sr-only`, humanized enum, derived-only privacy, `EmptyState` reuse) surfaced one worthwhile cleanup — applied it by extracting the duplicated evidence-drill-down markup into a shared `EvidenceDetails` now used by both `AccelerationScreen` and `BlockCard`. Build green; verified via Playwright/demo in light + dark wide and the 420px compact layout (1 TECHNIQUE play renders, no overflow, Agent screen unregressed, ledger BlockCard evidence still expands).
