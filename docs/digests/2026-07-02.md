# Here's what Claude shipped overnight — 2026-07-02

**TL;DR** — 25 improvements shipped · Acceleration Engine v2 complete (E1–E5: memory, feedback loop, 2 new detectors) · 4 curator passes · all 20 Next items cleared · build green throughout · 0 reverts

---

## Shipped

### Performance & Robustness (5 commits)

- **Memoize LedgerScreen render-hot derivations** (#193, `components/ledger/LedgerScreen.tsx`) — wrapped `classifiedSessionIds`, `unclassifiedSessionCount`, `visibleBlocks`, and `current` in `useMemo` with minimal deps; pure refactor, zero output change, per-keystroke allocation drops eliminated.

- **Export Saved Skills as Anthropic Agent Skills** (#194, `feat`, `SavedSkillsScreen.tsx` + `lib/dataExport.ts`) — new "Export SKILL.md" button on the Saved Skills screen; serializes persisted recipes to the Anthropic Agent Skills format.

- **Guard `ConfidenceChip` against NaN/Infinity** (#195, `components/common/ConfidenceChip.tsx`) — extended early-return to `!Number.isFinite(value)` so a malformed import's confidence shows "Unscored" instead of "Needs review NaN%".

- **Extract + guard BlockCard local-time helpers** (#200, `components/ledger/BlockCard.tsx`, `lib/format.ts`) — moved `toLocalTimeInput`/`applyLocalTime` to `lib/format.ts`; hardened `applyLocalTime` with a `Number.isFinite` guard so a malformed `hhmm` string can't throw `NaN` into `toISOString()`.

- **Consolidate ActivityHeatmap label helpers** (#201, `lib/format.ts`, `components/ledger/ActivityHeatmap.tsx`) — three sibling exports added to `lib/format.ts` (`formatHourCompact`, `formatHourA11y`, `formatRelativeDayLabel`); private duplicates in `ActivityHeatmap.tsx` deleted.

- **DRY the slice(-N) rolling-window logic** (#202, `packages/inference/src/capacity.ts`) — extracted `sliceLastN<T>(items, n)` pure helper; both `summarizeForecastAccuracy` and `computeCapacityBaselines` route through it; latent `array.slice(-0)` bug (returns full array, not empty tail) hardened.

### Insight & UX Enhancements (9 commits)

- **Work-mode allocation split on Weekly screen** (#203, `packages/domain/src/taxonomy.ts`, `components/capacity/WeeklyCapacityScreen.tsx`, `styles.css`) — second "How the week fragmented" block showing Deep-work / Reactive / Collaborative / Fragmented / Blocked split; `modeColors` map mirrors `categoryColors`.

- **Fragmentation & WIP point-cost in committed load** (#204, `packages/domain/src/models.ts`, `packages/inference/src/capacity.ts`, `components/common/RiskRow.tsx`, `WeeklyCapacityScreen.tsx`, `styles.css`, `demoData.ts`) — new `fragmentation_penalty_pct` / `wip_penalty_pct` fields on `WeeklyCapacitySnapshot`; "Costs ~N% of your committed week" caption on each risk row when penalty > 0.

- **Forecast bias direction** (#205, `capacity.ts`, `lib/format.ts`, `ForecastAgentPanel.tsx`, `TrendsScreen.tsx`) — new `mean_signed_error_pts` field + `forecastBiasPhrase()` humanizer ("tends to over-/under-predict by ~N pts") on both accuracy surfaces; suppressed when near zero.

- **Meeting density as a 5th Trends series** (#206, `TrendsScreen.tsx`, `styles.css`) — `meeting_pct` added to `SERIES`; legend, polyline, dots, and sr-only data table all inherit it in lockstep; amber `#f59e0b` `.trend-meeting` class; `betterWhen: "lower"`.

- **Reactive-load concentration index** (#207, `capacity.ts`, `WeeklyCapacityScreen.tsx`) — new `concentration_pct` / `concentration_day_count` / `concentration_is_clustered` fields; batchable cluster cue shown when the top 2 days lead an even spread by ≥ 15 pts AND ≥ 3 active days remain.

- **Undo last correction in Daily Review** (#208, `App.tsx`, `DailyReviewScreen.tsx`, `ScreenRouter.tsx`, `hooks/useBlocksLedger.ts`, `styles.css`) — `Undo2`-icon affordance in both review-queue and all-confirmed states; replays the inverse relabel as a fresh correction + audit event; scoped to single-field `MANUAL` relabels; gated on block still holding the changed value to prevent lying "Reverted…" toasts.

- **Import delta for Outlook .ics re-imports** (#209, `App.tsx`, `lib/audit.ts`, `ScreenRouter.tsx`, `SetupScreen.tsx`, `styles.css`) — diffs parsed events against stored `calendarEvents` before merge (added / updated / unchanged via `calendarEventChanged`); toast reads "12 events imported (+3 new, 1 updated, 8 unchanged)"; lingering `.import-delta` Settings line; audit event enriched with counts.

- **Fix empty-week Summary confidence `--` placeholder** (#210, `WeeklyCapacityScreen.tsx`, `styles.css`) — replaced `<strong>--</strong>` with a muted em-dash `—` via `.summary-score-empty` class (`aria-hidden`); `title` + `.sr-only` now read "No capacity estimate yet — confidence appears once work blocks exist".

- **Agent "Copy response" clipboard-lie fix** (#197, `components/agent/AgentScreen.tsx`) — last holdout of the clipboard-false-success pattern; success state + toast now gated behind resolved `await`; catch pushes the shared "Couldn't copy to the clipboard" error toast.

### A11y / Screen Reader (5 commits)

- **SavedSkills "Remove" button aria-label** (#198, `SavedSkillsScreen.tsx`) — `aria-label=\`Remove ${skill.title} from your library\``; one-line additive change mirroring the per-instance convention.

- **Trends chart non-visual data path** (#199, `TrendsScreen.tsx`) — visually-hidden sr-only `<table>` linked via `aria-describedby`; `scope="col"` per series, `scope="row"` per week; values byte-identical to existing dot-title format; no visual change.

- **Block-field relabel dropdowns per-block aria-label** (#211, `BlockCard.tsx`) — `Work category — ${block.project_name}` / `Planned status — …` / `Work mode — …`; visible `.tag-field-label` stays generic.

- **Block Confirm / Exclude / time-edit buttons per-block aria-label** (#217, `BlockCard.tsx`) — `Confirm — ${project_name}` / `Exclude — …` / `Edit block time range — …` / `Save time changes — …` / `Cancel time edit — …`; existing `role="alert"` error announcement on Save unaffected.

- **Acceleration play-card controls per-play aria-label** (#218, `AccelerationScreen.tsx`) — `signal.title` folded into Save / "I acted on this" / Dismiss / "Save to library" / Copy buttons; toggle aria-labels mirror transient state ("Saved"/"Save", "Acted on"/"I acted on this"); visible labels stay generic per convention.

### Acceleration Engine v2 — memory, feedback & coverage (5 commits)

- **E1 — "Acted on" Play state** (#212, `localStore.ts`, `usePersistence.ts`, `App.tsx`, `AccelerationScreen.tsx`, `ScreenRouter.tsx`, `lib/audit.ts`, `demoData.ts`, `styles.css`) — third persisted array `actedOnPlayIds` following the C3 persist-user-actions recipe end-to-end (all 4 `App.tsx` hydration sites, both `localStore.ts` read paths, demo seed). `markPlayActedOn` audits via new `"acted_on"` action; `unmarkPlayActedOn` undo. Play card: `aria-pressed` toggle with `CircleCheck`↔`CheckCircle2` swap + `.play-action-acted.is-acted` in `--info` blue.

- **E2 — Weekly acceleration snapshot + cross-week recurrence** (#213, `models.ts`, `localStore.ts`, `usePersistence.ts`, `App.tsx`, `useDerived.ts`, `packages/inference/src/accelerate.ts`, `AccelerationScreen.tsx`, `styles.css`, `demoData.ts`) — persisted `accelerationHistory: PersistedAccelerationSnapshot[]` (id/type/minutes summary only, never evidence or window titles); retention effect mirrors `snapshotHistory`; `recurrenceBySignalId` memo feeds `buildAccelerationSignals`; each signal gains `recurrence_weeks` + `.play-recurring-badge` (amber, `Repeat` icon). Two-phase ranking ensures recurrence adjusts ordering only — never which signals surface or their estimated minutes.

- **E3 — Realized-savings track record** (#214, `accelerate.ts`, `useDerived.ts`, `App.tsx`, `ScreenRouter.tsx`, `AccelerationScreen.tsx`, new `AccelerationTrackRecord.tsx`, `lib/format.ts`, `styles.css`, `demoData.ts`) — flagship: turns forward-looking estimates into a scored historical record. `scoreRealizedSaving` inverts each detector's type-aware `captureFraction`; `buildRealizedSavings` scores acted-on plays against the following completed week; `summarizeRealizedSavings` rolls up beat/met/missed. Demo shows a distinct beat/on-track/below trio. Three correctness bugs fixed by self-review: "min/week" mislabel on a cross-week sum, false beats when a signal fell off the capped top-6, and mid-week score jitter from the in-progress current week.

- **E4 — Reactive-load Play** (#215, `accelerate.ts`, `useDerived.ts`) — new `detectReactiveLoad` mines the pre-computed `InterruptionLoadAnalysis`; estimate = `REFOCUS_MINUTES_PER_SWITCH × max(0, burst_count − REACTIVE_BATCH_WINDOWS)`, unifying both `technique` detectors on the same Mark-et-al. refocus constant. Focus-guard suggestion gated on core hours (after-hours peak → no "protect 8pm" advice). Stable hash id so E2 recurrence and E3 track record follow it.

- **E5 — Calendar/meeting time-sink detector** (#216, `accelerate.ts`, `useDerived.ts`, `demoData.ts`) — biggest coverage win: mines `OutlookCalendarEvent[]` for (1) recurring low-value meetings (same normalized title ≥ 3×, `technique` Play, estimate = `SAVINGS_FRACTION` × series minutes) and (2) back-to-back-meeting days (< 30 min focus gap, ≥ 2 tight transitions, estimate = `REFOCUS_MINUTES_PER_SWITCH` × tight transitions). Threaded via new `AccelerationMiningInput.calendarEvents` + `weekCalendarEvents` memo in `useDerived`. Demo seeds a daily standup Mon–Fri; `?demo=1&screen=accelerate` shows a distinct 5th "Recurring meeting" card (~19 min).

---

## Backlog curation

4 curator commits, all with Playwright visual passes:

| Commit | Time (UTC) | Notes |
|--------|-----------|-------|
| `e82ac7e` (#196) | Jul 1 12:47 | Promoted UI & UX Polish above Performance & Robustness; reconciled all 5 open items against code (verified genuinely open, corrected stale line refs); reordered by user impact (Agent copy → SavedSkills Remove → Trends table → BlockCard helpers → ActivityHeatmap). |
| `a6a4451` | Jul 1 18:47 | 39-screenshot visual pass (all screens × light/dark wide + 420px-narrow). Confirmed 7 Insight & UX Enhancement items genuinely open; top pick (work-mode allocation) confirmed polish-optimal. Trimmed Done to 10. |
| `f53532c` | Jul 2 02:03 | 30-screenshot visual pass (all screens). Replenished UI & UX Polish backlog with 2 source-verified items (flagged-capture discard needs ConfirmDialog; Narrative/Audit clipboard-lie). Trimmed Done to 10. |
| `5d2a11e` | Jul 2 06:48 | 37-screenshot visual pass. Replenished empty UI & UX Polish backlog with 2 source-verified a11y items (block Confirm/Exclude repeated-control gap; Acceleration play-card repeated-control gap). Trimmed Done to 10. |

---

## Status

| Bucket | Count |
|--------|-------|
| Done (trimmed to) | 10 |
| In Progress | 0 |
| Next — UI & UX Polish | 0 (**empty**) |
| Next — Performance & Robustness | 0 (**empty**) |
| Next — Acceleration Engine | 0 (all shipped) |
| Next — Insight & UX Enhancements | 0 (all shipped) |
| Next — Acceleration Engine v2 (E1–E5) | 0 (all shipped) |
| Next — Intelligence Engine | 0 (placeholder header only) |
| Next — Trust & Verification UX | 0 (placeholder header only) |
| Next — Capacity Model Calibration | 0 (placeholder header only) |
| **Total Next** | **0** |

**The entire Next backlog is cleared.** All 20 items open as of yesterday's digest were shipped overnight. The improve loop is stalled until the next curator run replenishes the queue.

Nothing skipped or reverted. Build passed on every commit (Stop hook autorun).
