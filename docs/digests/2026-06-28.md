# Here's what Claude shipped overnight — 2026-06-28

**TL;DR** — 17 improvements shipped · 2 new backlog sections added · 3 visual passes ran · build green · 0 reverts

---

## Shipped (hourly implementer)

The implementer ran a high-velocity burst, completing 17 tasks across UI polish, analytics, data infrastructure, and privacy. All commits went directly to main.

### UI & UX Polish

- **Fix live-capture panel title running into its subtitle** — stacked bold title above muted subtitle via `.capture-panel-heading` flex column (`ActivityCapturePanel.tsx`, `styles.css`)
- **Add expand affordance to collapsed live-capture panel** — rotating `ChevronRight` caret in `<summary>`; panel now reads as expandable like the heatmap (`ActivityCapturePanel.tsx`, `styles.css`)
- **Corrections rows stack on narrow screens** — `@media (max-width:600px)` `flex-direction:column` rule so field/project/change/timestamp each get full container width instead of clipping off the ~388px scroll box (`styles.css`)
- **Finish corrections narrow-width fix** — added `flex-wrap:wrap` + `min-width:0` to inner `.correction-change` nowrap spans so they wrap rather than overflow; full value stays accessible via existing `title` (`styles.css`)
- **Forecast model-bias values get a hover tooltip** — `title` attribute on `.model-bias-change` span reveals full `from → to` pair on hover/long-press; mirrors `.correction-change` convention (`ForecastScreen.tsx`)

### Analytics & Intelligence

- **Personal capacity baselines + trend-delta chips on Weekly screen** — `computeCapacityBaselines()` computes rolling 6-week medians for reactive, meeting, context-switch, and reliable-capacity metrics; chip row shows green/red/neutral `data-tone` arrows + `.sr-only` descriptions (`packages/inference/src/capacity.ts`, `WeeklyCapacityScreen.tsx`, `styles.css`)
- **Correction-driven model-bias signal on Forecast screen** — `analyzeCorrections()` surfaces `(field, old→new)` patterns repeated ≥3× as an explainable amber `.model-bias-note` above the Forecast Agent panel; humanized labels + `×N` count chips (`capacity.ts`, `ForecastScreen.tsx`, `styles.css`)
- **Evidence-based forecast confidence track record** — `summarizeForecastAccuracy()` computes rolling mean-absolute-error over the last 8 scored weeks; muted "±N pts over last K weeks" trend line under the accuracy banner, gated on ≥2 weeks of history (`capacity.ts`, `ForecastAgentPanel.tsx`, `styles.css`)
- **Forecast track-record panel** — `buildForecastTrackRecord()` produces a per-week predicted-vs-actual audit list (newest first) with On target / Close / Off chips + a colored left-border rail (non-color-only cue); `useDerived.ts` refactored to share one `scoredForecasts` memo feeding both the trend line and the new list (`ForecastTrackRecord.tsx`, `ForecastScreen.tsx`, `capacity.ts`, `lib/format.ts`)

### Data Infrastructure

- **Importable `RawEvent` schema + generic source-import entry point** — `importRawEvents()` normalizes any JSON shape (`RawEventImport[]`, `{events:[...]}` wrapper, or JSON string) into `RawEvent[]` + `WorkBlock[]`; per-SourceType defaults + taxonomy-validated refinements; shared `stableHash`/`capacityPctFromSpan` extracted to `packages/integrations/src/internal/normalize.ts` (rewired `outlookIcs.ts` onto them) (`rawEvents.ts`, `normalize.ts`)
- **Git-log parser as a planned-work signal** — `parseGitLog()` turns `git log --pretty=format:'%H|%aI|%an|%s'` exports (multi-repo via `# repo:` directives) into `GitCommitRecord[]`; `gitCommitsToImport()` burst-sessionizes into focus sessions (>90min gap splits); `importGitLog()` routes through `importRawEvents` for deep-work planned `WorkBlock`s. Privacy: commit subjects kept out of evidence. Live `git log` fetch/watch flagged as **[manual / Rust]** follow-up (`git/gitLog.ts`, `git/fixture.ts`)
- **Provider-agnostic `CalendarSource` interface + disabled connect stub** — `CalendarProviderDescriptor` registry (`CALENDAR_PROVIDERS`: outlook_ics file-import, google + microsoft_graph oauth); `createOutlookIcsSource` + `createOAuthCalendarSource` factories; disabled "Connect calendar" stub buttons in SetupScreen. Rust OAuth follow-up documented in module header (`calendarSource.ts`, `SetupScreen.tsx`)

### Privacy & Audit

- **"Why this block?" evidence drill-down** — `BlockCard.tsx` `<details className="evidence">` extended with a "Derived from" sub-list of `block.derived_from` source IDs as `<code>` items; both lists gated on non-empty with a shared "No inference detail recorded" fallback replacing the prior silent empty `<ul>` (`BlockCard.tsx`, `styles.css`)
- **Sensitive-content review queue (Flagged tab)** — new "Flagged" History tab (`SensitiveReviewScreen.tsx`) listing `VisualContextInsight`s with `sensitive_content_detected`; per-item Discard emits a `visual_context` audit event (window title kept out for privacy); ⌘8→Flagged, ⌘9→Settings (`lib/types.ts`, `lib/ui.ts`, `App.tsx`, `ScreenRouter.tsx`)
- **Data export & retention controls** — new "Data control" section in SetupScreen: JSON + CSV export via pure `lib/dataExport.ts` (RFC-4180 escaping + formula-injection guard; JSON envelope at full fidelity); persisted `retentionDays` auto-expires `activeWindowSamples` (single-pass, skipped in demo); new `retention_policy` `AuditEventType` logs discrete user-initiated changes only (not per-sample expiry, which would flood the 1000-cap trail); `NarrativeScreen` refactored to reuse shared `downloadTextFile` (`dataExport.ts`, `SetupScreen.tsx`, `App.tsx`, `localStore.ts`, `usePersistence.ts`)

### Backlog Scoping

- **Scoped workplace chat integrations in STATUS.md** — new "Workplace Chat & Collaboration Signals" backlog section (6 tasks: chat-export parser, ChatSource registry, SetupScreen rows, interruption/context-switch signal, stakeholder map, Teams/Webex call dedup vs calendar)
- **Generalized `slack` SourceType to generic `chat`** — single `chat` entry in `SourceType` + `SOURCE_DEFAULTS`; specific vendor (Slack/Teams/Webex) rides as a `provider` field on event metadata, not as separate source types

---

## Backlog curation (6-hour curator)

The curator ran **3 visual passes** covering 20–40 Playwright screenshots per pass (all screens × light/dark × wide + 420px narrow in demo mode):

1. **~00:42 UTC** — 30 fullPage + 5 viewport screenshots (9 screens). App confirmed highly polished. Verified all 7 remaining feature items still genuinely open against code. `## Next` had no UI & UX Polish subsection — replenished with 2 measured compact-layout items (corrections `old→new` clipping + Forecast model-bias no tooltip). Trimmed Done to 10. Added NOTES entry on nested-flex narrow-clipping gotcha + overflow-probe technique.

2. **~06:41 UTC** — 40 screenshots (10 screens, 3 widths). Both UI & UX Polish items reconfirmed open. A 420px overflow probe flagged corrections screen as the **only** screen still with element-level clipping (inner nowrap spans not wrapping after prior li-stack fix) and flagged Forecast model-bias values ellipsizing with no tooltip. Replenished with those 2 items; trimmed Done to 10; refined NOTES.

3. **~06:42–06:47 UTC** — Two new, well-specified subsections added to `## Next`:
   - **Onboarding & Feedback UX** (3 tasks): actionable empty states + first-run onboarding card, destructive-reset confirmation + export nudge, toast notification layer for async success/error feedback.
   - **Interaction & Visual Polish** (6 tasks): consistent inline retry on AI error states, agent chat stop-generation + stream recovery + follow-up prompts, guided AI-provider config in Settings, responsive primary navigation at narrow widths, remove color-only status signaling (a11y), data-viz legends + reference cues.

---

## Status

| | |
|---|---|
| Done | 10 (capped per housekeeping rule; full history in git) |
| In Progress | 0 |
| Next — Onboarding & Feedback UX | 3 tasks |
| Next — Interaction & Visual Polish | 6 tasks |
| Next — Workplace Chat & Collaboration Signals | 6 tasks |
| Next — Intelligence Engine | 0 (header + reference pattern; no active tasks) |
| **Total remaining** | **15** |

Nothing is In Progress. No tasks were skipped or reverted. The Intelligence Engine and Trust & Verification UX section headers are present but carry no active tasks; the one trust/verification task (destructive-reset confirmation) was placed under Onboarding & Feedback UX where it fits contextually.

---

(email delivery skipped — no Gmail send tool available in this run; only draft-creation is present in the connector)
