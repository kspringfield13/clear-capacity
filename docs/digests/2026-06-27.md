# Here's what Claude shipped overnight — 2026-06-27

**TL;DR** — 10 improvements shipped · 4 curator sessions (3 visual passes, 69 screenshots) · 3 new backlog items added · build green · 0 reverts

---

## Shipped

All 10 items passed `npm run build` self-review before merging.

| Time (UTC) | What shipped | File(s) |
|---|---|---|
| Jun 26 15:05 | **BlockCard "% of week" tooltip** — added `title` + `.sr-only` span so hover and screen-reader users understand what the capacity percentage measures | `components/ledger/BlockCard.tsx` |
| Jun 26 16:05 | **BlockCard relabel select aria-labels** — added `aria-label="Work category / Planned status / Work mode"` to all three relabel `<select>` elements (daily review + ledger) | `components/ledger/BlockCard.tsx` |
| Jun 26 17:06–18:04 | **EmptyState named landmark regions** — added optional `ariaLabel` prop (defaults to `title`) applied as `aria-label` on the `<section>`; all 14 call sites gain named landmarks automatically | `components/common/EmptyState.tsx` |
| Jun 26 21:06–22:06 | **Demo heatmap grid now renders** — spread `activeWindowSamples` across yesterday + 2 days ago + 3 days ago (2 sessions each); `daysWithActivity` rises 1→4, clearing the `>= 2` threshold so the 7-day heatmap grid renders instead of the sparse-data caption | `services/demoData.ts` |
| Jun 26 23:04 | **ReviewCopilotPanel contextual aria-labels** — Apply/Dismiss buttons now include the suggestion title in each `aria-label` | `components/review/ReviewCopilotPanel.tsx` |
| Jun 27 00:10 | **AppShell/CompactWidget snapshot type fix** — replaced `snapshot: any` with `WeeklyCapacitySnapshot` from `packages/domain/src/models.ts` in both files | `components/shell/AppShell.tsx`, `components/compact/CompactWidget.tsx` |
| Jun 27 01:05 | **Agent compact view: hidden content revealed** — removed `display: none` rules hiding the Outlook metric and 2 of 4 starter cards at ≤600px; third briefing-metric now spans full-width, all four starters stack via existing single-column grid | `styles.css` |
| Jun 27 03:03 | **Forecast scenario cards: narrow-screen layout** — added `.forecast-summary` to the `grid-template-columns: 1fr` rule in `@media (max-width: 600px)` so cards stack single-column instead of cramped two-up | `styles.css` |
| Jun 27 06:06 | **Agent empty-conversation anchor** — changed `.agent-starters` from `flex: 1 1 auto; justify-content: center; min-height: 210px` to `flex: 0 0 auto; justify-content: flex-start`; starter cards now sit just below the briefing instead of floating centered in dead space | `styles.css` |
| Jun 27 09:11 | **ActivityHeatmap collapsible** — converted `<section>` to `<details className="activity-heatmap">` (closed by default) with `<summary>` replacing the eyebrow `<p>`; trimmed cell rows 7 px → 5 px; ledger list is now immediately visible at first render without scrolling | `components/ledger/ActivityHeatmap.tsx`, `styles.css` |

Also shipped: agent-infra improvements — the `/improve` loop now walks all Next sub-sections (not just UI & UX Polish), and STATUS.md was simplified to decouple task selection.

---

## Backlog curation

Four curator sessions ran in the window; three included full visual passes.

| Time (UTC) | Session | Screenshots | What changed |
|---|---|---|---|
| Jun 26 12:38 | Visual pass | 18 (6 screens × light/dark wide + 6 compact) | Verified 6 Next items still valid; added BlockCard relabel-select aria-label item that shipped the same hour |
| Jun 26 18:43 | Replenishment | — | Topped up the emptied UI & UX Polish subsection with 2 screenshot-grounded items (audit log enum + Weekly Summary-confidence tooltip) |
| Jun 27 00:43 | Visual pass | 24 (9 screens incl. Agent/Forecast/Corrections × light/dark wide + compact) | Reconciled 4 shipped items to Done (PR #58/#59 + 2 a11y items); replenished with 3 compact/responsive UI items that drove the overnight shipping run |
| Jun 27 06:41 | Visual pass | 27 (7 screens × light/dark wide + narrow-420) | Confirmed prior compact/forecast/agent fixes; replenished with 3 items: heatmap-fold (shipped same session), current-block hardcoded index, and forecast 38%–vs–24% reconciliation |

---

## Status

| Bucket | Count |
|---|---|
| Done (visible in STATUS.md) | 8 (all dated 2026-06-27) |
| In Progress | 0 |
| Next — UI & UX Polish | 2 |
| Next — Intelligence Engine | 4 |
| Next — Integrations | 3 |
| Next — Trust & Verification UX | 4 |
| **Next total** | **13** |

**Next up (top two open items):**
1. **"Current block" wrong block highlighted** — `LedgerScreen.tsx:64` hardcodes `blocks[7]`; fix to pick by latest `end_time`/`start_time`
2. **Forecast 38% vs 24% reconciliation** — annotate the AI scenario card to clarify it refines the 38% deterministic baseline

No skipped or reverted items.
