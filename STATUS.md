# STATUS.md — ClearCapacity Improvement Loop

The `/improve` loop reads this file first and writes it last on every run.

**Guardrails**
- Scope: edit `apps/desktop/src/` and `packages/` only.
- Gate: `npm run build` must pass before a task is marked done.
- One task per run.

**Ordering** — the loop takes the first unchecked `- [ ]` task reading top-to-bottom
through **Next**. Sections are in priority order; within a section, tasks are
dependency-ordered, so the topmost open task is always the right pick. Tasks tagged
**[manual / Rust]** need native work outside loop scope — ship the loop-safe slice
described in the task and flag the rest as a follow-up.

---

## In Progress
_(none)_

## Next

### Interaction & Visual Polish
_From the 6-hour visual UX pass (2026-06-30, fifth run — rendered daily/weekly/narrative/ledger/audit/setup + forecast/trends/agent/corrections/flagged in light/dark wide + the 420px compact layout via Playwright/demo). The app is highly polished across themes and the compact layout — no dark-mode contrast or narrow-clipping regressions; the prior demo-staleness theme is fully resolved (live `computeWeeklyCapacitySnapshot` numbers now reconcile with seeds/copy). The fullPage "content below the fold" look is the known fixed-viewport internal-scroll artifact, NOT a bug — don't re-flag it. The remaining open gaps this run are concentrated in the Review Copilot suggestion card, which lags the rest of the app's humanize-enums / metric-tooltip / explainability conventions. All items below are front-end-only and loop-safe (`apps/desktop/src/`). Open the file/lines and confirm the issue still exists before coding._

### Intelligence Engine
_Reference pattern: persisted `forecastHistory` + `scoreForecastAccuracy` — mirror it for retained-history work. The chat-metadata inference family (`analyzeInterruptionLoad`, `summarizeChatStakeholders` in `packages/inference/src/capacity.ts`) is the current reference for new metadata-only signals; EXTEND the shared week-scoped `weekChatEvents` memo in `useDerived.ts` rather than re-filtering per consumer._

### Trust & Verification UX
_Reference surfaces: the filterable Audit trail, the Corrections log, and the per-block "Why this estimate?" evidence + "Derived from" drill-down (`BlockCard.tsx`). New trust work should reuse the `AuditEvent` recipe (the 4-file `AuditEventType` flow) and humanize enum/value strings via `lib/format.ts`, never render raw snake_case._

### Capacity Model Calibration
_Distilled from `docs/heuristics-vs-research.md` — a research pass that checked the model's hand-set constants against queueing theory (M/M/1 `1/(1−ρ)`), Mark et al. (CHI 2008) on interruption cost, and Pencavel on working hours. The model's *directions* are well-grounded; the *exact numbers* are hand-tuned. These items sharpen the worst-justified constants. All in `packages/inference/src/capacity.ts` (loop-safe), some with a thin UI surface. Read the doc's matching section before coding; keep every change explainable (it feeds the narrative/forecast the user reviews)._
---

## Done
_Prior entries live in git history and merged PRs._

- [x] **Name the block(s) a Review Copilot suggestion will change** (2026-06-30) — the Review Copilot suggestion card (`components/review/ReviewCopilotPanel.tsx`) showed only a bare "N block(s)" count, so a user couldn't tell WHICH block a suggested "Add note"/"Relabel" touched before clicking Apply — the lone surface lagging the app's cite-your-evidence convention. Threaded a memoized `work_block_id → project_name` (the BlockCard `<h3>` headline) `Map` from `DailyReviewScreen.tsx` (built over all `blocks`, so verified-block titles still resolve) into the panel as a new required `blockTitles` prop. New module-scope `affectedBlocksLabel(ids, blockTitles)` helper renders the affected title(s) inline: count from `ids.length` (byte-identical to the old singular/plural rendering), names deduped via `Set` (a merge can touch several blocks sharing one project_name → avoids "X, X"), collapsed to "first two, +K more" beyond two, with a `title` hover listing every name. Demo shows "1 block · Revenue attribution mismatch" (demo-suggestion-1 → demo-blocker), matching the convention. Frontend-only (`ReviewCopilotPanel.tsx` + `DailyReviewScreen.tsx`). Build green; passed self-review (`/code-review` high — 0 findings: count parity, type-predicate null/empty-title narrowing, dedup, empty-ids/>2-names edges, single cross-file consumer all clean).
- [x] **Announce the "Copied" state on the Agent copy-response button** (2026-06-30) — the copy-response icon button in `components/agent/AgentScreen.tsx` swapped its icon to a `Check` on success but kept a static `title`/`aria-label` of "Copy response", so screen-reader and tooltip users got no confirmation the copy landed (the icon swap is invisible to SR). Mirrored the existing visual predicate (`copiedMessageId === m.id`, already gating the `Check`/`Copy` icon on the same line) into BOTH the `title` and `aria-label`: copied → "Copied", else → "Copy response", matching the icon-button title+aria a11y convention (NOTES "Icon-only button a11y"). Purely additive accessible-name change — the non-copied branch is byte-identical to the old static strings, the `copyMessage(m)` onClick and icon logic are untouched, and the temporary "Copied" name reverts to "Copy response" when `copiedMessageId` resets (same lifecycle as the icon). `AgentScreen.tsx` only; no CSS, no new state. Build green; passed self-review (focused adversarial pass: predicate matches the icon swap exactly, no inverted condition, no removed guard, `copyMessage` call + cross-file consumers unaffected).
- [x] **Humanize the Review Copilot suggestion action + gloss its confidence %** (2026-06-30) — added an exhaustive `reviewActionLabel(action: ReviewCopilotAction): string` to `lib/format.ts` (`Record<ReviewCopilotAction,string>` so TS fails the build on a new enum member; `confirm`→"Confirm", `relabel`→"Relabel", `exclude`→"Exclude", `merge`→"Merge blocks", `split`→"Split block", `note`→"Add note") and rendered it in `ReviewCopilotPanel.tsx:53` instead of the raw lowercase `ReviewCopilotAction` enum (was "note · 91%" → now "Add note · 91%"), closing the lone remaining "never render raw enums" violation. Gave the `· N%` confidence a `title` ("How confident the AI is in this suggested cleanup action") + explanation-only `.sr-only` span per the `ConfidenceChip`/`MetricCard` title+sr-only convention. Frontend-only (`format.ts` + `ReviewCopilotPanel.tsx`). Build green; passed self-review (exhaustive record verified against the union, sr-only is explanation-only not a value restatement, only display consumer of `suggestion.action` touched — App.tsx/useReviewCopilot uses stay raw logic/audit).
- [x] **Core inference file `capacity.ts` was a binary blob (2 NUL bytes) — unsearchable by grep/ripgrep** (2026-06-30) — `analyzeCorrections`'s dedup key (`packages/inference/src/capacity.ts`) used two literal `\0` (NUL, 0x00) bytes as the field delimiter (`field\0old\0new`) — a collision-safe choice (label values like `"Planned analysis / project work"` carry spaces/slashes, so a space delimiter would alias) that nonetheless turned the codebase's most-grepped inference file into a BINARY blob: `file` reports it as "data" and grep/ripgrep skip NUL-containing files by default, so the loop literally could not `Grep` the file for `export function`/`computeCapacityBaselines`/etc. (hit this mid-run — "binary file matches" with no results). Replaced the NUL-delimited template literal with a printable `JSON.stringify([correction.field, correction.old_value, correction.new_value])` composite key: injective over ALL string triples (strictly MORE collision-safe than the NUL key, which only worked because NUL never appears in values), fully plain-text, and idiomatic. Behavior is byte-identical for realistic inputs — verified the demo's 7 corrections still yield the same 2 systematic biases (planned_status planned→unplanned ×3, category mislabel ×3, both count 3 ≥ threshold; the `project_name` correction stays excluded as a non-label field; `labelCount` = 6). After the fix `file` reports "JavaScript source, UTF-8 text" and grep/ripgrep search it normally. `capacity.ts` only (NUL count 2→0); no behavior/type/UI change, returned `CorrectionBiasAnalysis` shape untouched so the Forecast `.model-bias-note` consumer is unaffected. Build green; passed self-review (`/code-review` medium — 0 findings: line-by-line type-check, removed-NUL-invariant trace, cross-file consumer check, demo-bias equivalence all clean). NOTE: the STATUS "Next" backlog had no concrete `- [ ]` items left (prior runs cleared them; the Capacity Model Calibration doc batch is complete) — this defect was discovered while grepping `capacity.ts` to verify the top "demo staleness" theme, which is itself fully resolved for all visible surfaces.
- [x] **"Reliable new work" card helper claims "room to the 80% knee" even when already past it** (2026-06-30) — the helper copy on the Weekly "Reliable new work" hero MetricCard (`WeeklyCapacityScreen.tsx:202`) was unconditional: `${committed}% already committed · room to the 80% knee`. But `reliable_new_work_capacity_pct = clamp(80 − committed, 0, 40)`, so when `committed_utilization_pct >= 80` the model clamps reliable headroom to **0** while the helper still promised "room to the 80% knee" — a copy/value contradiction for any over-the-knee user. Made the copy conditional via a new module-scope `RELIABILITY_KNEE_PCT = 80` const (mirrors `TARGET_UTILIZATION_PCT` in `capacity.ts`, documented in a comment): `committed >= 80` → "…past the 80% knee", else "…room to the 80% knee". The const is interpolated into BOTH branches so the threshold and the displayed "80%" can never drift. Demo is unaffected (demo `committed = 58 < 80` → `room to the ${80}% knee` renders byte-identical to the old literal, verified by replicating `computeWeeklyCapacitySnapshot` over the 9 demo blocks: committed 58, reliable 22). `WeeklyCapacityScreen.tsx` only; no CSS, no MetricCard/structure change, `pct()` formatting identical. Build green; passed self-review (adversarial diff re-read: boundary `==80` reads "past" = "no headroom" correctly; `committed > 100` pathological case still reads "past"; the `< 80` cap-at-40 case still correctly says "room"; helper is a cheap inline string, no dep-array impact).
- [x] **Demo Trends chart renders two of four flagship lines dead-flat** (2026-06-30) — the seeded `snapshotHistory` (`services/demoData.ts`) varied only `reactive_pct`/`reliable_new_work_capacity_pct` (+ baseline-only `meeting_pct`/`context_switch_score`) per week, so the other two series the `TrendsScreen` chart plots — `allocated_pct` (base 92) and `deep_work_pct` (base 38) — sat at the `demoSnapshot` default across all three history weeks, rendering as dead-flat placeholder lines (NOTES "demo seed must exercise every flagship visual"). Added per-week `allocated_pct`/`deep_work_pct` overrides telling a coherent story (calm earlier week alloc 86 / deep 44 → busy middle week 95 / 31 → recovering week 90 / 39), so all four Trends lines now show natural cross-week movement and non-zero legend deltas. Confirmed scope is Trends-only: `computeCapacityBaselines` medians just reactive/meeting/context/reliable and `scoredForecasts` reads only `reliable_new_work_capacity_pct` (both verified in `useDerived.ts`), so the baseline chips and the seeded forecast track-record "On target/Off/Close" chips (actuals 26/29, unchanged) are byte-identical; `deep_work_pct < allocated_pct` holds each week (subset invariant). `demoData.ts` only. Build green; passed self-review (adversarial diff re-read + cross-file consumer trace: the two new fields are read by `TrendsScreen` alone, values are 0–100 and internally consistent, no narrative/baseline/forecast figure shifts).
- [x] **"80% knee" jargon on the Reliable-new-work hero card has no plain-language gloss** (2026-06-30) — added an optional `title?: string` prop to the shared `MetricCard` (`components/common/MetricCard.tsx`) that renders both as a hover tooltip on the `.metric-card` container AND an explanation-only `.sr-only` span (matching the `.summary-score` title+sr-only convention — title on the container, pure explanation in the hidden span, NOT a value+label restatement). The Weekly "Reliable new work" card (`WeeklyCapacityScreen.tsx:202`) now passes the plain-language gloss "Past ~80% utilization, delays grow sharply — we hold back the last ~20% as buffer", so the "80% knee" queueing-theory jargon in the helper is decodable on hover and via screen reader. Purely additive: `title` defaults `undefined` → no attribute + no sr-only span, so the other three hero cards (and any other `MetricCard` call site) are byte-identical to before. Build green; passed self-review (additive a11y-only diff: backward-compatible optional prop, `.sr-only` already in `styles.css`, no hardcoded hex/raw-enum, no other call sites affected).
- [x] **Demo manager summary cites "24%" the deterministic model contradicts** (2026-06-30) — the hardcoded demo manager-ready narrative (`services/demoData.ts:360`) attributed "**24% reliable capacity** … next week" to "the current model", but 24% is the *AI forecast's* next-week figure (Forecast tab + the `forecast_agent` audit event) — the deterministic `computeWeeklyCapacitySnapshot` computes a different number over the demo blocks, so the Summary tab contradicted the Weekly hero. **The curator's stated "17%" was itself STALE**: it predated #150, which consolidated the demo blocks from 9→5 distinct `project_name`s, dropping `wipPenalty` 10→5 and lifting reliable capacity 17→**22**. Verified the LIVE deterministic value by replicating `computeWeeklyCapacitySnapshot` over the 9 demo blocks (`included=9`, projects=5, recurring=28, reactive=24, carry=4, ctx=0.33, wip=0.51 → frag=4, wipP=5, **committed=58, reliable=22**). Reworded the demo string to cite the deterministic **22% reliable** + **~58% already committed** (mirroring the live narrative's committed-utilization framing), keeping it textually distinct from the AI forecast's 24% per the deterministic-vs-AI convention. `demoData.ts` only — the seeded `demoSnapshot`/`snapshotHistory` (lines 48/355–357) feed only Trends/baselines, not the live hero, so they're untouched and out of scope. Build green; passed self-review (high-effort `/code-review`: no findings — the change is a single copy string citing script-verified figures; the other `24%`/`24`/`15`/`31` figures in the file are all AI-forecast scenarios, correctly distinct).
- [x] **Demo "WIP overload" risk bar pins at 100/100** (2026-06-30) — consolidated the demo work blocks (`services/demoData.ts`) from **9 distinct `project_name`s to exactly 5** so the live `computeWeeklyCapacitySnapshot` feeds `activeProjectCount = 5` into the super-linear `wipLoadScore = pow(5/7, 2) ≈ 0.51` (was `pow(9/7,2)` → clamped `1.0`), rendering the Weekly "WIP overload" RiskRow at ~51/100 (mid-range, matching the seeded `wip_load_score: 0.52`) instead of an alarming 100/100. Merges chosen for natural-reading initiatives: demo-dashboard → "Capacity model v2"; both calendar meetings (planning sync + product review) → "Weekly operating metrics" alongside demo-reporting; demo-blocker ("Marketing attribution refresh") → "Revenue attribution mismatch" alongside demo-attribution; demo-retention and demo-requirements stay distinct. Updated `demo-correction-2`'s `new_value` ("Executive capacity dashboard" → "Capacity model v2") so the seeded correction log stays consistent with the dashboard block's new project. `demoData.ts` only; project_name feeds ONLY `activeProjectCount`, so the lone side effect is the intended ~5pp lift in reliable capacity (lower WIP penalty). Build green; passed self-review (adversarial diff re-read: confirmed exactly 5 distinct names, all 9 blocks `included` since the blocker has `blocker_flag:true`, window-title samples + calendar event titles are separate concepts left intact, a single project spanning two stakeholder groups is realistic, no other capacity aggregate depends on project_name).
- [x] **After-hours reactive-load signal** (2026-06-30) — extended `analyzeInterruptionLoad` (`packages/inference/src/capacity.ts`), the chat-metadata inference family, with a metadata-only "after-hours" signal: two new required fields `after_hours_message_count` + `after_hours_pct` on `InterruptionLoadAnalysis`. Reactive messages are bucketed by their burst's local START hour (reusing the `startDate` already derived for the weekday bucketing) and counted as after-hours when the hour is `< CORE_HOURS_START (8)` or `>= CORE_HOURS_END (18)` — new hand-set constants commented against `docs/heuristics-vs-research.md §5` (Pencavel, diminishing returns past long hours, applied to *when* load lands). `after_hours_message_count` is a strict subset of `message_count` by construction (both add the same `messages`, the after-hours branch only when `messages > 0`), so it can never exceed the total. Surfaced as a muted `.interruption-peak-note` footnote in the Weekly chat-interruption panel (`WeeklyCapacityScreen.tsx`, reused class — no CSS change) gated on `after_hours_message_count > 0`: "N% of reactive messages (K of M) arrived outside core hours (8am–6pm) — chat bleeding into personal time." Demo: shifted `demo-chat-3` from minute 300 (Wed 14:00) to 600 (Wed 19:00) so the footnote renders (5 of 25 → 20%) — same day/channel/volume, so peak (Monday)/calm (Thursday)/stakeholder shares and the analysis window (bounded by Thursday's event) are all provably unchanged. Build green; passed self-review (2 parallel finders + own pass): correctness clean except two finder items, both applied — (1) floored `after_hours_pct` to 1 when any after-hours volume exists so a non-zero count never displays beside "0%" (the `1 of 300` round-to-0 case); (2) reworded the footnote from "before 8am or after 6pm" to "(8am–6pm)" since the `>= 18` boundary is inclusive of 6pm sharp.
- [x] **[larger] Target-utilization capacity model** (2026-06-29) — replaced the fixed "100% baseline minus penalties, clamped 0–40%" `reliable_new_work_capacity_pct` formula in `computeWeeklyCapacitySnapshot` (`packages/inference/src/capacity.ts`) with a principled target-utilization model (doc §1). New `TARGET_UTILIZATION_PCT = 80` constant (the M/M/1 ~80% queueing knee). The forward-committed load — `recurring + carryover + reactive×0.72 + fragmentation + wip` penalties, i.e. the SAME terms the old formula subtracted from 100 — is now summed into a new `committed_utilization_pct` snapshot field (current utilization), and reliable new work = `clamp(80 − committed, 0, 40)`: headroom that brings TOTAL utilization up to the knee rather than filling to 100%. `MAX_RELIABLE_NEW_WORK = 40` is KEPT as the old-behavior guardrail (never over-promise on a near-empty week); `BASELINE_CAPACITY = 100` stays the accounting denominator (unchanged "100% weekly capacity model"). New required field threaded through `WeeklyCapacitySnapshot` (`models.ts`) + the demo seed (`demoData.ts`, `committed_utilization_pct: 52` reconciling the seed's `reliable 28` → 80). Narrative copy (`generateWeeklyNarrative` summary_text + manager_ready_summary) and the Weekly "Reliable new work" MetricCard helper now cite the committed-utilization + 80% knee story ("X% already committed · room to the 80% knee"). Forecast prompt untouched — its "Clamp to 0-40%" instruction stays valid since the 40% cap is retained (no prompt version bump). Build green; passed self-review (8-angle code-review: no surviving correctness findings — all snapshot constructors updated, no other full literals, consumers read-only; demo history seeds' committed value is never surfaced in the hero cards which always render the live computed snapshot; applied one JSDoc-accuracy fix so the doc no longer claims committed+reliable always lands ≤80% when already over the knee).
- [x] **Make the WIP penalty super-linear** (2026-06-29) — `wip_load_score` in `computeWeeklyCapacitySnapshot` (`packages/inference/src/capacity.ts`) was a linear, forgiving `uniqueProjectCount / 10`; replaced it with a quadratic `clamp(Math.pow(activeProjectCount / 7, 2), 0, 1)` so context-switching cost curves upward as the doc §3 (`heuristics-vs-research.md`) research argues (fragmentation likely *under*-weighted; switching cost ~combinatorial). The new curve sits near the old linear value around 5 projects, is gentler below (few projects = little switching), and is harsher above — saturating the penalty at 7 concurrent projects (the "badly overloaded" knee). Verified `score(5)=0.51 > 5×score(1)=0.10` (super-linear) and edge-safe: 0 projects → 0 (no NaN), >7 → clamped to 1. The `Set(...project_name)` construction is byte-identical (null/undefined handling unchanged) and the downstream `× 10` penalty + `clamp` are preserved, so the penalty still caps at 10pp; field stays 0–1. All three consumers (`models.ts` type, Weekly `WIP overload` RiskRow, `agentTools.ts`) treat it as a 0–1 index — none assume linearity; demo seeds the value directly so its display is unchanged. Updated the inline rationale comment with the §3 citation. `capacity.ts` only; no UI/CSS. Build green; passed self-review (8-angle equivalent: correctness/edge cases, removed-behavior invariants, cross-file consumer trace, conventions — no findings).
- [x] **Trends x-axis week ticks are cryptic ("W26") with no full-form tooltip** (2026-06-29) — each x-axis week tick `<text>` in `components/capacity/TrendsScreen.tsx` now carries a `<title>{formatIsoWeekLabel(week.week_id)}</title>` child (e.g. "Week 26, 2026"), mirroring the existing per-dot `<title>` so hovering a "W26" axis label reveals the full week and the label↔dot week reference is consistent. SVG `<title>` is non-visual (tooltip + accessible name only), so the rendered "W26" tick is visually unchanged; `formatIsoWeekLabel` was already imported and used by the dot titles. `TrendsScreen.tsx` only, no CSS. Build green; passed self-review (additive one-line diff: `<title>` is valid as a child of SVG `<text>`, identical pattern to the `<circle>` dots already in the file, no behavior/layout change).
---

## Never
- `apps/desktop/src-tauri/` (Rust shell) — needs manual Tauri testing.
- `.env` or any secret.
- `vite.config.ts` port (5173) / `tauri.conf.json`.
