# STATUS.md — ClearCapacity Improvement Loop

The `/improve` loop reads this file first and writes it last on every run.

**Guardrails**
- Scope: edit `apps/desktop/src/` and `packages/` only.
- Gate: `npm run build` must pass before a task is marked done.
- One task per run.

**Ordering** — the loop takes the first unchecked `- [ ]` task reading top-to-bottom
through **Next**. Sections are in priority order; within a section, tasks are
dependency-ordered, so the topmost open task is always the right pick. Tasks tagged
**[manual / Rust]** need native work outside loop scope — ship the loop-safe slice
described in the task and flag the rest as a follow-up.

---

## In Progress
_(none — a fresh curation pass (2026-07-01) reviewed the app end-to-end and replenished the depleted backlog with eight concrete, verified, loop-safe slices below. Two exploration passes surfaced ~30 candidates; each item here was re-read against the actual code to drop false positives (AuditLogScreen/CorrectionsScreen already have their empty states, StackedBar/ActivityCapturePanel already guard their edge cases, MetricCard already has sr-only glosses). What remains is genuinely open.)_

## Next

### Performance & Robustness
_Self-contained correctness/efficiency slices found by reviewing the render hot-paths and the enum/number rendering surface. Each is one file (occasionally with a shared helper in `lib/format.ts` or `capacity.ts`), gated on `npm run build`._

- [ ] **Memoize the LedgerScreen render-hot derivations** — `components/ledger/LedgerScreen.tsx` recomputes four non-trivial derivations on **every** render (they sit in the function body, not a `useMemo`): `classifiedSessionIds` (a `new Set(blocks.flatMap(b => b.derived_from))`, lines ~50), `unclassifiedSessionCount` (a full `activeWindowSessions.filter`), `visibleBlocks` (the multi-field lowercase search filter over all blocks), and `current` (a full `blocks.reduce` latest-by-time scan). Only `visibleBlocks` depends on `searchQuery`; the other three depend on `blocks`/`activeWindowSessions` only, so typing in the search box re-runs the Set build + session filter + reduce needlessly (and vice-versa). Wrap each in a `useMemo` with the correct deps (`classifiedSessionIds`/`current` → `[blocks]`; `unclassifiedSessionCount` → `[activeWindowSessions, classifiedSessionIds]`; `visibleBlocks` → `[blocks, searchQuery]`, computing the lowercased needle inside). Pure refactor — identical output, fewer allocations per keystroke. Mirror the memoization convention already used in `useDerived.ts`. `LedgerScreen.tsx` only. Gate: `npm run build`.

- [ ] **Guard `ConfidenceChip` against a NaN confidence** — `components/common/ConfidenceChip.tsx` special-cases `value === 0` → "Unscored" but a `NaN` value falls through: `Math.round(NaN * 100)` is `NaN` and the chip renders "**Needs review NaN%**" (NaN < 0.74, so it also picks the `low` class). `WorkBlock.confidence` is a plain `number` with no non-NaN guarantee (a malformed import or a divide-in-derivation could produce it), and this chip is rendered per-block across the ledger/daily surfaces. Extend the early return to `if (value === 0 || !Number.isFinite(value))` → the existing "Unscored" chip, so a missing/garbage score reads as an honest absence rather than "NaN%". `ConfidenceChip.tsx` only. Gate: `npm run build`.

- [ ] **DRY the `slice(-N)` rolling-window logic in capacity.ts** — `packages/inference/src/capacity.ts` repeats the "take the last N entries of a chronologically-sorted array" pattern across the baseline/accuracy-trend/track-record helpers (each with its own `*_WINDOW_WEEKS` const + inline `.slice(-N)`). Extract a tiny pure `sliceLastN(items, n)` helper (with a `n <= 0` guard returning `[]`) next to the existing helpers and route the window sites through it, keeping each named window const as the argument so the semantics stay self-documenting. Pure refactor — no behavior change; confirm the scored/forecast outputs are byte-identical by re-reading each call site. Read the surrounding code first so the helper matches the file's existing helper style. `capacity.ts` only. Gate: `npm run build`.

### UI & UX Polish
_Small, visible-surface consistency + a11y slices. Reuse the established shared components (`EmptyState`, `ConfirmDialog`), the `lib/format.ts` humanizers, and the error-toast copy pattern; never render raw snake_case or hardcode a design value._

- [ ] **Agent chat "Copy response" button falsely reports success when the clipboard is unavailable** — the last holdout of the clipboard-lie pattern the recent D3 slice fixed everywhere else. `components/agent/AgentScreen.tsx` `copyMessage` (~line 522) does `await navigator.clipboard.writeText(...)` then unconditionally sets the "Copied" check + pushes a success toast, and is invoked via `void copyMessage(m)` — so on an insecure webview where `navigator.clipboard` is undefined, or when the write rejects, the promise rejects unhandled while the UI still shows "Copied" and toasts success. Wrap the write in `try/catch`: run `setCopiedMessageId` + the success toast **only after** the `await` resolves, and in the catch push the shared `{ tone: "error", message: "Couldn't copy to the clipboard" }` toast (matching the `NarrativeScreen`/`SavedSkillsScreen` reference copy verbatim). `AgentScreen.tsx` only. Gate: `npm run build`.

- [ ] **Consolidate the ActivityHeatmap hour/day label helpers into `lib/format.ts`** — `components/ledger/ActivityHeatmap.tsx` carries four private formatters (`getDayLabel`/`getDayFullLabel` and `formatHour`/`formatHourA11y`) that duplicate time-of-day + relative-day logic the app formats elsewhere (`lib/format.ts` already exports `formatHourOfDay`). The heatmap's compact "12a/12p" and a11y "12 am/12 pm" variants differ from `formatHourOfDay`'s "12am/12pm", so add a small `variant`/style option (or a sibling `formatHourCompact`/`formatHourA11y` pair) to `lib/format.ts` and a `formatRelativeDayLabel(diffDays, { long })` helper covering the Today/Yesterday/weekday cases, then have the heatmap import them. Keep output identical to today's rendered labels (re-read before/after). Reduces duplication and makes the formatters reusable. `ActivityHeatmap.tsx` + `lib/format.ts`. Gate: `npm run build`.

- [ ] **Extract + guard the BlockCard local-time helpers** — `components/ledger/BlockCard.tsx` defines file-local `toLocalTimeInput(iso)` and `applyLocalTime(iso, hhmm)` (ISO ↔ "HH:MM") that are the natural home of `lib/format.ts` (the only other place editing block times would reuse them). Move both to `lib/format.ts` and import them. While moving, harden `applyLocalTime`: `hhmm.split(":").map(Number)` yields `NaN` for a malformed value and `d.setHours(NaN, NaN)` produces an `Invalid Date` whose `.toISOString()` **throws** — guard with `Number.isFinite(hours) && Number.isFinite(minutes)` and return the original ISO unchanged when the parse fails. `BlockCard.tsx` + `lib/format.ts`. Gate: `npm run build`.

- [ ] **Give the Trends chart a non-visual data path** — `components/capacity/TrendsScreen.tsx` renders per-point values only in SVG `<title>` tooltips (hover-only) and the four-series isolation is driven by `onMouseEnter`/`onMouseLeave` on the legend + line groups, so keyboard and screen-reader users can read the top-level `aria-label` ("Line chart of weekly capacity…") but cannot reach any actual week/series value. Add a visually-hidden (`.sr-only`) data summary — e.g. a `<table>` or a `<ul>` per series listing each in-view week's label + rounded percentage (reuse `formatIsoWeekLabel` + `pct`), rendered alongside the chart and referenced via `aria-describedby` on the `<svg>`. Purely additive (no visual change); the legend deltas already carry direction, so this fills the missing point-level detail. `TrendsScreen.tsx` (+ minimal `.sr-only` reuse in `styles.css` if needed). Gate: `npm run build`.

- [ ] **Name the SavedSkills "Remove" button for assistive tech** — `components/accelerate/SavedSkillsScreen.tsx` gives its Copy button a descriptive `aria-label` ("Copy this recipe to the clipboard") but the sibling **Remove** button (~line 95) has only a generic `title` + visible "Remove" text, so multiple saved cards expose an ambiguous "Remove" to a screen reader with no indication of *which* skill. Add `aria-label={`Remove ${skill.title} from your library`}` (mirroring the Copy button's descriptive-label convention) so each Remove action is self-identifying. Small a11y consistency fix. `SavedSkillsScreen.tsx` only. Gate: `npm run build`.

### Acceleration Engine
_New flagship AI surface (2026-06-30 brainstorm). Mines the user's observed work — `ActivitySession[]`, `WorkBlock[]`, calendar load, and the correction trail — into evidence-cited **Plays**: AUTOMATE (a generated, reusable AI "skill" recipe grounded in the user's real projects/tools), TOOL (a tool recommendation matched to an observed time-sink), and TECHNIQUE (a trick tied to an observed anti-pattern). Two layers, mirroring the Forecast Agent: a **deterministic miner** in `packages/inference/` that produces ranked signals with full evidence + estimated time saved (runs locally, no AI, always on, privacy-safe), and an **optional opt-in AI synthesis** layer that authors the polished Plays. The AI call routes through the EXISTING `ai_complete` Tauri command (`services/aiComplete.ts`) so the whole feature is loop-safe — NO Rust work. Hard privacy rule: the deterministic miner may read raw window titles locally, but the AI layer sends **derived signals only** (app-name sequences, category/duration/time-of-day stats, counts) — never raw window titles — gated and audited like every other AI command. Build on the established patterns: `forecastAgentPrompt.ts`/`useForecastAgent.ts` for the AI flow, the 4-file `AuditEventType` recipe for audit, the `Screen`/`ScreenRouter`/`ui.ts` trio for the new surface, and the "demo seed must exercise every flagship visual distinctly" convention for the wow. Tasks are dependency-ordered — ship one loop-safe slice per run, gate on `npm run build`._

### Insight & UX Enhancements
_Near-term, high-leverage additive slices found by reviewing the app end-to-end (2026-07-01). The richest seam is **inference the model already computes but never surfaces** — cheap to expose, immediately useful, and it deepens the "everything explainable" contract. Each was re-read against the actual code to confirm it's genuinely open (dropped false positives: the meeting-density baseline chip already exists in `BASELINE_METRICS`, and Acceleration `saved_to_library` is already audited). All loop-safe (`apps/desktop/src/` + `packages/` only); humanize every enum/value via `lib/format.ts`, reuse existing components/tokens, and keep the derived-only privacy contract. Independent items — order is by value, not dependency; gate each on `npm run build`._

- [ ] **Surface the work-mode allocation split on the Weekly screen** — `computeWeeklyCapacitySnapshot` returns BOTH `category_allocation` and `work_mode_allocation` (`capacity.ts` ~line 145), but `WeeklyCapacityScreen.tsx` only renders the category grid (~line 263); the Deep-work / Fragmented / Reactive mode split is computed every week and thrown away. Add a second allocation block beside/below the category one showing the mode breakdown (reuse the existing category-card / `BarLine` / `StackedBar` rendering + tokens, and `lib/format.ts` for labels), so users see *how* the week fragmented, not just *what* they worked on — the same distinction the context-switch burden hints at, made concrete. `WeeklyCapacityScreen.tsx` only (data already in the snapshot). Gate: `npm run build`.

- [ ] **Expose the fragmentation & WIP point-cost baked into committed load** — the "Delivery risk modifiers" section (`WeeklyCapacityScreen.tsx` ~line 304) shows `context_switch_score` and `wip_load_score` as abstract 0–100 indices, but the model actually converts them into concrete penalties folded into `committed_utilization_pct` (`capacity.ts` ~line 104–113: `fragmentationPenalty = contextSwitchScore × 12`, `wipPenalty = wipLoadScore × 10`). Expose those two point-contributions (add `fragmentation_penalty_pct` / `wip_penalty_pct` to `WeeklyCapacitySnapshot` — additive, safe for the persisted `snapshotHistory`) and surface them so the abstract index reads as "context-switching is costing ~8% of your committed week" rather than a bare "/100". Translates a hand-tuned index into the reviewable number it drives (fits the Capacity Model Calibration ethos). `capacity.ts` + `WeeklyCapacityScreen.tsx`. Gate: `npm run build`.

- [ ] **Show forecast bias direction, not just magnitude** — `ForecastAccuracy` computes `signed_error_pts` (positive = over-predicted, `capacity.ts` ~line 155) per week, but `summarizeForecastAccuracy` (~line 200) rolls up ONLY `mean_abs_error_pts`, discarding whether the model systematically over- or under-predicts. Add `mean_signed_error_pts` to `ForecastAccuracyTrend` (average the signed errors over the same window, reusing `scoreForecastAccuracy`) and surface it wherever the "averaged ±N pts of error" line renders (`TrendsScreen.tsx` / `ForecastAgentPanel.tsx`) as "…and tends to over-predict by ~N pts" — a self-correcting signal (consistent over-prediction ⇒ discount future forecasts). `capacity.ts` + `TrendsScreen.tsx`/`ForecastAgentPanel.tsx` + `lib/format.ts`. Gate: `npm run build`.

- [ ] **Add meeting density as a Trends series** — `TrendsScreen.tsx` charts four series (allocated / reactive / deep-work / reliable, the `SERIES` array ~line 18) but `meeting_pct` is retained in every snapshot and never trended, so silent meeting creep is invisible over time. Add `meeting_pct` as a fifth series ("Meeting density", `betterWhen: "lower"`) with a new `trend-*` CSS class in the existing palette convention (`currentColor`-driven, tokens only) and the matching legend entry. Small, self-contained, and it pairs with the existing per-week meeting-density RiskRow + baseline chip. `TrendsScreen.tsx` + `styles.css`. Gate: `npm run build`.

- [ ] **Reactive-load concentration index in the interruption panel** — `analyzeInterruptionLoad` already knows `active_day_count`, `peak_day`, and `peak_day_message_count` (`capacity.ts`), but never expresses how *concentrated* the reactive load is — 72% of the week's messages landing in 2 of 5 active days is a *batchable* problem; an even spread is endemic. Add a derived `concentration_pct` (share of total reactive messages in the busiest day, or top-2 days, over the active window — reuse the existing `dayMessages` map, no new inputs) and surface it in the interruption note ("most of your reactive load clustered into N days — protecting the others could reclaim focus"). Metadata-only, so privacy holds structurally. `capacity.ts` + `WeeklyCapacityScreen.tsx`. Gate: `npm run build`.

- [ ] **Undo the last correction from the Daily Review** — the review flow records corrections via `onRelabel` (`DailyReviewScreen.tsx`) into the audit trail, but there's no quick reversal: users must re-open the field and set it back, or leave the Review screen. Add an "Undo last correction" affordance (gated on `corrections.length > 0` and the latest correction being today's) that replays the inverse relabel of the most recent correction. Wire a small `onUndoLastCorrection` handler from `App.tsx` (the owner of `corrections` + `blocks`; it re-applies the `old_value` via the existing relabel path and audits the reversal as a normal correction — non-destructive, redoable). Reuses the existing correction/audit machinery; humanize the toast via `lib/format.ts`. `App.tsx` + `DailyReviewScreen.tsx`. Gate: `npm run build`.

- [ ] **Show an import delta when re-importing an Outlook `.ics`** — re-importing the same/updated calendar file currently reads as a silent no-op (Settings shows only "Imported {timestamp}"). When `onImportOutlookIcs` completes, diff the parsed events against the currently-stored `calendarEvents` (added / updated / removed counts — pure frontend comparison in `App.tsx`, no Rust/network), surface a "Imported 12 events (+3 new, −1 removed, 8 unchanged)" toast, and enrich the `calendar_import` audit event details with the counts (extend the existing event via the audit recipe). Reassures the user their calendar stayed in sync and makes re-imports auditable. `App.tsx` + `lib/audit.ts` (+ `SetupScreen.tsx` for the summary line). Gate: `npm run build`.

### Acceleration Engine v2 — memory, feedback & coverage
_The v1 engine (above) is complete: three deterministic miners (`detectRepetitiveSequences`/`detectTimeSinks`/`detectContextSwitchHotspots` in `packages/inference/src/accelerate.ts`), the rank-and-cap aggregator, the opt-in AI synthesis layer, and the Saved Skills library. Its ceiling is that every Play is mined from **this week only** and thrown away each render — it has no memory and no feedback loop, and it's blind to calendar + the reactive-chat load `capacity.ts` already measures. This batch adds both. The first three items are a **dependency chain** (`E1 acted-on state → E2 weekly history/recurrence → E3 realized-savings track record`); the last two (`E4`, `E5`) are independent new detectors and can be pulled forward if a coverage win is wanted first. All loop-safe (frontend + `packages/`, no Rust): reuse the persisted-history recipe (`PersistedAppState`/`usePersistence`/the four `App.tsx` hydration sites/demo seed), the forecast's `forecastHistory` + `scoreForecastAccuracy` + track-record UI as the proven mirror, the 4-file `AuditEventType` recipe, and the derived-only privacy contract (app names/categories/counts/minutes only — NEVER raw window titles). Ship one slice per run, gate on `npm run build`._

- [ ] **E1 — "Acted on" Play state** — the foundation the feedback loop measures against. Today a Play is only save/dismiss (`dismissedPlayIds`/`savedPlayIds` on `PersistedAppState`); there's no way to record that the user actually *acted* on a Play, so the track record (E3) can't know which signals to score. Add a third persisted `actedOnPlayIds: string[]` (the `parseStringIdList` + both-read-paths + `usePersistence` + four-`App.tsx`-hydration-sites recipe C3 established), an `App.tsx` `markPlayActedOn(play)` handler that audits the discrete action via `createAccelerationPlayAuditEvent` (extend its action union with `"acted_on"` — `acceleration_engine`, `derived_only`, `window_titles:false`) and an unaudited `unmarkPlayActedOn(id)` undo, and an "I acted on this" toggle on the `AccelerationScreen` play card (`aria-pressed`, state-mirrored `title`/label per the icon-swap convention, e.g. `CircleCheck`↔`CheckCircle2`). Demo seed gains `actedOnPlayIds: []` (required field). Privacy holds structurally. `models.ts`(if a shared type is touched) + `localStore.ts` + `App.tsx` + `AccelerationScreen.tsx` + `lib/audit.ts` + `demoData.ts`. Gate: `npm run build`.

- [ ] **E2 — Weekly acceleration snapshot + cross-week recurrence** — one week of evidence is the engine's weakest link; a 2pm hotspot seen four weeks running is a real habit, a one-off is noise. Persist a per-ISO-week snapshot of the mined signals (mirror `snapshotHistory`/`PersistedSnapshotRecord`): a new `accelerationHistory: PersistedAccelerationSnapshot[]` on `PersistedAppState` where each record is `{ week_id, generated_at, signals: { signal_id, type, estimated_minutes_saved_per_week }[] }` (store the derived summary only — id/type/minutes — never evidence strings, to keep it compact and privacy-trivial), captured once per week the same way the capacity snapshot is retained, parsed via a new `parseAccelerationHistory` in both read paths, written through `usePersistence`, hydrated at all four `App.tsx` sites, and seeded in demo. Then compute a `recurrenceBySignalId` map (count of prior weeks each currently-mined `signal_id` appeared) in `useDerived.ts` and surface it on the card: a "recurring N weeks" badge (reuse the `.play-ai-badge`/`.play-type-chip` a11y pattern, existing tokens only) and, in `buildAccelerationSignals` or a thin post-rank pass, a gentle rank/label boost for persistent signals (keep the estimate itself deterministic and explainable — recurrence adjusts *ordering/emphasis*, documented in evidence, not the minutes). `localStore.ts` + `App.tsx` + `useDerived.ts` + `AccelerationScreen.tsx` + `styles.css` + `demoData.ts` (+ a small helper in `packages/inference/`). Gate: `npm run build`.

- [ ] **E3 — Realized-savings track record** — the flagship: turn the engine's estimates from claims into a proven record. Depends on E1 (which Plays to score) + E2 (the week-over-week history to score against). Mirror the forecast accuracy machinery (`scoreForecastAccuracy`/`summarizeForecastAccuracy`/`buildForecastTrackRecord` in `capacity.ts`, surfaced in `TrendsScreen`/`ForecastScreen`): for each Play the user marked acted-on in a prior week, compare the cited category's/hotspot's minutes that week vs. the following week (using the retained `accelerationHistory` + weekly capacity/interruption data) and score whether the observed reduction met, missed, or beat the `estimated_minutes_saved_per_week`. Add pure scoring helpers in `packages/inference/` (one scored entry per acted-on Play per week, latest wins), a `useDerived` memo pairing them, and a track-record surface on the Acceleration/Saved-Skills area ("your Plays have reclaimed ~X of the ~Y min they projected") with per-Play rows — humanize every enum/value via `lib/format.ts`, never raw snake_case. Over time this also exposes whether the flat `SAVINGS_FRACTION = 0.25` is well-calibrated. Privacy: scores are derived from retained derived-only summaries. `capacity.ts`(or a sibling) + `useDerived.ts` + `AccelerationScreen.tsx`/a track-record component + `lib/format.ts`. Gate: `npm run build`.

- [ ] **E4 — Reactive-load Play from the existing interruption analysis** (standalone; fastest high-value slice) — `capacity.ts` already computes `analyzeInterruptionLoad` (peak day, peak hour, after-hours %, @-mention share) and `useDerived` already exposes `interruptionLoad`, but the Acceleration Engine ignores it. Emit a `technique` Play — "batch reactive comms into two windows" / "guard {peak_hour} for focused work" — grounded in that analysis, with evidence citing the peak hour, the reactive share, and after-hours load (all metadata-only figures already computed). Thread the analysis (or the week-scoped chat events) into `buildAccelerationSignals` via a new optional field on `AccelerationMiningInput` (keep the existing detectors' signatures intact; the aggregator already fans inputs across miners), and estimate reclaimable minutes conservatively from the observed reactive minutes with a documented, well-grounded fraction (reuse the Mark et al. refocus grounding already cited in `detectContextSwitchHotspots`). Near-zero new inference; high synergy with the Weekly interruption panel users already see. `accelerate.ts` + `useDerived.ts` (+ a wire-through in `App.tsx`/screen props if the signature widens). Gate: `npm run build`.

- [ ] **E5 — Calendar / meeting time-sink detector** (standalone; biggest coverage win) — the miners read blocks (`detectTimeSinks`) and sessions (sequence/hotspot detectors) but `calendarEvents` are never mined, so the single largest reclaimable sink for analysts — meetings — is invisible to the engine. Add a fourth deterministic detector in `accelerate.ts` (e.g. `detectMeetingLoad`) that mines `OutlookCalendarEvent[]` for recurring low-value meetings (same title/attendee shape recurring ≥ the `MIN_RECURRENCES` threshold), back-to-back stacks with no focus gap, or meeting-heavy days, and emits `technique`/`tool` Plays ("this weekly recurring meeting could be async", "protect a focus block on {day}"). Thread `calendarEvents` into `AccelerationMiningInput` (mirror E4's optional-field approach), reuse the shared `SAVINGS_FRACTION`/hour-label/`stableHash` helpers and the standalone-ranking + aggregator-dedup conventions, and keep evidence to derived meeting metadata (counts, durations, recurrence, day-of-week) — never event bodies or attendee PII beyond what the app already surfaces. Include a demo seed so `?demo=1&screen=accelerate` showcases the new Play distinctly (the "every flagship visual distinct in demo" convention). `accelerate.ts` + `useDerived.ts` + `demoData.ts` (+ wire-through). Gate: `npm run build`.

### Intelligence Engine
_Reference pattern: persisted `forecastHistory` + `scoreForecastAccuracy` — mirror it for retained-history work. The chat-metadata inference family (`analyzeInterruptionLoad`, `summarizeChatStakeholders` in `packages/inference/src/capacity.ts`) is the current reference for new metadata-only signals; EXTEND the shared week-scoped `weekChatEvents` memo in `useDerived.ts` rather than re-filtering per consumer._

### Trust & Verification UX
_Reference surfaces: the filterable Audit trail, the Corrections log, and the per-block "Why this estimate?" evidence + "Derived from" drill-down (`BlockCard.tsx`). New trust work should reuse the `AuditEvent` recipe (the 4-file `AuditEventType` flow) and humanize enum/value strings via `lib/format.ts`, never render raw snake_case._

### Capacity Model Calibration
_Distilled from `docs/heuristics-vs-research.md` — a research pass that checked the model's hand-set constants against queueing theory (M/M/1 `1/(1−ρ)`), Mark et al. (CHI 2008) on interruption cost, and Pencavel on working hours. The model's *directions* are well-grounded; the *exact numbers* are hand-tuned. These items sharpen the worst-justified constants. All in `packages/inference/src/capacity.ts` (loop-safe), some with a thin UI surface. Read the doc's matching section before coding; keep every change explainable (it feeds the narrative/forecast the user reviews)._

---

## Done
_Prior entries live in git history and merged PRs._

- [x] **Empty forecast categories render an empty `<ul>` instead of an explicit "None identified"** (2026-07-01) — Trust & Verification UX slice, self-identified after the Next backlog ran dry (all four theme headers' referenced work is complete in code). The four `ForecastList` tiles on the Forecast surface (Constraints / Risk flags / Recommended actions / Assumptions, `components/common/ForecastList.tsx`, rendered from `ForecastAgentPanel.tsx`) each rendered an UNCONDITIONAL `<ul>` — so a live AI forecast returning `[]` for a category (a reachable state: a clean week legitimately has no risk flags; the domain type is plain `string[]` with no non-empty guarantee) showed a titled-but-blank list. That reads as a semantically-empty "list, 0 items" to screen readers and as a data gap / looks-broken to sighted users, when the accurate signal is the POSITIVE "the model considered this and flagged nothing." Gated the list on `items.length > 0` and render a muted `.forecast-list-empty` "None identified" `<p>` placeholder otherwise — the 4-cell grid stays stable (placeholder, not `null`, so no reflow) and the meaning is explicit. New `.forecast-list-empty` CSS uses only the existing `--text-muted` token (defined in all three theme blocks — light + both dark variants), 13px to match `.forecast-list li`, italic to read as a soft absence. `ForecastList.tsx` + `styles.css`. Build green; passed self-review (adversarial diff re-read across all angles: `items` is a required non-null `string[]` so the length guard is safe; the truthy branch preserves the original list rendering + keys verbatim; the only callers are the 4 `ForecastAgentPanel` sites and the signature is unchanged so nothing breaks; fix lives in the shared component so all four categories benefit — right altitude, not a per-category special case; inline muted placeholder is the correct depth vs. the full-screen `EmptyState` component; token theme-complete; demo forecast arrays are all non-empty so demo mode is unchanged).
- [x] **Surface systematic (repeated) relabels in the corrections log** (2026-07-01) — Trust & Verification UX slice. The Forecast "Model bias from your corrections" panel already surfaces a "×N" count for identical (field, old→new) corrections (`analyzeCorrections`, `capacity.ts`), but the Corrections history (`CorrectionsScreen.tsx`) rendered every edit as a flat chronological row, so a one-off tweak read the same as a 3× systematic pattern actively biasing the model. Added a `repeatCounts` `useMemo` over ALL corrections (not the search-filtered subset, so the count reflects true recurrence regardless of the active query) keyed by `JSON.stringify([field, old_value, new_value])` — the SAME dedup key `analyzeCorrections` uses, so values with spaces/slashes can't alias — skipping no-op (old===new) edits; each `rows` entry carries its `repeatCount`, and rows with `repeatCount >= 2` render a subtle info-tinted `.correction-repeat` pill ("repeated N×") inline after the old→new change, with a `title` tooltip + an explanation-only `.sr-only` gloss (per the mirror convention — no restating the visible count). New `.correction-repeat` CSS uses only existing `--border`/`--surface-muted`/`--info` tokens (theme-complete); `flex: 0 0 auto` + `nowrap` and no auto-margin so the timestamp keeps its own `margin-left:auto` right-alignment while the badge annotates the change. `CorrectionsScreen.tsx` + `styles.css`. Build green; passed self-review (count computed over unfiltered corrections so filtering can't undercount, JSON-triple key matches the inference dedup semantics, no-op edits ignored, `>= 2` gate = "recurs", sr-only is explanation-only, tokens defined in both themes, layout preserves the time right-alignment).
- [x] **Copy buttons falsely report success when the clipboard is unavailable** (2026-07-01) — UI & UX Polish slice applying the D3 clipboard pattern to the last two copy buttons that still lied. The Narrative "Copy as Markdown" (`NarrativeScreen.tsx`) and Audit "Copy JSON" (`AuditEventRow.tsx`) buttons both called `void navigator.clipboard?.writeText(...)` then unconditionally flipped to "Copied"/"JSON Copied" (Narrative also toasted success) — so on an insecure webview where `navigator.clipboard` is undefined, or when the write rejects, they announced success while nothing was copied. Extracted an `async handleCopyMarkdown`/`handleCopyJson` mirroring `AccelerationScreen`/`SavedSkillsScreen`: non-optional `navigator.clipboard.writeText` (a missing clipboard now throws into the catch), `setCopied(true)` + the success toast run ONLY after the `await` resolves, and the catch pushes the shared "Couldn't copy to the clipboard" error toast. `AuditEventRow` had no toast channel, so threaded a required `pushToast` prop through `AuditLogScreen` (its only consumer) and the `ScreenRouter` call site (which already holds `pushToast`). `NarrativeScreen.tsx` + `AuditEventRow.tsx` + `AuditLogScreen.tsx` + `ScreenRouter.tsx`. Build green; passed self-review (both handlers set the copied/success state only on the resolved-await path, error toast on the failure path, `tone:"error"` is a valid `ToastTone`, `pushToast` threaded to the sole `AuditLogScreen`/`AuditEventRow` call sites, matches the D3 reference verbatim incl. the error copy).
- [x] **Confirm before discarding a flagged capture** (2026-07-01) — UI & UX Polish slice. The "Discard" button on the Flagged Captures screen (`components/audit/SensitiveReviewScreen.tsx`) permanently removed a flagged visual-context insight from local storage (and, via `discardVisualInsight` in `App.tsx`, appended a `visual_context` audit event) with NO confirmation — the only irreversible destructive action in the app that skipped the shared confirm modal. Gated it behind the shared `components/common/ConfirmDialog.tsx` (the "Reset data" reference convention): a local `pendingDiscardId: string | null` `useState`, a `pendingInsight` lookup (`flagged.find` by id → falls back to `null` so a stale id can't render an empty dialog), the Discard button now sets the pending id, and the `tone="danger"` (default) dialog names the app + derived `activity_summary` in the description ("permanently removes the flagged {app} capture (\"{summary}\") from local storage and records the action in your audit history. It can't be undone.") — matching the intro's own irreversibility claim, which the `discardVisualInsight` audit event verifiably backs. `onConfirm` discards then clears the pending id; `onCancel` clears. Privacy holds — `activity_summary` is a derived insight already shown on the card, never a raw window title. `SensitiveReviewScreen.tsx` only. Build green; passed self-review (state cleared on both paths, null-safe pending lookup, description's audit claim confirmed against `discardVisualInsight`, ConfirmDialog convention followed exactly).
- [x] **Give the @-mention interruption tile scale (share of reactive messages)** (2026-07-01) — Intelligence Engine slice (Acceleration Engine + Capacity Model Calibration batches complete). The four Weekly `.interruption-stat` tiles all conveyed scale (burst = frequency, /hr = rate, deep-work = "% · N of M") except **direct @-mentions**, a bare count — "5 direct @-mentions" reads identically whether it's 5 of 400 messages (low) or 5 of 12 (high), yet @-mentions are the panel's own "sharpest interruption signal". Added `mention_pct: number` to `InterruptionLoadAnalysis` (`capacity.ts`): share of reactive messages that were direct @-mentions, `mentionCount > 0 && messageCount > 0 ? min(100, max(1, round(mentions/messages×100))) : 0` — floored to 1 when non-zero (mirrors `after_hours_pct`, so a real count never shows beside "0%") and capped at 100 (a malformed export reporting more mentions than messages can't exceed 100%), with the `messageCount > 0` guard so a mentions-only 0-message burst can't divide by zero (Infinity). Enriched the tile's `title` + `.sr-only` (`WeeklyCapacityScreen.tsx`) to state the denominator + share ("N of M messages @-mentioned you directly (X%)" / "X% of this week's M reactive messages pulled you in by name — the sharpest interruption signal, hardest to batch or defer"), matching the deep-work tile's "N of M" self-documenting convention. Both gated on `mention_pct > 0` (NOT `mention_count > 0`) — a self-review fix: `mention_pct > 0` holds exactly when both counts are > 0, so the "N of 0 messages (0%)" contradiction from a mentions-only malformed burst can never render (falls back to the original generic strings). The sr-only adds the share/denominator the visible count omits, per the "mirror only when it adds info" convention (like the deep-work tile). Metadata-only (counts/share, never message text) so privacy holds structurally; `interruptionLoad` is live-derived (no persisted/demo field), and demo chat events carry mentions so demo showcases a real % (not the fallback). `capacity.ts` + `WeeklyCapacityScreen.tsx`. Build green; passed self-review (adversarial diff re-read: pct arithmetic all-finite/bounded 0–100, `messageCount > 0` guard prevents Infinity, floor-to-1 mirrors `after_hours_pct`, the `mention_pct > 0` gate proves the denominator is non-zero so "of 0" is unreachable, the sr-only adds omitted scale rather than restating the visible count, no consumer/demo breakage from the additive field).
- [x] **Peak reactive HOUR of the peak day** (2026-07-01) — Intelligence Engine slice extending the chat-metadata inference family (`analyzeInterruptionLoad`, `capacity.ts`) with the time-of-day axis the existing `peak_day`/`calm_day`/`after_hours` signals can't show. Added a new `peak_hour: number | null` field computed per-(day,hour): a second `dayHourMessages` Map keyed by `dayIndex * 24 + startHour` (populated in the SAME `messages > 0` guard block as `dayMessages`/`afterHoursMessages`, reusing the already-computed `startHour`, so a malformed 0-message burst can't inflate it), then after the weekday-peak loop scan hours 0–23 of `peakDayIndex` with strict `>` (ascending → earliest-hour tie-break, mirroring the `peak_day`/`calm_day` conventions). Day-SCOPED (the peak day's own peak hour) not week-wide, so "peaked on Monday around 10am" is precise, never conflating the overall busiest hour with a different day's. Invariant held + documented: `peak_hour` is non-null exactly when `peak_day` is (the peak day always carries ≥1 message-bearing hour bucket), so it renders inside the EXISTING `.interruption-peak-note` (gated `peak_day && active_day_count >= 2`) — no new `<p>`, no new render gate, and a defensive `peak_hour !== null` guard keeps `formatHourOfDay(number)` TS-safe. New `formatHourOfDay(hour)` helper in `lib/format.ts` (12-hour "10am"/"2pm" label, matching accelerate.ts's local `hourLabel` convention; normalizes via `((round % 24)+24)%24`) — inference returns the raw 0–23 number, the UI humanizes (the "never render raw enum/value" rule). Dropped an initially-added `peak_hour_message_count` field during self-review: it had no UI reader and would have read redundantly in demo ("9 of 9"); `peakHourMessages` stays as the loop's running-max accumulator only. No new persisted/demo field — computed from existing `weekChatEvents`; privacy holds structurally (buckets counts by hour, never message text/window titles). `capacity.ts` + `lib/format.ts` + `WeeklyCapacityScreen.tsx`. Build green; verified via Playwright/demo (weekly note renders "Reactive load peaked on Monday around 10am — 9 messages across 4 active days" — correct spacing, no conflation, after-hours note unregressed). Passed self-review (peak_hour⇔peak_day invariant, day-scoped no-conflation, earliest-hour tie-break determinism, same 0-message guard as siblings, dropped the unused field).
- [x] **Mark AI-authored Acceleration content on the play card** (2026-07-01) — Trust & Verification UX slice derived from the top open theme (Acceleration Engine + Capacity Model Calibration are complete; this fits the "everything reviewable / AI is opt-in" contract). The `accelerationPlays` merge (`App.tsx`) overlays AI-authored `detail`/`recipe`/`recommended_tools` onto a deterministic signal by `signal_id`, but the card gave NO per-play cue that its guidance was AI-written — a reviewer couldn't tell model-derived facts from AI prose. Added a required `authored: boolean` to `AccelerationPlay` (`models.ts`; documented that the estimate/confidence/evidence stay model-derived either way), set `authored: Boolean(match)` in the merge memo (the ONLY `AccelerationPlay` construction site — verified via grep; `saveSkill`/demo build `SavedSkill`, unaffected), and rendered an "AI-authored" `.play-ai-badge` in the card header (`AccelerationScreen.tsx`) gated on `signal.authored`: a `Sparkles` pill mirroring the existing `.play-type-chip` a11y pattern (title tooltip + explanation-only `.sr-only` clarifying the AI wrote the description/recipe/tool picks while the estimate/confidence/evidence stay derived from observed work; icon `aria-hidden`). The type-chip is now wrapped with the badge in a new `.play-header-tags` flex group so the header keeps its exactly-two-child `space-between` layout (`styles.css`; `.play-ai-badge` uses only existing `--info`/`--surface-muted`/`--border` tokens, defined in every theme block — no dark override, no new token). Demo (`generatedPlays: null`) yields `authored: false` for every card so the badge is correctly hidden — demo deliberately showcases the deterministic "no AI, no network" path. Build green; passed self-review (two independent adversarial reviewers — my own re-read + a parallel finder agent — both found 0 defects: `Boolean(match)` correct, single construction site, space-between layout preserved, flex-wrap holds at narrow widths, a11y matches the type-chip convention, badge claim accurate for any `match`, all tokens theme-complete).
- [x] **D3 — Saved Skills library** (2026-07-01) — shipped the durable library that makes AI-authored AUTOMATE recipes reusable beyond the session. New `SavedSkill` domain type (`models.ts`) snapshots a play's recipe TEXT (title/detail/recipe/tools/minutes/`saved_at`, keyed/deduped by `signal_id`) — not just the `signal_id` — so a saved skill survives regeneration AND the miner retiring the signal. Persisted via the retained-history recipe: new `savedSkills: SavedSkill[]` on `PersistedAppState` (`localStore.ts`) parsed in BOTH read paths via a new `parseSavedSkills` (non-array → `[]`; drops entries lacking a string `signal_id` or a non-empty `recipe`), written through `usePersistence` (interface field + dep), and hydrated at ALL FOUR `App.tsx` sites (initial `useState`, async-load, `resetLocalData`, the `usePersistence` call). Two `App.tsx` handlers: `saveSkill(play)` snapshots + upserts by `signal_id` and audits the discrete action (`createAccelerationPlayAuditEvent` gained a third `"saved_to_library"` action — `acceleration_engine`, `derived_only`, `window_titles:false`); `removeSkill(id)` is the undo path (not audited, mirrors `unsavePlay`). The Acceleration play card's recipe `<details>` gained a `.play-recipe-actions` row — a **Copy** button (robust `try/catch` around non-optional `navigator.clipboard.writeText`, "Copied" icon-swap mirrored into `title` + `aria-label`, error toast on failure) and a **Save to library** toggle (`Library` icon, `aria-pressed`, `.is-saved` `--info` tint), shown only when `signal.recipe` exists; a header "View N saved skills" link surfaces the library when non-empty. New `skills` surface via the add-a-screen recipe (`Screen` union + `screenLabels` + `primarySectionForScreen`→"agent" + a third `sectionViews("agent")` sub-tab "Ask | Accelerate | Saved skills" + `ScreenRouter` render + props threaded from `App.tsx`): `components/accelerate/SavedSkillsScreen.tsx` lists saved recipes (newest-first, humanized type chip, recipe `<pre>`, tool chips, per-card Copy + Remove) with a header **Export skills** button → new `serializeSavedSkills` (JSON/CSV) in `lib/dataExport.ts` + `downloadTextFile`; `EmptyState` when the library is empty. Demo seeds one AUTOMATE skill (`savedSkills` required field). Privacy holds structurally — every stored/exported/audited field is derived (recipe is AI-authored from derived signals), never a raw window title. Build green; verified via Playwright/demo (skills screen renders the seeded card + Export in light + dark wide and 420px; Acceleration screen unregressed, "View 1 saved skill" link renders; type chip humanized to "Automate"; no console/page errors). Passed self-review (two parallel finders — correctness + conventions/a11y — confirmed the full 4-site persistence recipe, both localStore read paths, add-a-screen completeness, save/dedup by id, no window-title leak, humanized enum, EmptyState/downloadTextFile reuse, and the mirrored "Copied" convention; their one worthwhile finding — Copy announced success even when the clipboard is absent/rejects — was fixed in both copy handlers).
- [x] **Acceleration header prints the reclaimable total twice** (2026-07-01) — the `.screen-subhead` on `AccelerationScreen.tsx` bolded "…could reclaim roughly **{totalSaved} min/week**" while the `.acceleration-total` pill beside it showed the identical "~{totalSaved} min · est. saved / week" — the same figure twice, side by side. Reworded the subhead to "Mined locally from your observed work — no AI, no network. {N} {plays}, each citing the evidence it was derived from; review before you act." dropping the duplicated number while keeping the play count and the cites-its-evidence/review-before-you-act framing; the scannable pill is now the sole home for the total (unchanged). `totalSaved` stays referenced by the pill so no unused-var break. `AccelerationScreen.tsx` only. Build green; passed self-review (pure copy edit — pill untouched incl. its title+`.sr-only` gloss, singular/plural play wording preserved, no logic/a11y/behavior change).
- [x] **Acceleration Play action rows don't bottom-align across cards** (2026-07-01) — changed `.play-actions` in `styles.css` from `margin-top: 2px` to `margin-top: auto` (+ `padding-top: 2px` to keep the prior 2px breathing gap) so the Save/Dismiss row pins to the card foot. `.play-actions` is the last child of the flex-column `.play-card`, so in the `align-items:stretch` `.play-grid` the auto top-margin absorbs the free space in shorter cards (e.g. an AUTOMATE card lacking an AI recipe/tools) and the action rows now bottom-align across the row. `styles.css` only. Build green; passed self-review (last-child + flex-column confirmed, gap still separates from evidence, padding-top preserves prior spacing, no a11y/behavior change).
- [x] **D2 — useAcceleration hook (opt-in AI synthesis via `ai_complete`)** (2026-07-01) — shipped the Acceleration Engine's optional AI layer, mirroring `useForecastAgent` end-to-end. New `hooks/useAcceleration.ts`: builds the derived-only prompt (`buildAccelerationPrompt` from D1) over the deterministic `accelerationSignals`, routes it through the EXISTING generic `ai_complete` command via `aiCompleteJson<AuthoredAccelerationPlays>` + `jsonSchemaFormat("acceleration", accelerationSchema)` (NO Rust), and — on success — re-whitelists every authored field (filtering `data.plays` to currently-mined `signal_id`s, deduping `recommended_tools` so duplicate names can't collide on React keys, NaN-guarding the numbers), persists a `PersistedAccelerationRecord`, and emits a `derived_only` `acceleration_engine` audit event (`sent_to_openai: true`, `window_titles: false`); the error path audits the failure. The discrete AI call is the audited "generated" action A1/C3 deferred. Persistence follows the C3 recipe: new `generatedPlays: PersistedAccelerationRecord | null` on `PersistedAppState` (`localStore.ts`) parsed in BOTH read paths via a new `parseAccelerationRecord` (malformed → null → deterministic-only render), written through `usePersistence` (field + dep array), and hydrated at ALL FOUR `App.tsx` sites (initial `useState`, async-load, `resetLocalData` + `resetAcceleration()`, the `usePersistence` call). The authored payload is merged onto the live signals by `signal_id` in an `accelerationPlays` memo — deterministic figures (title/estimate/confidence/evidence) stay authoritative; the AI only overlays `detail`/`recipe`/`recommended_tools` (recipe null / no tools when unauthored or for a no-longer-mined signal). `AccelerationScreen.tsx` renders the AUTOMATE `recipe` (collapsible `<details className="play-recipe">`, native marker like `.evidence summary` — no double-marker) and TOOL `recommended_tools` (chips), plus an opt-in synth control below the header: an AI-key-gated "Generate / Regenerate skills" `primary-action` (disabled while "Authoring skills…" per `useAsyncStatus`, `formatAuditTime` "generated" note, privacy reassurance copy) OR, when no key, a dashed hint + "Open Settings" CTA; failures surface via the shared `InlineError` (+ an App-level retry toast keyed on `accelerationError`, mirroring forecast). Demo seed gains `generatedPlays: null` (required field). Privacy holds structurally (prompt/schema whitelist derived signals only; the miner never emits window titles). Verified via Playwright/demo in light + dark wide (3 deterministic plays + the no-key hint render cleanly; `.acceleration-synth` reflows). Build green; passed self-review (two parallel finders — correctness/cross-file found 0 bugs and confirmed 4-site hydration, merge-memo null-safety, both localStore read paths, hook ordering, and the derived-only privacy contract; conventions/a11y found + fixed the `<details>` double-marker and the tool-chip duplicate-key risk).
- [x] **D1 — AI synthesis prompt + schema (derived signals only)** (2026-06-30) — laid the AI layer's foundation for the Acceleration Engine with two new, currently-unwired `services/` files (D2 imports them). `accelerationSchema.ts`: a strict-JSON `accelerationSchema` (`as const`) for `{ plays: [...] }`, each play `{ signal_id, type, detail, recipe, recommended_tools, estimated_minutes_saved_per_week, confidence }` — every property in `required`, `additionalProperties:false`, nullable `recipe` via `["string","null"]`, `type` enum mirrored from the domain `AccelerationPlayType` union (`accelerationPlayTypes` const) so adding a play type is one edit; plus `ACCELERATION_INSTRUCTIONS` (the system prompt, modeled on `WORK_BLOCK_CLASSIFIER_INSTRUCTIONS`) and the matching `AuthoredAccelerationPlay`/`AuthoredAccelerationPlays` TS interfaces D2 will parse via `aiCompleteJson<…>`. `accelerationPrompt.ts`: `buildAccelerationPrompt({ weekRangeLabel, signals })` + `ACCELERATION_PROMPT_VERSION`, modeled on `forecastAgentPrompt.ts` (structured-context JSON, strict-JSON-only instruction). Hard privacy rule enforced structurally: `summarizeSignal` WHITELISTS fields explicitly (never spreads the signal) so a future `AccelerationSignal` field can't silently widen what's sent; `AccelerationSignal` has no `window_title` field at all, and the prompt sends `derived_from_count` (a number) rather than the opaque source ids — a doc comment asserts the no-raw-titles contract. No network code (D2 wires the `ai_complete` call). `accelerationSchema.ts` + `accelerationPrompt.ts`, both new. Build green; passed self-review (strict-schema well-formedness — all-required/additionalProperties-false/nullable-via-array — verified; enum sync to the domain union confirmed; privacy contract holds by construction (whitelist + no title field + count-not-ids); both files export only used members so no unused-locals break; no consumers yet so nothing else can regress).
- [x] **C3 — Persist + audit Play actions** (2026-06-30) — made Acceleration Plays actionable + durable. Added two persisted id sets to `PersistedAppState` (`localStore.ts`): `dismissedPlayIds`/`savedPlayIds` (arrays of `signal_id`), parsed in BOTH read paths via a new `parseStringIdList` (non-array → `[]`, filters non-strings) and written through `usePersistence` (interface field + effect dep). Plays are DERIVED each render (no demo override), so dismiss/save are keyed by the deterministic `signal_id` — a hidden/saved play survives a reload and the miner re-deriving. `App.tsx` gained four handlers + full hydration (initial `useState`, async-load effect, `resetLocalData`, the `usePersistence` call): `dismissPlay`/`savePlay` follow the discrete-action audit recipe (update visual state, then `if (isDemoMode) return` BEFORE the audit append, then `createAccelerationPlayAuditEvent(...).slice(-1000)`); `restoreDismissedPlays`/`unsavePlay` are undo paths (no audit). New `createAccelerationPlayAuditEvent` (`lib/audit.ts`) mints the `acceleration_engine` event at `derived_only` privacy with details = action/signal_id/play_type/minutes/derived_from + `window_titles:false` — privacy holds structurally (the miner never emits raw titles; `summary` embeds the derived `signal.title` only). Decided "generated" is NOT audited here: the deterministic miner re-derives continuously and logging each would flood the capped trail (the documented high-frequency-effect rule) — the "generated" event lands in D2 where the AI call is the discrete action. `AccelerationScreen.tsx` filters dismissed (`visibleSignals`), marks saved (Bookmark↔BookmarkCheck toggle with `aria-pressed` + state-mirrored `title`/text per the icon-swap a11y convention), and gains a third render branch for the all-dismissed case (EmptyState + "Restore dismissed plays" CTA) plus an inline `.acceleration-restore` link when `dismissedCount > 0` — so dismiss is never a dead end. Completed the 4-file `AuditEventType` recipe A1 deferred: the `acceleration` filter chip + `filterMatches` (`AuditLogScreen.tsx`) and a distinct violet `.audit-badge.acceleration_engine` in both the light + dark blocks (`styles.css`). Demo seed (`demoData.ts`) gained `dismissedPlayIds: []`/`savedPlayIds: []` (required-field). Build green; passed self-review (two parallel finders — correctness/cross-file + conventions/a11y — both 0 findings: persistence round-trip intact across both read paths, all four hydration sites present, demo-skip-audit ordering correct, three screen branches mutually exclusive, save-toggle dispatch correct, 4-file badge recipe complete, all CSS tokens defined in both themes, derived-only privacy holds; the lone note — a saved-then-dismissed play retains its saved id while hidden — is consistent state, not a defect).
- [x] **C2 — Jaw-dropping demo seeds** (2026-06-30) — engineered the demo data so the deterministic Acceleration miner (`buildAccelerationSignals`) surfaces exactly three vivid, evidence-cited Plays in `?demo=1&screen=accelerate` (~100 min/week reclaimable): an **AUTOMATE** "revenue-report rebuild" (a unique Hex→Looker→Teams app handoff recurring 3×, 37 min/wk), a **TOOL** "SQL / data modeling time-sink" (3 new recurring non-deep "query cleanup" `WorkBlock`s, 42 min/wk), and a **TECHNIQUE** "2pm context-switch hotspot" (a 14:00 reactive churn, 21 min/wk). KEY CONSTRAINT discovered + held: the live weekly capacity snapshot is computed from `blocks` only (not sessions), and the curated demo reconciles at **22% reliable / 58% committed** across Weekly/sidebar/Agent/narrative — so the AUTOMATE+TECHNIQUE evidence was added as session `samples()` (zero capacity impact), while the TOOL's 3 new SQL blocks (+7%) were OFFSET by trimming the two existing reactive blocks (`demo-retention` 13→8, `demo-attribution` 11→6) so reliable/committed land back on exactly 22/58 — preserving the reconciliation and avoiding any re-tune of the forecast/narrative/history fixtures. Robustness: all added `samples()` land in free slots on the FIXED-hour past days (never on the `now`-relative "today" sessions, whose `activeStart` shifts with the demo-open hour) — an overlap would interleave samples minute-by-minute and corrupt sessionization (verified: identical 3-signal output across 08:30/12:00/18:00/23:30 opens and Mon/Wed refs). Privacy holds structurally — the miner emits app names/counts/minutes/hour-labels only, never the synthetic `window_title`s. `demoData.ts` only (also bumped the classification audit "7→10 sessions" to track the new non-calendar block count). Build green; passed self-review (two parallel finders — correctness found 0 bugs and confirmed no sample overlaps / sound scope / no privacy leak; consistency found the 15%-vs-fixtures tension, which the capacity-offset rewrite then eliminated by restoring 22/58).

---

## Never
- `apps/desktop/src-tauri/` (Rust shell) — needs manual Tauri testing.
- `.env` or any secret.
- `vite.config.ts` port (5173) / `tauri.conf.json`.
