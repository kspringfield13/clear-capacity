# STATUS.md — ClearCapacity Improvement Loop

The `/improve` loop reads this file first and writes it last on every run.

**Guardrails**
- Scope: edit `apps/desktop/src/` and `packages/` only.
- Gate: `npm run build` must pass before a task is marked done.
- One task per run.

**Ordering** — the loop takes the first unchecked `- [ ]` task reading top-to-bottom
through **Next**. Sections are in priority order; within a section, tasks are
dependency-ordered, so the topmost open task is always the right pick. Tasks tagged
**[manual / Rust]** need native work outside loop scope — ship the loop-safe slice
described in the task and flag the rest as a follow-up.

---

## In Progress
_(none)_

## Next

### UI & UX Polish
- [ ] **Corrections `old → new` change line clips at narrow widths** — the prior fix stacked `.corrections-list li` into a column at ≤600px, but the change line itself (`.correction-change` in `components/review/CorrectionsScreen.tsx`) is still a non-wrapping `inline-flex` of two `white-space: nowrap` `.correction-old`/`.correction-new` spans. Measured at a 420px viewport the change line is ~406px inside a ~388px list, so long category labels ("Planned analysis / project work → Ad hoc stakeholder requests") overflow and clip off the right with no scroll. In the `@media (max-width: 600px)` block in `styles.css`, give `.corrections-list li .correction-change` `flex-wrap: wrap` and add `min-width: 0` to `.correction-old`/`.correction-new` so the segments wrap (or ellipsize) instead of clipping. `styles.css` only.
- [ ] **Forecast model-bias values truncate with no tooltip** — the "Model bias from your corrections" rows in `components/capacity/ForecastScreen.tsx` ellipsize `.model-bias-from`/`.model-bias-to` at narrow widths (e.g. "Planned analysi…", "Ad hoc stakeho…"), so the specific mislabel signal is unreadable on a phone/touch. The sibling `.correction-change` in `CorrectionsScreen.tsx` already carries a `title` with the full `old → new`; mirror it here by adding `title={`${humanize…(from)} → ${humanize…(to)}`}` to the `.model-bias-change` span so hover/long-press reveals the full values. `ForecastScreen.tsx` only.

### Intelligence Engine
_Reference pattern: persisted `forecastHistory` + `scoreForecastAccuracy` (PR #19) — mirror it for retained-history work._

### Trust & Verification UX
- [ ] **Data export & retention controls** — (a) export the work ledger + audit trail to JSON/CSV from `SetupScreen.tsx`; (b) add a user-set retention window that auto-expires `activeWindowSamples` older than N days. Both loop-safe; reinforce the local-first positioning.
- [ ] **Forecast track-record panel** *(builds on PR #19)* — add a "Forecast track record" list to `ForecastScreen.tsx` showing predicted-vs-actual per past week with On target / Close / Off chips, so the model can be audited over time. Reads the existing `forecastHistory`.

---

## Done
_Prior entries live in git history and merged PRs._

- [x] **Sensitive-content review queue** (2026-06-28) — new "Flagged" tab under History (`components/audit/SensitiveReviewScreen.tsx`) listing every `VisualContextInsight` with `sensitive_content_detected === true` (sorted newest-first), each with a per-item "Discard" action. Added the `"sensitive"` `Screen` value (`lib/types.ts`), `screenLabels` entry, `primarySectionForScreen` → `history`, and a `sectionViews("history")` "Flagged" tab (`lib/ui.ts`); wired the screen + a new `onDiscardInsight` prop through `ScreenRouter.tsx`. `App.tsx` `discardVisualInsight(insightId)` removes the insight from `visualContextInsights` state (so persistence drops it) and appends a `visual_context` audit event (`source: "privacy_control"`, `privacy_level: "local_only"`, `title: "Flagged capture discarded"`) — details carry only `app_name`/`captured_at`/`insight_id` (window_title kept OUT, privacy). Remapped ⌘8→Flagged, ⌘9→Settings in the keyboard map. Seeded a flagged demo insight (`demo-visual-2`, Mail/comp review) so the tab isn't empty in demo. New token-based `.sensitive-*` styles (amber "Sensitive" pill via `--amber-soft`/`--warning`, `--danger` "Screenshot retained"); `.sensitive-screen` opted into the ≤760px `display:block` reflow + the `.screen-header` flex-pin group. Build green; passed self-review (no correctness findings; applied the one consistency fix — header flex-pin).
- [x] **"Why this block?" evidence drill-down** (2026-06-28) — extended the `<details className="evidence">` disclosure in `components/ledger/BlockCard.tsx` with a labeled "Derived from" sub-list of `block.derived_from` (the inference path / source IDs), rendered as `<code>` items under an uppercase `.evidence-derived-label`. Both the evidence and derived-from lists are now gated on non-empty arrays, with a shared "No inference detail recorded for this block." fallback when both are empty (previously an empty `<ul>` rendered). New token-based `.evidence-derived*` / `.evidence-empty` styles (light + dark via `--text-subtle`/`--text-muted`) in `styles.css`; `code` uses `word-break: break-all` so long source IDs wrap. `BlockCard` renders in both ledger and review screens, so the path now shows in both. Build green; passed self-review (no findings).
- [x] **Automated calendar sync** *(loop-safe slice)* (2026-06-28) — new `packages/integrations/src/calendar/calendarSource.ts`: a provider-agnostic `CalendarSource` contract (`sync(weekId, input?) → { events, work_blocks }`) plus a `CalendarProviderDescriptor` registry (`CALENDAR_PROVIDERS`: outlook_ics `file_import`/loopSafe, google + microsoft_graph `oauth`/not-loopSafe), `getCalendarProvider`, and two factories — `createOutlookIcsSource` (wraps `parseOutlookIcs` + `outlookEventsToWorkBlocks`, the one source that runs in pure JS today) and `createOAuthCalendarSource(descriptor, fetcher)` which adapts a native-supplied `CalendarEventFetcher` to the same contract and reuses the shared `outlookEventsToWorkBlocks` so OAuth meetings normalize identically to the `.ics` path. `SetupScreen.tsx`: added an "Automated calendar sync" data-source row with a disabled "Connect calendar" stub — maps the `oauth` descriptors to disabled `.settings-control` buttons (title + `aria-label="…(coming soon)"`, inherit the global `button:disabled` styling). New `.calendar-connect-options` flex-column wrapper in `styles.css`, added to both the medium + narrow `@media` placements so it reflows like `.settings-control`. **Rust follow-up** (documented in the module header): add a Tauri OAuth command per provider, widen `OutlookCalendarEvent.source` from the `"outlook_ics"` literal to a `"outlook_ics" | "google" | "microsoft_graph"` union, then build the frontend source via `createOAuthCalendarSource` with an `invoke()` fetcher and flip the descriptor's `loopSafe` + Connect button. Build green; passed self-review (no findings).
- [x] **Git activity as a planned-work signal** (2026-06-28) — new `packages/integrations/src/git/gitLog.ts` (pure parser) + `fixture.ts` (sample export). `parseGitLog(content, options?)` turns a `git log --pretty=format:'%H|%aI|%an|%s'` export into `GitCommitRecord[]`: `#`-comment lines are skipped, a `# repo: <name>` directive scopes the commits that follow (so multiple repos concatenate into one export), only the first three `|` are field separators (so `|` survives in subjects), and malformed/bad-date lines are dropped (mirrors `parseOutlookIcs` leniency). `parsePrNumber` extracts PR refs from both squash (`(#NN)`) and merge (`Merge pull request #NN`) subjects. `gitCommitsToImport` groups commits per repo, sorts by time, and splits into focus sessions wherever two consecutive commits are >`sessionGapMinutes` (default 90) apart; each session spans `leadMinutes` (default 30) before its first commit through its last, keyed `project_name = project_hint = repo`. `importGitLog(content, options?)` runs the full pipeline through the shared `importRawEvents` so capacity/id/dedup heuristics stay identical to every other source — emitting deep-work, planned `WorkBlock`s. The live `git log` fetch/watch is **[manual / Rust]** (process spawn + fs in `src-tauri/`) — flagged as a follow-up. Self-review fixed: `leadMinutes:0` no longer collapses a lone commit to a zero-length span that `importRawEvents` would silently drop (start clamped to ≥60s before end); `event_id` now uses the full commit SHA (globally unique) instead of a `slug(repo)+short_hash` that could collide; dropped raw commit subjects from `metadata` (privacy — kept commit count/authors/PR refs only); removed dead `is_merge`/`short_hash` fields and an unused `gitLogToWorkBlocks` wrapper. Build green; verified via standalone smoke test (8 commits→4 sessions, 2 malformed dropped, PRs 214/98, pipe-subject preserved, `leadMinutes:0` keeps all blocks); passed self-review.
- [x] **Importable `RawEvent` schema** (2026-06-28) — new `packages/integrations/src/import/rawEvents.ts`: `importRawEvents(payload, options?)` normalizes a documented JSON shape (`RawEventImport[]`, an `{events:[...]}` wrapper, or the JSON string of either) into `RawEvent[]` + derived `WorkBlock[]`, returning `{events, work_blocks, skipped}`. Per-`SourceType` defaults (window/calendar/browser/slack/task/git/manual → category/mode/planned_status/stakeholder/confidence) map each event; a source may refine via optional `category`/`mode`/`planned_status`/`project_name`/`capacity_pct` (validated against the taxonomy arrays, else default). week_id derived per event via a local ISO-week helper mirroring `lib/date.ts` (kept local — integrations must not import from `apps/desktop`); `weekId`/`userId` overridable. Records lacking required fields, with an unknown `source_type`, or an invalid span (`end>start`) are dropped into `skipped` (no silent loss). Privacy: `window_title`/`file_path` are preserved on the `RawEvent` but kept OUT of the displayed `evidence[]`. Extracted shared `stableHash` + `capacityPctFromSpan` + `WEEKLY_BASELINE_MINUTES` into `packages/integrations/src/internal/normalize.ts` and rewired `outlookIcs.ts` onto them (was a 3-way copy). Self-review fixed: unknown source now skipped (was relabeled "manual"), non-object `metadata` sanitized to `{}`, and `work_block_id` keyed off `eventId` alone so block+event dedup together (no dangling `derived_from`). UI consumption deferred to the Git-import task. Build green; verified via standalone smoke test (dedup, skip counts, linkage, metadata sanitize); passed self-review.
- [x] **Ledger work-block list starved on narrow screens** (2026-06-28) — verified already solved: the `@media (max-width: 760px)` block already sets `.ledger-screen { display: block }` + `.screen { overflow-y: auto }` (since PR #34), so at ≤600px the `.ledger-list` flows to full content height and the screen scrolls naturally — no internal-scroll starvation. Confirmed empirically at 420px and 580px (list `clientHeight===scrollHeight`; `.screen` scrollHeight≫clientHeight). No code change needed.
- [x] **Corrections rows clip the `old → new` change at narrow widths** (2026-06-28) — added a `@media (max-width: 600px)` rule in `styles.css` that stacks `.corrections-list li` (`flex-direction: column; align-items: flex-start; gap: 4px`) so the field pill, project, `old → new` change, and timestamp each get the full container width instead of being clipped off the right of the ~388px scroll box. Reset `.correction-change` to `flex: 0 1 auto; max-width: 100%` (so it sizes to content rather than growing along the new vertical main axis) and zeroed the `time` `margin-left: auto` so it left-aligns with the stack. Scoped to `.corrections-list` so the `.history-panel`/`.copilot` history lists are untouched; selectors are later-in-source at equal-or-higher specificity than the base `.history-list li` rules. `styles.css` only. Build green; passed self-review.
- [x] **Evidence-based forecast confidence** (2026-06-28) — added pure `summarizeForecastAccuracy(scored)` + `ForecastAccuracyTrend` type to `packages/inference/src/capacity.ts` alongside `scoreForecastAccuracy`: takes `{week_id, predicted_pct, actual_pct}[]`, reuses `scoreForecastAccuracy(...).error_pts` per week for consistent rounding, sorts by `week_id`, averages the most recent `ACCURACY_TREND_WINDOW_WEEKS` (8) into a rolling mean-absolute-error, returns `null` when empty. `useDerived.ts`: new `forecastAccuracyTrend` memo that pairs each target week's latest forecast (dedup by `generated_for_week`, latest `generated_at` wins) with its actual reliable capacity (from `snapshotHistory`, live snapshot preferred for the current week) — threaded `snapshotHistory` into the hook. Rendered a muted `.forecast-accuracy-trend` line ("Forecasts have averaged ±N pts over the last K weeks") beneath the accuracy banner in `ForecastAgentPanel.tsx`, gated on `week_count >= 2` so it never duplicates the single-week banner; bold `±N pts`, token-based light+dark styles in `styles.css`. Threaded the trend through `App.tsx` → `ScreenRouter` → `ForecastScreen`. Seeded 2 extra demo `forecastHistory` entries (targeting prior snapshot weeks, actuals 26/29) so demo shows a 3-week track record. Verified rendering in light + dark via demo mode (shows "±5 pts over the last 3 weeks"). Self-review dropped a redundant `role="status"` (matches the sibling `.forecast-baseline-note` convention). Build green; passed self-review.
- [x] **Correction-driven bias signal** (2026-06-27) — added pure `analyzeCorrections(corrections)` to `packages/inference/src/capacity.ts` (`CorrectionBias`/`CorrectionBiasAnalysis` types): counts each `(field, old→new)` pattern across the label fields (`category`/`mode`/`planned_status`/`stakeholder_group`/`blocker_flag` — free-text/time edits excluded), skips no-op edits, dedups via a `\0`-delimited Map key (collision-safe since category labels contain spaces), and surfaces any pattern repeated `≥ SYSTEMATIC_CORRECTION_THRESHOLD` (3) sorted by count desc; domain-typed and unit-testable like `scoreForecastAccuracy`. `ForecastScreen.tsx`: computes it via `useMemo` (hook before the `hasWorkBlocks` early return — Rules of Hooks safe) and renders an explainable amber `.model-bias-note` above the Forecast Agent panel, gated on `biases.length > 0`; each row humanizes `field`/values via `fieldLabel` + `humanizeCorrectionValue` (planned_status → "Planned/Unplanned"; category values are already human), with an `×N` count chip + `.sr-only` description. Threaded `corrections` through `ScreenRouter` → `ForecastScreen`. New token-based `.model-bias-*` styles (light + dark) in `styles.css`. Seeded 5 extra demo corrections in `demoData.ts` so demo mode showcases two biases (planned→unplanned ×3, category mislabel ×3). Verified rendering in light + dark via demo mode. Build green; passed self-review.
- [x] **Personal baselines + trend deltas** (2026-06-27) — added pure `computeCapacityBaselines(history)` + private `median()` to `packages/inference/src/capacity.ts` (rolling medians over the most recent 6 prior-week snapshots for `reactive_pct`, `meeting_pct`, `context_switch_score`, `reliable_new_work_capacity_pct`; domain-typed, self-sorting by `week_id`, `null` when no history). `WeeklyCapacityScreen.tsx`: threaded `snapshotHistory` in (via `ScreenRouter` + `App.tsx`), computes baselines from weeks strictly before the viewed week, renders a "vs your N-wk median" chip row (`BASELINE_METRICS` config; `scale: 100` lifts the 0–1 context-switch index onto the /100 RiskRow scale; `betterWhen` drives a green/red/neutral `data-tone` + up/down/flat arrow). Chips gated on `week_count >= 2` and a non-empty viewed week; each carries a `title` + `.sr-only` delta description. New `.baseline-chip*` token-based styles (light + dark) in `styles.css`. Fixed a latent demo-seed scale bug: `demoData.ts` seeded `context_switch_score`/`wip_load_score` on a 0–100 scale (44/52) while the live model emits 0–1 — corrected to 0.44/0.52 (and history 0.38/0.49/0.45) so demo deltas read correctly. Verified rendering in light + dark via demo mode. Build green; passed self-review.

---

## Never
- `apps/desktop/src-tauri/` (Rust shell) — needs manual Tauri testing.
- `.env` or any secret.
- `vite.config.ts` port (5173) / `tauri.conf.json`.
