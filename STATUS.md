# STATUS.md — ClearCapacity Improvement Loop

The `/improve` loop reads this file first and writes it last on every run.

**Guardrails**
- Scope: edit `apps/desktop/src/` and `packages/` only.
- Gate: `npm run build` must pass before a task is marked done.
- One task per run.

**Ordering** — the loop takes the first unchecked `- [ ]` task reading top-to-bottom
through **Next**. Sections are in priority order; within a section, tasks are
dependency-ordered, so the topmost open task is always the right pick. Tasks tagged
**[manual / Rust]** need native work outside loop scope — ship the loop-safe slice
described in the task and flag the rest as a follow-up.

---

## In Progress
_(none)_

## Next

### Interaction & Visual Polish
_New batch from the 6-hour visual UX pass (2026-06-29, second run). The app is highly polished across light/dark + the 420px compact layout — a per-screen DOM probe at 420px confirmed there is NO broken layout: every content screen internal-scrolls within the fixed-viewport shell (the macOS-window model) and already carries the scroll-shadow affordance, so the "content cut off below the fold" look in full-page screenshots is a capture artifact, not a bug (don't re-flag it). These are clarity/consistency refinements verified against BOTH the rendered demo screens AND the named source lines. All front-end-only and loop-safe (`apps/desktop/src/`, plus `styles.css`). Open the file/lines and confirm the issue still exists before coding — several previously-listed items were found already solved this pass (the Forecast `×3` model-bias badge already has `title`+`.sr-only`; the Trends legend deltas already carry an `.sr-only` "N points higher/lower than X weeks ago" explanation; the BlockCard relabel selects already have visible field labels)._
- [ ] **Agent briefing metric tiles lack the title + sr-only treatment** — in `components/agent/AgentScreen.tsx` the three `.briefing-metrics` tiles (~lines 582–586: `Planned` / `Reactive` / `Outlook`) each render a bare `<span>{label}</span><strong>{value}</strong>` with NO `title` tooltip and NO `.sr-only` explanation, unlike the Weekly metric cards and the four `.interruption-stat` tiles which all carry both (the established "a labeled % needs a hover tooltip stating what it measures + an `.sr-only` mirror" convention). Add a plain-language `title` + explanation-only `.sr-only` span to each tile so a new user can decode "Planned 32%" / "Reactive 24%" / "Outlook · Low carryover risk" without prior context. `AgentScreen.tsx` only (`.sr-only` already defined in `styles.css`).
- [ ] **Sidebar nav doesn't surface the ⌘1–9 screen shortcuts** — `App.tsx` registers ⌘1–9 jump-to-screen shortcuts (`SCREEN_KEYS`, ~line 559) but they are invisible in the UI: the `.nav-item` buttons in `components/shell/AppShell.tsx` (~lines 92–104) and the bottom `.settings-button` carry no `title`/`aria-keyshortcuts`. Add the shortcut to each nav button that maps to one (Today ⌘1, Week ⌘2, History ⌘5, Settings ⌘9) via `title="Today (⌘1)"` + `aria-keyshortcuts="Meta+1"` so the existing power-user shortcuts become discoverable; leave the Agent item (which has no shortcut in `SCREEN_KEYS`) hint-free. `AppShell.tsx` only.

### Intelligence Engine
_Reference pattern: persisted `forecastHistory` + `scoreForecastAccuracy` — mirror it for retained-history work. The chat-metadata inference family (`analyzeInterruptionLoad`, `summarizeChatStakeholders` in `packages/inference/src/capacity.ts`) is the current reference for new metadata-only signals; EXTEND the shared week-scoped `weekChatEvents` memo in `useDerived.ts` rather than re-filtering per consumer._
- [ ] **Name the calmest active weekday for protected deep work** — `analyzeInterruptionLoad` (`packages/inference/src/capacity.ts`) already reports `peak_day` (busiest reactive weekday) and the Weekly `.interruption-peak-note` footnote says "Consider protecting the quieter days for deep work" without naming which. Mirror the existing `peak_day` weekday-bucketing (metadata-only: `messages` counts over active days, same lowest-index tie-break) to also report a `calm_day` — the lowest-volume *active* weekday — returning null when `active_day_count < 2`. Surface it in the same footnote ("…your quietest active day was **Thursday**."), gated on `calm_day && active_day_count >= 2`. `capacity.ts` + `components/capacity/WeeklyCapacityScreen.tsx`.

### Trust & Verification UX
_Reference surfaces: the filterable Audit trail, the Corrections log, and the per-block "Why this estimate?" evidence + "Derived from" drill-down (`BlockCard.tsx`). New trust work should reuse the `AuditEvent` recipe (the 4-file `AuditEventType` flow) and humanize enum/value strings via `lib/format.ts`, never render raw snake_case._
- [ ] **At-a-glance source-origin chip on each work block** — `components/ledger/BlockCard.tsx` shows confidence + blocker badges in `.block-chips` but nothing about *where* a block came from; provenance is only reachable by expanding the "Derived from" drill-down. Add a small muted origin chip to `.block-chips` (Calendar / Workplace chat / Activity capture) derived from the `work_block_id` prefix — `calendar-outlook-` → Calendar; `imported-` → Workplace chat (chat is the only `imported-` source surfaced in the UI today); else Activity capture — reusing the same prefix convention `App.tsx` already keys cross-source dedup off of. New token-based light+dark `.block-origin` chip in `styles.css`; renders on both the ledger and Today review cards. (If a future non-chat `imported-` source lands, refine the label off `derived_from`/`evidence` — leave a code comment.)

---

## Done
_Prior entries live in git history and merged PRs._

- [x] **Surface peak reactive chat day in the interruption panel** (2026-06-29) — `analyzeInterruptionLoad` (`packages/inference/src/capacity.ts`) reported reactive-chat *counts* but never *when* in the week the load concentrated. Extended it (metadata-only: spans + `messages` counts, no text) to bucket reactive message volume by local weekday (`new Date(start).getDay()`) and report three new fields — `peak_day` (weekday name, null when no message volume), `peak_day_message_count`, and `active_day_count` (distinct message-bearing weekdays). Peak picked by ascending-index iteration + strict `>` from a 0 baseline → deterministic lowest-index-wins tie-break, `peak_day` null for a burst-only/zero-message week. Surfaced as a `.interruption-peak-note` footnote in the Weekly interruption panel ("Reactive load peaked on **Monday** — 9 messages across 4 active days. Consider protecting the quieter days for deep work."), gated `peak_day && active_day_count >= 2` so a single-day week shows no noise. New token-based light+dark `.interruption-peak-note`/`strong` CSS. Renders in demo (chat seed spans Mon–Thu; peaks Monday/9). Picked from the Intelligence Engine category — the Next backlog held only the three category headers, no concrete `- [ ]` items. Build green; passed self-review (2 parallel finders + own adversarial pass: correctness clean `[]`; applied the edge-case finder's cleanup — bucket only message-bearing days so a malformed 0-message burst can't inflate `active_day_count` or trip the ≥2-day gate — plus two comment-accuracy fixes; the only construction site is the single return, all consumers are read-only passthrough, `getDay()` local-time matches the caller's local-calendar week filter).
- [x] **Surface stakeholder-map truncation footnote** (2026-06-29) — the "Who your reactive time served" chip row on `WeeklyCapacityScreen.tsx` slices to the top 4 groups (`summarizeChatStakeholders` → `groups = ranked.slice(0, limit)`) but never signaled when more existed, so with >4 stakeholder groups the shown `share_pct`s silently summed to <100% with no explanation — a clarity + trust gap. Surfaced the already-computed-but-unused `ChatStakeholderSummary.group_count` (distinct groups before the top-N cut) as a muted footnote "Top N of M groups by reactive volume — K more not shown", gated on `group_count > groups.length` (always-safe: `groups.length ≤ group_count` since `groups` is a slice of the `group_count`-length `ranked`). New `.stakeholder-chips-note` rule (token `var(--text-subtle)`, 11px, `flex-basis:100%` so it drops to its own line in the `.baseline-chips` flex row, theme-safe via tokens). Picked from the top "Interaction & Visual Polish" category — the Next backlog held only the three category headers, no concrete `- [ ]` items. Build green; passed self-review (2 parallel finders: correctness clean `[]`; applied the conventions fix — corrected a stale CSS comment that promised a "100%" string the final copy doesn't render; kept the dedicated class since `.capacity-caption` is right-aligned/weighted, not a clean reuse).
- [x] **Prune persisted `chatEvents` by `retentionDays`** (2026-06-29) — the retained chat `RawEvent` store (`chatEvents`, added for the interruption/stakeholder signals) grew unbounded, unlike `activeWindowSamples` which the retention effect already prunes — a privacy gap since retention is user-controlled. Extended the single retention-expiry `useEffect` in `App.tsx` to also prune `chatEvents` against the same `Date.now() - retentionDays*…` cutoff, using `event.timestamp_end` as the age point (the conservative choice — keeps an event until its latest moment ages out) and the identical same-reference-when-unchanged functional-update guard (`kept.length === current.length ? current : kept`) so adding `chatEvents` to the dep array can't loop. Demo-mode + `retentionDays === null` early-return unchanged; pruned array persists via the existing `usePersistence` `chatEvents` dep; per-row expiry stays unaudited (only the discrete policy change in `changeRetentionDays` is logged, matching the sample-expiry rationale). Verified safe for current-week signals: `weekChatEvents` filters by `timestamp_start` against the current ISO week (≤7 days) and the minimum `retentionDays` option is 7, so the `timestamp_end >= cutoff` filter can never drop a current-week event. Build green; passed self-review (8-angle code-review agent: confirmed no render loop, no current-week signal loss, NaN-tolerant like the sibling sample filter, persistence wired, conventions clean).
- [x] **"Chat interruption load" tiles have inconsistent, hover-only tooltips** (2026-06-29) — all four `.interruption-stat` tiles in `components/capacity/WeeklyCapacityScreen.tsx` now carry a plain-language `title` (added to tiles 1 "reactive bursts" + 3 "direct @-mentions"; tiles 2 + 4 already had one) AND a matching `.sr-only` explanation span (none had one before), matching the `ConfidenceChip` / `.block-capacity` title+`.sr-only` convention so a new user can decode the metrics without a mouse. Each `.sr-only` span is **explanation-only** (e.g. "A reactive burst is a cluster of chat messages within about 20 minutes."), NOT a restatement of the visible `<strong>` value + `<span>` label — this avoids the screen-reader double-announcement the self-review flagged (the visible value+noun is already in the accessibility tree); tile 4's sr-only carries the additive "{interrupted_count} of {block_count} deep-work blocks overlapped a chat burst" breakdown the visible `%` doesn't convey. Singular/plural ternary on tile 1 unchanged. `WeeklyCapacityScreen.tsx` only (`.sr-only` already defined in `styles.css`). Build green; passed self-review (single review agent on the additive diff: confirmed JSX valid, plural correct, all four explanations match the `InterruptionLoadAnalysis` field semantics; applied the double-announcement cleanup — sr-only is now explanation-only like `.summary-score`, not a value-repeating mirror like the baseline chips).
- [x] **BlockCard relabel selects have no visible field label for sighted users** (2026-06-29) — the three `.tag-grid` relabel `<select>`s in `components/ledger/BlockCard.tsx` (Work category / Planned status / Work mode) now each sit inside a `<label className="tag-field">` flex-column cell with a small uppercase micro-label span above (`.tag-field-label` reuses the `.baseline-chips-label` token treatment: `var(--text-subtle)`, 11px, `font-weight:600`, `letter-spacing:0.04em`, `text-transform:uppercase`), so a sighted analyst sees what each dropdown controls instead of three bare values. Wrapping in a real `<label>` also associates the visible text with the control (click-to-focus); the existing `aria-label` (SR name) + value `title` (hover-reveal of the current truncated value) are kept unchanged. New `.tag-field`/`.tag-field-label` rules in `styles.css` (token-based → light+dark) plus a `.ledger-screen` density override (`gap:3px`, label `10px`) to keep the compact Activity cards tight. No `.tag-grid` selector assumed selects were direct children (all existing rules use the `.tag-grid select` descendant form, verified), so the desktop 3-col grid + the ≤600px/≤760px `grid-template-columns:1fr` stack still apply to the new `.tag-field` cells. Build green; passed self-review (additive markup; confirmed no `>`/`nth-child` selector breakage, aria-label redundancy with the wrapping label is harmless — identical text, aria-label wins the accessible name).
- [x] **Scroll-shadow affordance on internally-scrolling content screens** (2026-06-29) — `.capacity-screen`, `.forecast-screen`, and `.settings-screen` (all `overflow-y: auto` and taller than a standard desktop window — Capacity ≈1046px / Forecast ≈1054px / Settings ≈1681px in a ~600–650px scroll viewport, confirmed via Playwright) now carry the proven `.ledger-list` `background-attachment: local`/`scroll` scroll-shadow: two local-attachment cover gradients (`var(--background)`, slightly taller 24px/40px bands to clear the roomier card padding) hide the edge shadows at a scroll boundary; two scroll-attachment radial `--scroll-shadow` shadows reveal when content sits above/below the fold — so the bottom-of-Capacity interruption panel + stakeholder chips, Forecast constraints/risk-flags, and Settings retention/export/Advanced rows now get a visible "more below" cue. One new combined rule in `styles.css`, `background-color: var(--background)` longhand placed AFTER the `background` shorthand (else it resets to transparent). Verified in demo (Playwright, light+dark): shadows hide cleanly at the top boundary, reveal mid-scroll, no layout regression; Settings' sticky `.screen-header` covers its top shadow (acceptable). Build green; passed self-review (verbatim application of a verified pattern; empty-state screens just show plain background — no overflow, no spurious band; decorative-only, no a11y impact).
- [x] **Teams/Webex call-as-meeting dedup vs calendar** (2026-06-29) — chat exports can now carry `call`/`huddle` surfaces (Teams/Webex meetings, Slack huddles): `chatExport.ts` extends `ChatSurface` + `CHAT_SURFACES` with them, and `chatMessagesToImport` groups bursts by **provider + kind** (call vs. text) so a call never sessionizes with adjacent text pings — call bursts emit `Meetings / stakeholder syncs` / `Collaborative` / `fixed` `RawEventImport`s tagged `metadata.kind: "call"` (text bursts keep `kind: "message"` + the reactive defaults). Text-burst `event_id` is unchanged (`chat-<provider>-<iso>`); only call bursts take a `chat-call-` prefix, so re-importing a pre-existing export still upserts reactive blocks instead of duplicating them (self-review CONFIRMED finding). New pure `dedupeChatCallsAgainstCalendar(chatBlocks, existingBlocks)` + `spansOverlap` in `packages/integrations/src/chat/callDedup.ts`: half-open `[start,end)` overlap (touching endpoints don't overlap), drops chat *call* blocks (category Meetings) whose span overlaps any existing calendar meeting block, keeps every reactive block, returns `{ kept, deduped }`. Wired into **both** import paths in `App.tsx` so the result is import-order-independent: chat-import dedups new call blocks against current calendar meetings; calendar-import (symmetric) drops previously-imported chat call blocks now covered by the new calendar meetings (candidates = `imported-` prefix, targets = `calendar-outlook-` blocks). Call events are excluded from the retained reactive `chatEvents` store (`metadata.kind !== "call"`) so they don't inflate the interruption/stakeholder signals; the import is always audited (even when every block was a dedup), with a toast that reports the deduped count. Verified via a 26-check tsc+node smoke (call/huddle→meeting mapping, span padding, overlap/no-overlap/empty-calendar/non-meeting-target/touching-spans/idempotent-reimport/reactive-untouched) + `npm run build`. Passed self-review (8-angle code-review): fixed the event_id idempotency regression and the order-dependent calendar-path gap both finders surfaced, and made the all-deduped case still emit an audit event.
- [x] **Stakeholder collaboration map from chat metadata** (2026-06-29) — new pure `summarizeChatStakeholders(chatEvents, {limit})` + `ChatStakeholderGroup`/`ChatStakeholderSummary` types in `packages/inference/src/capacity.ts` (sibling to `analyzeInterruptionLoad`, same metadata-only guard preamble): ranks the week's stakeholder groups (channels / DMs) by reactive message *volume* and reports each group's `share_pct` + always-exact `burst_count`. Reads ONLY `metadata.channels` (labels) + `metadata.messages` (count) + spans — never message text. A burst spanning multiple channels splits its volume evenly (float weight drives the share, never a misleading rounded count); unlabeled (DM-only) bursts bucket under "Direct & untagged". Returns `null` when there's no reactive volume (`totalMessages === 0`) so a zero-message export never renders a row of "0%" chips. Surfaced as a "Who your reactive time served" chip row on `WeeklyCapacityScreen` reusing the `.baseline-chip` styling (new `.stakeholder-chips` margin override so it sits below the interruption note, not pulled up like the metric-card baseline chips), gated `isCurrentWeek && groups.length > 0`. Threaded `useDerived` (new shared `weekChatEvents` memo feeding both `interruptionLoad` and `chatStakeholders`, week-scoped) → App → ScreenRouter → Weekly. Verified via a 15-check tsc+node smoke (empty/non-chat→null, demo shares 36/28/20/16, multi-channel even split, tiny-burst no count-contradiction, unlabeled bucket, zero-volume→null, bad-data resilience) + `npm run build`. Passed self-review (8-angle code-review): dropped the lossy per-group `message_count` (multi-channel rounding produced "0 messages / 33% share" contradictions and counts that didn't reconcile with the total) in favor of share% + exact burst_count, and added the `totalMessages === 0 → null` guard.
- [x] **Chat interruption / context-switch signal** (2026-06-29) — new pure `analyzeInterruptionLoad(chatEvents, workBlocks)` + `InterruptionLoadAnalysis` type in `packages/inference/src/capacity.ts` (alongside `analyzeCorrections`): from chat `RawEvent`s it quantifies reactive-burst frequency (`burst_count`), interruption density (`messages_per_active_hour`), direct `@`-mentions, and how often bursts interleave deep-work blocks (`interrupted_deep_work_pct`), scoped to a chat window; returns `null` when no chat data so the caller hides the panel. Metadata-only (reads `messages`/`mentions` counts + spans, never text). Surfaced as an explainable `.interruption-note` panel on `WeeklyCapacityScreen` (mirrors `.model-bias-note`, gated `isCurrentWeek && interruptionLoad`). Plumbed a persisted chat-`RawEvent` store via the retained-history recipe: `chatEvents` on `PersistedAppState` + `parseChatEvents` guard (validates `source_type`/timestamps/metadata-object) in both `readPersistedState` branches, `usePersistence` field+dep, App `useState`/hydrate/reset/persist, `importWorkplaceChat` now retains `result.events` (deduped by `event_id`), `useDerived` computes `interruptionLoad` **scoped to the current ISO week**, threaded App→ScreenRouter→Weekly. Demo seeds 4 chat bursts (2 overlapping deep-work blocks → 67% interrupted). Verified via a 14-check tsc+node smoke (empty/non-chat→null, counts/density/interleave, bad-data resilience) + `npm run build`. Passed self-review (8-angle code-review): fixed the strongest finding (analysis was all-week aggregated but shown under a current-week panel → now week-scoped), hardened `parseChatEvents` against null `metadata` (crash path) + made the helper self-protecting (`metadata ?? {}`), and clarified the density stat's tooltip.
- [x] **Trends multi-line chart has no legend↔line hover crosslink** (2026-06-29) — added the `hoveredSeries` isolate-on-hover crosslink to `TrendsScreen.tsx`, mirroring the SOLVED `WeeklyCapacityScreen`/`StackedBar` `hoveredCategory` pattern. New `useState<string | null>` keyed by `series.key` (which equals the legend memo's `item.key`, so the link is bidirectional); both the legend `<span>`s and the SVG `<g className="trend-series">` groups get `onMouseEnter`/`onMouseLeave` setting/clearing it plus an inline `{ opacity: hoveredSeries && hoveredSeries !== key ? 0.3 : 1, transition: "opacity 0.12s" }` (same inline-style convention as `StackedBar`, so the global reduced-motion reset applies). Hovering the line stroke or a dot (the `<g>`'s only hit areas, since `.trend-line` is `fill:none`) dims the three non-hovered series + their legend rows to 0.3; hovering a legend row does the same. Per-dot `<title>`/`role="img"` a11y untouched. No `styles.css` change needed (inline opacity). Build green; passed self-review (additive only — legend rows and series groups had no prior style/handlers; `item.key`/`series.key` confirmed identical `keyof Snapshot` strings so the crosslink resolves; early-return guards the `weeks.length < 2` path).

---

## Never
- `apps/desktop/src-tauri/` (Rust shell) — needs manual Tauri testing.
- `.env` or any secret.
- `vite.config.ts` port (5173) / `tauri.conf.json`.
