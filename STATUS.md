# STATUS.md — ClearCapacity Improvement Loop

The `/improve` loop reads this file first and writes it last on every run.

**Guardrails**
- Scope: edit `apps/desktop/src/` and `packages/` only.
- Gate: `npm run build` must pass before a task is marked done.
- One task per run.

**Ordering** — the loop takes the first unchecked `- [ ]` task reading top-to-bottom
through **Next**. Sections are in priority order; within a section, tasks are
dependency-ordered, so the topmost open task is always the right pick. Tasks tagged
**[manual / Rust]** need native work outside loop scope — ship the loop-safe slice
described in the task and flag the rest as a follow-up.

---

## In Progress
_(none)_

## Next

### Interaction & Visual Polish
_From the 6-hour visual UX pass (2026-06-30, seventh run — rendered daily/weekly/narrative/ledger/audit/setup/forecast/trends/agent/corrections in light + dark wide plus the 420px compact layout via Playwright/demo, 27 screenshots). The app stays highly polished across themes and the compact layout — no dark-mode contrast or narrow-clipping regressions; every flagship metric tile carries the title+`.sr-only` gloss (including the four Forecast scenario cards, now done), the search-empty/loading/disabled states and the destructive Reset confirm are all in place, and the live demo numbers reconcile (22% reliable / ~58% committed across Weekly, sidebar, Agent, and the narrative). The fullPage "content below the fold" look is the known fixed-viewport internal-scroll artifact, NOT a bug — don't re-flag it. The lone visible demo defect this run is the Forecast scenario duplicate below; the next flagship UI is the Acceleration screen (C1, next section). All items here are front-end-only and loop-safe (`apps/desktop/src/`). Open the file/lines and confirm the issue still exists before coding._

### Acceleration Engine
_New flagship AI surface (2026-06-30 brainstorm). Mines the user's observed work — `ActivitySession[]`, `WorkBlock[]`, calendar load, and the correction trail — into evidence-cited **Plays**: AUTOMATE (a generated, reusable AI "skill" recipe grounded in the user's real projects/tools), TOOL (a tool recommendation matched to an observed time-sink), and TECHNIQUE (a trick tied to an observed anti-pattern). Two layers, mirroring the Forecast Agent: a **deterministic miner** in `packages/inference/` that produces ranked signals with full evidence + estimated time saved (runs locally, no AI, always on, privacy-safe), and an **optional opt-in AI synthesis** layer that authors the polished Plays. The AI call routes through the EXISTING `ai_complete` Tauri command (`services/aiComplete.ts`) so the whole feature is loop-safe — NO Rust work. Hard privacy rule: the deterministic miner may read raw window titles locally, but the AI layer sends **derived signals only** (app-name sequences, category/duration/time-of-day stats, counts) — never raw window titles — gated and audited like every other AI command. Build on the established patterns: `forecastAgentPrompt.ts`/`useForecastAgent.ts` for the AI flow, the 4-file `AuditEventType` recipe for audit, the `Screen`/`ScreenRouter`/`ui.ts` trio for the new surface, and the "demo seed must exercise every flagship visual distinctly" convention for the wow. Tasks are dependency-ordered — ship one loop-safe slice per run, gate on `npm run build`._

- [ ] **C1 — Acceleration screen (deterministic-only, always on).** Add `accelerate` to the `Screen` type (`lib/types.ts`), route it in `components/shell/ScreenRouter.tsx`, and place it under the **Agent** section in `lib/ui.ts`. New `components/capacity/AccelerationScreen.tsx` (or a new `components/accelerate/` folder matching the per-feature grouping) renders `accelerationSignals` as Play cards: type chip (humanized via `lib/format.ts`, never raw enum), title/detail, estimated time-saved metric (MetricCard `title` + explanation-only `.sr-only` convention), and an evidence drill-down (mirror BlockCard's "Derived from"). Deterministic data only — works with no AI, no API key. Build + `/verify` (UI task).

- [ ] **C2 — Jaw-dropping demo seeds.** Seed 3–4 vivid `AccelerationSignal`/`AccelerationPlay` examples in `services/demoData.ts` (one per type; e.g. a Monday revenue-report rebuild detected 3 weeks running with a generated skill recipe, a SQL-formatting time-sink tool rec, a 2pm context-switch technique), each with realistic evidence + minutes-saved so the demo surface lands the wow. Demo-only; never touches real state. `demoData.ts` only.

- [ ] **C3 — Persist + audit Play actions.** Persist dismissed/saved Plays via `services/localStore.ts` (hydrated by `usePersistence`), and emit an `acceleration_engine` `AuditEvent` (via `createAuditEvent` in `lib/audit.ts`) when a Play is generated, saved, or dismissed — derived_only privacy, following the Forecast Agent audit shape. Wire dismiss/save handlers from `App.tsx` down through the screen. Build + `/verify`.

- [ ] **D1 — AI synthesis prompt + schema (derived signals only).** Add `services/accelerationSchema.ts` (structured-output schema for an array of authored Plays: `recipe`, `recommended_tools`, refined `detail`, `estimated_minutes_saved_per_week`, `confidence`) and `services/accelerationPrompt.ts` (`buildAccelerationPrompt` + a `PROMPT_VERSION`), modeled on `forecastAgentPrompt.ts`. The prompt is fed **derived `AccelerationSignal[]` only** — assert in code/comment that no raw `window_title` is included — and asks the model to author a concrete, runnable skill recipe for AUTOMATE signals, specific tool names for TOOL signals, and an actionable trick for TECHNIQUE signals, grounded in the cited evidence. No network code here. Both files new.

- [ ] **D2 — useAcceleration hook (opt-in, loop-safe via `ai_complete`).** New `services/`-driven `hooks/useAcceleration.ts` mirroring `useForecastAgent.ts`: build the prompt from `accelerationSignals`, invoke the EXISTING generic `ai_complete` command through `services/aiComplete.ts` (no new Rust command), parse against the D1 schema, persist the authored Plays, and emit the `acceleration_engine` audit event (sent_to_openai: true, derived_only). Gated on an AI key being present and explicitly opt-in (a "Generate skills" button on the screen, with the `useAsyncStatus` "Thinking…" pattern). Wire into `App.tsx`/`ScreenRouter`. Build + `/verify`.

- [ ] **D3 — Saved Skills library.** Let the user save an AUTOMATE Play's generated recipe to a small persisted library (copy-to-clipboard with the "Copied" a11y announce convention, and export via `lib/dataExport.ts`), so generated skills are reusable beyond the session. Surface a "Saved skills" view in the Acceleration screen. Build + `/verify`.

### Intelligence Engine
_Reference pattern: persisted `forecastHistory` + `scoreForecastAccuracy` — mirror it for retained-history work. The chat-metadata inference family (`analyzeInterruptionLoad`, `summarizeChatStakeholders` in `packages/inference/src/capacity.ts`) is the current reference for new metadata-only signals; EXTEND the shared week-scoped `weekChatEvents` memo in `useDerived.ts` rather than re-filtering per consumer._

### Trust & Verification UX
_Reference surfaces: the filterable Audit trail, the Corrections log, and the per-block "Why this estimate?" evidence + "Derived from" drill-down (`BlockCard.tsx`). New trust work should reuse the `AuditEvent` recipe (the 4-file `AuditEventType` flow) and humanize enum/value strings via `lib/format.ts`, never render raw snake_case._

### Capacity Model Calibration
_Distilled from `docs/heuristics-vs-research.md` — a research pass that checked the model's hand-set constants against queueing theory (M/M/1 `1/(1−ρ)`), Mark et al. (CHI 2008) on interruption cost, and Pencavel on working hours. The model's *directions* are well-grounded; the *exact numbers* are hand-tuned. These items sharpen the worst-justified constants. All in `packages/inference/src/capacity.ts` (loop-safe), some with a thin UI surface. Read the doc's matching section before coding; keep every change explainable (it feeds the narrative/forecast the user reviews)._

---

## Done
_Prior entries live in git history and merged PRs._

- [x] **Demo forecast seeds "Likely" identical to "AI reliable estimate"** (2026-06-30) — all four demo forecasts in `services/demoData.ts` seeded `likely_capacity_pct === reliable_new_work_capacity_pct`, so the Forecast Agent panel rendered "AI reliable estimate 24%" beside "Likely · expected case 24%" — a duplicate-looking metric. The task flagged the two rendered seeds (the `generatedForecast` next-week card at 381 and the history week at 412); fixed ALL four for a coherent seed, nudging `likely` to `reliable + 2` in each (next week 24→26; history weeks 24→26, 15→17, 31→33) so likely sits as the expected-case middle just above the protected reliable figure. Every one keeps `conservative ≤ likely ≤ optimistic` (14≤26≤34 / 13≤26≤30 / 9≤17≤23 / 21≤33≤39) and stays near `reliable`, and each computed `likelyLeft` (60/76/57/67%) stays inside the 12–88% range-label visibility gate so the "Likely" marker still renders. No copy cites `likely` (the managerSummary cites the unchanged reliable=24 / live deterministic 22%), so no value/copy contradiction introduced. `demoData.ts` only. Build green; passed self-review (range invariant + marker-gate + no-copy-collision all verified; per the "demo seed must exercise every flagship visual distinctly" convention).
- [x] **B4 — Ranked signal aggregator** (2026-06-30) — added `buildAccelerationSignals({ blocks, sessions }): AccelerationSignal[]` to `packages/inference/src/accelerate.ts`, the single entry point that fans `{blocks, sessions}` across the three deterministic miners (B1 `detectRepetitiveSequences`, B2 `detectTimeSinks`, B3 `detectContextSwitchHotspots`), dedupes, ranks, and caps. Ranking is a shared `compareSignals` total order: `signalScore = estimated_minutes_saved_per_week × confidence` descending, then raw minutes descending, then `signal_id` ascending (deterministic, no NaN — every emitter `Math.round`s minutes and `Number(toFixed(2))`s confidence). Dedup has two layers: (1) defensive `signal_id` Map dedup (detectors mint unique, prefix-namespaced ids today, but a future detector could collide — keep the higher-ranked via `compareSignals(signal, existing) < 0`); (2) a nested-evidence collapse restricted to `automate` signals — B1 intentionally emits BOTH the 2-gram and 3-gram of one app flow, so iterating `ranked` best-first and dropping a candidate whose `derived_from` set is a sub/superset (`isSubsetOrEqual` both directions) of an already-kept automate signal keeps only the top-ranked of each nested cluster. The collapse is `automate`-only by design (the safe choice): `tool` signals own disjoint per-category block-id sets and `technique` signals are one-per-hour, so a generic nested collapse could wrongly drop a distinct hotspot whose sessions happen to overlap another's. `candidateEvidence.size > 0` + `keptEvidence.size === 0` guards stop an empty `derived_from` from being treated as a subset of everything. Capped to `MAX_ACCELERATION_SIGNALS = 6` with an early `break` once `kept` reaches the cap (bounds the O(n²) collapse) plus a defensive trailing `slice`. `useDerived.ts` exposes `accelerationSignals` via a new memo over a shared `weekBlocks` memo (lifted out of `interruptionLoad`'s inline filter so neither re-filters the ledger per consumer — the documented "compute the week filter ONCE" rule) and the existing `activeWindowSessions`. Privacy holds structurally: B4 only merges/passes through the detectors' already-derived-only evidence + ids, introducing no new raw-title exposure. `accelerate.ts` + `useDerived.ts`. Build green; passed self-review (two parallel finders — correctness + cleanup/conventions — surfaced 0 correctness bugs; applied the one worthwhile nit, the early-break cap bound; the duplicated detector standalone sorts are documented-intentional "B4 re-ranks" order and out of B4 scope).
- [x] **B3 — Deterministic context-switch hotspot detector** (2026-06-30) — added the third Acceleration miner, the pure `detectContextSwitchHotspots(sessions: ActivitySession[]): AccelerationSignal[]`, to `packages/inference/src/accelerate.ts`. Orders sessions by `start_time` (reuses the in-file `comparableStartMs` + the `l===r?0:r>l?-1:1` compare form), walks consecutive pairs, and counts a "switch" when `prev.app_name !== curr.app_name` — the same real-handoff notion `detectRepetitiveSequences` uses and the session-level analog of capacity's block-mode `context_switch_score` (both treat reactive app-hopping as the fragmentation cost). Each switch is bucketed by the LOCAL hour the user switched *into* (`localHourOfDay` → `getHours()`, NaN-guarded via `Number.isFinite`, matching capacity's local `getHours()` interruption bucketing; an unparseable time is skipped, not counted). Emits one `type:"technique"` signal per hotspot hour, where a hotspot = `count ≥ MIN_HOTSPOT_SWITCHES (=MIN_RECURRENCES=3)` AND concentrated (`activeHours ≤ 1` OR `count ≥ meanPerActiveHour × HOTSPOT_CONCENTRATION_FACTOR 1.5`) — so evenly-spread switching flags nothing while a dominant hour surfaces. `estimated_minutes_saved_per_week = round(REFOCUS_MINUTES_PER_SWITCH 3 × count)` (3 min/switch is a conservative reclaimable figure, cited well below Mark et al. CHI 2008's ~23-min full-disruption cost; `≤0` skipped though unreachable at count≥3). `confidence = min(0.9, 0.5 + (count−3)×0.05 + share×0.1)` (2-dp); `evidence` = derived counts only (switch count + an `hourWindowLabel` like "2pm–3pm" + `share%` floored to 1 + refocus minutes — NO window titles); `detail` carries the batching/focus angle; `derived_from` = the session ids on either side of the bucketed switches (deterministic `Set` insertion order); deterministic `signal_id = technique-<stableHash(hotspot:<hour>)>`. Results sorted desc by estimated minutes with a `signal_id` tie-break (B4 re-ranks). New helpers `localHourOfDay`/`hourLabel`/`hourWindowLabel` are local (no cross-import, per the file header's no-apps/integrations rule). `accelerate.ts` only. Build green; passed self-review (two parallel finder agents — correctness + reuse/conventions — both 0 findings: comparator total/NaN-free, walk visits each pair once, 0/1-session → `[]` via the `totalSwitches===0` guard, division-by-zero impossible (`activeHours≥1` once switches exist), confidence bounded 0.5–0.9, fully deterministic, privacy = counts/hour-window/share only; renamed a local `window` const → `timeWindow` to avoid shadowing the global).
- [x] **B2 — Deterministic time-sink detector** (2026-06-30) — extended `packages/inference/src/accelerate.ts` with the pure `detectTimeSinks(blocks: WorkBlock[], sessions: ActivitySession[]): AccelerationSignal[]`, the second Acceleration miner. Tallies the user's reviewed `WorkBlock[]` by `WorkCategory`, restricted to the four tool-able favorites (`TOOLABLE_CATEGORIES` ReadonlySet: Recurring reporting / Admin / coordination / SQL / data modeling / query work / Dashboard development / edits), accumulating block count, total minutes, low-deep minutes (`mode !== "Deep work"`), block ids, and a `project_name → minutes` map. Minutes are derived from `estimated_capacity_pct` via the new `minutesFromCapacityPct` helper (`(pct/100) × WEEKLY_BASELINE_MINUTES`, `40*60`, NaN-guarded through the existing `finiteMinutes`; const kept local because inference must not import the integrations/apps baseline). Emits one `type: "tool"` signal per category that is recurring (`blockCount ≥ MIN_TIMESINK_BLOCKS = MIN_RECURRENCES = 3`) AND has non-deep time (`lowDeepMinutes > 0`, which also guarantees `totalMinutes > 0` so the share denominator is safe), skipping any whose `estimatedSaved = round(SAVINGS_FRACTION 0.25 × lowDeepMinutes)` rounds to 0. `confidence = min(0.9, 0.5 + (blockCount−3)×0.08 + lowDeepShare×0.1)` (2-dp), evidence = derived lines (category + hours/week + non-deep share + recurrence count + dominant project via the new `dominantProject` helper — labels/counts only, NO window titles; `project_name` is a derived label, safe), `derived_from` = the block ids, deterministic `signal_id = tool-<stableHash(timesink:<category>)>`. Results sorted descending by `estimated_minutes_saved_per_week` with a `signal_id` tie-break (B4 re-ranks; this is just a sensible standalone order). `sessions` is accepted for detector-signature parity (B4 fans the same inputs across B1–B3) but unused — `void sessions` documents the intent (the miner works from the reviewed blocks). `accelerate.ts` only; reuses the in-file `stableHash`/`finiteMinutes`/`SAVINGS_FRACTION`/`MIN_RECURRENCES`. Build green; passed self-review (high-effort `/code-review` — 0 findings: NaN-guarded minutes, division-by-zero impossible (lowDeep>0 ⇒ total>0), confidence bounded 0.5–0.9, fully deterministic (Map insertion order + explicit sort + strict-`>` tie-break in `dominantProject`), privacy = category/hours/counts/project-label only, empty/all-deep input → `[]`, `estimatedSaved===0` skipped).
- [x] **B1 — Deterministic repetitive-sequence detector** (2026-06-30) — new `packages/inference/src/accelerate.ts` exports the pure `detectRepetitiveSequences(sessions: ActivitySession[]): AccelerationSignal[]`, the first deterministic Acceleration miner. Orders sessions by `start_time` via a `comparableStartMs` helper (the `Number.isFinite` → `NEGATIVE_INFINITY` convention, NOT `?? 0`; sort uses the compare form `l===r?0:r>l?-1:1` so two malformed times never re-introduce `NaN`), then slides windows of length 2–3 over the consecutive `app_name` stream, counting each n-gram via a `Map` (skipping all-same-app windows, which are runs not handoffs). Any n-gram recurring ≥3 times emits one `type: "automate"` signal: `estimated_minutes_saved_per_week` = `round(SAVINGS_FRACTION 0.25 × sum of involved sessions' duration_minutes)` (each duration `Number.isFinite`-guarded), `confidence` = `min(0.95, 0.5 + (count−3)×0.1)` (0–1, 2-dp), `evidence` = derived lines (app flow + counts + minutes — app names ONLY, no window titles), `derived_from` = the involved `session_id`s, and a deterministic `signal_id = automate-<stableHash(seq)>`. Dedup/ranking across detectors is deferred to B4 (the aggregator), so length-2 and length-3 n-grams of the same flow are both emitted here by design. `accelerate.ts` only (new file, picked up by the inference tsconfig `include` glob). Unit-exercise reasoning: empty/short input → `[]` (the `start+length<=len` guard); `[A,B,X,A,B,Y,A,B]` → one `(A,B)` signal count 3; `[A,A,A]` → no signal (all-same skip). Build green; passed self-review (adversarial re-read: ascending sort verified, both-`-Infinity` returns 0, overlapping-window tally correct, privacy = app-names/counts/minutes/opaque-ids only, NaN-duration guarded, degenerate `estimatedSaved===0` is realistically unreachable and ranks last in B4).
- [x] **A1 — Acceleration domain types + audit event** (2026-06-30) — laid the type foundation for the new Acceleration Engine surface in `packages/domain/src/models.ts`: `AccelerationPlayType = "automate" | "tool" | "technique"`; `AccelerationSignal` (deterministic miner output — `signal_id`/`type`/`title`/`detail`/`evidence: string[]`/`estimated_minutes_saved_per_week`/`confidence`/`derived_from: string[]`, with a doc comment asserting the derived-only-no-window-titles privacy rule); and `AccelerationPlay extends AccelerationSignal` adding the AI-authored payload `recipe: string | null`, `recommended_tools: string[]`, `dismissed: boolean`. Added `"acceleration_engine"` to the `AuditEventType` union. The exhaustive `Record<AuditEventType,string>` in `auditTypeLabel` (`lib/format.ts`) forced one extra line — added `acceleration_engine: "Acceleration"` so `tsc -b` (the build gate, which IS the type-check for this task) passes; this is the only behavior-free spillover. Deferred the rest of the 4-file `AuditEventType` recipe (the `AuditLogScreen` filter chip + the tinted badge CSS) to the task that actually EMITS the event (C3/D2) — no `acceleration_engine` audit event exists anywhere yet (not in demo, not live), so the missing filter/badge are inert and would render nothing. Types only, no behavior, no new consumers. Build green; passed self-review (diff matches the spec field-for-field; `AccelerationPlay`'s `extends` correctly inherits all signal fields; the format.ts label is the minimal forced change; no exhaustive switch/record over either new type exists yet so nothing else breaks).
- [x] **Forecast scenario cards lack the title + `.sr-only` metric convention** (2026-06-30) — the four `.forecast-summary` cards in `components/capacity/ForecastAgentPanel.tsx` (AI reliable estimate / Conservative / Likely / Optimistic) were the lone holdout from the metric `title`+`.sr-only` sweep: bare `<span>`/`<strong>`/`<small>` with no hover tooltip and no SR gloss. Added a plain-language `title` on each card container `<div>` + an explanation-only `.sr-only` span as the last child, following the `.summary-score`/`MetricCard` convention exactly (explanation of what each scenario means, NOT a value+label restatement): AI reliable estimate = the AI's refined primary estimate shown with its confidence; Conservative = the protected low-end figure to plan/commit against; Likely = the expected-case middle of the range; Optimistic = the high-end if risks clear. `.sr-only` is `position:absolute` so it doesn't disturb the card flex layout; no CSS/structure change. `ForecastAgentPanel.tsx` only. Build green; passed self-review (faithful replication of the production MetricCard pattern — title on container, explanation-only sr-only last child, no double-announce of the visible value+label, layout untouched). The metric title+`.sr-only` sweep is now app-wide.
- [x] **Malformed `DURATION` mints an invalid calendar end date** (2026-06-30) — verified already safe; no change needed. The item claimed `outlookIcs.ts:116` had an unguarded NaN (`Number(match?.[1]) ?? 0` with the `?? 0` outside `Number`), but the actual code is `Number(parsed.value.match(/PT(\d+)M/)?.[1] ?? 0)` — the `?? 0` is applied to the optional-chained capture group BEFORE `Number()`, exactly the fix the item itself recommended. So `minutes` is always finite: a match guarantees an all-digit `(\d+)` group → finite `Number`; no match → `undefined ?? 0` → `Number(0)` = 0. The described NaN path is not reachable; the Robustness/Edge-Cases batch (format/blocks/outlookIcs) is now complete. Moved to Done per the reconcile-against-reality rule (no diff shipped).
- [x] **Invalid-date comparisons silently mis-rank the activity ledger's app summaries** (2026-06-30) — `summarizeRecentSessions` (`lib/blocks.ts`) ranked sessions by `new Date(start).getTime()`: a malformed `start_time` → `NaN`, which silently broke ordering at two sites. (1) Line 48's "is this session newer?" pick (`new Date(session.start_time) > new Date(existing.latest_start_time)`) is always `false` when either time is invalid (`NaN > x` / `x > NaN`), so the `window_title`/`latest_start_time` froze on the first-seen session regardless of which was actually latest. (2) Line 59's sort comparator `new Date(right).getTime() - new Date(left).getTime()` returns `NaN` for a malformed `latest_start_time`, leaving the app-summary order undefined. Added a module-scope `comparableStartMs(iso)` helper returning the finite ms or `Number.NEGATIVE_INFINITY` (per the malformed-timestamp `Number.isFinite` convention, NOT `?? 0` which misses `NaN`); both sites now route through it. Invalid times map to `-Infinity` so a bad timestamp can never win "latest" (sorts/ranks as oldest), and the sort uses a compare form (`leftMs===rightMs?0:rightMs>leftMs?1:-1`) instead of subtraction so two unparseable times (`-Infinity - -Infinity`) don't re-introduce `NaN`. For valid timestamps the behavior is byte-identical — the `>` compare and the descending sign of the sort are preserved (sort only reads the comparator's sign). `lib/blocks.ts` only; no type/UI change, `summarizeRecentSessions` return shape untouched. Build green; passed self-review (adversarial re-read: valid-path sign-equivalence verified, all four invalid combos at line 48 keep a deterministic non-overwriting pick, both-invalid sort returns stable 0, no NaN reachable).
- [x] **`formatRange` rendered "NaN min" for a malformed block time** (2026-06-30) — `formatRange` (`lib/format.ts`) computed `Math.round((end.getTime() - start.getTime()) / 60000)` with no finite guard, so an empty/malformed `start_time`/`end_time` rendered "… (NaN min)" wherever a block time-range shows. Factored the time-range `head` (start–end string, computed exactly as before so the end-time format call stays unconditional — its pre-existing "Invalid Date" rendering for a bad end is unchanged) and added a `Number.isFinite(startMs) && Number.isFinite(endMs)` guard: invalid → return `head` with no duration suffix; valid → byte-identical `(N min)` suffix. `lib/format.ts` only; return type stays `string` so all consumers are unaffected. Build green; passed self-review (NaN-only path returns head, finite path arithmetic identical, no regression to the unconditional end-time format).
- [x] **Deterministic weekly narrative claimed "near the 80% knee" even when committed load is past it** (2026-06-30) — `generateWeeklyNarrative` (`packages/inference/src/capacity.ts`) ended BOTH user-reviewed strings with unconditional knee copy — `summary_text`: "…reliable new-work capacity is estimated at X% — enough to stay near the ~80% utilization knee where delivery reliability holds"; `manager_ready_summary`: "…on top of roughly Y% already committed — keeping total load near the ~80% reliability knee". But `committed_utilization_pct` is an UNBOUNDED penalty sum (`recurring + carryover + reactive×0.72 + fragmentationPenalty + wipPenalty`) that routinely exceeds 80 on a busy week, where `reliable_new_work_capacity_pct = clamp(80 − committed, 0, 40)` clamps to **0** — so the manager-ready summary handed to a reviewer said "0% reliable capacity … keeping total load near the 80% knee where reliability holds", a direct copy/value contradiction (the user is OVER the knee with no headroom). This is the exact clamp-copy bug already fixed on the Weekly "Reliable new work" MetricCard (`committed >= 80 → "past"` else `"room to"`); the deterministic narrative was the remaining surface that hadn't adopted it. Added an `overKnee = committed_utilization_pct >= TARGET_UTILIZATION_PCT` branch (reuses the in-file `TARGET_UTILIZATION_PCT = 80`, since inference must NOT import the apps-side `RELIABILITY_KNEE_PCT`) feeding two clauses interpolated into the strings: over → "already past the ~80% utilization knee where delivery reliability degrades" / "putting total load past the ~80% reliability knee, so new work should wait until committed load drops"; under → the original "enough to stay near…/keeping total load near…" wording, kept BYTE-IDENTICAL (the threshold is interpolated as `~${TARGET_UTILIZATION_PCT}%` = "~80%"). Demo is unaffected (demo `committed = 58 < 80` → under-branch renders identically). `capacity.ts` only; return shape unchanged (two strings), no string-matching consumer (demo has its own hardcoded narrative). Build green; passed self-review (8-angle equivalent: under-branch byte-parity verified, removed strings' behavior preserved in else, `WeeklyNarrative` shape/consumers untouched, boundary `==80` reads "past"=no-headroom correctly mirroring the card fix, deterministic narrative needs no prompt version bump).

---

## Never
- `apps/desktop/src-tauri/` (Rust shell) — needs manual Tauri testing.
- `.env` or any secret.
- `vite.config.ts` port (5173) / `tauri.conf.json`.
