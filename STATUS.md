# STATUS.md — ClearCapacity Improvement Loop

The `/improve` loop reads this file first and writes it last on every run.

**Guardrails**
- Scope: edit `apps/desktop/src/` and `packages/` only.
- Gate: `npm run build` must pass before a task is marked done.
- One task per run.

**Ordering** — the loop takes the first unchecked `- [ ]` task reading top-to-bottom
through **Next**. Sections are in priority order; within a section, tasks are
dependency-ordered, so the topmost open task is always the right pick. Tasks tagged
**[manual / Rust]** need native work outside loop scope — ship the loop-safe slice
described in the task and flag the rest as a follow-up.

---

## In Progress
_(none)_

## Next

### Interaction & Visual Polish
_New batch from the 6-hour visual UX pass (2026-06-30, fourth run — rendered daily/weekly/narrative/ledger/audit/setup in light/dark wide + the 420px compact layout via Playwright/demo). The app remains highly polished across themes and the compact layout (no dark-mode contrast or narrow-clipping regressions; the fullPage "content below the fold" look is the known fixed-viewport internal-scroll artifact, NOT a bug — don't re-flag it). This run's theme: several demo seed values + hardcoded copy have gone STALE relative to what the current model computes live from the demo blocks — the Weekly/sidebar numbers come from `computeWeeklyCapacitySnapshot` in `useDerived.ts:45`, NOT the seeded `demoSnapshot`, so the recent model changes (#147 super-linear WIP, #148 target-utilization) now make the live demo numbers diverge from the seeds/copy. All items below are front-end-only and loop-safe (`apps/desktop/src/`). Open the file/lines and confirm the issue still exists before coding._
- [ ] **Demo manager summary cites "24%" the deterministic model contradicts** — the hardcoded demo manager-ready narrative (`services/demoData.ts:360`) reads "The current model indicates **24% reliable capacity** for new planned work next week", but the live-computed deterministic snapshot shows **17%** across the Weekly hero, sidebar, and header in demo (the 24% is the *AI forecast's* next-week figure, shown separately on the Forecast tab + audit log). Attributing 24% to "the current model" makes the Summary tab visibly contradict Weekly for a new evaluator. Update the demo string to cite the deterministic 17% (or reword to attribute the 24% to the AI forecast, keeping the deterministic-vs-AI labels distinct per the existing convention). `demoData.ts` only.
- [ ] **"80% knee" jargon on the Reliable-new-work hero card has no plain-language gloss** — the Weekly "Reliable new work" `MetricCard` helper (`WeeklyCapacityScreen.tsx:202`) reads "63% already committed · room to the **80% knee**", and `MetricCard` (`components/common/MetricCard.tsx`) renders the helper as a bare `<small>` with no tooltip — "the 80% knee" is queueing-theory jargon a new analyst can't parse. Add an optional `title` (and `.sr-only` explanation) prop to `MetricCard` and pass a plain-language gloss on the Reliable-new-work card (e.g. "Past ~80% utilization, delays grow sharply — we hold back the last ~20% as buffer"), so the headline metric is decodable without prior knowledge. `apps/desktop/src/` only.

### Intelligence Engine
_Reference pattern: persisted `forecastHistory` + `scoreForecastAccuracy` — mirror it for retained-history work. The chat-metadata inference family (`analyzeInterruptionLoad`, `summarizeChatStakeholders` in `packages/inference/src/capacity.ts`) is the current reference for new metadata-only signals; EXTEND the shared week-scoped `weekChatEvents` memo in `useDerived.ts` rather than re-filtering per consumer._

### Trust & Verification UX
_Reference surfaces: the filterable Audit trail, the Corrections log, and the per-block "Why this estimate?" evidence + "Derived from" drill-down (`BlockCard.tsx`). New trust work should reuse the `AuditEvent` recipe (the 4-file `AuditEventType` flow) and humanize enum/value strings via `lib/format.ts`, never render raw snake_case._
### Capacity Model Calibration
_Distilled from `docs/heuristics-vs-research.md` — a research pass that checked the model's hand-set constants against queueing theory (M/M/1 `1/(1−ρ)`), Mark et al. (CHI 2008) on interruption cost, and Pencavel on working hours. The model's *directions* are well-grounded; the *exact numbers* are hand-tuned. These items sharpen the worst-justified constants. All in `packages/inference/src/capacity.ts` (loop-safe), some with a thin UI surface. Read the doc's matching section before coding; keep every change explainable (it feeds the narrative/forecast the user reviews)._
---

## Done
_Prior entries live in git history and merged PRs._

- [x] **Demo "WIP overload" risk bar pins at 100/100** (2026-06-30) — consolidated the demo work blocks (`services/demoData.ts`) from **9 distinct `project_name`s to exactly 5** so the live `computeWeeklyCapacitySnapshot` feeds `activeProjectCount = 5` into the super-linear `wipLoadScore = pow(5/7, 2) ≈ 0.51` (was `pow(9/7,2)` → clamped `1.0`), rendering the Weekly "WIP overload" RiskRow at ~51/100 (mid-range, matching the seeded `wip_load_score: 0.52`) instead of an alarming 100/100. Merges chosen for natural-reading initiatives: demo-dashboard → "Capacity model v2"; both calendar meetings (planning sync + product review) → "Weekly operating metrics" alongside demo-reporting; demo-blocker ("Marketing attribution refresh") → "Revenue attribution mismatch" alongside demo-attribution; demo-retention and demo-requirements stay distinct. Updated `demo-correction-2`'s `new_value` ("Executive capacity dashboard" → "Capacity model v2") so the seeded correction log stays consistent with the dashboard block's new project. `demoData.ts` only; project_name feeds ONLY `activeProjectCount`, so the lone side effect is the intended ~5pp lift in reliable capacity (lower WIP penalty). Build green; passed self-review (adversarial diff re-read: confirmed exactly 5 distinct names, all 9 blocks `included` since the blocker has `blocker_flag:true`, window-title samples + calendar event titles are separate concepts left intact, a single project spanning two stakeholder groups is realistic, no other capacity aggregate depends on project_name).
- [x] **After-hours reactive-load signal** (2026-06-30) — extended `analyzeInterruptionLoad` (`packages/inference/src/capacity.ts`), the chat-metadata inference family, with a metadata-only "after-hours" signal: two new required fields `after_hours_message_count` + `after_hours_pct` on `InterruptionLoadAnalysis`. Reactive messages are bucketed by their burst's local START hour (reusing the `startDate` already derived for the weekday bucketing) and counted as after-hours when the hour is `< CORE_HOURS_START (8)` or `>= CORE_HOURS_END (18)` — new hand-set constants commented against `docs/heuristics-vs-research.md §5` (Pencavel, diminishing returns past long hours, applied to *when* load lands). `after_hours_message_count` is a strict subset of `message_count` by construction (both add the same `messages`, the after-hours branch only when `messages > 0`), so it can never exceed the total. Surfaced as a muted `.interruption-peak-note` footnote in the Weekly chat-interruption panel (`WeeklyCapacityScreen.tsx`, reused class — no CSS change) gated on `after_hours_message_count > 0`: "N% of reactive messages (K of M) arrived outside core hours (8am–6pm) — chat bleeding into personal time." Demo: shifted `demo-chat-3` from minute 300 (Wed 14:00) to 600 (Wed 19:00) so the footnote renders (5 of 25 → 20%) — same day/channel/volume, so peak (Monday)/calm (Thursday)/stakeholder shares and the analysis window (bounded by Thursday's event) are all provably unchanged. Build green; passed self-review (2 parallel finders + own pass): correctness clean except two finder items, both applied — (1) floored `after_hours_pct` to 1 when any after-hours volume exists so a non-zero count never displays beside "0%" (the `1 of 300` round-to-0 case); (2) reworded the footnote from "before 8am or after 6pm" to "(8am–6pm)" since the `>= 18` boundary is inclusive of 6pm sharp.
- [x] **[larger] Target-utilization capacity model** (2026-06-29) — replaced the fixed "100% baseline minus penalties, clamped 0–40%" `reliable_new_work_capacity_pct` formula in `computeWeeklyCapacitySnapshot` (`packages/inference/src/capacity.ts`) with a principled target-utilization model (doc §1). New `TARGET_UTILIZATION_PCT = 80` constant (the M/M/1 ~80% queueing knee). The forward-committed load — `recurring + carryover + reactive×0.72 + fragmentation + wip` penalties, i.e. the SAME terms the old formula subtracted from 100 — is now summed into a new `committed_utilization_pct` snapshot field (current utilization), and reliable new work = `clamp(80 − committed, 0, 40)`: headroom that brings TOTAL utilization up to the knee rather than filling to 100%. `MAX_RELIABLE_NEW_WORK = 40` is KEPT as the old-behavior guardrail (never over-promise on a near-empty week); `BASELINE_CAPACITY = 100` stays the accounting denominator (unchanged "100% weekly capacity model"). New required field threaded through `WeeklyCapacitySnapshot` (`models.ts`) + the demo seed (`demoData.ts`, `committed_utilization_pct: 52` reconciling the seed's `reliable 28` → 80). Narrative copy (`generateWeeklyNarrative` summary_text + manager_ready_summary) and the Weekly "Reliable new work" MetricCard helper now cite the committed-utilization + 80% knee story ("X% already committed · room to the 80% knee"). Forecast prompt untouched — its "Clamp to 0-40%" instruction stays valid since the 40% cap is retained (no prompt version bump). Build green; passed self-review (8-angle code-review: no surviving correctness findings — all snapshot constructors updated, no other full literals, consumers read-only; demo history seeds' committed value is never surfaced in the hero cards which always render the live computed snapshot; applied one JSDoc-accuracy fix so the doc no longer claims committed+reliable always lands ≤80% when already over the knee).
- [x] **Make the WIP penalty super-linear** (2026-06-29) — `wip_load_score` in `computeWeeklyCapacitySnapshot` (`packages/inference/src/capacity.ts`) was a linear, forgiving `uniqueProjectCount / 10`; replaced it with a quadratic `clamp(Math.pow(activeProjectCount / 7, 2), 0, 1)` so context-switching cost curves upward as the doc §3 (`heuristics-vs-research.md`) research argues (fragmentation likely *under*-weighted; switching cost ~combinatorial). The new curve sits near the old linear value around 5 projects, is gentler below (few projects = little switching), and is harsher above — saturating the penalty at 7 concurrent projects (the "badly overloaded" knee). Verified `score(5)=0.51 > 5×score(1)=0.10` (super-linear) and edge-safe: 0 projects → 0 (no NaN), >7 → clamped to 1. The `Set(...project_name)` construction is byte-identical (null/undefined handling unchanged) and the downstream `× 10` penalty + `clamp` are preserved, so the penalty still caps at 10pp; field stays 0–1. All three consumers (`models.ts` type, Weekly `WIP overload` RiskRow, `agentTools.ts`) treat it as a 0–1 index — none assume linearity; demo seeds the value directly so its display is unchanged. Updated the inline rationale comment with the §3 citation. `capacity.ts` only; no UI/CSS. Build green; passed self-review (8-angle equivalent: correctness/edge cases, removed-behavior invariants, cross-file consumer trace, conventions — no findings).
- [x] **Trends x-axis week ticks are cryptic ("W26") with no full-form tooltip** (2026-06-29) — each x-axis week tick `<text>` in `components/capacity/TrendsScreen.tsx` now carries a `<title>{formatIsoWeekLabel(week.week_id)}</title>` child (e.g. "Week 26, 2026"), mirroring the existing per-dot `<title>` so hovering a "W26" axis label reveals the full week and the label↔dot week reference is consistent. SVG `<title>` is non-visual (tooltip + accessible name only), so the rendered "W26" tick is visually unchanged; `formatIsoWeekLabel` was already imported and used by the dot titles. `TrendsScreen.tsx` only, no CSS. Build green; passed self-review (additive one-line diff: `<title>` is valid as a child of SVG `<text>`, identical pattern to the `<circle>` dots already in the file, no behavior/layout change).
- [x] **Persistent sidebar "Reliable capacity" figure lacks the title + sr-only convention** (2026-06-29) — the always-visible `.side-metric-value` in `components/shell/AppShell.tsx` (sidebar "Reliable capacity" number) now carries a plain-language `title` ("Estimated share of next week that can absorb new planned work without slippage") + an explanation-only `.sr-only` span, matching the `.summary-score` reference (title on the container, explanation — NOT a value+label restatement — inside). This was the lone metric figure still missing the title+sr-only pair (per NOTES) — a new user reading "Reliable capacity 38% / Next week" now gets a decode on hover and via screen reader. The `.sr-only` span is `position:absolute` so the existing flex layout is visually unchanged; the no-signal "--" state keeps the same explanation just as `.summary-score` shows "--" with its title. `AppShell.tsx` only (`.sr-only` already in `styles.css`). Build green; passed self-review (additive a11y-only diff: explanation-only sr-only per convention, no layout/`nth-child` breakage, title describes the metric independent of data state matching the reference).
- [x] **Search inputs only clear via Escape — add a visible clear (×) button** (2026-06-29) — both `.search-box` inputs (`LedgerScreen.tsx` + `AuditLogScreen.tsx`) now render a trailing lucide-`X` clear button (`type="button"`, `aria-label="Clear search"`, `className="search-box-clear"`) shown only when the input has a value, so mouse/touch users have a discoverable reset (was Escape-only — invisible and focus-gated). Ledger clears `searchQuery`; audit clears `query` ONLY and leaves the filter chips (matches its inline-vs-Escape distinction per NOTES — the Escape handler still also resets the filter). Each click restores focus to the input via a `useRef<HTMLInputElement>` (`onClick={() => { setX(""); ref.current?.focus(); }}`) so activating the button doesn't drop focus to `<body>` when it unmounts — standard clear-field behavior, keyboard/SR-safe. New token-based `.search-box-clear` rule in `styles.css` (transparent → `var(--surface-muted)` hover, `var(--text-subtle)` → `var(--text)`; both tokens theme per light/dark, no separate dark override needed). Build green; passed self-review (additive diff, no regression to existing Escape clears; the conditional render keeps the button out of the DOM when empty; focus-restore added during review to fix the introduced focus-loss-on-unmount a11y wart).
- [x] **Re-baseline the "dense meetings" narrative trigger to the user's own median** (2026-06-29) — `generateWeeklyNarrative` (`packages/inference/src/capacity.ts`) flagged `denseMeetings = snapshot.meeting_pct >= 18`, an absolute cut that fires for almost everyone (collaboration is ~85% of the work week; doc §4 calls it a cry-wolf signal). Added an optional `baselines?: CapacityBaselines | null` second param: when `≥ 2` prior weeks of history exist it now flags the week only when `meeting_pct` runs `> meetingMedian × 1.25` (≥25% above the user's OWN rolling median — the `> median` bare cut would fire ~half the time by construction), else falls back to the absolute `>= 18`. Threaded a `capacityBaselines` memo into `useDerived.ts` (prior weeks strictly before `currentWeekId`, mirroring the `WeeklyCapacityScreen` baseline-chip machinery via `computeCapacityBaselines`) and passed it to the only `generateWeeklyNarrative` call site; every other importer uses `ReturnType<>` typing only, so the optional param is backward-compatible. Kept the pure-function shape and the explicit `< 2 weeks` fallback. `meetingMedian` is provably non-null whenever `week_count >= 2` (the median is null only for an empty window → `week_count 0`), so the `!== null` guard is belt-and-suspenders that also satisfies the `number | null` type. Build green; passed self-review (adversarial re-read: no other call sites need the param, prior-week filter matches the chip baseline, no-history case is behavior-identical, degenerate median≈0 correctly flags any meeting week for a no-meeting user).
- [x] **Document the hand-set capacity constants with citations** (2026-06-29) — comments-only pass over the four worst-justified magic numbers in `packages/inference/src/capacity.ts`, each now pointing to `docs/heuristics-vs-research.md` and naming its basis: the `MAX_RELIABLE_NEW_WORK = 40` cap (queueing-theory M/M/1 `1/(1−ρ)` ~80% knee, §1), the `carryoverRiskPct × 0.55` discount (hand-tuned spill fraction, §1–2), the `reactivePct × 0.72` penalty (CHI-2008 interruption stress-cost, §2), and the narrative `context_switch_score >= 0.45` fragmentation trigger (~85%-collaboration / ~47s-attention, likely under-weighted, §3). No behavior change, no UI — confirmed comments-only via `git diff`; `npm run build` green. Smallest item in the new Capacity Model Calibration batch, done first so the remaining three (meeting re-baseline, super-linear WIP, target-utilization model) edit already-annotated constants. Passed self-review (adversarial re-read of the diff: each comment's citation + cited basis verified against the doc section it names; the narrative comment correctly annotates the 0.45 *narrative* threshold, distinct from the unrelated 0.6 proactive-nudge cut; non-ASCII chars in comments — `ρ`/`−`/`§`/`—` — compile fine, the file already carries similar glyphs).
- [x] **At-a-glance source-origin chip on each work block** (2026-06-29) — `BlockCard.tsx` (shared by the Activity ledger + Today review queue) now leads `.block-chips` with a small muted origin chip naming where the block came from, so provenance is visible without expanding the "Derived from" drill-down. New pure `blockOrigin(work_block_id)` helper maps the same prefix convention `App.tsx` keys cross-source dedup off of: `calendar-outlook-` → "Calendar"; `imported-` → "Workplace chat" (the only `imported-` source surfaced in the UI today — comment flags refining off `derived_from`/`evidence` if a future non-chat `imported-` source lands); else → "Activity capture". Each chip carries a plain-language `title` (e.g. "Imported from your Outlook calendar"); the visible label is self-descriptive so no `.sr-only` needed (matches the `ConfidenceChip` `<span>`+`title` convention). New token-based `.block-origin` chip in `styles.css` (`var(--border)`/`var(--surface-muted)`/`var(--text-subtle)` → light+dark automatically; min-height/padding/radius matched to the sibling `.blocker-badge`). Also aligned the two demo calendar-meeting block ids (`demo-planning`/`demo-product-review`, both evidence "Imported from Outlook calendar", referenced nowhere else) to the production `calendar-outlook-demo-*` convention so the demo showcase reads "Calendar" on those cards instead of mislabeling them "Activity capture". Build green; passed self-review (2 parallel finders + verify): correctness clean — confirmed real calendar blocks are `calendar-outlook-<hash>` (`outlookIcs.ts`), chat imports are `imported-<hash>`, capture blocks use `ai-session-`/`session-` (correctly fall through), and the demo id rename breaks nothing; applied the finder's padding-parity cleanup (`0 9px`→`0 8px` to match `.blocker-badge`).
- [x] **Name the calmest active weekday for protected deep work** (2026-06-29) — `analyzeInterruptionLoad` (`packages/inference/src/capacity.ts`) reported `peak_day` (busiest reactive weekday) but the Weekly `.interruption-peak-note` footnote only said "Consider protecting the quieter days" without naming one. Mirrored the existing `peak_day` weekday-bucketing to also compute `calm_day` — the lowest-volume *active* weekday — from the SAME `dayMessages` map (message-bearing days only): ascending-index iteration + strict `<` from a `+Infinity` baseline → lowest-index-wins tie-break (mirrors the peak's lowest-index tie-break), guarded `dayMessages.size >= 2` so a single-active-day week leaves `calm_day` null (no quieter day to contrast). New `calm_day: string | null` field on `InterruptionLoadAnalysis`. Surfaced in the footnote ("…your quietest active day was **Thursday** — consider protecting it for deep work."), gated on `calm_day && calm_day !== peak_day` (the outer `<p>` already gates `peak_day && active_day_count >= 2`, which guarantees `calm_day` is non-null) — the original "protect the quieter days" copy stays as the fallback when calm equals peak (the all-equal-volume tie). Demo seeds Mon=9/Tue=7/Wed=5/Thu=4 → peak Monday, calm Thursday (distinct), so the clause renders in demo. `capacity.ts` + `WeeklyCapacityScreen.tsx` only; no CSS change (reuses `.interruption-peak-note`/`strong`). Build green; passed self-review (single construction site so TS enforces the new field everywhere; tie → fallback copy avoids a contradictory "peaked AND quietest on same day"; `<2 active days` → null verified against the `dayMessages.size >= 2` guard).
---

## Never
- `apps/desktop/src-tauri/` (Rust shell) — needs manual Tauri testing.
- `.env` or any secret.
- `vite.config.ts` port (5173) / `tauri.conf.json`.
