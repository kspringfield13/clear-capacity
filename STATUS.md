# STATUS.md — ClearCapacity Improvement Loop

The `/improve` loop reads this file first and writes it last on every run.

**Guardrails**
- Scope: edit `apps/desktop/src/` and `packages/` only.
- Gate: `npm run build` must pass before a task is marked done.
- One task per run.

**Ordering** — the loop takes the first unchecked `- [ ]` task reading top-to-bottom
through **Next**. Sections are in priority order; within a section, tasks are
dependency-ordered, so the topmost open task is always the right pick. Tasks tagged
**[manual / Rust]** need native work outside loop scope — ship the loop-safe slice
described in the task and flag the rest as a follow-up.

---

## In Progress
_(none)_

## Next

### UI & UX Polish
_Small, visible-surface consistency + a11y slices. Reuse the established shared components (`EmptyState`, `ConfirmDialog`), the `lib/format.ts` humanizers, and the error-toast copy pattern; never render raw snake_case or hardcode a design value._

- [ ] **Weekly capacity category-isolation is mouse-only** — In `components/capacity/WeeklyCapacityScreen.tsx` (~lines 309–329) the category `.allocation-row`s and the `StackedBar` segments (`components/common/StackedBar.tsx`) dim their peers to spotlight one category on `onMouseEnter`/`onMouseLeave` (`hoveredCategory`), but there is no keyboard/focus path, so keyboard users can't trigger the same isolation that mouse users get. Mirror the mouse handlers with `onFocus`/`onBlur` on the category rows (+ `tabIndex={0}` and a `:focus-visible` cue in `styles.css`) so focusing a row spotlights its bar segment — completing keyboard parity for an existing interaction, matching the app's a11y bar (the Trends sr-`<table>`, the `role="meter"` RiskRows). Keep the new tab-stops ordered after the visible rows; the underlying percentages are already SR-readable via the segment `aria-label`s, so this only adds the isolation affordance.

### Performance & Robustness
_Self-contained correctness/efficiency slices found by reviewing the render hot-paths and the enum/number rendering surface. Each is one file (occasionally with a shared helper in `lib/format.ts` or `capacity.ts`), gated on `npm run build`._

### Acceleration Engine
_New flagship AI surface (2026-06-30 brainstorm). Mines the user's observed work — `ActivitySession[]`, `WorkBlock[]`, calendar load, and the correction trail — into evidence-cited **Plays**: AUTOMATE (a generated, reusable AI "skill" recipe grounded in the user's real projects/tools), TOOL (a tool recommendation matched to an observed time-sink), and TECHNIQUE (a trick tied to an observed anti-pattern). Two layers, mirroring the Forecast Agent: a **deterministic miner** in `packages/inference/` that produces ranked signals with full evidence + estimated time saved (runs locally, no AI, always on, privacy-safe), and an **optional opt-in AI synthesis** layer that authors the polished Plays. The AI call routes through the EXISTING `ai_complete` Tauri command (`services/aiComplete.ts`) so the whole feature is loop-safe — NO Rust work. Hard privacy rule: the deterministic miner may read raw window titles locally, but the AI layer sends **derived signals only** (app-name sequences, category/duration/time-of-day stats, counts) — never raw window titles — gated and audited like every other AI command. Build on the established patterns: `forecastAgentPrompt.ts`/`useForecastAgent.ts` for the AI flow, the 4-file `AuditEventType` recipe for audit, the `Screen`/`ScreenRouter`/`ui.ts` trio for the new surface, and the "demo seed must exercise every flagship visual distinctly" convention for the wow. Tasks are dependency-ordered — ship one loop-safe slice per run, gate on `npm run build`._

### Insight & UX Enhancements
_Near-term, high-leverage additive slices found by reviewing the app end-to-end (2026-07-01). The richest seam is **inference the model already computes but never surfaces** — cheap to expose, immediately useful, and it deepens the "everything explainable" contract. Each was re-read against the actual code to confirm it's genuinely open (dropped false positives: the meeting-density baseline chip already exists in `BASELINE_METRICS`, and Acceleration `saved_to_library` is already audited). All loop-safe (`apps/desktop/src/` + `packages/` only); humanize every enum/value via `lib/format.ts`, reuse existing components/tokens, and keep the derived-only privacy contract. Independent items — order is by value, not dependency; gate each on `npm run build`._

### Acceleration Engine v2 — memory, feedback & coverage
_The v1 engine (above) is complete: three deterministic miners (`detectRepetitiveSequences`/`detectTimeSinks`/`detectContextSwitchHotspots` in `packages/inference/src/accelerate.ts`), the rank-and-cap aggregator, the opt-in AI synthesis layer, and the Saved Skills library. Its ceiling is that every Play is mined from **this week only** and thrown away each render — it has no memory and no feedback loop, and it's blind to calendar + the reactive-chat load `capacity.ts` already measures. This batch adds both. The first three items are a **dependency chain** (`E1 acted-on state → E2 weekly history/recurrence → E3 realized-savings track record`); the last two (`E4`, `E5`) are independent new detectors and can be pulled forward if a coverage win is wanted first. All loop-safe (frontend + `packages/`, no Rust): reuse the persisted-history recipe (`PersistedAppState`/`usePersistence`/the four `App.tsx` hydration sites/demo seed), the forecast's `forecastHistory` + `scoreForecastAccuracy` + track-record UI as the proven mirror, the 4-file `AuditEventType` recipe, and the derived-only privacy contract (app names/categories/counts/minutes only — NEVER raw window titles). Ship one slice per run, gate on `npm run build`._


### Intelligence Engine
_Reference pattern: persisted `forecastHistory` + `scoreForecastAccuracy` — mirror it for retained-history work. The chat-metadata inference family (`analyzeInterruptionLoad`, `summarizeChatStakeholders` in `packages/inference/src/capacity.ts`) is the current reference for new metadata-only signals; EXTEND the shared week-scoped `weekChatEvents` memo in `useDerived.ts` rather than re-filtering per consumer._

### Trust & Verification UX
_Reference surfaces: the filterable Audit trail, the Corrections log, and the per-block "Why this estimate?" evidence + "Derived from" drill-down (`BlockCard.tsx`). New trust work should reuse the `AuditEvent` recipe (the 4-file `AuditEventType` flow) and humanize enum/value strings via `lib/format.ts`, never render raw snake_case._

### Capacity Model Calibration
_Distilled from `docs/heuristics-vs-research.md` — a research pass that checked the model's hand-set constants against queueing theory (M/M/1 `1/(1−ρ)`), Mark et al. (CHI 2008) on interruption cost, and Pencavel on working hours. The model's *directions* are well-grounded; the *exact numbers* are hand-tuned. These items sharpen the worst-justified constants. All in `packages/inference/src/capacity.ts` (loop-safe), some with a thin UI surface. Read the doc's matching section before coding; keep every change explainable (it feeds the narrative/forecast the user reviews)._

---

## Done
_Prior entries live in git history and merged PRs._

- [x] **Delivery risk modifiers panel gives no at-a-glance read on the 0–100 scale direction** (2026-07-02) — UI & UX Polish slice on the Weekly screen. In `components/capacity/WeeklyCapacityScreen.tsx` the five `RiskRow`s (Context switch burden / WIP overload / Carryover risk / Meeting density / Active blockers) carried the "0 = minimal … 100 = very high" scale meaning ONLY in per-row hover `tooltip`s, so a sighted analyst couldn't tell a higher score is *worse* without hovering each row. Added one muted sub-caption between the "Delivery risk modifiers" / "forecast inputs" `.section-title` and the `.risk-list` — "Higher scores add more delivery risk to next week — lower is better." — reusing the existing muted-note styling by folding a new `.risk-scale-note` selector onto the `.committed-breakdown-note` rule (12px, `--text-subtle`, both themes) plus a thin `-2px 0 10px` margin override, so no tokens/padding are re-declared. Accurate for all five rows (higher = worse on every one), read by SR in document order right after the header. `WeeklyCapacityScreen.tsx` + `styles.css`. Build green; passed self-review (all five RiskRow directions confirmed higher-is-worse; additive-only, no behavior/layout regression; shared-selector reuse avoids the chip/note duplication a cleanup finder flags).
- [x] **Agent "Clear" chat wipes the conversation with no confirmation** (2026-07-02) — UI & UX Polish slice; the last destructive/irreversible action in the app that bypassed the shared `ConfirmDialog`. In `components/agent/AgentScreen.tsx` the header "Clear" button called `clearChat()` directly, wiping all messages AND removing `CHAT_STORAGE_KEY` from `localStorage` the instant it was clicked. Gated it behind a `tone="danger"` `ConfirmDialog` (default tone) via a local `confirmingClear` `useState`, mirroring the CorrectionsScreen reset pattern: the button now sets the flag, the dialog `onConfirm` closes it then runs the wipe, and `onCancel` clears the flag. Description names the irreversibility ("This clears this saved conversation from your device. It can't be undone."). Reused the shared `ConfirmDialog` (a11y focus-trap/Esc/overlay-cancel/focus-restore already built in) — no new markup or CSS. `AgentScreen.tsx` only. Build green; passed self-review (clearChat empties `messages` which hides the trigger button as before; focus-restore onto the unmounted trigger is a harmless no-op; the dialog default tone is already `danger`).
- [x] **Explain what makes up the committed-load number on the Weekly hero** (2026-07-02) — Insight & UX Enhancements slice (the backlog's concrete checkbox tasks are all Done and every Capacity-Calibration item — incl. the bold target-utilization model — is shipped; this fills a genuinely-open "surface inference the model computes but never shows" gap). The target-utilization model makes `committed_utilization_pct` the center of the reliable-capacity math (`reliable = 80 − committed`), and the Weekly hero surfaces "X% already committed", but the user could never see WHAT that committed load is made of. Added a `committedBreakdown` memo + a `.committed-breakdown` section under the hero metrics that decomposes committed utilization into its five model inputs — Recurring & fixed (`recurring_pct`), Reactive ×0.72 (the 0.72-discounted contribution), Carryover risk (`carryover_risk_pct`), Fragmentation (`fragmentation_penalty_pct`), WIP load (`wip_penalty_pct`) — each gated `> 0`, with a plain-language note explaining the 0.72 reactive discount (Mark et al., CHI 2008) and that the parts add up to committed. **Zero-drift exactness (the load-bearing bit):** `capacity.ts` computes `committed = round(recurring + carryover + reactive*0.72 + frag + wip)` where every term EXCEPT reactive is already an integer, so `round(int_sum + reactive*0.72) = int_sum + round(reactive*0.72)`; deriving the reactive part as the REMAINDER (`committed − recurring − carryover − frag − wip`) equals `round(reactive*0.72)` exactly and the five parts sum to the displayed committed with no rounding drift and NO `capacity.ts`/demo-reconciliation change (proven over 200k randomized cases). View-layer only; reuses the existing `baseline-chips-label`/`baseline-chip-row`/`baseline-chip`/`baseline-chip-metric`/`baseline-chip-delta` styling (matching the stakeholder-chips precedent) so only a thin `.committed-breakdown` column wrapper + `.committed-breakdown-note` are new CSS. `WeeklyCapacityScreen.tsx` + `styles.css`. Build green; passed self-review (correctness finder: no bugs, remainder-exactness + non-negativity + no-clamp-break confirmed; cleanup finder's REUSE finding applied — 6 duplicated chip classes collapsed onto the existing baseline-chip classes).
- [x] **Audit-log "Copy JSON" buttons announce identically across the event list** (2026-07-02) — UI & UX Polish a11y slice on the trust/verification surface. In `components/audit/AuditEventRow.tsx` the per-event "Copy JSON" button (rendered once per event by `AuditLogScreen`'s `filteredEvents.map`) carried only a generic icon + visible text with no `aria-label`, so across the audit list a screen reader listing controls heard "Copy JSON" repeated identically for every event with no cue which event each acts on (the ambiguous-repeated-control gap already fixed for BlockCard/PlayCard/SavedSkills/SensitiveReview). Folded `event.title` (the required `AuditEvent` headline string already rendered in the `<summary>`) into an `aria-label` and mirrored the transient copied state into it (`Copy JSON for ${title}` ↔ `JSON copied for ${title}`) so overriding the visible text with an accessible name doesn't drop the success cue the visible "JSON Copied" swap was carrying — matching the icon-swap/toggle accessible-name convention. Visible text stays generic per the convention; only the accessible name got the identifier. `AuditEventRow.tsx` only. Build green; passed self-review (`event.title` is a required non-null string, no null/undefined in the label; no visible-text/handler/icon/copied-timer regression — only the accessible name enriched).
- [x] **Acceleration play-card controls announce identically across the play grid** (2026-07-02) — UI & UX Polish slice on the flagship Acceleration surface. In `components/accelerate/AccelerationScreen.tsx` the `PlayCard` action buttons "Save" / "I acted on this" / "Dismiss" carried only a state `title` + generic visible text with no `aria-label`, and the recipe `<details>` "Save to library" button had NO `aria-label` while its sibling Copy button already did — so across the `.play-grid` a screen reader listing controls heard the same action names repeated identically for every play with no cue which play each acts on (the ambiguous-repeated-control gap already fixed for BlockCard/SavedSkills). Folded `signal.title` (the required `.play-title` `<h3>` headline string) into each button's `aria-label`; for the three toggle buttons (Save/Acted-on/Save-to-library) the aria-label mirrors the transient state ("Saved"/"Save", "Acted on"/"I acted on this", "In library"/"Save to library") so overriding the visible text with an accessible name doesn't drop the state cue — matching the icon-swap accessible-name convention — and the Copy button's existing copied-state mirror is preserved with the title appended. Visible labels stay generic per the convention; only the accessible names got the identifier. `AccelerationScreen.tsx` only. Build green; passed self-review (only accessible names enriched — no visible text, handler, aria-pressed, or state-announcement regression; `signal.title` is a required non-null string already rendered in the headline).
- [x] **Block Confirm / Exclude buttons announce identically across the review list** (2026-07-02) — UI & UX Polish slice, the app's core review surface. In `components/ledger/BlockCard.tsx` the "Confirm" (~line 189) and "Exclude" (~line 193) action buttons carried only generic visible text and no `aria-label`, and the three per-block time-range edit buttons (Save/Cancel/Edit, ~lines 95/104/120) carried generic static `aria-label`s — so across the Daily Review queue and the Activity ledger a screen reader listing form controls heard the same names repeated identically for every block with no cue which block each acts on (the ambiguous-repeated-control gap already fixed for the three relabel `<select>`s in the SAME card). Folded `block.project_name` (the card `<h3>` headline, a required `WorkBlock` string already used by the sibling selects) into each button's `aria-label` (`Confirm — ${project_name}` / `Exclude — …` / `Edit block time range — …` / `Save time changes — …` / `Cancel time edit — …`); visible labels stay generic per the convention, and the Save button's error state remains announced separately via its existing `role="alert"` sr-only span. `BlockCard.tsx` only. Build green; passed self-review (only accessible names enriched — no visible text, click handler, or error-announcement regression; matches the documented per-instance-accessible-name convention exactly).
- [x] **E5 — Calendar / meeting time-sink detector** (2026-07-02) — Acceleration Engine v2, biggest coverage win: `calendarEvents` were never mined, so meetings — analysts' single largest reclaimable sink — were invisible to the engine. Added a fourth deterministic detector `detectMeetingLoad` in `packages/inference/src/accelerate.ts` that mines `OutlookCalendarEvent[]` for (1) recurring low-value meetings (same normalized title observed ≥ `MIN_MEETING_RECURRENCES`=`MIN_RECURRENCES` times → an async-update `technique` Play, estimate = shared conservative `SAVINGS_FRACTION` × series minutes) and (2) the day whose meetings stack back-to-back with no real focus gap (`< MEETING_FOCUS_GAP_MINUTES`=30, ≥ `MIN_BACK_TO_BACK_TRANSITIONS`=2 → a "protect a focus block on {weekday}" `technique` Play, estimate = shared `REFOCUS_MINUTES_PER_SWITCH` × total tight transitions across the week, reusing the same Mark-et-al. per-switch constant the other refocus detectors price a switch at). Threaded via a new optional `AccelerationMiningInput.calendarEvents` (mirrors E4's field; existing detector signatures untouched; the aggregator fans `detectMeetingLoad(calendarEvents ?? [])` into the mined list) and a new week-scoped `weekCalendarEvents` memo in `useDerived.ts` (filters to the current ISO week like `weekChatEvents`, added to the `accelerationSignals` dep array). New local helpers (`eventMinutes`/`normalizeMeetingTitle`/`weekdayLabel`/`localDateKey`) alongside the reused `SAVINGS_FRACTION`/`REFOCUS_MINUTES_PER_SWITCH`/`stableHash`; standalone estimate-desc ordering + stable `technique-${stableHash("meeting-recurring:"+title)}` / `technique-${stableHash("meeting-focus-guard")}` ids (distinct hash seeds — no collision with hotspot/reactive-load ids) so E2 recurrence + E3 track record follow them. Both Plays set `derived_from: []` (mined from aggregate meeting metadata, not enumerable source ids — matches the E4 reactive-load Play; the evidence lines carry metadata-only provenance). Demo seeds a daily 15-min "Daily analytics standup" Mon–Fri so `?demo=1&screen=accelerate` shows a distinct 5th "Recurring meeting" card (~19 min) — verified via a compiled `tsx` harness across ref hours (5 cards, the other four detectors' minutes unchanged since standups are calendar events, not blocks, so the curated capacity snapshot is untouched; edge cases — empty/below-threshold/malformed-times/blank-title — all emit 0 plays). Privacy holds: only counts/durations/recurrence/day-of-week + the meeting title (same class of user-facing label the `tool` detector already sends as `project_name`, and already surfaced by the app as calendar work-block labels) — never event bodies, no attendee PII beyond a count. `accelerate.ts` + `useDerived.ts` + `demoData.ts`. Build green; passed self-review (correctness/removed-behavior/cross-file/reuse/altitude/conventions/privacy angles — no findings survived; sole caller safe via optional field, no enum rendered raw, harness-proven behavior).
- [x] **E4 — Reactive-load Play from the existing interruption analysis** (2026-07-02) — Acceleration Engine v2 slice; the engine was blind to the reactive-chat load `capacity.ts` already measures. Added a fourth deterministic detector `detectReactiveLoad` in `packages/inference/src/accelerate.ts` that turns the pre-computed `InterruptionLoadAnalysis` (from `analyzeInterruptionLoad`) into one `technique` Play — "Batch reactive comms into set windows", with a focus-guard angle. Threaded via a new optional `AccelerationMiningInput.interruptionLoad` field (existing detector signatures untouched; the aggregator fans it into the mined list) and wired from `useDerived.ts` (the `interruptionLoad` memo already existed one hook above). **Self-review hardening (8 finder angles → 3 agents):** the initial estimate keyed off `active_hours` (chat-burst wall duration), which a line-scan agent showed (a) doesn't track interruption intensity — a single 3h/3-message window over-claimed ~36 min and could evict real automate/tool Plays from the top-6 cap — and (b) double-counts overlapping bursts. Reworked the estimate to the refocus tax of interruptions BEYOND a couple of set windows: `REFOCUS_MINUTES_PER_SWITCH × max(0, burst_count − REACTIVE_BATCH_WINDOWS)`, reusing the SAME Mark-et-al.-grounded per-interruption constant the sibling `detectContextSwitchHotspots` prices app switches at (this also resolved a cleanup finding that the two `technique` detectors used unrelated formulas for the same citation). A cross-file agent caught a second bug: the "guard {peak} for focused work" suggestion fired even when the peak fell after-hours (advising the user to protect an 8pm slot for deep work, contradicting the after-hours-bleed cue the Play itself raises) — now the guard suggestion is gated on the peak being inside core hours (`REACTIVE_CORE_HOURS_START/END`, mirroring capacity's window), while the peak is still cited in evidence at any hour. Verified with a compiled `tsx` harness: demo `?demo=1&screen=accelerate` shows a distinct 4th card (~6 min, others' minutes unchanged by the new input); edge cases (after-hours peak omits the guard; 1-burst/3h and 2-overlapping-burst loads emit 0 plays; null/undefined safe) all hold. As a `technique` the estimate IS the reclaimable minutes, so it scores against the E3 realized-savings machinery with a capture fraction of 1; stable `technique-${stableHash("reactive-load")}` id (no collision with hotspot ids) so E2 recurrence + E3 follow it. Privacy holds structurally (evidence carries burst/message/mention counts, hours, and time-of-day/percentage stats — never message text; `derived_from: []` since the Play is mined from aggregate metadata, handled everywhere). Documented known limitation: reactive-load and context-switch-hotspot refocus estimates can partially overlap in the summed headline (both `technique`, never cross-deduped) — the headline is an explicit "conservative planning aid, not a guarantee". `accelerate.ts` + `useDerived.ts`. Build green; passed self-review.
- [x] **E3 — Realized-savings track record** (2026-07-02) — the Acceleration Engine v2 flagship: turns the engine's forward-looking `estimated_minutes_saved_per_week` claims into a proven record. Added pure scoring helpers in `packages/inference/src/accelerate.ts` mirroring the forecast machinery (`scoreForecastAccuracy`/`summarizeForecastAccuracy`/`buildForecastTrackRecord`): `scoreRealizedSaving` (inverts each detector's estimate back into observed minutes via a type-aware `captureFraction` — `1/SAVINGS_FRACTION` for automate/tool since their estimate is the conservative 25% cut, `1` for technique whose estimate IS the refocus minutes — then rates beat/met/missed against the projection with a `max(5, 20%)` tolerance), `buildRealizedSavings` (one entry per acted-on play per completed week, scoring each week against the following one), and `summarizeRealizedSavings` + `RealizedSavings{Entry,Summary,Week,Rating}` types. A `useDerived` `realizedSavings`/`realizedSavingsSummary` pair feeds a new `AccelerationTrackRecord.tsx` surface on the Acceleration screen (rows humanized via `realizedSavingsRatingLabel`/`accelerationTypeLabel`/`formatIsoWeekLabel`, per-play title from the live plays with a type-label fallback, blue/green/amber beat/met/below chips reusing `--blue/green/amber-soft`). Demo seeds two acted-on plays + tuned `accelerationHistory` estimates so `?demo=1&screen=accelerate` shows a distinct beat (SQL sink 44→24), on-track (2pm hotspot 18→3), and below-estimate (hotspot 21→18) trio (verified via a compiled `tsc`+node harness). Privacy holds structurally (reads only the id/type/minutes summaries). Build green; passed self-review (8-angle finder pass → three material correctness fixes applied: (1) the summary headline was mislabeled "min/week" on a cross-week SUM — reworded to a cumulative total with a beat/met/below breakdown so a big beat can't mask misses; (2) treating a signal's ABSENCE next week as "resolved" fabricated false large beats when it merely fell off the capped top-6 — now only present-in-BOTH-weeks transitions are scored; (3) the still-accumulating current week caused mid-week score jitter — now excluded via a `currentWeekId` filter so only completed weeks score; the time-less-`actedOnPlayIds` limitation is documented and the copy reframed as correlational, not causal).
- [x] **E2 — Weekly acceleration snapshot + cross-week recurrence** (2026-07-02) — Acceleration Engine v2 slice; the memory the E3 track record scores against. Persisted a per-ISO-week `accelerationHistory: PersistedAccelerationSnapshot[]` on `PersistedAppState` following the `snapshotHistory` recipe end-to-end: the `PersistedAccelerationSnapshot`/`PersistedAccelerationSignalSummary` interfaces + a defensive `parseAccelerationHistory` (validates `week_id`/`signals`, keeps only well-formed `{signal_id, type∈union, finite minutes}` entries so a corrupt blob can't feed the recurrence counter an `undefined` key) in BOTH `localStore.ts` read paths, the `PersistableState` field + effect dep in `usePersistence.ts`, all FOUR `App.tsx` hydration sites (initial `useState`, async-load, `usePersistence({...})`, `resetLocalData`), a new retention effect mirroring the snapshot one (one record/week, latest-wins, `.slice(-24)`, demo/empty guarded) that stores only the derived id/type/minutes summary — **sorted by `signal_id` so the equality guard compares sets not the recurrence-driven display order** — and `accelerationHistory` seeded in demo (3 prior weeks whose ids match the live-mined demo signals, verified via a compiled `tsx`-style harness, so the 2pm hotspot/SQL sink/Hex→Looker→Teams cards show distinct "Recurring 3/2/1 weeks" badges). Added a `recurrenceBySignalId` memo in `useDerived.ts` (count of PRIOR weeks — `week_id < currentWeekId`, so writing the current week never feeds back → no render loop) threaded into `buildAccelerationSignals` via a new optional `AccelerationMiningInput.recurrenceBySignalId`; the miner annotates each signal with a `recurrence_weeks` domain field (added to `AccelerationSignal`) + an evidence line, and surfaces a `.play-recurring-badge` (amber, `--warning`/`--surface-muted`/`--border` tokens only, `Repeat` icon, `title` + explanation-only `.sr-only`, mirroring `.play-ai-badge`). **Self-review hardening (finder agents):** the recurrence boost is applied as a TWO-PHASE ranking — selection/dedup/nested-`automate`-collapse/cap run on the recurrence-FREE `compareSignals`, then a final `compareByRecurrence` re-sorts only the survivors — so recurrence adjusts ordering/emphasis but can NEVER change which signals surface or their displayed `estimated_minutes_saved_per_week` (harness-proven: MINUTES_CHANGED false, identical surviving set with/without recurrence). Privacy holds structurally (ids/types/counts/minutes only). `models.ts` + `localStore.ts` + `usePersistence.ts` + `App.tsx` + `useDerived.ts` + `accelerate.ts` + `AccelerationScreen.tsx` + `styles.css` + `demoData.ts`. Build green; passed self-review (6 finder angles → the one real invariant violation, recurrence leaking into the nested-collapse minutes swap, fixed via two-phase ranking; demo id coupling verified live; no render loop confirmed).
- [x] **E1 — "Acted on" Play state** (2026-07-02) — Acceleration Engine v2 slice; the foundation the E3 feedback loop scores against. Added a third persisted `actedOnPlayIds: string[]` alongside `savedPlayIds`/`dismissedPlayIds`, following the C3 persist-user-actions recipe end-to-end: the `PersistedAppState` field + JSDoc (`localStore.ts`), `parseStringIdList` in BOTH read paths, the `PersistableState` field + effect dep in `usePersistence.ts`, all FOUR `App.tsx` hydration sites (initial `useState`, async-load, the `usePersistence({...})` call, `resetLocalData`), and `actedOnPlayIds: []` in the demo seed (required field). Added an `App.tsx` `markPlayActedOn(signal)` handler (no-op guard → functional-update guard → `if (isDemoMode) return` → audit `.slice(-1000)`, mirroring `savePlay`) that logs the discrete action via `createAccelerationPlayAuditEvent` — extended its action union with `"acted_on"` plus the matching `titles`/`summaries` map entries (`acceleration_engine`, `derived_only`, `window_titles:false`, an already-existing AuditEventType so no new 4-file badge/filter recipe needed) — and an unaudited `unmarkPlayActedOn(id)` undo. Surfaced an "I acted on this" toggle on the `AccelerationScreen` play card (`aria-pressed`, `CircleCheck`↔`CheckCircle2` icon swap with the state mirrored into the visible label AND `title`; visible text is the accessible name so no `aria-label` needed), threaded `actedOnPlayIds`/`onMarkPlayActedOn`/`onUnmarkPlayActedOn` through `ScreenRouter`, and added a `.play-action-acted.is-acted` rule (`--info` blue, distinct from Save's `--success` green) plus `flex-wrap: wrap` on `.play-actions` for the now-3-button row. Privacy holds structurally (ids only). `localStore.ts` + `usePersistence.ts` + `App.tsx` + `AccelerationScreen.tsx` + `ScreenRouter.tsx` + `lib/audit.ts` + `demoData.ts` + `styles.css`. Build green; passed self-review (two finder agents confirmed no other `PersistedAppState` literal omits the field, the write path spreads+deps it, the handlers structurally match save/unsave, and the toggle's a11y/CSS/wrap are all correct).
- [x] **Block-field dropdowns give screen readers no block context** (2026-07-02) — UI & UX Polish slice. The three relabel `<select>`s in `components/ledger/BlockCard.tsx` (~lines 159 / 167 / 175) carried generic `aria-label`s ("Work category" / "Planned status" / "Work mode"), so a screen reader listing form controls across the Ledger or Daily Review heard the same three names repeated for every block with no cue about *which* block each edits — the ambiguous-repeated-control gap already fixed for SavedSkills "Remove" and the Review Copilot suggestion. Folded the block's `project_name` (the card `<h3>` headline) into each `aria-label` (`Work category — ${block.project_name}` / `Planned status — …` / `Work mode — …`); the visible `.tag-field-label` span and the truncation-affordance `title` (which shows the current VALUE, not the block) are unchanged. `BlockCard.tsx` only. Build green; passed self-review (`project_name` is a required string on `WorkBlock` already rendered in the headline; visible labels stay generic per the convention — only the accessible name gets the identifier; no other per-instance control in the card regressed).

---

## Never
- `apps/desktop/src-tauri/` (Rust shell) — needs manual Tauri testing.
- `.env` or any secret.
- `vite.config.ts` port (5173) / `tauri.conf.json`.
